"""🆕 Agent 卡片层（L3 人审门）。

设计依据：docs/ai-agent-erp-handbook 第三章。三条原则各防一个失效模式：

  一、单一访问路径  工具不写 SQL，只调 router 函数
  二、事实源在服务端 卡片只带 ID，facts 由后端用 current 重查
                    防的是「屏幕上写同意 ¥4,800，按钮实际批了 ¥48,000」
  三、能力可枚举    卡片类型白名单；端点在前端常量表里，模型永远不产出 URL

本包只做「提案 + 呈现」，一行写操作都没有：用户点按钮之后，走的是他自己的 token
打现有业务端点，跟他在财务页面上点「审批通过」是同一个 HTTP 请求。
"""
