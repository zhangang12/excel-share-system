# 系统金额逻辑全面审计报告（2026-09-09）

**范围**：采购/收货/请款/付款/供应商应付、仓库库存金额与项目材料成本、OA 费用、售后费用、运费、销售合同额与开票、工资、
财务支出总览/成本归集/毛利榜/资金面板、AI 助手金额工具、前端金额输入与展示。

**方法**：① 5 个域并行做逐行代码审查；② 对生产库跑只读 SQL 交叉对账（2,792 条采购明细、4,154 条库存流水、132 张请款单、
118 张 OA 单、8 条售后、139 条销售台账、33 条工资月表）；③ 每个真问题修代码 + 写测试锁死（新增 82 项测试全部通过，
既有 23 份相关测试通过）。

**一句话结论**：合计、除零、空值、冲红这些基础算法基本是对的；真正会算错钱的集中在**请款付款链路没有校验和并发守卫**、
**收货流水与采购明细不同步**、**几处报表口径各自为政**。这次全部修掉，另有 4 个口径问题需要老板拍板（见第四节）。

---

## 一、生产数据对账结果（改代码前的实际状况）

| 检查项 | 结果 | 处理 |
|---|---|---|
| 请款单 Σ分配 = 请款金额 | 131 张已付全部相符；#58 分配为 0（均摊路径） | 正常 |
| 已付款 ≠ 请款金额 / 空 | 0 张 | 正常 |
| 分配超收货金额 | 0 条 | 正常 |
| 明细已付 vs 请款回写 | 按采购单合计全对（差 0.00）；39 条行内分布不同是「整单维护记首行」的既有口径 | 正常 |
| **采购单 Σ已付 > Σ收货** | **TH20260724-025 收货 15 已付 30**（手工多记） | 代码已堵口，数据请采购改回 |
| 已开票单 Σ开票 vs Σ收货 | 190 张全相符 | 正常 |
| 已到货但 收货金额 ≠ 数量×单价 | 20 条，都是手工改过金额/单价的（如 #571 单价 44 收货 18,744） | 数据录入问题，见第四节③ |
| **未到货但已有收货金额** | 90 条 ¥62,707（对公全款 26 条已付 ¥33,928；账期 42 条 ¥22,995 未付；现金 22 条） | 口径问题，见第四节① |
| 已到货但收货金额=0（没定价） | 224 条 | 采购要补价，否则项目成本缺这块 |
| 收货流水金额 ≠ 明细收货金额 | 50 条（净差 ¥695，绝对差 ¥15,313），31 条是 #329 修复前的存量 | `ops/audit_money_backfill.py` 可修 |
| **未定价收货流水 amount=0 而非 NULL** | 210 条、193 个物料；34 个物料加权均价被拉低（液压站 1700→850），估算库存/成本少算 ¥18,496 | 代码已修，存量用脚本置 NULL |
| 采购数量 ≠ 流水数量 | 5 条（#1350 采购 6 / 仓库 10 等） | 见第四节③ |
| 负库存物料 | 0 | 正常 |
| 冲红完整性（原单标记/配对/金额） | 全部一致 | 正常 |
| OA settle_amount / 负数 / 跨月 | 0 条核定、0 负数、0 跨月 | 正常 |
| **OA 自己批自己的单** | 9 次（王芹自批两张对公付款 ¥33,478 / ¥13,281 等） | 代码已堵 |
| 售后 cost = Σ明细 | 全相符 | 正常 |
| 销售台账 | 1 张 draft ¥17,200 被算进销售总额；22 张已作废 ¥679,123 未被算（正确） | 代码已修 |
| 运费 / 工资 | 无负数、无异常 | 正常 |

## 二、修掉的问题（按"会算错钱"优先）

### 采购→请款→付款
1. **付款金额不校验**：0 元、负数、超请款额都能「已付款」并按比例回写明细。现在 `0 < 实付 ≤ 请款额`。
2. **付款/删除无并发守卫**：出纳双击或付款与删除同时发生 → 明细已付累加两次。现在用条件 UPDATE 原子翻转状态，第二个请求 409。
3. **请款单与明细脱钩**：Σ分配≠请款额、分配可超余额、可跨供应商、可空明细都放行。现在建单/重提全部校验。
4. **已付金额三个写入口互不知情**（请款付款累加 / 整单维护覆盖 / 编辑明细）：整单维护后再走请款付款 → 同一笔钱记两遍。
   现在付款后按采购单合计核 Σ已付 ≤ Σ收货，超了拒绝并说明原因。
5. **删除已付请款单反冲 `max(0,…)` 静默截断**：对不上就悄悄吞掉，供应商已付从此错。现在对不上直接拒绝删除。
6. 分摊用 4 位小数无兜余 → 改 2 位小数末行兜余，Σ回写精确等于实付；删除反冲与付款共用同一分摊函数。
7. 整单维护已付 ≤ Σ收货、对账状态白名单；编辑明细手填已付 ≤ 收货；有请款/付款的明细不能改供应商、不能删。
8. 批量开票不给金额时开票金额默认=收货金额（原来停在 0，列表「已开票」账目却「待开票」）。
9. 报表：季度加上界；月度趋势的已付按付款月归集。

### 仓库 / 项目材料成本
10. 未定价收货的流水金额写 0 而不是 NULL → 稀释加权均价、躲过缺价审计。已改 NULL。
11. 采购改数量不同步入库流水（#1350 采购 6 仓库 10）。现在同步，改小前先查库存不能为负。
12. 冲红采购入库单只动仓库，明细仍「已到货」→ 毛利榜里成本消失、应付照算。现在冲红=退回待收货。
13. 一键领用同一物料出现两行时各按全部现存领 → 负库存。已递减。
14. 调拨可把别的项目已收货的料调给新项目 → 成本双计。现在只能动通用池。
15. 收货腿 SQL SUM 跳过 NULL：部分行无价按 0 计且不亮"缺价"。已按均价补并计缺价。
16. 冲红过的明细在 Postgres 下删不掉（冲红对以外键指向明细）。删前解除关联。

### 报表 / OA / 售后 / 销售 / 工资 / AI 助手
17. 毛利榜直发腿把**未到货**的采购（收货金额下单时就=订单金额）算成项目成本（90 行 ¥62.7k）。现在只算已到货且未经仓库的。
18. 待主管审批/被退回的销售订单合同额进了销售总额、业绩榜、毛利收入。已排除。「未开票」不再把税票 0/"/" 的不开票项目算进去。
19. 按月归集用 UTC（每月 1 日 0–8 点的单跑到上个月）→ 一律北京时间；部门报表对时间戳用 LIKE 在 PG 会 500 → 已改。
20. OA：财务汇总/下钻漏了「审完待付款」状态（2 张对公 ¥22,500 在成本归集里有、汇总里没有）→ 三处同一状态集。
21. OA：任何环节任何人都能写核定金额、无上下限、不留痕 → 只有财务环节（或管理员）能写，0≤核定≤申请，写理由和审计。
22. OA：可以审批自己提交的申请 → 403（与「不能批自己的请款」「不能给自己标付款」同源）。已付款的单不能删。
23. OA/售后按月：不再用 updated_at（随任何更新漂移）；已付款按付款月，其余按审批通过月。支出总览与成本归集同口径。
24. 售后：费用行可为负/NaN；审批通过后重传发票可任意改金额不留旧值；已报销的能作废/删除 → 全部堵住。
25. 销售：合同额改动审计只记 action 不记数字 → 记旧→新；销售员改已生效订单金额推送主管；申请开票中锁金额。
26. 工资导入：「6000元」「—」「10%」静默变 0 并整行覆盖 → 整批拒绝并指出第几行哪一列；同工号两行由 500 变成可读提示。
27. AI 助手：库存漏排被冲红原单（冲红后仓库页 0、助手答 100）；已作废项目的尾款仍催办；OA 卡片显示申请额而非核定额。
28. 全部金额 schema 加非负 + 拒绝 NaN/Infinity（一条 NaN 落库会让所有财务报表 500）。采购明细单价/收货金额保留可负（优惠行）。

### 前端
29. 请款总额可手改后与分配脱钩、付款金额无上限 → 提交前校验、付款框 `max=请款额`。
30. **所有金额输入框粘贴 "12,500.00" 会被截成 12**（Element Plus 用 parseFloat）→ 统一挂解析器去掉千分位/货币符。
31. 四套金额格式化口径（仓库单价 ¥0.35 显示 ¥0、售后费用两页数字不同）→ 统一到分。
32. 抽屉「未付款/未开票」用 Math.max(0) 把多付/多开票遮成 0 → 直接显示负数并标红；售后费用行禁负数；仓库单价 4 位小数。

## 三、验证情况
- 新增测试 3 份 82 项全部通过；既有请款/OA/仓库/售后/采购相关 23 份测试通过（5 份夹具因"同一人自提自批""没定价就请款""付清后再请款"被新规则拦，改夹具不改规则）。
- 前端 `vue-tsc` 类型检查通过（见发版记录）。
- **未做**：真 Postgres 上跑测试——本机没有 PG，把代码拷进生产容器跑被自动模式拦下。发版后用只读报表接口（毛利榜、成本归集、支出总览、销售报表、库存金额、部门报表带 month 参数）逐个打一遍确认无 500、数字与预期一致。

## 四、需要老板拍板的口径（代码没动）
1. **未到货计入采购额/应付**：`received_amount` 下单时就等于订单金额，供应商账目一览的"欠款"和采购汇总报表都含未到货的 ¥62,707
   （账期 42 行 ¥22,995 一分未付）。要不要改成"到货才算应付"？改的话应付会少约 ¥29k，采购报表口径也随之变。
2. **期初余额忽略日期**：腾丰 2026-08-06 期初欠 ¥123,713，但系统里还有该供应商 7-29～8-6 的 3 行 ¥12,492。如果期初已含这 3 行，
   现在欠款重复了 ¥12,492。请王芹确认期初是否含这三行；确认后代码按 `balance_date` 之后才累计。
3. **5 条采购数量与仓库流水不一致**（#1350 250L浇筑桨 采购 6/仓库 10 等）——是采购填错还是仓库多收，谁改谁。
4. **存量数据修复**：`ops/audit_money_backfill.py`（默认预演）：210 条无价流水 amount 0→NULL；31 条老流水金额同步到明细收货金额。
   同意后在服务器跑 `--apply`。另 TH20260724-025 已付 30 请采购改回 15；224 条已到货未定价请采购补价。

## 五、附：生产库未触碰
本次所有生产库操作均为只读 SELECT；没有改任何业务数据。代码尚未发版（等确认）。
