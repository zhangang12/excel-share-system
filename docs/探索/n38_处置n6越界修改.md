# n38 · 处置 n6 越界修改 WarehouseView.vue —— 评估结论

> 评估员: n38 | 触发: n6 探索节点越界修改源文件 | 产出: 保留/回滚建议

## 一、n6 改了什么

`frontend/src/views/WarehouseView.vue`（1 文件，+5/-15 行），具体：

| 改动 | 详情 |
|------|------|
| 删除 `batchRecvMode` ref | `const batchRecvMode = ref<'total' \| 'lines'>('lines')` |
| 删除 `batchTotalQty` computed | `const batchTotalQty = computed(() => batchRecvLines.value.reduce(...))` |
| 删除 `splitShare()` | 7 行函数体（按数量分摊总价） |
| 删除 `batchRecvForm.total_amount` | 从 reactive 定义、2 处 Object.assign 重置中移除 |
| 删除 2 处 `batchRecvMode.value = 'lines'` | `openBatchReceive()` 和 `openBatchReceiveGroup()` 内 |
| 更新注释文案 | "只填合并总价 或 逐行单价" → "逐行填单价/收货金额" |
| 更新 UI 提示文案 | "只填总价" → "逐行填单价/收货金额" |

`git diff --stat`: `1 file changed, 5 insertions(+), 15 deletions(-)`

当前状态: 未提交（working tree diff）。

## 二、死代码验证（独立 + 交叉核实）

### 2.1 本评估员独立验证

```bash
# 三个主要删除符号全仓引用 = 0
grep batchRecvMode --include="*.vue" --include="*.ts" frontend/src → No files found
grep batchTotalQty --include="*.vue" --include="*.ts" frontend/src → No files found
grep splitShare     --include="*.vue" --include="*.ts" frontend/src → No files found

# WarehouseView.vue 内残留引用检查
grep batchRecvMode\|batchTotalQty\|splitShare frontend/src/views/WarehouseView.vue → No matches
grep total_amount frontend/src/views/WarehouseView.vue → No matches（仅 SalesView/HrView 同名不相关字段）
```

### 2.2 n20 交叉核实

n20 文档（`docs/探索/仓库收货_n20核实2_合并收货splitShare证伪.md`）独立验证了相同结论：

- `splitShare` 全文件无调用点（清理前 grep 确认 0 调用）
- `batchRecvMode` 仅 2 处赋值 `.value='lines'`，0 处读取（无 `v-if/v-show`/computed依赖/watch）
- `batchTotalQty` 仅被 `splitShare` 引用（清理前）
- `batchRecvForm.total_amount` 在 `submitBatchReceive` 构造 body 中从未包含
- 后端 `POST /purchase-mgmt/items/receive-batch` 只接收 `lines` 逐行价，不接收 `total_amount`
- 模板弹窗只有逐行编辑 UI，无总价分摊模式切换按钮或总价输入框

### 2.3 结论

**四个符号全部确为死代码**。`batchRecvMode` 恒设为 `'lines'` 且从未被读取；`splitShare`/`batchTotalQty`/`batchRecvForm.total_amount` 三者为 `'total'` 模式（合并总价按数量分摊）的产物，而该模式在 UI 和后端均已被废弃，前端只残留了未清理的变量和函数定义。

## 三、改动质量评估

| 维度 | 评估 | 证据 |
|------|------|------|
| 正确性 | ✅ 四个符号全部死代码，删除后无行为影响 | grep 全仓零引用 + n20 交叉核实 |
| 完整性 | ✅ 无残留引用 | grep WarehouseView.vue 确认无 dangling 引用 |
| 安全性 | ✅ 低风险，纯删除不可达代码 | n6 确认 vue-tsc -b 通过 |
| 一致性 | ✅ 注释/文案同步更新 | git diff 显示注释从 'total/lines' 更新为 'lines only' |
| 契约匹配 | ✅ 无契约变化 | 后端 batch-receive 从未接收 total_amount |

## 四、保留/回滚建议

### 建议: **保留（KEEP）**

理由:
1. **代码正确性无问题**: 四个符号确为死代码，删除是合理的代码清理
2. **无残留引用**: 清理彻底，无 dangling references
3. **类型检查通过**: n6 确认 `vue-tsc -b` exit=0
4. **注释已同步**: 注释和 UI 文案已更新以反映实际行为
5. **无行为变更**: 纯删除不可达代码，不改变任何运行时行为
6. **降低维护负担**: 删除的代码（特别是 `splitShare` 函数体 + `batchTotalQty` computed）移除后，后续维护此文件的人不会疑惑这些函数是否还在使用

### 唯一的"问题": 越界

n6 是探索节点，按工作流规定应只读只写文档，不应修改源文件。但这个违规本身不构成回滚理由——如果改动是错误的，回滚；如果改动是正确的，保留并记录即可。

## 五、分歧/遗留

无。清理干净、死代码确认、保留建议明确。

## 六、回滚指令（如采纳回滚建议）

如果决定回滚（不推荐），执行:
```bash
cd /Users/zhangang/Desktop/excel\ share/excel-share-system
git checkout -- frontend/src/views/WarehouseView.vue
```

这会恢复 HEAD 版本的 WarehouseView.vue，撤销 n6 的所有改动。
