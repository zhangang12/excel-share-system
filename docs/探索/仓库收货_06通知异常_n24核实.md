# n24 核实 · 仓库收货通知推送与异常处理路径

> 核实对象：n8 文档 `/docs/探索/仓库收货_06通知异常.md`
> 方法：逐条对照代码行号，读原文件验证；不做修改
> 结论：**PASS** — n8 文档 18 项核心代码级结论与源码一致，1 项 minor（分类标签），1 项额外发现（stale docstring）

---

## 核实清单（逐条对照）

### §1.1 调度与 flock

| 结论 | 来源行号 | 代码实况 | 判据 |
|---|---|---|---|
| lifespan 注册 scheduler | `main.py:65` | `@asynccontextmanager async def lifespan(app: FastAPI):` | ✅ |
| asyncio.create_task 启动 | `main.py:95` | `task = asyncio.create_task(overdue_scheduler())` | ✅ |
| 六个 scan 各自独立 try/except + 独立会话 | `overdue.py:400-430` | 六个独立 `try/except` 块，各自 `async with SessionLocal() as db` | ✅ |
| scan_po_arrival_overdue 调用 | `overdue.py:425-429` | `try: async with SessionLocal() as db: await scan_po_arrival_overdue(db) except ...` | ✅ |
| flock 保证单实例 | `overdue.py:371-388` | `fcntl.flock(fh.fileno(), fcntl.LOCK_EX \| fcntl.LOCK_NB)` | ✅ |

### §1.2 扫描条件

| 结论 | 来源行号 | 代码实况 | 判据 |
|---|---|---|---|
| expected_arrival 非空 | `:327-328` | `PurchaseItem.expected_arrival.isnot(None), PurchaseItem.expected_arrival != ""` | ✅ |
| expected_arrival <= today_s | `:330` | `PurchaseItem.expected_arrival <= today_s` | ✅ |
| arrival_date 为空或 '' | `:331-332` | `or_(PurchaseItem.arrival_date.is_(None), PurchaseItem.arrival_date == "")` | ✅ |
| 全量扫描无 limit | `:326-335` | `select(...).where(...)` 无 `.limit()` | ✅ |

### §1.3 15:00 时间窗

| 结论 | 来源行号 | 代码实况 | 判据 |
|---|---|---|---|
| hour < 15 即跳过 | `:319-323` | `now = datetime.now(_CN_TZ); if now.hour < 15: ... return {"scanned": 0, "notified": 0}` | ✅ |

### §1.4 幂等键

| 结论 | 来源行号 | 代码实况 | 判据 |
|---|---|---|---|
| biz_type + biz_id + 当日 | `:338-346` | `select(Message.created_at).where(biz_type == "po_arrival_overdue", biz_id == it.id)` + `_cn_date(ts) == today_s` | ✅ |

### §1.5 推送口径（只推采购员本人）

| 结论 | 来源行号 | 代码实况 | 判据 |
|---|---|---|---|
| 只推 buyer_id | `:359-364` | `if it.buyer_id: await push_message(db, to_user_id=it.buyer_id, ...)` | ✅ |
| buyer_id 为空不推 | `:361` | `if it.buyer_id:` — 空 buyer_id 时整段跳过 | ✅ |
| 注释记录收窄历史 | `:359-360` | 注释"只推采购下单人本人提醒及时跟进，不再推主管/管理层" | ✅ |

### §1.6 N+1

| 结论 | 来源行号 | 代码实况 | 判据 |
|---|---|---|---|
| 循环内逐条查 Message | `:339-344` | `for it in items: r2 = await db.execute(select(Message.created_at)...)` | ✅ |
| lazy load supplier.name | `:351` | `sup = it.supplier.name if it.supplier else "—"` — ORM lazy 加载 | ✅ |

### §2 收货完成零推送

| 结论 | 来源行号 | 代码实况 | 判据 |
|---|---|---|---|
| receive_item 无 push_message | `:2013-2046` | 函数内无 `push_message` 调用；结尾 `await db.commit()` 后直接返回 | ✅ |
| receive_batch 无 push_message | `:2049-2107` | 循环内调 `_finish_receive`，循环外仅 `await db.commit()` + 返回列表 | ✅ |
| upload_receipt 无 push_message | `:2111-2127` | `save_upload + commit + return`，无推送 | ✅ |
| _finish_receive 无 push_message | `:1992-2010` | `_writeback_sheet_row → _auto_stock_in → _sync_txn_amount → _maybe_auto_reconcile`，全为数据操作 | ✅ |
| push_message 仅出现于非收货路径 | 全文件 grep | 6 处：`:1788`（批量改预计到货通知）/`:1836`（单条改预计到货通知）/`:2569`（请款提交）/`:2652`（请款审批）/`:2746`（请款驳回）/`:2817`（请款重提） | ✅ |
| warehouse_router 的 logistics 推送非收货 | `warehouse_router.py:1438-1440` | `packlist_status = "ready"` → 推 logistics — 这是备货完成通知，非采购收货 | ✅ |

### §3.1 get_db 隐式回滚

| 结论 | 来源行号 | 代码实况 | 判据 |
|---|---|---|---|
| 无 try/except，无显式 rollback | `database.py:28-30` | `async def get_db() -> AsyncSession: async with SessionLocal() as session: yield session` | ✅ |

### §3.2 事务原子性

| 结论 | 来源行号 | 代码实况 | 判据 |
|---|---|---|---|
| receive_item 单 commit | `:2043-2044` | `await _finish_receive(db, ...); await db.commit()` | ✅ |
| receive_batch 统一 commit | `:2097-2105` | `for it in ordered: await _finish_receive(...); await db.commit()` — 循环内逐条 _finish_receive，循环外统一 commit | ✅ |

### §3.3 幂等性

| 步骤 | 来源行号 | 代码实况 | 判据 |
|---|---|---|---|
| _auto_stock_in check-then-act | `:1409-1412` | `select(WhTxn).where(purchase_item_id == item.id, is_reversal == False); if ex.scalars().first(): return` | ✅ |
| _sync_txn_amount 覆盖写 | `:1443-1459` | `for t in tr.scalars().all(): t.unit_price = ... ; t.amount = ...` — 直接覆盖 | ✅ |
| _maybe_auto_reconcile 单向状态机 | `:724-736` | `if item.invoice_status != "待对账": return` — 只从不推进，不回退 | ✅ |

### §3.4 push_message commit 契约

| 结论 | 来源行号 | 代码实况 | 判据 |
|---|---|---|---|
| push_message 自己 commit | `notify.py:76` | `await db.commit()` | ✅ |
| 无用户 → return 0 不 commit | `:60-62` | `if not user_ids: ... return 0` — 未调 db.commit() | ✅ |
| 企微失败仅 warning | `:82-83` | `except Exception as e: log.warning("企微推送失败（站内消息已落）: %s", e)` | ✅ |

### §4.1 _auto_stock_in 并发双流水

| 结论 | 来源行号 | 代码实况 | 判据 |
|---|---|---|---|
| check-then-act 无唯一约束 | `:1409-1412` + `models.py:413` | 查询 `WhTxn.purchase_item_id == item.id` 后直接 `db.add(WhTxn(...))`；列定义 `purchase_item_id: ... index=True`，无 `unique=True` | ✅ |

### §4.2 _next_ref 撞号

| 结论 | 来源行号 | 代码实况 | 判据 |
|---|---|---|---|
| COUNT+1 无锁 | `warehouse_router.py:619-625` | `n = (r.scalar() or 0) + 1; return f"{prefix}{ymd}-{n:03d}"` — 无 FOR UPDATE 或 SERIALIZABLE | ✅ |
| ref_no 仅 index | `models.py:418` | `ref_no: Mapped[str] = mapped_column(String(32), index=True)` | ✅ |

### §4.3 _next_po_no 撞号

| 结论 | 来源行号 | 代码实况 | 判据 |
|---|---|---|---|
| MAX+1 无锁 | `:757-774` | `select(PurchaseItem.po_no).where(...).distinct()` + 内存取 max + 1 | ✅ |

### §5.1 列表截断

| 结论 | 来源行号 | 代码实况 | 判据 |
|---|---|---|---|
| _RECV_LIMIT_DEFAULT = 2000 | `:1877` | `_RECV_LIMIT_DEFAULT = 2000` | ✅ |
| _RECV_LIMIT_MAX = 5000 | `:1878` | `_RECV_LIMIT_MAX = 5000` | ✅ |
| stmt.limit(limit) | `:1921` | `r = await db.execute(stmt.limit(limit))` | ✅ |
| keyword 含 source_sheet_id 回溯分支 | `:1909-1916` | `from_sheet = select(Datasheet.id).join(Project).where(Project.code.ilike(kw))` + `source_sheet_id.in_(from_sheet)` 分支 | ✅ |

### §5.2 receiving/meta

| 结论 | 来源行号 | 代码实况 | 判据 |
|---|---|---|---|
| pending_count / received_count 独立 count | `:1969-1973` | `select(func.count(...)).where(arrival_date.is_(None) ...)` | ✅ |
| 全量供应商下拉 distinct | `:1975-1981` | `select(Supplier.id, Supplier.name).join(PurchaseItem)...distinct().order_by(...)` | ✅ |
| 返回 limit 字段 | `:1988` | `"limit": _RECV_LIMIT_DEFAULT` | ✅ |

---

## 分类标签 minor（非事实错误）

n8 §2 将 `:1788/:1836` 两处 `push_message` 归类为"采购审批"，实际上它们是「预计到货变更通知」——在 `batch_update_expected_arrival`（`:1755`）和 `update_item`（`:1794`）中，推给 buyer_lead/manager/admin。不影响结论"收货路径零 push_message"。

---

## 额外发现：scan_po_arrival_overdue 的 docstring 过期

| 位置 | 内容 | 问题 |
|---|---|---|
| `overdue.py:312-313` | 函数 docstring 写「该明细的采购员(buyer_id) + 采购主管(buyer_lead) + 全体管理层(admin/manager)」 | **过时**，2026-07-26 已收窄为只推 buyer_id |
| `overdue.py:359-360` | 代码注释 + 实际逻辑 | 正确，`if it.buyer_id: push_message(to_user_id=it.buyer_id)` |

这是典型的代码注释未同步更新，不影响功能但会误导后续读者。n8 文档正确指出了实际行为（只推 buyer_id），此处仅记录 docstring 的过时状态。

---

## 排除的猜想验证

n8 §8 列出的 6 条排除猜想均已在本次核实中双重确认：

1. 「收货后推采购员」→ 确认三个端点无 push_message
2. 「warehouse_router 的 logistics 推送是收货通知」→ 确认是发货清单备货流程（`warehouse_router.py:1438-1440`）
3. 「scan_po_arrival_overdue 推主管/管理层」→ 确认代码已收窄为只推 buyer_id
4. 「WhTxn.purchase_item_id 唯一约束保幂等」→ 确认 `index=True` 无 `unique=True`
5. 「收货列表有分页能翻完 2000 条」→ 确认只有 limit 无 offset
6. 「get_db 异常时显式 rollback」→ 确认无 try/except

---

## 判决

**Verdict: PASS**

n8 文档中关于通知推送与异常处理路径的 18 项核心代码级结论全部经源码逐行验证通过。1 项分类标签不影响结论。额外发现 1 条 stale docstring（不影响功能，n8 文档本身描述的是实际行为而非 docstring）。

核实方式：逐行对照以下文件的指定行号，不做任何代码修改。
- `backend/app/overdue.py:311-430`
- `backend/app/routers/purchase_mgmt_router.py:720-736, 757-774, 1404-1459, 1755-1790, 1825-1841, 1875-1957, 1960-1989, 1992-2010, 2013-2046, 2049-2107, 2111-2127, 2555-2573`
- `backend/app/routers/warehouse_router.py:619-625, 1428-1443`
- `backend/app/notify.py:29-84`
- `backend/app/database.py:28-30`
- `backend/app/models.py:404-425`
- `backend/app/main.py:65, :95`
