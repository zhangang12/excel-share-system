# n19 · 核实 #1 · 证伪：收货 tab 工具条说明文案

**判决：FAIL（说法不成立）**

## 说法回顾

> 收货 tab 工具条说明文案过时：WarehouseView.vue:1408 仍写「合并零件可勾选多条『合并收货』只填总价」，但 #1 已删掉「只填总价按数量分摊」模式（batchRecvMode 恒为 'lines'，弹窗只有逐行填价），文案误导用户以为可以只填总价。

证据引用：`:1408` 与 `:591-592`、`:654-661`

## 核实过程

### 1. 说法声称的 1408 行不是文案，是表头列标签

```
1408: <el-table-column prop="po_no" label="采购单号" width="205" :show-overflow-tooltip="false">
```

（文件：`frontend/src/views/WarehouseView.vue:1408`）

该行是 el-table 的列定义，label="采购单号"，根本不是工具条说明文案。行号错位。

### 2. 工具条文案实际在 1398 行，且内容已更新为"逐行填单价/收货金额"

```
1398: <span class="muted small">采购下单的物料到货后，在这里核对规格、填送货单号/到货日期；单价未填的（后填价格）在此补上。合并零件可勾选多条「合并收货」，逐行填单价/收货金额。</span>
```

（文件：`frontend/src/views/WarehouseView.vue:1398`）

文案末尾明确写的是**"逐行填单价/收货金额"**，而非说法声称的"只填总价"。这与当前实际行为一致。

### 3. batchRecvMode 变量已不存在

`grep batchRecvMode` 在 WarehouseView.vue 中无结果。该变量已被移除。

### 4. batchTotalQty / splitShare 死代码也已清理

`grep batchTotalQty` 和 `grep splitShare` 在 WarehouseView.vue 中均无结果。这些函数在 n6 探索阶段已确认仅为死代码引用，已被清理。

### 5. 合并收货弹窗确实是纯逐行模式

弹窗注释（第 1952 行）虽仍写 "只填合并总价 或 逐行单价"，但：

- **弹窗内说明文案**（第 1976 行）：`逐行填单价/收货金额（单价可留空，货到再补；填单价自动按数量算收货金额，金额可再手动改）。`
- **弹窗表格**（第 1978-1988 行）：`batchRecvLines` 表格只有"名称/数量/单价/收货金额"四列，每行独立输入单价和金额。无总价分摊控件。
- **第 600 行注释**确认当前逻辑：`#293 合并收货逐行：填单价 → 收货金额自动=数量×单价(两位小数)；金额仍可手动覆盖，再改单价时重算`

没有 batchRecvMode（lines/total）切换开关，没有总价输入框，没有分摊按钮。就是纯逐行模式。

### 6. 唯一可挑剔的残留：弹窗注释（第 1952 行）

```
1952: <!-- 🆕 需求四：合并零件收货（只填合并总价 或 逐行单价）+ 需求十四 收货单 -->
```

这个 HTML 注释里的"只填合并总价 或 逐行单价"是过时的描述，因为实际已经不支持"只填合并总价"了。但这是 HTML 注释（用户看不见），且说法未将它列为证据。不影响用户使用的同义性。

## 结论

说法声称的四个要点逐一核实：

| 要点 | 说法 | 实际 |
|------|------|------|
| 行号 | `:1408` 是文案 | `:1408` 是表头列标签"采购单号" |
| 文案内容 | "只填总价" | "逐行填单价/收货金额" |
| batchRecvMode | 恒为 'lines' | 变量已不存在（死代码清理） |
| batchTotalQty/splitShare | 存在死代码 | 已清理（grep 无结果） |

当前状态：1398 行文案准确描述当前行为（逐行填价），batchRecvMode 已移除，batchTotalQty/splitShare 已清理。该说法不成立。

## 分歧/遗留

- n6 探索阶段已处理此问题（清理死代码 + 更新文案），说法提交时可能尚未更新到当前 HEAD。
- 弹窗 HTML 注释（1952 行）的"只填合并总价 或 逐行单价"描述略有过时，但仅为注释且用户不可见，影响可忽略。
