# 交接文档(维护者/AI 助手接手必读)

> 写给接手本项目的维护者——无论是人,还是新开的 Claude Code 会话。
> 这里记录的是**不在其他文档里的隐性知识**:开发工作流约定、生产环境踩过的坑、
> 排障流程、当前状态。系统性内容请按下面的文档地图去读对应文档。
> 最后更新:2026-07-08。
>
> **⚠️ 维护规则(用户指定):每次代码/流程变更,推送前必须同步更新本文档**——
> 至少刷新「六、当前状态与待办」;若变更涉及新的坑/约定/工作流调整,补进对应章节,
> 并更新顶部的"最后更新"日期。与变更同一个 commit 提交。

---

## 一、文档地图(按用途读)

| 想了解 | 读 |
|---|---|
| 业务需求与流程全貌 | `需求分析.md` → `业务确认结论.md` → `流程评审与问题清单.md` |
| 技术架构 | `技术方案.md`(FastAPI async + SQLAlchemy 2.0 + Vue3 + Element Plus + Postgres) |
| 角色与权限 | `权限矩阵.md` |
| 各模块设计 | `采购模块实施方案.md`、`仓库管理功能设计.md` |
| 历史需求台账 | `增量开发需求清单.md`(M01-M17 已全部完成) |
| **下一步做什么** | **`盈利改善功能规划.md`**(经营分析结论+三档功能路线图,当前的开发依据) |
| 给财务部的功能说明(可直接发给用户) | `财务部新功能说明.html`(支出总览/项目毛利/成本审计/资金面板 使用与口径) |
| 人事部模块(一期已上线,二三期待做) | `人事部功能设计.md`(花名册/证件/部门月度工资总额→毛利人工分摊/请假台账,分三期) |
| 部署/运维/应急 | `OPERATIONS.md`(全新部署 10 分钟流程、备份恢复、升级回滚)+ `DEPLOY-*.md` |
| 运维脚本 | `ops/` 目录(每个脚本头部有用法注释),`ops/README.md` |

## 二、开发工作流约定(Claude Code 会话)

### 分支与推送

- 开发分支:当前用 `claude/elegant-newton-vibso6`,同时推 `main`(用户习惯直接从 main 部署)。
  新会话会分配新的 claude/* 分支,沿用"开发分支 + main 双推"即可。
- **推送方式**:沙箱默认的 git 代理是只读的(push 返回 403)。可用方案是 PAT 直推:
  1. 仓库根目录建 `.env.local`(已在 .gitignore),内容一行:`GITHUB_PAT=github_pat_xxx`
     (**接手者用自己 GitHub 账号新生成 PAT**,旧 PAT 不随仓库/容器转移,也不应共用);
  2. 推送命令模式:
     ```bash
     set -a; source .env.local; set +a
     GIT_CONFIG_COUNT=0 GIT_TERMINAL_PROMPT=0 \
       git push "https://x-access-token:${GITHUB_PAT}@github.com/<owner>/excel-share-system.git" \
       "HEAD:main" "HEAD:refs/heads/<开发分支>" 2>&1 | sed -E 's#x-access-token:[^@]*@#x-access-token:***@#g'
     ```
     (末尾的 sed 是为了不在日志里泄露 token)
  3. 推送前检查快进安全:`git merge-base --is-ancestor origin/main HEAD`。
- **绝对不要**把 `.env.local`、PAT、任何密钥提交进仓库或写进代码/文档。

### 修改后的验证习惯(教训换来的,别省)

- 前端:`cd frontend && npx vue-tsc --noEmit` 必须过。
- 后端:**新写/改动的接口必须实际调用一遍**,不能只靠类型检查。
  (教训:`/sales/equipment-names` 曾引用不存在的 `SalesLedger.name` 字段上线才炸——
  设备名在 `Project.name` 上。)
- 后端改完至少要**真实 import 起一次服务**,`ast.parse` 只查语法查不出 NameError。
  (教训:d884356 里 `_DICT_ADMIN_ROLES` 定义在使用之后,Depends 默认参数 import 时求值,
  整个后端启动即崩,ast.parse 全绿。本机无 3.12 时可用 py3.9 venv + 给副本统一加
  `from __future__ import annotations` 起服务验证,见 2026-07-11 批次记录。)
- **涉及 SQL/schema 的改动要在真 Postgres 上验证**,不能只跑 SQLite(见第三节第 1 条)。
  沙箱里起真 PG:`service postgresql start`,已有测试用户 `pms`/`pms`,
  可 `su - postgres -c "psql -c 'CREATE DATABASE xxx OWNER pms;'"` 建临时库。
- 后端测试在 `backend/tests/`,可参考 `test_e2e_full_lifecycle.py` 的写法。

### 部署提醒(每次推完代码要告诉用户)

- **🆕 首选:用户在自己电脑跑 `bash ops/release.sh`**(2026-07-12 起)——本地一键 SSH 发版:
  自动 SSH 进服务器执行 `ops/upgrade.sh`(备份→拉码→重建→健康检查→失败自动回滚)。
  首次要 `cp .deploy.local.example .deploy.local` 填服务器信息(SSH 密钥免密,`.deploy.local` 已 gitignore)。
  用法见 `ops/README.md`「本地一键发版」。**推完代码提醒用户跑 `bash ops/release.sh` 即可**。
- 手动等价动作(用户直接在服务器上执行):
  ```bash
  git pull
  docker compose -f docker-compose.prod.yml --env-file .env.prod up -d --build
  ```
- **改了前端、或 backend/requirements.txt 加了依赖 → 必须 `--build` 全量重建**,只 restart 不生效。
  (release.sh 走的 upgrade.sh 默认就带 `--build`,已覆盖此要求。)
- 只改后端 py 代码理论上也走 `--build`(镜像里打包代码,不是挂载)。

## 三、生产环境踩坑记录(按重要性排序)

1. **SQLite vs Postgres 差异是本项目最大的隐性雷区**(沙箱开发用 SQLite,生产是 Postgres):
   - SQLite 不校验 `VARCHAR(n)` 长度,PG 严格校验。踩过:`material_dict.dtype VARCHAR(16)`
     存 `'supplier_category'`(17 字符)在生产炸掉。已修(现为 32)+ 生产已手工
     `ALTER TABLE material_dict ALTER COLUMN dtype TYPE VARCHAR(32);`(一次性,勿重复)。
   - PG 严格要求 ORDER BY 列出现在 GROUP BY 或聚合中(GroupingError),SQLite 不管。
     踩过:`oa_summary` 按 `Department.sort_order` 排序但没进 GROUP BY,生产 500。
   - **规则:任何带 GROUP BY/ORDER BY 组合、或新增字符串列的改动,上真 PG 跑一遍。**
2. **时区**:容器日志是 UTC,服务器/诊断包时间是 CST(UTC+8)。对日志时间时记得 +8
   (日志里 15:15 = 北京时间 23:15)。
3. **healthcheck 噪音**:`pg_isready` 不带 `-d` 时 dbname 默认=username,曾造成日志刷
   378 条 `FATAL: database "pms_prod" does not exist`。docker-compose 两个文件的
   healthcheck 都已加 `-d ${POSTGRES_DB}`,改 compose 时别丢掉。
4. **诊断脚本必须带超时**:`ops/diagnose.sh`/`health-check.sh` 里所有 docker/psql 调用
   都包了 `timeout -k 3 <N>`、curl 带 `--connect-timeout/--max-time`。
   原因:被诊断的服务卡死时,不带超时的诊断脚本会跟着一起卡死。新加诊断命令保持这个模式。
5. **中文 PDF**:服务端 PDF 用 reportlab 内置 CID 字体 `STSong-Light`,无需外部字体文件。
   沙箱里 pdftoppm 渲染中文报 "Missing language pack for Adobe-GB1" 是渲染器缺
   `poppler-data`,不是 PDF 本身的问题。
6. **金额字段全是 float**:增量清单 M10 就标注过应改 Numeric/分存储,一直未做。
   做财务精算类功能时注意累计误差,新表建议直接用 Numeric。
7. **WebSocket 的广播跨不过 worker —— 协同编辑实时同步大约一半时候是失灵的**
   （2026-08-15 查 redis 时顺带发现，**尚未修，用户说先放着**）。
   生产跑 `uvicorn --workers 4`（4 个独立进程），而 `ws_router.ConnectionManager.rooms`
   是个**进程内的普通 dict**——广播只到得了同一个进程里的连接。
   线上实测（`/ws/datasheets/{一个没人用的房间号}`，两人先后进同一个房间，跑 20 轮）：
   **A 收到「B 进来了」11 轮、丢 9 轮 = 送达率 55%**。
   最坑的是它**完全静默**：格子就是不动，人只会以为对方还没改、或者以为系统卡了，
   不会来报 bug——所以这个问题能一直藏着。
   两条修法（都需要一个进程外的中转）：① redis pub/sub（容器现成的，见下）；
   ② Postgres `LISTEN/NOTIFY`（少一个组件，但要单开一条常驻连接监听）。
8. **redis 是残留，不是这套系统的一部分**（2026-08-15 查证）：
   不在 `docker-compose.prod.yml` 里（所以每次发版 compose 都报 orphan container 警告），
   代码里从没 import 过（只有 `config.py` 一行没人读的 `redis_url`、
   `requirements.txt` 一条没人用的依赖）。线上实测：跑了 3 周、`DBSIZE=0`、`keyspace_hits=0`。
   **排障别去查它**。要么删掉，要么拿去修上面第 7 条——用户 2026-08-15 决定先不动。
   ⚠️ 它 `maxmemory=0`（不限内存）：现在没人写所以没事，哪天真启用**必须先设上限**。

## 四、生产排障流程(用户报"服务器报错"时)

1. 让用户在服务器跑 `bash ops/diagnose.sh`,会产出 `pmsdiag<时间戳>.tar` 诊断包
   (含各容器日志、健康检查、可能附带报错截图 base64)。
2. 拿到包后:解包 → 后端日志里 **grep `未捕获异常`**(这是 500 traceback 的可靠锚点),
   截图类文件是 base64,解码出来看。
3. 定位后:本地复现(优先真 PG)→ 修 → 推 main → 提醒用户 pull + rebuild。
4. 常用:`ops/logs.sh`(看日志)、`ops/db-shell.sh`(进库)、`ops/health-check.sh`(体检)。

## 五、关键业务约定(代码里不明显、口头确认过的)

- **采购员分表权限(R4)**:`fangbusen`=外协;`wangqin`=不锈钢+激光;`lixinxin`=标准件+电工;
  其余(采购主管/管理员)全可见。"从清单下单"的清单类型下拉和默认值都跟这个权限走
  (与采购部项目一览的可见列权限一致)。
- **采购员"谁建谁看"(需求五, 与 R4 并存但语义不同)**:`_buyer_restricted(u)` = 有 `buyer` 角色
  且无 `buyer_lead/finance/admin/manager`。命中者:采购明细按 `buyer_id`、供应商按 `Supplier.created_by`、
  汇总报表按 `buyer_id` 只看自己建的;管理层看全部。**遗留供应商 `created_by=NULL` 对所有采购员可见**
  (否则老供应商没人能选)。R4 是"按清单类型可见列",需求五是"按创建人行级隔离",两者叠加。
  - **请款记录按下单采购员隔离(2026-07-08)**:`PaymentRequest` 无 `buyer_id`,请款列表
    (`GET /payment-requests`)与 overview 待审计数原按 `requester_id`(发起人)过滤,现改为经
    `PaymentRequestItem.item_id → PurchaseItem.buyer_id` 反查——"单子的采购员是本人"即可见
    (覆盖代发起/一单混多采购员场景;任一关联明细是本人下的单就可见)。口径与采购明细/供应商账目一致。
- **安装/售后统一(需求一)**:`aftersales.kind` = `aftersales`(售后,problem 必填) / `install`(安装,
  problem 选填,空则存"安装登记")。同一张表、同一审批流、同一财务同步;财务口径叫"安装/售后费用"。
- **请款审批↔付款职责分离(需求十六)**:`/payment-requests/{id}/pay` 校验 `finance_approver_id != current.id`,
  审批人不能给自己审过的单付款(内控)。付款弹窗展示供应商 bank_name/bank_account/tax_no + 关联采购单(po_no)。
  若财务只有 1 人,审批后需 admin/manager 或另一财务付款。
  - **财务页请款/付款拆两个 tab(2026-07-08)**:`FinanceView.vue` 原 `pay_requests` 单 tab 拆成
    `请款审批`(name=`pay_requests`,状态 pending/approved/rejected,不含已付款;操作:审批通过/拒绝/删除)
    + `付款`(name=`pay_payment`,状态 approved待付/paid已付;操作:记录付款/删除)。后端端点不变,纯前端按状态分流。
- **物料需求领用出库(需求二)**:`/wh/demand/{pid}/issue` 按 lines(material_id+qty)登记 `out` 流水
  (source=领料出库, project=项目号),超现存自动截断。采购收货→自动入库(`_auto_stock_in`)本就有,这是补出库侧。
- **仓库一键清空(需求十五)**:`/wh/clear-all` 需 body `confirm=="清空仓库"`,清空 wh_txns+wh_materials
  (不动供应商/采购/项目/字典),仅 warehouse_lead+管理层。前端 prompt 要求输入确认词。
- **库存/成本仅管理层(需求六)**:`/wh/inventory-value`、`/wh/project-cost` 收紧为 `require_roles()`
  (只 admin/manager);财务页"库存/成本" tab 也 `v-if isManager`。财务(非管理层)看不到。
- **开票号(需求十三)**:`/items/set-invoice-no` 对多条明细统一 `invoice_no`,并把各行
  `invoice_amount = received_amount`、状态置"已开票"。前端在采购明细勾选行(含合并父行)→"维护开票号"。
- **合并零件收货只填总价(需求四)**:`/items/receive-batch` 传 `total_amount` 则按各行 `qty` 权重分摊到
  `received_amount`(末行兜余数),单价=金额/数量;或传 `lines` 逐行填价。收货单(图片)走 `/items/{id}/receipt`。
- **"合并父行"是前端虚拟分组,后端无父行字段**:`PurchaseItem` 无 `parent_id/is_merged`,同一采购单多零件行
  共享 `po_no`,前端按 `po_no` 分组(≥2 行成可展开父行,单行/无 po_no 散单平铺)。采购明细页
  (`PurchaseMgmtView.vue` `groupedItems`)与**仓库采购收货页(2026-07-08 新增,`WarehouseView.vue` `groupedRecv`)**
  用同一套分组:父行聚合数量/金额,送货单号/到货日期/开票号/开票日期多值时显示"多个"。存量数据靠 `po_no` 自然分组,无需迁移。
  - **仓库收货合并(需求二)**:采购收货列表按 `po_no` 合并;父行「合并收货」按钮直接对整单各零件行走
    `/items/receive-batch`(送货单号/到货日期/收货单图片统一维护写整批),叶子行仍单条收货。选择列对父行禁用
    (`:selectable`),避免选中虚拟父行。
  - **开票号/开票日期上主汇总(需求三)**:`invoice_no`/`invoice_date` 字段/端点(`/items/set-invoice-no`)/表单
    本已打通,本次补:①`invoice_date` 登记进 `_NEW_COLUMNS`(存量 PG 库自动 ADD COLUMN 防缺列);
    ②合并父行聚合展示 `invoice_no`/`invoice_date`(原父行开票号列写死空,已改为显示聚合值/"多个")。
- **受管理字典**:`material_dict` 用 dtype 区分 category/unit/material_grade/supplier_category,
  物料类别与供应商分类共用一套,管理层在界面维护。
- 采购单号前缀是 `TH`(曾是 CG,已改)。
- 出库"领用项目"**必选**(2026-07-11 起):либо选项目либо勾「非项目领用」+原因(写入 source/party)。
  历史无主出库在财务部「成本审计」页可事后补选项目归集。
- 项目↔销售台账严格 1:1(`uq_ledger_project`);销售下单一个事务建
  Project+SalesLedger+Shipment+各部门 DeptOrder。

## 六、当前状态与待办(2026-07-16)

- **📮 #410 / #411 / #412（2026-08-25）**

  - **#411（李新新）采购下单加物料联想** —— 和 **#410（王利利）「入库不会到仓库生成的物料里」是同一件事的两头**。
    收货过账是按「名称完全相等 + 规格完全相等」找物料的（`purchase_mgmt_router` 收货那段），
    找不到就**新建一条**。所以采购手打的写法只要差一个字、或规格一个填一个空，就多出一条重复物料。
    线上实测：采购明细 1962 条里 **200 条**的「名称+规格」在物料主数据里对不上；
    仓库已攒出 **10 组**「同名、一条规格空一条有」的重复，**其中 9 组带着流水或库存**（不能直接删）。
    做法：`/wh/materials/suggest` 从「只按名称搜」扩到**名称+规格都能搜**，
    采购下单弹窗的「名称」「规格型号」两列都换成 el-autocomplete，选中把两个字段一起补齐。
    ⚠️ el-autocomplete 的 `item.value` 必须等于它绑定的字段，所以名称列和规格列**要各用一个取数函数**
    （共用一个的话，在规格列里选中会把规格写成名称）。
    ⚠️ **只堵住了新增，存量那 10 组没动** —— 合并要搬流水、动库存和成本，得业务方拍板。
  - **#412（杨坛）OA 加「已付款」** —— 查下来这套东西**几乎全都已经存在**：
    待付款状态、mark-paid 接口、付款备注、回单上传、前端弹窗全是现成的，
    唯一的问题是 `mark_paid` 最后把状态**写回了 approved**，于是付过款的和只是批过的单
    在列表上一模一样（线上 **44 单**填了 `pay_at`、状态却都是 approved）。
    改动：① 状态留在 `paid`；② 对公必须传凭证、现金/对私不强制；③ 存量 44 单按
    `pay_at IS NOT NULL` 回刷（`backfill_oa_paid_status`，幂等）。
    ⚠️⚠️ **加新状态最危险的是漏改按 approved 取数的白名单** —— 漏一个就是「钱花了但报表里没有」。
    这次一共四处：`oa_router` 的 `/reports/summary`、`summary/detail`、`_OA_COST_STATUS`，
    以及 `finance_router` 的支出总览。抽了两个常量 `_OA_DONE_STATUS` / `_OA_COST_STATUS` 统一。
    `_OA_COST_STATUS` 那条注释本身就是上次同款事故留下的，**这是第二遍**。
    ⚠️ 回刷必须**排在白名单改好之后**：反过来的话 44 单的钱会当场从财务报表里消失。
    ⚠️ `reports_router` 的 13 周现金排程读 `pending_payment`，**刻意不动**——付掉了就该退出待付桶。
    测试里那条财务断言特意验过"能抓到漏改"：把白名单改回只认 approved，当月 OA 支出立刻从 1500 变 0。
    ⚠️ 既有的 `tests/test_oa_payment_request.py` 断言的是旧行为（标记已付款→已通过），
    本次一并更新到新口径——**不是测试挂了去改测试，是行为按反馈有意改了**。

- **📮 #408 / #409（赵仁辉）：一条是排序，一条是「东西没丢，只是没入口」**

  - **#409「之前反馈的问题都看不到了」——他没说错。** 工作台那块反馈面板只取
    「**待**我处理」的（设计师看 `pending_design`、管理层同样只看 `pending_design`），
    设计师一接收或驳回，那条就从面板上彻底消失，**界面上再没有任何入口**。
    线上 11 条反馈里 10 条是 archived / rejected_by_design，他只看得见 1 条。
    加了 `include_done=true` 和面板上的「看已处理的」勾选框。
    ⚠️ **这里最容易做错的是顺手把可见范围也放宽**：看历史只该放宽**状态**，不该放宽
    **能看谁的**——写成无条件放行的话，设计师一勾就能翻到同事名下的全部反馈。
    实现是逐角色分开写的：设计师仍限 `designer_uid == 我`、生产三组仍限 `created_by == 我`、
    只有设计负责人/管理层才是全量（他们本来就是全局视角）。测试
    `tests/test_fb409_feedback_history.py`（8 条）主要就是焊这条边界。
    待处理的几条永远排在最前，翻历史时别把要办的事埋掉。
  - **#408「未排序」**：财务「库存 / 成本」页签里两张表（库存金额、项目材料成本）
    默认按金额从大到小——这个默认是对的（先看占钱多的），但他要按项目编号找就只能一行行翻。
    给两张表都加了可点表头排序，**默认顺序不动**。
    ⚠️ 这两张都是模板列，`sortable` 必须配 `prop`（`code` / `cost` / `value`…），
    光写 `sortable` 排不动——加完在浏览器里真点了三次（默认→升序→降序）确认换序才算数。

- **🖥 服务器加了 2 GB swap（2026-08-24，用户拍板）**
  这台机器只有 3.5 GB 内存、原来**一点 swap 都没有**，历史上累计 9 次 OOM 强杀进程。
  ⚠️ **但 swap 不是主因的解药**：主因是发版堆下来的 437 个悬空镜像——dockerd 一扫就膨胀到 2.4–3 GB。
  8-15 清完镜像后**连续 8 天零 OOM**（期间发了好几轮版），平时内存只用 40%。
  所以 swap 的定位是**保险**（防发版 `--build` 那一下的瞬时尖峰），不是"内存不够要补"。
  `vm.swappiness` 一并从 0 调到 10——0 的话内核几乎不主动用 swap，等于只在最后一刻才顶上。
  ⚠️ **动 /etc/fstab 之后必须验**：`findmnt --verify` + `swapon --all`（幂等性）。
  那行写错下次重启就起不来，云主机上很难救。当时两条都验过，原 fstab 也备份了。
  查状态：`swapon --show`、`cat /proc/sys/vm/swappiness`。详见 OPERATIONS.md。
  **还没做**：容器 `mem_limit`；备份自动传云（要 bucket + 密钥）；用户电脑的 FileVault（得他本人开）。

- **📮 #407「怎么不能整单维护，都已经签收，已经填写价格」——她没说错，是提示在胡说**
  她勾同一供应商 22 条明细点「维护开票号」，被挡：「所选含 2 项尚未收货（收货金额为0）」。
  查生产：那 2 项（脚轮 2.5寸低重心）**到货日期确实填了**，缺的是收货金额——连单价都是空的。
  两个毛病缺一不可修：
  · **「尚未收货」措辞误导**：她签收过，看到这四个字只会觉得系统在胡说，压根不会想到是金额没填；
  · **只报个数不报是哪几项**：22 条跨 6 张采购单、要翻屏，根本找不出来。
  改成分清两种情况 + 点名到具体零件（名称·规格（项目编号）），前后端同口径：
  · 「已到货但没填收货金额」→ 去这几行「编辑」补单价或收货金额
  · 「还没收货」→ 先去「采购收货」收掉
  前端从一闪而过的红条 toast 换成 `ElMessageBox.alert`（能停住读、能列多行）。
  **生产体量**：「到货了但金额为0」156 条（其中 155 条连单价都没有）、「压根没到货」299 条——
  两种都不罕见，所以必须分开说，动作完全不同。
  测试 `tests/test_fb407_invoice_no_hint.py`（11 条）。
  ⚠️ 写这个测试踩的两个坑：① 端点是 **POST /items/set-invoice-no**、字段叫 `item_ids`，
  写成 `/items/invoice-no` 会被 `/items/{iid}` 抢先匹配、报「iid 必须是整数」；
  ② `PurchaseItemCreate` **没有 arrival_date 字段**（到货由收货流程写），建单时传了会被静默丢掉，
  要造「已到货但没金额」的样本只能直接改库。

- **📮 #405 请款自助撤销（李新新）已上线 —— 三路评审否掉了第一版方案，这里记的是为什么**

  原方案是「加一个 `cancelled` 软状态」。三路独立评审（财务口径 / 并发越权 / 一线使用）
  互不通气却撞在同一批问题上，**该方案在她本人的场景里比现状更差**：

  | 问题 | 后果 |
  |---|---|
  | `delete_item` 的校验**数关联行、不看状态** | 软状态留着关联行 → 她撤完照样删不掉采购明细，报错还把她指回请款记录（她刚从那儿来）——死循环 |
  | 允许撤「已批」 | 捅穿 #177（系统里唯一那道防重复付款闸门只认 pending/approved），撤掉重提 = 同一批明细可能付两次 |
  | 复用 `reject_reason` 三列 | 她的撤销原因在财务屏幕上渲染成「**拒绝**：客户改方案…」，归因反了；在她自己屏幕上是「—」 |
  | 全后端 0 处行锁 + 4 个 worker | 「先查状态再删/改」在并发下会动到已被审批的单 |

  **最终最小实现（用户拍板：先最小化，后面有要求再加）**：
  `DELETE /purchase-mgmt/payment-requests/{id}/self-cancel`，**只有「待审 + 本人发起」能撤，撤=物理删除**。
  - **为什么物理删除**：她说"临时不买了"，那条采购明细通常也要跟着删。删掉关联行，
    明细自然回「未付款」，#177 也自动放行，`delete_item` 的死循环不存在。
    代价是列表不留痕，只有审计日志记着——与财务现有的删除口径一致。
  - ⚠️ **真正的并发闸门是那条带 status 条件的 DELETE + rowcount 判定**，不是前面那次 SELECT。
    前面的 SELECT 只负责生成人话报错。审批若先落库，DELETE 匹配 0 行 → 409 让她刷新，
    而不是把一张已批的单删掉。测试里直接验了这条语句的语义。
  - 权限只认 `requester_id == current.id`，**不按"关联明细都是我的"判**：主管能引用别人的明细建单，
    那样算下级就能撤主管发起、财务已批的单；空明细单还会让集合判空退化成放行。
    生产实测 92 张单 requester 与明细采购员 100% 一致，用 requester_id 足够。
  - 撤销后补推一条给 `finance`（会扇出 finance_lead）：建单时推过「待审批」，
    而系统**没有消息撤回机制**，不补的话财务点进那条通知只会看到一张不存在的单。
  - 已批/已付仍走财务（withdraw-approval / 财务删除）。生产实测提交→批准中位 27 分钟，
    但 40% 超 1 小时、17% 超 4 小时，待审窗口够用。
  - 测试 `tests/test_payment_self_cancel.py`（18 条）。
    ⚠️ 其中「已付不许撤」那条一开始是**假通过**：付款那步失败了（`/pay` 收的是 **Form** 不是 JSON），
    单子还是 approved，走「已批」分支照样 400。已加前置断言坐实付款真的成功。

  ⚠️ **批量跑测试别用「grep 输出里有没有 PASSED」**：本仓库测试文件的收尾文案有
  `PASSED` / `ALL PASS` / `✅ 全部通过` / 无输出（pytest 风格）四种，grep 会把一堆好的判成失败
  （这次一度报 27 个失败，实际只有 14 个，且全是既有陈旧用例）。**用退出码**。

- **🐞 受限采购员打开 AI 卡片/每日简报直接 500（2026-08-22 查请款反馈时顺手发现）**
  `app/agent/cards/pay_req.py:59` 写的是 `PaymentRequestItem.purchase_item_id` —— **这个属性不存在**
  （该表只有 id/request_id/**item_id**/allocated_amount；`purchase_item_id` 是 `WhTxn` 的列，models.py:421）。
  ⚠️ **为什么能潜伏这么久**：① 导入不报错，SQLAlchemy 只在真取属性时抛 AttributeError；
  ② 整段包在 `if _buyer_restricted(current):` 里，**管理员走不到**，用管理员跑的测试和手工验证全绿；
  ③ 同文件 133 行早写过「关联字段是 item_id，不是 purchase_item_id」的警告，下面那段改对了，59 行漏了。
  生产实测：王芹、李新新（提这轮反馈的两个人）调用即炸，超级管理员正常。
  测试 `tests/test_agent_cards_buyer_scope.py` —— ⚠️ 它用 `raise_app_exceptions=False`，
  因为要断言的就是"不能 500"；用默认值的话未捕获异常会把整个测试文件掀掉，看不出是哪条挂的。
  **写这类隔离分支的测试，账号必须真的是受限角色**，否则等于没测。

- **📮 #406 请款记录搜索（王芹）已完成**；**#405 请款撤销（李新新）经评审后暂缓，见下**
  - #406：前端本地过滤（供应商/采购单号/项目编号/备注/金额/申请人/编号）。
    ⚠️ **一输入关键字就把状态页签跳回「全部」**：`prStatusFilter` 是普通 ref 会停在上次选的页签上，
    而生产上「待审/已驳」长期为 0，用户停在空页签上搜索会看到空表格 + 一句"怎么发起请款"的空态，
    只会以为搜索坏了。空态也按有无关键字分叉。
    ⚠️ 页签计数**跟随关键字收窄**（不是 #404 那种静默收窄）：旁边显示「命中 3 / 全部 11」。
    若计数保持全量，搜「俊帆」会看到「已付 (89)」但表格只有 11 行，那才是真的对不上。
    ⚠️ 那句 36 字的长提示挪进了问号 tooltip —— 1366px 笔记本上 radio+搜索框+长提示会把 filter-bar
    挤成两行，而表格高度是写死的 `calc(100vh - 300px)`，多一行就捅出视口变双层滚动。
    实测 1366×768：filter-bar 单行 30px，表格底 623 < 视口 768，不溢出。

- **📮 反馈：「另有 2 条无法安全展示」是什么意思（2026-08-26，改文案）**
  `H5ChatView.openApprovals` 里 `dropped = 后端给的卡片数 − 前端 `isKnownCard` 认识的数`。
  未登记的卡片类型整张不渲染（原则三），本身是对的。
  ⚠️ 但**文案是错的**：「无法安全展示」听着像出了安全事故，用户当场就来问了。
  实际原因几乎总是**版本差**——后端上了新卡片类型（这次是 `oa_approve`），
  手上的前端还不认识它。改成说清楚是新类型 + 怎么办（重开 APP / 刷新 / 去电脑端）。
  ⚠️ 以后**每加一类卡都会再触发一次**：后端先上、客户端后到，中间那段时间必然对不上。
  加新卡类型时记得同一批把 H5 发出去（网页版随 release、APP 走 `mobile/ship.sh` 且要开两次）。

- **📮 反馈：智能体的审批只有采购请款，OA 申请不显示（2026-08-23，已修）**

  根因很直接：`/api/agent/cards/pending` **写死只装 `assemble_pay_req_cards`**。
  ⚠️ **待办是按人算的，不是按模块算的** —— 他打开「待我审批」要的是今天该他批的
  **全部**，不是某一个模块里的那几张。现在改成按 `_PENDING_KINDS` 逐类装配，
  以后加类别往那个元组里加一行即可。
  ⚠️ 逐类 `try`：某一类装配炸了不能把整页带塌——那样用户是「审批打不开」，
  比少一类严重得多。

  **新增 `oa_approve` 卡**（`agent/cards/oa_req.py`）：
  - 端点 `PUT /oa/requests/{id}/approve` / `reject`
  - ⚠️⚠️ **权限判定复用 `oa_router._can_act_on_step`，一行都不重写。**
    那条规则三条路径且顺序敏感（没指定人按角色 / 指定了人本人+admin+manager 兜底 /
    代理人还要等满 `OA_DEPUTY_TAKEOVER_DAYS`=3 天，且 `activated_at` 为空时**不算**很久以前）。
    抄松了 → 卡上有按钮点下去 403；抄紧了 → 该他批的单在手机上看不见。
  - ⚠️ OA 是**多步**审批：`ref` 是申请单 id，但能不能批取决于
    `current_step_order` 指的那一步，判定必须拿**当前步**去问。
  - ⚠️ **批不了的不出卡**。请款那边刻意保留「自己提交的」并把按钮置灰（怕用户以为单子丢了），
    但那是**他自己的单**；OA 指定给别人的是**别人的活**，摆他手机上只添乱。
  - ⚠️ 只取 `status='pending'`。`pending_payment` 走的是 `mark-paid`，
    动作不是一套，混进来卡上会出现点了必然 400 的按钮。
  - ⚠️ `reject` 必须标 `needs_reason`：`OaRejectIn` 是 `min_length=1`，
    不标前端不弹输入框，点下去 422。
  - 提示做了三条：等 ≥7 天标出来；指定给别人而自己以管理层身份代批要说明；
    最后一步是财务的要提醒「批完进待付款，财务还要再点一次已付款」。

  **🚫 `mark-paid`（财务标记已付款）不做**，三个理由：
  1. 端点是 **multipart + 文件上传**（`Form()` + `File()`），卡片通道发的是 JSON，接不上；
  2. 这一步的价值恰恰在于**同时传付款回单**（反馈#395 就是为这个加的），
     手机上标已付款却不传回单，等于把刚补上的功能又绕过去；
  3. 生产当前 `pending_payment` = 0 条。

  文案同步：门户磁贴 desc 改成「待你审批的请款单和 OA 申请」；
  `CARD_ENTRIES` 加「待我审批的」「OA审批」（⚠️ 仍是**精确文案**，
  别改成模糊匹配——改过一次，把用户自己打的「查询一下所有的待审批的待办?」也劫持了）。

  测试：`tests/test_agent_oa_approve.py`（26 项断言：不重写权限谓词、指定给别人的看不见、
  管理层兜底能批但标明代批、申请人自己看不到、已批完/待付款不出卡、两类合并计数与金额）。

- **📌 「库存总览 / 出库下拉 要不要含项目物料」——两个答案不一样（2026-08-19 用户确认）**

  | | 含项目物料吗 | 为什么 |
  |---|---|---|
  | 库存总览 / 库存金额 | **不含** | #373/#374 口径：钱在收货那刻已计入该项目，再算一遍就是重复 |
  | **出库下拉** | **含** | 去掉的话仓库出不了货，见下 |

  ⚠️ **别想当然把项目料从出库下拉里去掉**。生产实测（2026-08-19，700 笔出库流水）：

  | 出库通道 | 项目物料 | 通用物料 |
  |---|---|---|
  | 物料需求「一键领用出库」 | 280 笔 | 14 笔 |
  | **出库登记（手填）** | **249 笔** | 157 笔 |

  对项目物料的出库有 **47% 走的是出库登记**，含当天的单子。去掉 = 仓库第二天就出不了这些货。
  （另：399 个有货的项目料**全部**都能在某个项目的「物料需求」里找到，不存在料被卡死领不出来。）
  成本上也不会重复计——按物料整体归属，不按数量拆，没有"A 项目收的料被 B 项目领走各算一遍"的口子。

  **既然保留，就必须让人一眼看出这是谁的料。** 原来只有"所有入库都指向同一个项目"的才显示
  【项目编号】，生产上 399 个有货项目料里 **135 个是多项目收过货的、一个标签都没有**，
  在下拉里跟公司备货长得一模一样。现在 `WhMaterialOut.project_codes` 带出**全部**编号：
  · 单一项目 →【2026-067】（同时反显，出库自动带项目）
  · 多项目 →【项目料·9 个项目】，鼠标悬停看具体是哪几个
  · 通用料 → 无标
  实测覆盖 **399/399**，通用料 0 误标。测试 `tests/test_wh_project_material_tag.py`（15 条）。

- **🐛 发待办死循环 + APP 输入框被输入法盖住（2026-08-19 生产实证，已修）**

  **① 发待办死循环——「工具结果不进跨轮 history」**

  上线当天的日志：
  ```
  15:11:05  测试   → 出草稿
  15:11:11  今天   → 又出草稿
  15:11:17  确认   → 又出草稿
  15:11:24  确认   → 又出草稿      （management_todos 一条都没多）
  ```
  第一版做成「工具返回 `confirm_token` → 下一轮模型带回来才发」。
  ⚠️⚠️ **根因：单轮 ReAct 里工具返回会以 `role=tool` 回灌给模型，
  但下一轮前端只传 user/assistant 的文本，工具返回全丢**——
  模型下一轮**根本看不见那个码**，只能重新拟一份草稿。
  **任何「这轮给模型一个凭据、下轮让它带回来」的设计都不成立。**

  改法：草稿落库（新表 `agent_drafts`）+ 卡片按钮。模型**只能拟**，
  真正的写在 `POST /api/agent/drafts/{id}/send`，由用户点「确认发出」触发——
  和请款审批、销售订单审批同一条路（原则：模型不在写的链路里）。
  - 工具签名里**没有** confirm 参数，源码里也不建 `ManagementTodo`（测试断言了这两条）
  - 草稿卡随 SSE `done` 的 `cards` 字段下发；不加这一步的话模型拟完了没按钮可点
  - 同人同事重复拟只留最新一张卡（否则点两张发两条）
  - 点过 / 过期(15 分钟) / 别人的，一律拒；标记 `used_at` 与建待办**同一个事务**
  - ⚠️ `_stale()` 要统一时区再比：**开发 SQLite 取出来是 naive、生产 Postgres 是 aware**，
    直接比较 `TypeError`，本地全绿一上生产就 500

  **③ 头一回发待办的人拿不到快捷人选**（同批修）
  `mgmt_todo_peers` 原来只查「我自己派过的人」。admin 从没派过（30 条是赵仁辉发的），
  于是候选为空 → 模型只能说「你要派给谁？直接说名字」，**引导退化成打字**，
  正是这个功能想避免的。现在没有自己的历史就用**全公司被派过活的人**兜底，
  并用 `from_my_history` 标明来源，提示词也跟着换一句。

  **② APP 里拼音候选框盖住输入框**

  ⚠️⚠️ 根因不是 CSS，是 `MainActivity` 里的
  `WindowCompat.setDecorFitsSystemWindows(getWindow(), false)`（沉浸式）——
  **它会让 AndroidManifest 里的 `adjustResize` 失效**：窗口不再为输入法缩小，
  系统认为「你自己处理 insets」。于是 IME 直接盖在页面底部的输入框上。
  两头一起修：
  - 原生：`setupImeInsets()` 用 `WindowInsetsCompat.Type.ime()` 把输入法高度垫成
    content 的底部 padding。**必须用 `ime()` 不是 `systemBars()`**——ime() 给的是
    整个输入法窗口高度，**候选条算在里面**；只看键盘会短一截，差的正好是候选栏。
    还要减掉 `navigationBars()`，那块 H5 已用 `env(safe-area-inset-bottom)` 垫过。
  - H5：`useViewport.ts` 用 `window.visualViewport` 驱动面板高度（走热更新，
    不用换 APK 就能生效），顺带把消息列表顶到底。取不到该 API 就什么都不做，
    保持 `100dvh` 兜底。

- **📌 管理层下发待办进智能体·第二批：在手机上发待办（2026-08-19，本次）**

  ⚠️ **范围就是杨坛和赵仁辉两个人**，收件人那一侧（回承诺/报进展/点完成）**不做**——
  企微应用的可见范围只有他俩，方步森、夏锟这些收件人根本打不开。

  **为什么不做「一句话解析」**：赵仁辉真实发过的 30 条，标题**平均 8.1 字、最短 2 字**
  （就是「采购」两个字）。他打的是**事项名**——没有人、没有时间，常常连动词都没有。
  所以改成**缺什么问什么、让他点**：
  - `mgmt_todo_peers` 给最近派过的 4 个人当快捷选项
    （11 个人分布很平 5/5/4/3/3…，没有压倒性常用人，按**最近**排比按最多排准）
  - 截止日快捷项固定「明天 / 后天 / 本周内 / 自己填」
    （87% 在 5 天内，明天 11 条、后天 8 条占一半还多）
  - 补充说明**不主动问**（30 条里 6 条根本没写正文）

  ⚠️⚠️ **这是整个智能体唯一一条写路径**，所以做成**两步确认**：
  第一次调 `mgmt_todo_send` 只出草稿、不写库，返回 `confirm_token`；
  模型把草稿念一遍让用户点确认，再把令牌原样传回来才真发。
  - 令牌见 `agent/confirm.py`：HMAC **绑内容**（不是绑记录 id——草稿阶段还没有行），
    模型编不出来；改一个字签名就对不上，也不能拿确认 A 的码去发 B。10 分钟过期。
  - ⚠️ 还要**防重发**：`_run_tool` 的去重只在本轮生效，下一轮模型拿同一个码再调
    就会凭空多出一条一模一样的待办、收件人再收一条通知。
    加了「同人同事同收件人 10 分钟内已发过就认作重复」。
  - ⚠️ 停用的人不能当收件人：创建接口会把他滤掉，表现是「发了但没人收到」，
    比报错还难查。
  - ⚠️ 人名匹配到多个（「夏」→ 夏锟/夏波）**给候选让用户挑，绝不猜**。
  - 通知文案与 `management_todo_router.create_todo` **保持一致**——
    收件人不该因为「管理层是在电脑上发的还是手机上发的」收到两种不同的通知。

  ⚠️ 系统提示词第 5 条从「只读」改成「除了下发待办一律只读」，
  并写清两步确认的流程。改这条时务必连带更新，否则模型会按老规矩拒绝用户。

  测试：`tests/test_agent_mgmt_send.py`——重点不是「能不能发出去」，
  而是「**能不能在没确认的情况下发出去**」（答案必须是不能）：
  草稿阶段零写库、篡改内容令牌作废、编的令牌不认、别人的令牌不认、
  换个动作不认、重复发认得出、非管理层一步都走不了。
  全量 111 通过 / 13 失败（存量）。

- **📌 管理层下发待办进智能体·第一批：盯 + 批顺延（2026-08-19，本次）**

  **依据**（生产库全量 35 条待办 / 38 条收件记录，不是估的）：

  | | |
  |---|---|
  | 谁在用 | 赵仁辉 30 条、杨坛 5 条；29 条填了截止日、9 条标紧急 |
  | 断在哪 | **7 条顺延申请挂着一条没批**、5 条承诺日已过、3 条根本没回复 |
  | 收件人 | 11 个人，方步森/夏锟/宫振兴/程言锋这些要跑车间 |

  **做了两件**：
  - `tools_entity.mgmt_todo_watch` —— 我发出去的待办谁没回、谁超期、谁申请顺延。
    只看自己发的（不是权限问题，是相关性：赵仁辉 30 条混进杨坛 5 条里两人都得先筛）。
  - `cards/mgmt_extend.py` + `mgmt_todo_extend` 卡片 —— 顺延申请当场同意/驳回。
  - 门户加两张磁贴，admin/manager 默认门户带上。

  ⚠️⚠️ **ref 是 `target_id` 不是 `todo_id`**。一条待办发给多人时每人一行
  （`management_todo_targets`），各自独立承诺、独立申请顺延。用 todo_id 会批错人。

  ⚠️⚠️ **assembler 里必须自己挡权限**。`GET /agent/cards/{type}` 只做登录校验，
  权限一律由各 assembler 按 `current` 过滤（pay_req 按销售归属、ledger_settle 按可见范围）。
  顺延申请没有「归属人」概念，对应端点是 `require_admin_or_manager`，
  装配这边不加同款闸，**任何登录用户都能读到谁在拖、事项标题、申请理由**——
  批不了但看得见，照样是越权。已加 `current.has_role("admin","manager")`，并有测试。

  ⚠️ **申请理由是 `extend_reason`，不是 `progress`**。progress 是平时报的进展，
  跟「为什么要顺延」两回事；取错就是把无关的话当理由摆在决策卡上。

  ⚠️ **`admin` 与 `manager` 的默认门户必须完全一致**（`test_agent_tools_sales` 锁着）。
  给 manager 加磁贴时 admin 要同步加，否则那条不变量当场断——本次就先断了一次。

  ⚠️ 工具的门要和端点的门一致：`mgmt_todo_watch` 用 `has_role("admin","manager")`
  而不是菜单键判断，因为这个功能没有独立菜单键，而端点是 `require_admin_or_manager`。
  不一致的话模型调得到、接口拒绝，白跑一轮还报错。

  **🚫 明确不做「手动催办」**——查出来系统**每天已经在自动催**
  （`overdue.scan_mgmt_todo`，幂等键 `biz_type='mgmt_todo_remind'` + target_id + 当日）：
  实测推了 **289 条、261 条已读（90%）**。这跟「83 条推送没人理」完全不是一回事。
  在一条已经有人看的通道上再加个手动按钮，只是重复打扰。

  **另一个查出来的结论**：顺延申请提交时**是推给管理层的**
  （`request_extend` 里 `to_user_id=t.todo.created_by`，7 条消息对上 7 条申请）。
  所以那 7 条不是「没看见」，是**看见了没地方顺手批**——这正是门户入口要解决的。

  测试：`tests/test_agent_mgmt_todo.py`（越权、口径对齐端点、ref 不串行、
  承诺日优先于截止日、人排第一列）；全量 109 通过 / 13 失败（存量）；`vue-tsc` 无报错。

- **⚠️ 目录改名会打断 worktree 和软链（2026-08-19 踩到）**
  `Desktop/excel share` 改成 `Desktop/同辉智能管理系统` 之后：
  - worktree 的 `.git` 文件和主仓 `.git/worktrees/<name>/gitdir` 都记着**旧绝对路径**，
    worktree 里任何 git 命令直接 `fatal: not a git repository`。
    修复：主检出跑 `git worktree repair <worktree 路径>`。
  - `ops/worktree.sh` 建的软链（`backend/.venv`、`frontend/node_modules`、
    `desktop/node_modules`、`.env.local`、`.deploy.local`、`.deploy.local.ota.pem`）
    也全指向旧路径，`.venv/bin/python` 直接 no such file。
    修复：逐个 `rm` 掉重新 `ln -s` 到新主检出。

- **🐞 #404（王利利）「收货完了都不会及时更新，出库的时候啥也找不到」——一个数组喂了三个地方**

  根因不在收货、也不在刷新，在 `WarehouseView` 里 **`materials` 一个数组同时喂着三处**：
  物料主数据的表格、库存总览的表格+KPI、**出入库登记的物料下拉**。
  而物料主数据的搜索是**走服务端**的（`kw` 传后端），一搜就把这个共享数组换成命中的那几条：

  | 操作 | 后果 |
  |---|---|
  | 在物料主数据搜过「轴承」 | 出库下拉只剩 35 条，刚收的货根本不在里面 |
  | 勾了「只看缺料」 | 更狠——下拉里只剩缺料的 |
  | 收货后 `refreshAfterReceive` | 调 `loadMaterials` **又把同一个 kw 带上去**，刷了等于没刷 |
  | 切菜单再回来 | 组件重挂载、`kw` 归空 → 又好了（这就是她说的"点项目目录再点出库"） |

  ⚠️ **教训：服务端筛选的结果不能当共享数据源。** 拆成两份，各管各的：
  · `materials` = **全量、永不带筛选** → 库存总览 + 出入库下拉
  · `matList`   = 物料主数据页签自己的结果 → 带 kw/库位/只看缺料
  库存总览那个搜索框原来也绑着同一个 `kw`，改成**本地过滤**（289 行本来就在内存里）。
  ⚠️ 顺带把库存总览的 **KPI 和低库存清单固定成全量**：「库存总价」是管理层当公司库存值看的数，
  敲个搜索词它就变成命中那几条的合计，读的人不会意识到；「一键提采购申请」要提的也是全部缺料。
  搜索时旁边显示「命中 20 / 455（上面的合计仍是全部）」。

  测试 `tests/test_fb404_material_scope.py`（16 条）锁后端契约：不带参数=全量、
  带 kw/low_only 会缩小（所以**不能**拿去喂下拉）、刚收的货立刻出现在全量里、
  `scope` 默认 all（挂了项目的料也要能出库）。

- **📮 反馈批次 2026-08-18（#401/#402/#403）**

  | # | 提出人 | 做了什么 |
  |---|---|---|
  | 401 | 李新新 | 请购单加**需求时间**；采购端按它排序，过期标红、三天内标橙 |
  | 402 | 李昌奇 | OA 加**对私付款申请**（付给个人，走个人账户） |
  | 403 | 赵仁辉 | 管理层待办「下发/监控」加 未完成/已完成/全部 筛选 |

  - **#401 need_days 由服务端算，不要在前端各算各的**：仓库端、采购端、设计师端三处
    都要显示"还剩几天"，前端各写一份迟早对不上，而且客户端时钟/时区不一定准。
    需求时间**选填**——仓库低库存一键提的单本来就没有具体日期，强制填只会逼人瞎选一个，
    反而让"有日期"这件事不可信。采购端排序：待处理在前 → 填了日期的在前 → 日期早的在前。
    ⚠️ 加「需求时间」行让 PDF 抬头表里备注从第 2 行挪到第 3 行，`SPAN` 要跟着加一条，
    漏了打出来是歪的。
  - ⚠️ **#402 加单据类型必须连审批链一起加**：新类型没链，用户一提交就撞
    「『XX部』的『对私付款申请』尚未配置审批流程」——**功能上线即不可用**。
    这是 2026-08-07 拆对公/现金时踩过的同一个坑，见 `split_payment_doc_type`。
    新增 `seed_payment_private_flow` 从**对公**复制（生产实测对公有 20 条链、覆盖 7 个部门）。
    对私同样是转账 → 账号/开户行照样必填；但整张单把"收款单位"改口叫"**收款人**"
    （含报错文案和详情页），付给张三的单子却提示"请填写收款单位"，人会以为自己选错了类型。
  - **#403 是纯前端**：`done_count/total` 后端本来就有，判定"这条办完了"= 收件人全部完成。
    别在前端遍历 targets 自己数——那样"完成 2/3"和筛选结果会对不上。默认停在「未完成」。
  - **顺手修**：`WarehouseView` 里 `.danger` 类**从来没定义过**，模板却在用
    （「驳回：xxx」那处），scoped 样式不会去别的组件找 —— 那段红字一直没红过。已补。
  - 测试 `tests/test_fb401_fb402_fb403.py`（24 条）。
    ⚠️ `tests/test_wecom_embed.py` 是 **pytest 风格**，直接 `python` 跑它什么也不做、
    没有输出——用"跑完 grep 输出"那种土循环统计会把它误判成失败，`pytest` 跑是 8 passed。

- **📈 Agent 会画图了（2026-08-18，本次）**
  用户原话：「要让 Agent 会画图，不然还是答的太枯燥了」。

  **交期看板配了一张发散条形图**：中线是今天，左边超期（红）、右边还剩（绿/橙）。
  「哪些项目快到期了」这问题，一张图比十几行数字快得多——而这正是两位管理层都会点的入口。

  ⚠️⚠️ **后端只吐数据，SVG 由前端拼。** 这和 `[[danger:…]]` 语义着色是同一条纪律：
     H5 的 markdown 是 `html: false` + `v-html` 成对的，后端一旦开始吐 SVG，
     XSS 防线当场归零——而工具数据里带着用户自由填的备注、物料名、OA 详情。
     - 后端 `render.chart_block()` → ```` ```pmschart ```` 围栏 + **纯 JSON**
       （测试断言块里不许出现 `<svg`/`<text`/`<rect`/`</`）
     - 前端 `markdown.ts` 的 `chartPlugin` 按数据拼 SVG，**结构由代码写死**，
       来自数据的只有数字和文字，文字一律 `esc()`（浏览器里实测过 `<img onerror>` 只当字面量）
     - JSON 坏了 → **什么都不画**，绝不回吐原文

  ⚠️ **由工具声明 `chart`，不是模型决定**。同「明细该不该出表格」的判断：
     实测模型不会主动 opt-in（它同时被「务必简短」和「别自己打明细」约束着），
     而工具最清楚自己这份数据有没有可比的量纲。

  ⚠️ **SVG 必须封顶宽度**（`max-width: 340px`）。viewBox 是 320 宽靠 `width:100%` 自适应，
     手机上正好铺满，但容器一宽就等比放大：实测 981px 视口下放大 **2.55 倍**，
     图里 11px 的字变成 **28px**，旁边表格还是 12.5px，一大一小很难看。
     封到 340 之后是 11.7px，和表格对齐。

  其他口径：一张图最多 8 条（手机一屏）；只有 1 条不画（一根条子不成图）；
  取不到数的行直接丢（没填交货日期的项目不占格）；
  `True` 要挡掉（Python 里它是 int 子类，不挡会画成一根长度 1 的条）。
  测试：`tests/test_agent_chart.py`（11 例）。

- **🎯 按杨坛/赵仁辉的真实使用做的针对性优化（2026-08-18，本次）**

  **先拉了两人近 60 天的审计日志和全部问答记录**，不是凭感觉挑的：

  | | 杨坛 · 561 次/55 天 | 赵仁辉 · 1118 次/57 天 |
  |---|---|---|
  | 主战场 | 销售台账 85、请款审批 43、发货收货人 39、开票 31、下单 18 | 物料分类 110、物料字典 37+23、库位 30、出入库 9+冲红 17 |
  | 提反馈 | 40 次 | **152 次**（需求主要来自他） |
  | 问 AI | 18 次 | 8 次（只有晨报/逾期任务） |

  ⚠️ **采购申请单 55 张，这两人一张都没提过**（王利利 46、宋朴 7、卢照坤 2；
     采购员是李新新 51 单）。赵仁辉在采购链条上只做过 `set_invoice_no` 16 次，
     是**录发票**不是下单。所以 H5 里**不做采购申请页**——那是给他一个他从不做的动作。
     真要做采购场景，对象是王芹/李新新/王利利，而且得先把他们加进企微应用的可见范围。

  **① `find_entity` 出表格**（对准杨坛 18 次提问里的 13 次）
  他的问法高度集中在「编号 ↔ 设备规格」互查：「200L的设备有哪几个编号」
  「5L双行星是哪个编号」「5L的设备有几台」「067是什么机器」。
  答案**其实都答对了**，但 7 条里 6 条 `rendered=false` —— 全是模型一行行打的文字墙。
  根因和 `get_project` 同一个：返回嵌套对象，渲染层只认 `items`/`columns`。
  - 项目候选补了 `status` 和 `customer`：他真正要分的是**在建还是已完成**、是哪家的货。
  - **在建的排前面**——已完成的只是背景，手机上一屏就几行，别让它占前排。
  - ⚠️ `count` 必须给。以前不给，审计日志把**答对了**的问答统统记成「查到 0 条」，
    看日志的人会以为这个场景是坏的——**差点按坏的去改**。
  - ⚠️ 只铺命中最多的那一类，其余留在 `matches` 里让模型在结论句提一句。
    混着铺的话项目要「状态/客户」、物料要「规格」、客户要「台账行数」，列对不上。
  - ⚠️ 候选的键名改了对齐渲染层 `_NAME_KEYS`：`name`→`customer`/`supplier`/`item_name`。
    **`skills.py:75` 漏改直接 KeyError 把技能打挂了**，已修并加了兜底取值。

  **② 首页缺料补规格**（对准赵仁辉）
  用户手机截图：「今天该管的」3 个名额里 2 个是一模一样的「丝攻 库存 0」。
  查生产库才知道**不是重复数据**——`wh_materials` 里「丝攻」有 6 条，
  分别是 M6/M8/M10/M12/M18/M22，是 6 个不同的料，简报只显示 `name` 把 `spec` 丢了。
  现在标题是「丝攻 M6 库存 0」，没规格的料不留多余空格。

  **③ 「补货」死链**
  缺料条目的 `card` 是 `None`，前端 `openBrief()` 照跳 `/chat?card=null`，
  而 `H5ChatView` 要求 `card` 是**非空串**才走卡片通道 → 点了什么也不发生。
  改成没有 `card` 就走对话，把 `ask`（「丝攻 M6 库存不够了，谁在管、之前从哪家买的」）带过去。

  测试：`tests/test_agent_find_table.py`、`tests/test_briefing_stock.py`；
  全量 105 通过 / 13 失败（那 13 个在未改动的 main 上同样全红，是存量问题）；`vue-tsc` 无报错。

- **⚡ 仓库模块性能专项（2026-08-15）**——用户点名"页面加载/数据加载/查询效率"。
  先量后改，四个瓶颈各自的证据和结论：

  | 问题 | 改前（生产实测） | 改后 | 怎么改的 |
  |---|---|---|---|
  | 物料需求总览 N+1 | **1331 条 SQL / 1237ms** | **13 条 / 232ms** | `_DemandCtx` 一次批量预取 |
  | 整站没开 gzip | JS 2.2MB + CSS 890KB + 物料 334KB **明文过网** | 开 gzip（约剩 1/4） | nginx `gzip on` + **`gzip_proxied any`** |
  | 九个页签全量渲染 | 仓库页 **44861 个 DOM 节点** | **2046** | 每个 `el-tab-pane` 加 `lazy` |
  | 大表无虚拟滚动 | 物料主数据 863 行一次铺完 | 50 行/页 | 渲染层分页，口径不动 |
  | 模型声明了却没建的索引 | 生产缺 **23 个** | 启动自动补 | `ensure_indexes()` |

  - ⚠️ **`gzip_proxied any` 是关键，光写 `gzip on` 等于没开**：本站前端和 API 全是
    `proxy_pass` 上来的，而 `gzip_proxied` 默认 `off` = 代理来的响应一律不压。
  - ⚠️ **nginx 配置改了不会自动生效**：`nginx/conf.d` 是 bind mount，`up -d` 不重建
    nginx 容器。已在 `ops/upgrade.sh` 加 `nginx -t && nginx -s reload`（先验后重载，
    配置写错就跳过、线上继续跑旧配置）。**没这一步的话：git pull 拉下来了、发版报成功、
    线上还是旧配置，一点提示都没有。**
  - ⚠️ **`Base.metadata.create_all` 只给"新建的表"建索引**。给一张已上线的表新加
    `index=True`，本地全新库一切正常（表是新建的），生产上那张表早就存在，索引
    **永远建不出来**——代码里写着有索引、线上全是顺序扫描，而且不报任何错。
    生产上这样缺了 23 个（含 `purchase_items.source_sheet_id`、`wh_txns.purchase_item_id`）。
    `ensure_indexes()` 逐个索引单开事务 + 各自 try：唯一索引撞上存量重复数据只跳过并告警，
    绝不能把启动带崩（启动挂了就是全站 502）。
  - ⚠️ **要"全部物料"时给 `_stock_map` 传 `None`，别拼 863 个 id 的 IN**：那样 PG 走不了
    纯聚合快路径，asyncpg 还会按占位符个数逐个 prepare，预编译缓存等于没有。
  - ⚠️ **`el-tab-pane` 的 `lazy` 默认是 false**：不写 lazy，九个页签的表格在页面一挂载
    时就全渲染成真 DOM（只是 `display:none` 藏着），点都没点过的也照渲。
  - **顺手修掉一个潜伏的不确定性**：物料需求明细的行顺序原来取决于数据库返回清单的顺序
    （没有 order by），同一个项目两次请求可能不一样。现在按业务顺序钉死
    标准件清单 → 电工采购单 → 不锈钢原料下料单。⚠️ 别改成按 id 排：电工采购单是老模板、
    id 通常最小，按 id 排会把它顶到最前面。
  - **验证方式（值得复用）**：把改完的 `warehouse_router.py` 源码 base64 送进生产容器，
    以 `app.routers._wh_new` 加载，**同一进程/同一个生产库/同一时刻**跑新旧两套实现逐字节 diff
    （只读、不部署）。结论：62 个项目明细**内容零差异**，16 个仅行顺序不同（即上面那条）。
    ——先抓 golden 再改代码那种比法在活系统上会误报（比对期间有人建了 3 个物料）。
  - 测试 `tests/test_wh_perf_demand_batch.py`（17 条）。最要紧的一条：**项目数 3→13，
    SQL 条数必须不变**——这才是这次改动的全部意义，别哪天被"顺手优化"回 N+1。

- **📮 反馈批次 2026-08-11/12（24 条，已完成 11 件）**
  队列一次涌进 24 条，按"能不能立刻定位"分批做。**已完成**：
  | # | 结论 |
  |---|---|
  | 366 / 380 | **待办不能编辑**——原来只有「撤销+重发」。新增 `PUT /management-todos/{id}` |
  | 383 / 384 | 项目表里「钣金装配表」列是**纯图标**、隔壁「外协加工」列是文字，两种风格 → 统一成「文字+图标」 |
  | 387 | 「字体重复」= 物料列显示 `J01-油缸法兰 · J01-油缸法兰`，规格段被合并收货串重了 |
  | 373 374 388 | **物料按有没有项目编号一刀切开**（下面「A 组口径重构」详述） |
  | 375 376 | 合并收货**会把各行的项目编号全覆盖成一个** → 改成逐行分派 |
  | 377 | 新增「库位存量物料 → 调至项目物料」 |
  | 389 390 | 项目材料成本 / 项目毛利支持展开明细（懒加载） |

- **📮 反馈批次 2026-08-14（#397/#398/#399/#400）**

  | # | 提出人 | 真正的问题 |
  |---|---|---|
  | 397 | 王芹 | 强制发货按钮**一直在**，是被列宽 + `show-overflow-tooltip` 裁掉了 |
  | 398 | 王利利 | 采购收货页 1202 行一次性全渲染，卡死 |
  | 399 | 杨坛 | 067 的装配组任务挂在**已删除的旧项目**上（数据缺口，非 bug） |
  | 400 | 周瑞 | 电工三步流第二步「电路」改名「安装调试」 |

  - ⚠️ **#397 是 #394 的续集，教训比 bug 本身重要**：#394 我改了权限 `v-if`，
    以为完事了，**没去看渲染出来长什么样**——按钮其实一直被裁着，用户第二次来报同一件事。
    根因：表格开了全局 `show-overflow-tooltip`，操作列 `width=200` 放不下两个按钮，
    超出部分被省略号静默吃掉。**操作列永远显式 `:show-overflow-tooltip="false"`**，
    按钮被截断是完全静默的，用户只会以为功能没做。这次是在浏览器里点开确认过才敢说修好。
  - **#398 后端不慢，慢的是 el-table**：实测 `/receiving` 0.04s / 0.09s，
    但已收货返回 **1.07 MB / 1202 行**，el-table 不做虚拟滚动、还要建父子树，浏览器直接卡住。
    数据本来就全在内存里，所以只在**渲染层**分页（默认 50 行/页），
    搜索/筛选口径一点没动（那几个框仍走后端全库，见 #330），翻页找不到的照样搜得到。
  - ⚠️ **#399 的续：我让杨坛「重新派一次装配组」，但根本没有那个入口**（他回问「怎么派？」）。
    派发按钮**只挂在「待分派」页签**，单子一派出去就离开那个列表，
    漏派的组此后再也补不了。已在「任务跟踪」加入口：
    · 后端 `dispatch_produce` 本来就幂等（已有的组改人、缺的组新建），一行没改；
    · 缺组时按钮标红显示「补派装配」，主管一眼看得出哪张单没派全；
    · 打开时把已派的人**预填**进去——不预填的话保存等于把已有的组也当成要改。
    为此给 `ProduceGroupBrief` 加了 `worker_id/worker_name`
    （⚠️ `ProduceGroupTask` 没有 `worker` 关系，取名字要**批量查再映射**，别写成 N+1）。
    测试 `tests/test_produce_redispatch.py`（19 条），重点锁「补派不冲掉已有的
    预计完成/完成状态/其它组的人」。
  - **#399 的原始诊断（数据缺口，不是 bug）**：067 有两条项目记录：
    `_deleted_252_2026-067`（已删）和 `2026-067`（在用）。四条分组任务里，
    **装配组那条挂在已删的旧项目上**，在用项目的生产单只派了钣金组+封板组。
    所以装配组页签当然找不到它——需要重新派一次装配组，不是代码问题。
    查法：`SELECT g.group, p.code, p.is_deleted, o.status FROM produce_group_tasks g
    JOIN projects p ON p.id=g.project_id LEFT JOIN dept_orders o ON o.id=g.order_id WHERE p.code LIKE '%067%'`
  - **#400 只改展示名，不动字段**：`wire_done_date` / `electric_done_flag` 保持原样——
    改字段名要连带迁移和回填，为一个称呼不值当。改的是 `done_label`、按钮、进度条、通知文案。

- **🔧 新建账号登录不了 = 用户名带了个空格（2026-08-13，线上真事）**
  `liulonglong` 新建后一直报「用户名或密码错误」。查出来库里存的是 `'liulonglong '`
  —— **尾部一个空格，12 个字符**。登录是精确匹配 `User.username == 输入值`，
  本人按正常拼写输入永远匹配不上；而报错只说「用户名或密码错误」，
  于是所有人都往密码上使劲，重置多少次都没用。
  排查也难：**肉眼看用户列表跟正常的一模一样**，得 `SELECT length(username)` 才看得出来。

  查这类问题的一句话：
  ```sql
  SELECT id, '['||username||']', length(username) FROM users WHERE username <> btrim(username);
  ```
  - 已修：线上那条 `UPDATE users SET username = btrim(username)`；全库扫过，没有第二个。
  - 已堵根因：`schemas._strip_username` 挂在 **UserCreate 和 LoginIn** 上
    （建账号 strip = 不让脏数据进库；登录也 strip = 存量脏数据修好前人还能登进来，
    顺带容错手机键盘长按空格、从表格复制粘贴带尾空格）。全角空格 `　` 一并处理。
  - ⚠️ **密码绝不能 strip**：空格可能是密码的一部分，strip 了等于偷偷替人改密码。
    测试里专门有一条反向断言锁死这点。
  - ⚠️ 重名判定跟着 strip 后的值走，否则能建出两个"同名"账号，谁也说不清哪个是真的。
  - `UserUpdate` 不含 username，改名没有第二条入口——建账号是唯一口子。
  - 测试 `tests/test_username_whitespace.py`（11 条）。

- **📮 反馈批次 2026-08-13（#393/#394/#395/#396，4 条全清）**

  | # | 提出人 | 真正的问题 |
  |---|---|---|
  | 393 | 超级管理员 | 料**领用出库后**「建议采购」又叫人买一遍；同一行还标着「需采购」 |
  | 394 | 王芹 | 强制发货 #367 做过了，但**只改了后端**，前端按钮还挂 `v-if="isMgr"` |
  | 395 | 计梦蝶 | OA「标记已付款」什么都填不了，回单没处传 |
  | 396 | 杨坛 | 待我审批/待付款没有未处理数量 |

  - ⚠️ **#394 是这批里最该记住的一条**：改权限**前后端要一起看**。
    `logistics_router` 的 `can_force` 早就含 `logistics` 角色了，可 `LogisticsView`
    那个「强制」按钮还是 `v-if="isMgr"`（=`auth.isAdmin`），物流的人**根本看不见按钮**，
    功能等于没做，用户只好再提一次。现已改为 `canForce = isAdmin || hasRole('logistics')`。
  - **#393 是真算错**，不只是标签难看：`suggest = 需求 − 现存`，料一领走现存归 0，
    就又叫人把需求量重买一遍。正确口径 `(需求 − 已领) − 现存`（`_demand_rows`）。
    前端那列「库存」也从 `有货?有货可出:需采购` 改成先看 `demandRemain`：
    领够了显示「已领完」——原来同一行「已领用 2/需求 2」「建议采购 0」跟「需采购」三个数自相矛盾。
  - **#395 备注必须用新字段 `pay_note`**，不能复用 `settle_note`：后者是核定金额时写的
    （为什么只批这么多），两件事挤一个字段，后写的会把当初核减的理由冲掉，月底查不出原因。
    回单走既有 `attachments(biz_type='oa_request', kind='pay_receipt')`，不新开表。
    ⚠️ **schemas 加了字段，`_req_out` 里也得赋值** —— 只加 schema 的话写进库了接口还是返回
    None，前端永远看不见（这次就先漏了，测试才抓出来）。
  - **#396 角标不能拿 `rows.length` 算**：`rows` 只装当前页签那一份数据，
    站在「待我审批」上时「待付款」根本没加载过。两个角标各自独立取，
    并在 `loadList()` 末尾统一刷新——审批/驳回/撤回/标记已付款都会走它，批一个减一个。

- **📮 售后报销取消发票硬闸 + 财务侧加筛选（2026-08-13）**
  「有的东西报销他是没票的」——原来缺一行发票就不许点「核对无误，安排报销」，
  财务只能一直退回、单子永远走不完。这条限制**拦不住乱报，只拦住了正常报销**。
  - 前后端**各有一道**闸，两处都要拆：`aftersales_router.reimburse`（400 拦截）
    和 `FinanceView.asReimburse`（前端直接 return）。只拆一边等于没拆。
  - 不拦 ≠ 不留痕：缺票时审计写 `缺 N 张发票仍安排报销`，返回文案也点明，
    前端二次确认弹窗标黄提示。要追责查审计日志。
  - 同时给财务「安装/售后费用」加了 类型/项目编号/提交人 筛选（做法同售后部那张台账）。
    ⚠️ 这张表有 `show-summary`，**合计必须跟着筛选走**，否则筛完只剩一行、合计还是全量。
    合计值是按数组下标对列的（11 列，费用在下标 7），加删列必须同步改那个数组（#361 踩过）。
  - `test_aftersales_reimburse_cost.py` 第 4 段原本锁的是旧行为（缺票必须拦），已反转；
    注意它在**另建一条无票记录**上验，不动主记录——提前把主记录推到 reimbursed
    会把后面验退回重传的几段全废掉。顺带那条 ¥88 也进了售后成本合计，断言已同步。

- **🔴 电工部三步流：主板完成 → 电路完成 → 上传电路图（2026-08-12 业务确认）**

  | 步 | 触发什么 | 落在哪个字段 |
  |---|---|---|
  | 主板完成 | **结考核**（效率/按时/逾期），状态仍进行中 | `mainboard_done_flag` + `done_date` |
  | 电路完成 | **部门完成**，物流 D5 闸门放行 | `electric_done_flag` + `wire_done_date` + `status=done` |
  | 上传电路图 | 必传，界面上催 | `attachments(order_output/circuit)` |

  这是全系统**唯一**「考核完成日 ≠ 部门完成日」的地方：`done_date` 是主板完成日，
  `wire_done_date` 才是实际完成日。`due_date`（预计完成）也是对着主板完成定的。

  - ⚠️ **拆开之后有三处连带逻辑必须一起改，漏一处线上就会出怪事**（都已改并写进测试）：
    · `overdue.scan_overdue` 要跳过 `mainboard_done_flag` 的电工单 ——
      不然考核都结了，还每天继续报逾期，提醒直接变废纸；
    · `reports_router._kpi_done` 要认这个标记 —— 只按 `status=="done"` 判的话，
      主板按时完成、电路还在做的单子会从当月报表里凭空消失，
      **按时率的分子分母一起少、数字看着还挺正常**，这种错最难发现；
    · `reopen` 要把三步标记全清 —— 漏清 `mainboard_done_flag`，返工后「主板完成」
      点不动（接口拦"主板已完成"），单子直接卡死。
  - ⚠️ **第二步不许碰 `done_date`**。碰了就等于用电路完成日重算效率，第一步的考核作废。
  - **电路图不参与 status**：业务明确要求不卡发货。电工忘传图就把整个项目顶住，
    代价太大（电工部现存 16 条逾期、最久 47 天）。只在卡片上标红催。
  - 存量数据走 `backfill_electric_mainboard`：老的已完成电工单补上三步标记
    （`wire_done_date = done_date`），否则界面把它们显示成"卡在第一步"。
    ⚠️ 必须排在 `close_legacy_electric_done_orders` **之后**，那条会先把卡住的单子置 done。
  - 顺手删了 `DeptWorkbenchView` 里「发货准备完成」那段死代码：#197 之后
    接线完成就直接 done，那个分支只有存量老单能走到。
  - 测试 `tests/test_electric_three_steps.py`（26 条），含发货闸门两态、逾期不重复报、
    返工清标记、存量回填。

- **📮 #392 售后台账加筛选（杨坛，2026-08-12）**
  「售后这个页面加一个 类型 / 项目编号 / 提交人 的筛选功能」。
  整张表本来就是一次 `/aftersales` 取回全量（线上 4 条），**在前端过滤即可，没动后端**——
  改成服务端筛选/分页反而要连带重做加载与 KPI 统计口径。
  下拉选项从数据里现算，新来的提交人/项目自动进选项，不写死。

- **📮 个人待办 #363/#381（一期已上线，2026-08-12）**
  设计稿 `个人待办功能设计.md`（业务已确认，5 个问题的答案写在文首）。
  新表 `personal_todos` + `/api/personal-todos`（7 个接口）+ 右下角待办弹窗第三个页签。

  | 业务确认 | 落地 |
  |---|---|
  | 角标 = 两者合计 | `badgeCount = myCount + psCount` |
  | 要挂项目、要紧急档 | `project_id` / `priority` |
  | 到期当天推一次企微 | `overdue.scan_personal_todos` |

  - ⚠️ **绝不与 `management_todos` 合表**。后者每条都是有交代的管理动作，
    承诺时间/进度/延期是留痕证据、删不得；个人待办随手记随手删。
    合表只有两个结局：个人待办被迫填一堆用不上的字段（没人用），
    或管理层待办的留痕被个人待办的随意删除污染。
  - ⚠️ **每个按 id 的接口都要 `user_id == current.id`**，统一走 `_own()`。
    只滤列表、详情/改/删按 id 直取 = 换个 id 就能改别人的待办。
    测试里专门有一组越权断言（乙改甲、**admin 改甲**都必须 404）。
  - ⚠️ **`PUT /reorder` 必须排在 `PUT /{tid}` 之前**，否则 "reorder" 被当成 tid → 422。
    （同采购路由 `batch-expected-arrival` 那个坑，写的时候又踩了一次。）
  - **到期只推当天这一次**，过期后只在列表里标红，不做天天推——
    自己设的闹钟天天响，人会把整个系统的通知一起静音（采购到货提醒 2026-07-26 的教训）。
  - UI 上「单独开一列」做成了**独立页签**而不是左右分栏：弹窗只有 720px，
    硬拆两栏每栏 350px，管理层待办那些卡片（标题+状态+三个按钮）会挤到不能用；
    页签在手机上也不用另做一套堆叠布局。
  - 测试 `tests/test_personal_todo.py`（33 条）。
  - **二期 H5（#382）已做**：`src/h5/H5TodoView.vue` + 路由 `/todos`，入口在首页顶栏（带角标）。
    ⚠️ 设计稿写的「放底部『我的』页」**落不了地——H5 根本没有底部导航、也没有「我的」页**
    （`H5App.vue` 只有一个 router-view，全部页面 = 登录/首页/聊天）。业务选「底部我的」
    而不是「首页」的意思是别挤占首页，所以做成独立一页、入口放顶栏，意图一样落到。
    交互按手机来：整行点一下打勾、左滑露删除、不做拖动排序。
    ⚠️ 左滑判定必须**先比横纵位移**（`|dy| > |dx|` 直接放行），否则上下滚动会被当成左滑，
    列表直接滚不动。
    接口与网页版完全同一套 `/personal-todos`，后端一行没为 H5 改过。
  - ⚠️ **`.claude/launch.json` 的 `h5-dev` 原来是坏的**（今天修了）：它跑的是
    `npx --prefix … vite`——用的是**网页版 config**，而且 `--prefix` 只改 npm 的包解析、
    vite 的 cwd 还在工作区根目录，`vite.config.h5.js` 根本找不到，起来就是白屏/404。
    正确写法：`cwd: excel-share-system/frontend` + `npm run dev:h5`，
    再带 `VITE_DEV_API` 指到本地后端（dev 下 `/api` 走 vite 代理，默认打 `backend:8000`）。
    H5 dev 的入口路径是 **`/h5/h5.html`**，不是 `/`。

- **📮 #369/#370 附图「点不开」——根因在客户端，不是文件格式（2026-08-12）**
  两条其实是同一件事：设计部工作台「待接收的问题反馈」里的**附图点了没反应**。
  （⚠️ 我一开始按文字猜成"老式 .doc 不能在线预览"，**看了截图才发现完全猜错**。
   这类只有三四个字的反馈，截图不看完不要动手。）

  根因：`FeedbackPanel.vue` 用的是 `window.open(URL.createObjectURL(blob))`，
  而 `desktop/main.js` 的窗口拦截是
  ```js
  if (/^https?:\/\//i.test(url)) shell.openExternal(url);
  return { action: 'deny' };
  ```
  `blob:` 开头既不匹配 http(s) 交给系统浏览器、也不允许开新窗 → **直接 deny，
  点了一点反应都没有，连报错都没有**。网页版好使，客户端必坏，而他用的是客户端。
  改成走全站统一的 `AttachmentPreview` 应用内弹窗。
  - ⚠️ **客户端里凡是 `blob:` / `data:` 的 `window.open` 都是这个下场**。
    要开附件一律用 `AttachmentPreview`；这次全站扫过没有第二处。

- **📮 #385 全流程进度同步到各部门（2026-08-12）**
  原来只有销售台账有「全流程进度」入口。别的部门看到一个项目编号，想知道它卡在哪一环
  （设计出图没有？采购到货没有？仓库备齐没有？）只能挨个 tab 翻。
  抽了 `components/ProjectFlowButton.vue`（按钮+弹窗+拉数据一体），挂在
  采购部项目一览 / 仓库物料需求总览 / 物流发货看板 / 各部门工作台「任务跟踪」。
  接口 `GET /projects/{pid}/workflow` 本来就是 `get_current_user`，后端没动。
  - ⚠️ **数据点开才拉**：这组件出现在成百上千行的表格里，挂载即请求会让每个列表页
    多打几百个请求。
  - ⚠️ **各表的项目主键字段名不一样**：采购部一览是 `project_id` 不是 `id`
    （按 `row.id` 传进去 `v-if` 直接不过，按钮压根不渲染、还不报错）。
    加这类跨页面组件时逐页看一眼实际字段，别照抄。

- **📮 A 组的连带修复（#391/#378，2026-08-12）**
  | # | 提出人 | 结论 |
  |---|---|---|
  | 391 | 李新新 | 「这个现有库存是库里有的（没有订单编号）还是我有订单已采购的？」——**问到真 bug 了** |
  | 378 | 超级管理员 | 预计到货只让维护一次 |
  | 379 | 超级管理员 | **早就在做**：`scan_overdue` 一直推给本部门主管+管理层，线上 3636 条已发 |

  - **#391 是这次最有价值的一条**。采购申请/从清单下单那列「现有库存」给的是**全部库存**，
    其中大半是别的项目已经买好、动不得的料（生产实测 517 种有货物料里 **394 种挂着项目编号**）。
    采购看到「现有库存 5」就不下单 → **系统性少采**。
    本地实测一张清单：内六角螺栓库里 380 但全属别的项目，建议采购由 **0 变成 200**；
    伺服电机 0 → 2。旧口径下这两行不会被勾选，采购单直接漏发。
    改法：`_build_stock_by_key` 返回 `(可用, 项目占用)`，**建议采购只减可用**，
    项目占用用灰字「+380项」提示。判据仍是 `_project_material_ids`，与仓库/财务同一个口径。
  - **#378 加锁但数据要看清**：1228 条有预计到货的明细里只有 57 条改过期、8 条改过两次、
    最多 2 次——「一路往后改」并不严重。锁照用户要求加了（普通采购填过就不能改，
    采购主管/管理层可改并留痕），但真正该盯的是 **28 条已过期仍未到货**。
    ⚠️ 单条和批量两个接口都要拦：只锁 `PUT /items/{iid}`，`batch-expected-arrival` 就是绕过口。
  - **采购收货逾期：管理层加回来，但走「每日一条汇总」（2026-08-12 用户要求）**。
    2026-07-26 曾把这个提醒收窄成只推下单采购员本人（注释原话「不再推主管/管理层
    (反馈信息太多)」）。用户要求把管理层加回来 ——
    **逐条扇出会是每天 93 条**（当时命中 31 条 × 2 manager + 1 admin），天天重复，
    一周就把管理层的消息列表淹了，然后整个系统的通知一起被静音，等于回到收窄前的老问题。
    所以：采购员本人**保持逐条**（他要按明细去催），管理层收**一条汇总**——
    共几条、压在谁手上、最久超期几天。幂等键 `biz_id = YYYYMMDD`，当天一条、跨天再推。
    要改回逐条：把 `scan_po_arrival_overdue` 末尾那段汇总换成在循环里对 manager/admin
    各 push 一次即可（`tests/test_po_overdue_digest.py` 会立刻红，那是有意的）。

  - **#378 的连带清理：首次填不再推「预计到货变更」留痕**。
    #378 之后普通采购能做的只剩首次填，而首次填照样推通知 → 每建一条采购明细
    就 ping 一次采购主管+管理层。历史 195 条通知里 **84 条是「由 未填 改为」（43%）**，
    纯噪音。现在只在 `old_ea` 非空（真改期）时推。口径同 2026-07-26 收窄到货提醒那次：
    信息太多等于没有信息。
  - ⚠️ **「buyer_lead / warehouse_lead 零人导致逾期推送落空」这个结论我当时说错了**，
    留个记录免得下次又绕进去：
    · `DEPTS` 只有 **design / electric / produce** 三个部门 —— 仓库、采购**根本没有
      带截止日期的部门任务单**，`scan_overdue` 碰不到它们；
    · 采购到期未收货走的是 `scan_po_arrival_overdue`，而它在 2026-07-26 就**有意收窄成
      只推下单采购员本人**（注释原话「不再推主管/管理层(反馈信息太多)」），
      `buyer_lead` 本来就不在收件人里。
    这两个空角色实际只影响：低于安全库存预警（`warehouse_router:811/873`）
    和预计到货改期留痕。都不是逾期提醒。
  - **#379 已经在做，查出来两件别的**：
    ① 18 条逾期任务里 **16 条是电工部**，最早应完成 2026-06-26（47 天），提醒天天发没人处理；
    ② **`buyer_lead` / `warehouse_lead` 角色一个人都没有**——这两个部门的任务逾期时
    「本部门主管」那一路推送落空，只有管理层收得到。属配置缺口，同设备白名单那次。
  - ⚠️ **改口径会掀翻既有测试，要分清是回归还是测试锁着旧行为**：
    `test_batch_expected_arrival` / `test_po_arrival_reminder` 都预填了 `expected_arrival`
    再让普通采购去改——那正是 #378 要禁的动作。改成从空开始（首次维护），
    锁本身在 `test_fb391_fb378.py` 里单独验。
  - ⚠️ **测 #378 必须验错误文案不能只验 403**：采购明细有行级隔离（`_buyer_restricted`），
    用 admin 建的单让采购去改，403 来自「无权编辑他人明细」，断言照样绿但什么都没验到。
    第一版就是这么写的，验的是空气。

- **🔴 A 组：仓库/成本口径重构（#373/374/375/376/377/388/389/390）——改动最大的一次，先看懂再动**

  **一句话**：物料按**有没有挂过项目编号的入库**分成「项目物料 / 通用物料」两边，
  同一笔钱只算一次。判据是 `warehouse_router._project_material_ids`，全系统唯一。

  | 口径 | 旧 | 新 | 生产实测 |
  |---|---|---|---|
  | 项目材料成本 | 领料出库 × 加权均价 | 挂项目**收货金额** + 超领从通用池扣 | ¥163,697 → **¥290,709** |
  | 库存金额 | 全部物料 现存×均价 | **只算通用物料** | ¥148,099 → **¥31,788** |
  | 未归集 | — | 项目物料上没归到项目的通用池余额 | ¥170 |

  为什么要改：现场料一到货就直接拉到工位用了，仓库的领料手续常年补不上——
  收货 ¥421,444，旧口径只认出 ¥163,697，**六成成本在系统里蒸发**，毛利榜全是假的。

  - ⚠️ **腿B（领料）必须卡「通用池」，两头都不能一刀切**。这是整个改动最难的一处，
    两个方向各踩过一次：
    · 见到出库就算 → 「A 项目收 100、B 项目领走 30」会被算两遍（生产上这种重算 **¥34,771**，占超领总额 99%）
    · 因为"物料是项目物料"就全不算 → 「通用螺栓领 120 给甲、剩下 380 调给乙」会把
      **甲已经发生的 ¥144 倒扣掉**（#377 上线当天就撞见，本地实测 13480 → 13336）
    · 正解：超领的部分**从通用池按量扣，扣得到多少算多少**。甲的 120 扣得出来 → 照算；
      B 领 A 的料时通用池是 0 → 不算，钱留在 A 那边。
    通用池 = 期初 + 无项目入库 − 无项目出库（含 #377 调拨转出腿、无主领料）。
  - ⚠️ **`_project_cost_map` 与毛利榜腿1 共用 `_project_cost_core`，明细也走同一份数据**。
    分头实现必然对不上——腿B 的池子分配依赖全局（谁先领谁扣得到），
    两处各算一遍，同一个项目在两页会显示两个数，毛利榜的可信度当场归零。
  - ⚠️ **`GET /wh/materials` 的 `scope` 默认必须是 `all`**，不是 `general`。
    这个接口同时喂着「物料主数据」和出库选料，默认过滤会让项目物料在主数据里凭空消失、
    出库时搜不到。库存总览的过滤是**前端按 `is_project_material` 做的**（一次请求两个 tab 用）。
  - **#377 的实现是两笔流水，不是改物料主数据**：无项目出库 + 挂项目入库。
    净库存不变，而物料随即①进该项目的物料需求 ②退出库存总览 ③成本归到该项目——
    三件事都由既有逻辑自动发生，不用为它写任何新的过滤。
  - **#375 的答案**：原来不但不分派，还会**把每行原有的编号全覆盖**
    （`receive_batch` 里 `if body.project_code is not None: it.project_code = _pcode`）。
    一车拉三个项目的料，收完货全挂到一个项目上，另两个项目在自己的物料需求里
    永远看不到这批料、成本也永远归不上去。现在：逐行 > 明细原有 > 整批兜底，
    整批编号**只填空行**。
  - **剩下的已知黑洞**：无主领料 ¥114,892（143 笔）——这是**改之前就有的**，
    成本审计页一直在列。配平算式在通用池被领成负数时不是恒等式，差额就出在这里。
  - 测试 `tests/test_fbA_project_material_scope.py`（53 条），锁的是口径自洽而不是具体数字：
    配平恒等式、项目物料领料不重复计、调拨不倒扣别的项目、明细加总==合计、两页同数。
    SQL 已在**生产 Postgres 上实跑验证**（4 条聚合 0 报错）。

  - **#366 的关键约束（改坏了就等于没修）**：改内容时**只增删收件人差集，绝不整批重建
    `targets`**。重建会把留下来那些人的**回复/承诺完成时间/进度全部清零**——
    而"撤销重发会丢掉这些"正是这条反馈的起因。
    生产证据：赵仁辉 02:41:52 下发「买一个快装弯头」→ 02:42:14 撤销 → 02:43:45 重发。
    另：只给**新增**收件人推「请回复承诺完成时间」，老收件人推「有变更」——
    他们可能早回过了，再要一次是骚扰。测试 `tests/test_fb366_todo_edit.py`（17 条）。
  - **#387 根因在数据不在显示**：合并收货把多行规格用「·」串起来，同名零件就串出重复段。
    没去改存量数据（动的是财务口径的字段，风险不值当），在显示层加
    `utils/format.ts: dedupSpec/specOf`——按「·」拆开去重，且规格与名称重复时不再拼。
    全站 7 处 `{{ name }} · {{ spec }}` 已统一走 `specOf()`。
  - **待办中的 13 条**分两组：① 新功能（363/381 个人待办、382 同步 APP、
    379 超期提醒、385 全流程进度同步）
    ② 待确认（369/370 老式 `.doc` 无法在线预览、371 成本口径问答、372 不统一、
    378 预计到货锁定、386 采购申请可见权限）。

- **📮 反馈批次 2026-08-11（10 条，已做 4 条，6 条待你定）**
  | # | 提出人 | 结论 | 状态 |
  |---|---|---|---|
  | 361 | 杨坛 | 售后台账没渲染「提交人」——接口一直返回 `created_by_name`，只是没这一列 | ✅ 已加（售后页 + 财务页） |
  | 368 | 杨坛 | 详情页返回写死 `{name:'projects'}`，而 `projects` 是**「项目详单」**、「项目目录」是 `overview`。从目录进去必然回错 | ✅ 改成回上一页 + 兜底 |
  | 365 | 计梦蝶 | 收款账号每次手敲 | ✅ `/oa/payee-accounts` 从历史单据抽账户，选收款单位自动带出 |
  | 360 | 赵仁辉 | 客户端**有**下载目录和系统通知，但通知被静音就毫无反馈；改下载位置的入口在**菜单栏**里，而窗口 `autoHideMenuBar` | ✅ 应用内提示 + 左下角「下载位置」按钮 |
  | 367 | 赵仁辉 | 强制发货**已存在**（他 08-11 02:29 用过），只是限 `is_mgr` | ✅ 已放给 `logistics` 角色（见下） |
  | 369 | 赵仁辉 | 「点不开」——日志已被发版重启冲掉，查不到；需问他点的是什么 | ⏸ |
  | 362 | 杨坛 | 流程本来就对，是**王芹的 `finance:aftersales` tab 被隐藏**了（见下） | ✅ 已放开 + 通知写明去哪办 |
  | 364 | 王芹 | 运费**早就计入**项目成本（毛利腿4，我方承担 ¥20,268）；真正的问题是 42 个项目在物流部根本不存在 | ✅ 项目编号全量同步 + 筛选改发货状态（见下） |
  | 366 / 363 | — | 描述不足 / 新功能 | ⏸ |

  - ⚠️ **#361 加列时改了 `summary-method`**：财务那张表是 `show-summary`，
    合计值按**数组下标**对列，插一列不同步改数组，「合计」就会落到隔壁列上。
  - ⚠️ **#365 只在账号和开户行都为空时才带出**：人已经手填过就不覆盖——
    偷偷改掉用户填的收款账号是这类「智能填充」最容易闯的祸。
    同一单位换过账号时给**最近一次**的（给旧账号钱就打飞了）。
  - ⚠️ **#360 IPC 新增 4 个频道**，main.js 与 preload.js 的频道名必须逐字一致，
    打错了不报错、只是永远收不到消息。加完用
    `comm -3 <(grep -oE "'pms-desktop:[a-z-]+'" main.js|sort -u) <(... preload.js ...)` 对一遍差集。

  - **⚠️ #367 放开强制发货 = D5 闸门实际上不再拦人了（用户 2026-08-11 明确批准）**
    `POST /logistics/{sid}/ship` 本来就 `require_roles("logistics")`——能调它的只有
    物流 + 管理层。把 `force` 也放给 `logistics` 之后，**没有任何角色会被 D5 拦住**，
    它从「硬闸」变成「提醒 + 留痕」。角色表里**没有** `logistics_lead`，
    `logistics` 目前只有王芹一人；以后若新增物流部员工，他们同样能强制发货，
    真要区分得先加一个「物流主管」角色。
    补偿手段（不能删）：① 审计记 `FORCE 未完成：xx部`（原来只有一个 FORCE，
    事后查不出漏了哪道工序）② 每次绕过都推 manager/admin 告警——
    **没人看得见的旁路等于没有旁路**。闸门正常通过时两者都不触发，避免天天误报没人看。
    测试 `tests/test_fb367_force_ship.py`（11 条断言）+ 反证。

  - **⚠️ #362 的教训：工作流派给「角色」，但界面藏在「按人配的 tab 权限」后面，
    配置一改活就静默卡住，系统一声不吭。**
    现场：售后报销流程本身完全正确——杨坛审批 → `pay_status=checking` → 推消息给
    `finance` 角色。王芹确实收到了消息，但她的 `hidden_tabs` 里有 `finance:aftersales`，
    「财务部 → 安装/售后费用」那个 tab 对她是隐藏的，**打开系统根本找不到在哪核对**。
    3 单 ¥9,636.90 就这么卡了三天，日志里没有任何错误。
    已做：① 把该 tab 从王芹的 hidden_tabs 里去掉（16→15 条）
          ② 通知文案带上位置：「请到『财务部 → 安装/售后费用』…点『核对无误，安排报销』」
    **排查同类问题的方法**：先确认 `push_message(to_role=...)` 的目标角色有谁，
    再逐个查这些人的 `users.hidden_tabs` 里有没有藏着对应的 tab——
    「有权限的角色」和「看得见的界面」是两套配置，它们对不上时不会报错。

  - **#364 的实情**：发货运费**早就计入项目成本**——`reports_router` 项目毛利腿4
    （`Shipment.freight_cost`，`freight_payer='我方'`），生产上 15 张单 ¥20,268 已在算。
    王芹感觉"没统计上去"，是因为她的 `hidden_tabs` 把 `finance:pnl`（项目毛利）、
    `finance:expense`（支出总览）、`finance:audit`（成本审计）、`finance:fund` **全隐藏了**——
    她做成本核算却看不到任何一个成本视图。
    ⚠️ 放开这几个 tab 等于让她看到**全公司所有项目的合同额和毛利率**，
    属于数据可见范围的决策。**用户 2026-08-11 明确说这块不做**，只做项目编号同步。

  - **#364 实际做的两件事（2026-08-11）**
    1. **项目编号全量同步到物流发货部**：`backfill_shipments` 原来只补
       `status == "进行中"` 的项目（注释写着"已完成视为历史已交付不补"），
       结果 **42 个项目从来没有 Shipment 行**（37 个已完成），
       而发货看板是从 Shipment 出发查的——这些项目在物流部**根本不存在**，
       想录运费都无从下手。去掉状态限制，改为所有未删项目都补。迁移本来就每次启动跑、幂等。
    2. **看板筛选从「项目状态」改成「发货状态」**（未发货 / 已发货 / 全部）。
       旧的「进行中」= 未发货 **且** 项目状态≠已完成，把 **27 张**「未发货但项目被手工
       标成已完成」的单挡在两个筛选之外，只有「全部」才看得到。新口径只看 `Shipment.status`。
    - **侧边栏「待发货」角标 = 看板「未发货」，同一个口径**（`pending_count` 只看
      `Shipment.status`，不看项目状态）。我一开始让角标只数「进行中」项目，想把补录的
      历史单排除在待办之外；**用户否掉了，是对的**——角标显示 32、点进去看到 96 行，
      人会以为系统在骗他，比数字大更糟。那些历史单是**真实欠账**：系统里确实没有它们的
      发货记录，本来就该被看见。角标从此会是 96 左右，随录入逐步降下来。
    - ⚠️ 后端保留了 `proj_status` 参数并映射到新口径（进行中→未发货、已完成→已发货），
      否则没升级的客户端筛选会**静默失效**。
    - 顺带删掉了 `board()` 里「已完成」那条以 Project 为主的分支：它的 WHERE 要求项目
      必须有 shipped 的发货单，所以里面「项目没有 Shipment」的兜底永远走不到，是死代码。
    - 测试 `tests/test_fb364_logistics_board.py`（10 条断言）+ 反证。

  - **🖥️ 客户端不能粘贴文字（2026-08-11 修）**
    Electron 的 `Ctrl+C/V/X/A/Z` 是**靠应用菜单里的 `editMenu` 系列 role 注册**的。
    `buildMenu()` 自定义了「操作」菜单却没有「编辑」菜单，于是输入框里按 Ctrl+V **毫无反应**。
    菜单栏是 `autoHideMenuBar` 隐藏的不影响——**注册了就生效，看不见也没关系**。
    已补中文「编辑」菜单（撤销/重做/剪切/复制/粘贴/粘贴为纯文本/删除/全选），
    并加了右键菜单（`setupContextMenu`）：菜单栏看不见，右键是很多人的第一反应。
    ⚠️ 以后再动 `buildMenu()`，**不要把 editMenu 那几个 role 删掉**。

- **🔐 设备闸（客户端设备ID名单）排障要点（2026-08-11）**
  - **免验证码的四种情况**：① `admin` 角色（恒免，目前只有「超级管理员」）②内网 IP
    ③ 桌面客户端 **且** 设备ID在名单里（`device_gate_enabled=1` 时）④ 闸门总开关关闭。
  - ⚠️ **设备名单只对桌面客户端生效**。`desktop_exempt()` 第一句就是「不是桌面端→不免闸」，
    根本不看设备ID。所以**网页版 / 手机 H5 一律要验证码**，录多少 ID 都没用。
    杨坛天天被要码就是这个——他在用手机登。
  - ⚠️ **设备ID不随升级变**。`%APPDATA%\同辉项目管理\device.json` 首次启动生成后一直用；
    `productName`/`appId` 从没改过，NSIS 也没设 `deleteAppDataOnUninstall`（默认 false），
    **卸载重装都保留**。只有换电脑 / 换 Windows 账号 / 重装系统 / 手工删 `%APPDATA%` 才会变。
    实证：15 个用户各只有 1 个 ID，从 1.0.x 一路升到 1.0.42 没变过；
    新 ID 的出现日期是散的，不扎堆在发版日。
  - ⚠️ 客户端写 `device.json` 失败会**自动上报**（管理页「客户端故障」看得到）。
    目前 0 条，所以不是杀毒软件拦 `%APPDATA%` 那类问题。
  - **2026-08-09 07:04 打开设备闸时名单只录了 17 条、漏了 8 台在用机器**，
    等于给方步森/王芹/杨倩凭空加了一道闸。2026-08-11 已补齐（21 条），
    近 7 天活跃的 18 台设备现已全部在册。名单里 `cd9312e6-…` 对应不上任何真实设备（录错的），可删。
  - **排障顺序**：先看 `app_settings` 两个开关 → 再比 `desktop_clients` 与名单的差集
    （差集里的人就是会被要码的）→ 最后看 nginx 日志区分客户端/网页（UA 带 `pms-desktop/x.y.z … Electron`）。

- **📋 nginx 访问日志已落盘（2026-08-11）**
  - 原来只进 `docker logs`，**一发版重启容器就清零**——查三天前的访问记录查不到，
    只能靠推断。已改为 bind mount `./nginx/logs:/var/log/nginx`。
  - ⚠️ 原来挂的 `nginx_logs` **命名卷是无效的**：命名卷首次创建会用镜像内容初始化，
    而镜像里 `access.log` 是指向 `/dev/stdout` 的**软链**，卷里存的就是那条软链。
    bind mount 的目录是空的，nginx 才会建真文件。
  - 代价：`docker logs pms2_nginx` 不再有访问/错误日志，改看 `nginx/logs/access.log`。
  - 轮转在 `ops/setup-cron.sh` → `/etc/logrotate.d/pms-nginx`（daily，留 14 天，压缩）。
    ⚠️ `postrotate` 里的 `nginx -s reopen` 不能省：logrotate 只是改名，
    不发信号 nginx 会继续往旧句柄写——新文件永远空的，旧文件还在悄悄涨。
  - **改完要在服务器上跑一次 `sudo bash ops/setup-cron.sh`**，否则轮转不生效。

- **🔍 采购收货到底过账了什么（2026-08-09 排查结论，先记住这三句）**
  1. **收货只自动入库，不自动出库**。`_auto_stock_in` 全函数只有一处 `db.add(WhTxn)`，
     `direction="in"` 写死。生产实测：1027 条已收货明细 → 1026 条入库、**0 条出库**。
  2. `models.py:690` 和 `_auto_stock_in` docstring 里那句「is_stock=False → 收货自动
     入库+出库(直发项目)」是**过时注释**，代码在「库位管理批次」里已删。
     `is_stock` 现在是**死字段**——全后端没有一个 `if` 读它，前端开关也已删除。
  3. `project_code` 只做两件事：反查项目 id 打在**入库**流水上、让这批料出现在该项目的
     「物料需求」里等人领。**它不触发出库、不计项目成本**。
  - **成本口径的时点**（容易被问到）：下单填了单价 → 毛利表「直发/外协」腿先计一笔；
    收货生成流水 → 这条明细被 `not_in(linked)` 踢出该腿，成本**反而消失**；
    仓库领料出库 → 这时才按加权均价计回去。所以**收了货不领料，项目材料成本就是 0**。
  - ⚠️ **不要改成「带项目编号就自动出入库」**：生产上 1027/1027 收货明细**全部**带项目编号，
    这条规则等于"全部自动出库"，库存永远显示 0，库位管理/盘点/缺料预警一起作废
    （自动出库当初正是为了库位管理才删掉的）。要做直发应该是下单时**显式勾选**。

- **🐛 采购收货两个静默失败（2026-08-09 修复）**
  | 问题 | 现象 | 改法 |
  |---|---|---|
  | 冲红后重新收货，库存不动 | 接口 200、提示「收货成功」，库存纹丝不动 | `_auto_stock_in` 幂等守卫补 `reversed == False` |
  | 收货后改订单编号，流水还挂旧项目 | 明细上编号改对了，料在正确项目的「物料需求」里永远不出现 | 新增 `_sync_txn_project`，两条收货路径 + 明细 PUT 都调 |
  - **根因（同一个）**：冲红是**保留原单**、另插反向单、把原单标 `reversed=True`
    （原单的 `is_reversal` 仍是 False）。只滤 `is_reversal` 的守卫永远命中原单。
    同文件删除明细那段两个条件都滤了，是漏写。`_sync_txn_amount` 一并补上——
    否则冲红后重新收货会留下两条非冲红流水，改价把旧单也改了，与冲红单对不上、净额差一截。
  - 两个都**不报错、界面还提示成功**，人工不可能发现。测试
    `tests/test_recv_reversal_and_project_sync.py`（17 条断言）+ 反证：
    去掉修复后 5 条立刻红。生产存量 0 例，属于修在爆发前。
  - **订单编号自由输入的坑**：收货弹窗的下拉原来**只列项目**，「备用/车间耗材」这 8 个
    合法非项目编号（`material_dict.dtype='order_no'`）得靠人手打——于是生产上打出了
    「车间消耗」「消耗」「消耗品」三个「车间耗材」的变体，还有打错的 `2025-087`
    （项目不存在，¥10,208 成了无主料）。现在下拉分「非项目（字典维护）/ 项目」两组供选，
    仍手打未知编号则就地黄字提示（不硬拦——总有临时情况）。
    判定口径与财务「成本审计 → 孤儿采购」完全一致（项目编号 ∪ order_no 字典），
    那张报表**早就存在**，只是没人看：现存 7 个未知编号 / 17 条 / ¥11,637。

- **🆕 企业微信内嵌：可点消息 + 静默登录（2026-08-10，本次；代码已上，开关默认关）**

  **先说现状（查出来的，不是猜的）**：企微推送**早就是通的**——
  `gettoken` 实测 `errcode:0`，`.env.prod` 里 CorpId/AgentId/Secret 三个都已填，
  **29 个在职用户里 22 个已绑 `wxid`**。所以缺的从来不是凭证，是「点开就能用」。

  | 本次加的 | 说明 |
  |---|---|
  | `settings.public_base_url` | 对外 https 根地址。**留空 = 消息退回纯文本、OAuth 关闭**，与现状完全一致 |
  | `settings.wecom_oauth_enabled` | 静默登录开关，**默认 False** |
  | `notify._wecom_payload()` | 配了域名就发 `textcard`（可点，落到 `/h5/`），否则发 `text` |
  | `GET /api/auth/wecom/entry` | 配在企微工作台的入口，302 到企微授权 |
  | `POST /api/auth/wecom` | code → `getuserinfo` 拿 UserId → 按 `users.wxid` 找人 → 发 token |
  | `frontend/src/h5/wecom.ts` | 企微 UA 里带 `wxwork` 时，第一次导航自动拿 code 换 token |

  - ⚠️ **两个开关默认都关，上线不改变任何现有行为**。域名验证没做完就打开，
    用户点进来只会看到企微的报错页——**比没有链接更糟**。
  - ⚠️ **静默登录不走外网验证码闸门，这是有意的**：现行闸门是
    「输密码 → 把 6 位码**推到企微** → 再输码」，也就是说
    **「能看到企微」本来就是现行的第二因子**。走 OAuth 的人已经在企业微信
    客户端里通过了企业身份认证，用的是同一个信任锚点，只是省掉打字。
    `gate.desktop_exempt`（桌面客户端免闸）是同一类先例。
    **前提是 corpid/secret 只在服务端、code 一次性且 5 分钟过期**——破了任一条豁免就不成立。
  - ⚠️ **认出企微身份 ≠ 给系统账号**：`wxid` 找不到人时只报 403，
    **绝不自动建账号**。企微通讯录里有访客和外部联系人，自动建号等于开后门。
    测试里专门断言了「403 之后用户数一个没多」。
  - ⚠️ `wecom_userid_by_code` **复用 `_wecom_token()` 的缓存**，别另开一套：
    同一应用的 access_token 只认最新签发的那个，两套缓存会来回把对方顶掉。
  - ⚠️ 前端摘 code **两个地方都要看**：企微把 `?code=` 挂在 hash 之前
    （`/h5/?code=X#/`），hash 路由跑起来后可能变成 `/h5/#/?code=X`。
    摘完立刻从地址栏抹掉——留着刷新会拿**已用过**的 code 再试一次，必然失败。

  **✅ 2026-08-16 已开通并实测通过**（`PUBLIC_BASE_URL` + `WECOM_OAUTH_ENABLED=true` 已上生产）：

  | 验的什么 | 结果 |
  |---|---|
  | `/api/auth/wecom/entry` | 307 → 企微授权页，`scope=snsapi_base`、回调地址编码正确 |
  | 假 code 打 `/api/auth/wecom` | 401「企业微信身份校验失败」——说明**真去问了企微**，且没泄漏 errcode |
  | 回调地址 `/h5/?code=...` | 200，正常返回 H5 |
  | `WW_verify_l7PzWvOPuQQXUTtf.txt` | HTTP/HTTPS 都 200（**做微信提醒时就放好了**，这次直接复用） |

  - ⚠️⚠️ **`docker-compose.prod.yml` 不透传的变量，写进 `.env.prod` 也进不了容器**。
    第一版就漏了 `PUBLIC_BASE_URL` / `WECOM_OAUTH_ENABLED` 两个。
    这类问题最难查：配了、重启了、就是不生效，**哪儿都不报错**。
    加任何新配置项，改 `config.py` 的同时必须改 compose 的 `environment:`。
  - ⚠️ 企微后台的可信域名有**两个格子**，别填错：
    「可作为应用OAuth2.0网页授权功能的**回调**域名」才是静默登录用的；
    下面那个「可调用 JS-SDK、跳转小程序」是另一回事，填了也不顶用。

  **四端互不影响（发 OTA 前逐个证过，不是推断）**：

  | 端 | 结论 | 怎么证的 |
  |---|---|---|
  | 网页版 / 桌面客户端 | **完全不含企微代码** | `grep -rl wxwork frontend/dist/` = 0 个文件（客户端打的就是 `dist/`） |
  | 手机 APP | 代码在包里但**走不到** | 实测 Android WebView UA `/wxwork/i` 不命中；地址带 `?code=` 时应用仍直接去 `#/login`，`stripCode()` 没执行，也没发出任何 `/auth/wecom` 请求 |
  | 企业微信内嵌 | 正常命中 | 同一段正则对企微 UA（含 `wxwork/4.1.26`）返回 true |

  OTA 包 `2026.08.16-1` 已发（签名过 APK 公钥校验，`min_shell` 未抬 = 不用重装 APK）。

  **后台操作（已完成，留档给下次换应用时用）**：
  1. 应用 → 网页授权及 JS-SDK → 可信域名 `www.tonghuizhineng.top` →
     下载 `WW_verify_xxx.txt` 丢进 `nginx/wecom-verify/`（nginx 坑位和 compose 挂载都现成）
  2. 应用 → 工作台应用主页填 `https://www.tonghuizhineng.top/api/auth/wecom/entry`
  3. 应用 → 企业可信 IP 加 `8.141.123.141`（不加 `gettoken` 会被拒）
  4. 服务器 `.env.prod` 加 `PUBLIC_BASE_URL=https://www.tonghuizhineng.top`
     和 `WECOM_OAUTH_ENABLED=true`，重启后端

  测试：`tests/test_wecom_embed.py`（8 例，含「不自动建号」「停用账号拒绝」
  「企微挂了转 401 且不泄漏 errcode」）。

- **🆕 AI 助手：项目明细走表格 + 提问卡（2026-08-10，本次）**
  依据是杨坛 8-10 的真实使用记录（`/api/agent/chat-logs` 翻出来的）：
  **5 次问答里 3 次 `rendered=false`**，最高频的「查某个项目」反而没有表格；
  「037的电是谁做的」问了两遍都答不上来。

  | 改动 | 为什么 |
  |---|---|
  | `get_project` 摊出 `items` + `columns` | 以前只返回**嵌套对象**（dept_orders / produce_groups / purchase_pending 各一层），而渲染层只认 `items`/`suppliers`/`rows` 这类**列表**字段 → 明细全部退回成一堆文字 |
  | 补 `worker` / `supplier` 姓名 | 138 条部门单里有 `worker_id`，工具压根没把人名带出来；采购只有 `supplier_id` |
  | 部门/生产组/状态中文化 | 给业务看 `dispatched` / `electric` 等于没答 |
  | 生产拆到组就不报父单 | 原来「生产 / 生产·钣金 / 生产·装配」一件事报三行 |
  | `detail=purchase` | 问「还有什么没到」时明细换成 物料/供应商/超期 |

  - ⚠️ **`purchase_pending` 的键 `name` → `item_name`**（与表字段一致）。
    渲染层 `_NAME_KEYS` 按 `item_name` 才能匹配到「物料」表头，叫 `name` 会退化成「名称」。
  - ⚠️ **多项目时项目号并进 `item_name`，不单开一列**：渲染层会把「编号+名称」
    并成第一格，3 列预算就被吃掉一列，**供应商直接被挤没**（实测）。

  **🆕 提问卡（```ask 块）**：模型拿不准时不猜也不踢皮球，给几个可点的选项，
  点一下把预置问题直接发出去。覆盖「060 命中两个项目」这类歧义——
  原来模型只能在「猜一个」和「请提供更多信息」之间二选一，前者可能答错，
  后者要用户重新打字。
  - 后端 `extract_ask()` / `ask_to_text()`（`agent_router.py`），前端 `.ask-opt`（`H5ChatView.vue`）。
  - **渲染不了卡片的调用方（非流式 `/chat`、桌面端）降级成纯文本编号选项**，
    问题不会丢——那是模型唯一想说的话，丢了用户就收到一句没头没尾的结论。
  - 提问卡内容一并写进审计日志，否则复盘时看不懂那条记录。

  **🐛 顺带修掉两个泄漏（同一类，都会被用户看见）**：
  - `render` / `ask` 块的正则捕获组原来写死 `(\{.*?\})`，模型把块写成**数组**时
    正则整个不匹配 → **块原样漏给用户看**。改成 `(.*?)`：
    **先无条件删干净，再去解析**；解析失败顶多少一段明细，可接受。
  - 合法 JSON 但不是对象（`[1,2,3]`）时 `plan.get()` 直接抛异常 → 整条请求 500。

  测试：`tests/test_agent_ask_card.py`（10 例）、`tests/test_agent_project_detail.py`（25 项断言）；
  18 个 `test_agent_*` 全绿；`vue-tsc` 无报错。
  - ⚠️ **`tests/` 不能用 `pytest tests/` 一把收**：多数用例是独立脚本，
    import 期就设 `DATABASE_URL` 并 `os.chdir`，同进程收集会互相打架（100 个 collection error）。
    逐个 `python tests/xxx.py` 跑。全量 88 通过 / 13 失败，**那 13 个在未改动的 main 上同样全红**
    （销售、订单、物流、外协模板），是存量问题，不是本次引入的。

- **🔧 `ops/verify-shipped.sh` 原来查不到 H5 和后端的改动（2026-08-10 修）**
  它只 grep 网页版的 `/usr/share/nginx/html/assets/`，于是查任何 H5 的特征串
  都报「不在生产构建产物里」——**连早就上线的 `sumcard` 也查不到**，
  差点被当成发版失败去重发。查后端函数名同理，永远查不到。
  现在三处都搜，并在结果里标出命中在哪：
  | 产物 | 位置 | 来源 |
  |---|---|---|
  | 网页版 | `pms2_frontend:/usr/share/nginx/html/assets` | `npm run build` |
  | H5 助手 | `pms2_frontend:/usr/share/nginx/html/h5` | `npm run build:h5`（base=`/h5/`） |
  | 后端 | `pms2_backend:/app`（`*.py`） | Python 源码，没有构建产物 |

  **同一天第二处误报：「客户端落后」把 H5 也算进去了。** 桌面安装包打的只有网页版
  （`desktop-build.yml`：`npm run build` + `cp -R frontend/dist desktop/app`），
  `frontend/src/h5/` 根本进不了安装包——它走 `dist-h5/` 和手机 APP 的 OTA 通道。
  不排掉的话，改一行 H5 就报「客户端落后」，逼着白发一版客户端。
  现在按路径分开算，H5 的改动单独提示「要跑 `mobile/ship.sh`」。

- **⚠️ 主检出别当临时编辑区用（2026-08-10 踩到）**
  `models.py` 有一行注释改动（`fail_count` 的「>=5 锁定」在 7-28 就去掉了，
  gate.py 和 test_login_gate.py 早是新行为，只有这行没跟上）**悬在主检出的工作区里一整天没提交**。
  之后每个 worktree 都是从 `main` 拉的，拿到的都是旧注释，**谁也碰不到它**——
  worktree 隔离的是目录，不隔离「主检出里散着的未提交改动」。
  最后是 `worktree.sh merge` 的脏检查把它逼出来的。**临时改动也要落到某个 worktree 里。**

- **📮 反馈#359（杨坛）：「每个界面都加一个刷新的功能」——已做，顺带修出一个老 bug**
  - **他为什么提**：原来只有零星几个 tab 里有刷新按钮（财务部就只在「请款审批」
    那个 tab 有），他在别的 tab 上审批完看到的还是旧数据；想整页重载也没退路——
    桌面客户端 `autoHideMenuBar: true`，菜单里的「重新加载」根本看不见。
  - **做法**：新组件 `components/PageRefresh.vue`，放进**每个页面**的标题栏右侧
    （全站 29 个路由已逐个实测确认都有）。
    - ⚠️ **只重跑本页的数据请求，不重载页面**：当前 tab、筛选、展开行、
      滚动位置全部保住。整页重载会把这些打回默认，对"审批完看看新状态"
      这个场景反而更难用。
    - ⚠️ **有 tab 的页面必须带上当前 tab 的加载函数**（`onTab(tab)` / `onTabChange(tab)` /
      `onFinTab(tab)`）：那些数据是切 tab 时才拉的，只跑 onMounted 那套
      等于没刷新他正在看的东西。
    - AI 助手页只重拉模型列表/审计日志，**不动对话记录**——聊天页上一个
      「刷新」很容易被当成"清空对话"。
  - **🐛 顺带修的老 bug：页头控件一多就把标题压成竖条**。
    `.page-header` 是不换行的 flex，控件多的页面会把标题块挤到接近 0 宽。
    销售部（6 个按钮）在 1280 宽下**标题被压成一个字一行的竖条**，
    说明文字整段竖排，实测页头高 155px→442px。加刷新按钮只是把 1280 也压过了界，
    窄屏本来就这样。
    改法：`.page-header` 允许换行 + `h1` 不换行 + `justify-content:flex-end`。
    - ⚠️ 撑标题块**不能**用 `> *:first-child { min-width }`：项目详情页页头的
      第一个元素是「返回」按钮，会被撑成 280px。让 `h1` 不换行即可——
      标题块的最小宽度自然不小于标题本身，挤不下的控件只能换行。
    - ⚠️ `justify-content:flex-end` 的前提是**每个 `.page-header` 都有 `.spacer`**，
      否则标题会被推到右边（PlaceholderView 原来没有，已补）。
    - 实测 29 个路由：标题全部单行且靠左，刷新按钮都在可视范围内；
      销售部 442→93，比加按钮之前的 155 还矮。

- **📮 反馈批次 2026-08-08（3 条，全部完成）**
  | 提出人 | 问题 | 根因 / 做法 |
  |---|---|---|
  | 李新新 #358 | 现金账全额付清了，却显示「部分付款」 | 见下（显示口径 bug，生产上 35 张单中招） |
  | 杨坛 #357 | 售后审批时看不到具体费用清单 | 台账加展开行 + **审批确认框里列出每项费用**（展开行要主动点，确认框是必经的） |
  | 李新新 #356 | 备注怎么填？外购件只有图纸，仓库对料要详细尺寸 | 备注一直存得下、也印在采购单 PDF 上，但**采购收货那张表从不显示它**——收货页加备注列 + 整单维护里也能补写 |

  - **#358 根因（值得记住的一类错）**：付款是**按单付**的，供应商收的是一张采购单的钱；
    而付款状态是**按行**判的。「整单维护」为了让汇总合计恰好等于所填总额，
    故意把整单付款额记在**首行**、其余置 0（`set_group_summary`），
    于是除首行外全是「未付款」，前端合并父行 `every(已付款)` 不成立 → 「部分付款」。
    现场：`TH20260808-019` 共 13 行，收货 12.92 / 已付 12.92，只有 1 行带付款额。
    生产上 129 张已付清的采购单里 **35 张**这样显示错（170 行）。
    - **改法**：`_attach_pay_status` 末尾加一遍整单判定——整单收货额已付清就把该单
      **所有行**提升为「已付款」。
    - ⚠️ **只升不降**：Σ付 < Σ收 的单一行都不动。反过来做（整单没付清就把所有行标成
      未付款）会把「已经付了一部分」显示成「一分没付」，比原来的错误严重得多。
      「请款单只覆盖 5 行里的 2 行」这种真·部分付款也因此不受影响，逐行明细保住。
    - ⚠️ **合计必须回数据库按整单算**，不能只加接口这一页返回的行：
      分页/筛选时拿到的只是该单的一部分，按残缺合计判会把没付清的单标成已付款。
    - 验证：`tests/test_fb358_po_pay_status.py` 13 条断言；**反证**——去掉改动后
      其中 4 条立刻红（3 条"不许乱升"的断言前后都绿，说明没有过度放宽）。
    - **没动存量数据**：这是显示口径的问题，`paid_amount` 一分钱都没改写。
      重新分摊历史付款额也能修，但那是在生产上批量改财务数字，不值得。

- **🚨 生产事故：当天所有 OA 申请都提不了（2026-08-07，已修）**
  - **现象**：提交 OA 申请弹「数据已存在，不能重复」（409）。不是某个类型的问题，
    是**当天任何人、任何单据类型都提不了**。
  - **根因**：`_next_oa_no` 用的是 `count(distinct request_no) + 1`。
    当天有 `OA20260807-001 / -003 / -004`（002 被删过），count=3 → 生成 **004**
    → 004 已存在 → `request_no` 唯一约束冲突。
    **只要当天删过/撤过一张单，后面所有单都提不了。**
  - **改法**：改成取当天**最大序号 + 1**。
    ⚠️ 不能用字符串 `max()`：序号超过 999 变 4 位后，字符串比较会把 `"1000"`
       排在 `"999"` 前面。取回来按整数比（当天单量很小，成本可忽略）。
    ⚠️ 同时加了**并发重试**：两个人同时提交会读到同一个 max，撞了就回滚重算，
       最多 5 次。不重试的话并发提交会直接甩用户一个「数据已存在」，
       而他什么也没做错。
  - **教训**：流水号**永远不要用 count 生成**——它隐含"号是连续且从不删除"的假设，
    而这个假设在任何真实系统里都不成立。同类写法要一并排查。
  - 验证：`tests/test_oa_request_no.py` 8 条断言全绿（含缺号、不复用空缺号、
    跨天重置、>999、手工怪号不卡死）；并做了**反证**——用旧算法跑同样数据
    确实生成 004 撞号，确认修的就是这个问题。

- **📮 反馈批次 2026-08-07（7 条，全部完成）**
  | 提出人 | 问题 | 做法 |
  |---|---|---|
  | 周瑞 | 外网进入点开技术文档**还是**无法下载 | 见下（我上次没修对） |
  | 杨坛 | 付款申请拆对公/现金（审批流不同） | 新增 `payment_public`/`payment_cash`，旧 `payment` 停用不删 |
  | 杨坛 | 低于安全库存要能展开清单并直接提采购申请 | 预警框可展开成表 + 「一键提采购申请」带出缺口数量 |
  | 杨坛 | 出库登记要能搜采购单号再选物料 | 新端点 `GET /wh/po-items` + 出库单「按采购单号带入」 |
  | 杨坛 | 字典分类不要默认展开 | 默认全收起 + 展开状态记 localStorage + 一键展开/收起 |
  | 王利利 | 一键出库容易出错 | 需求明细默认「只看现在能出库的」+ 确认框列出到底出哪几样 |
  | 王芹 | 付款人看不到备注 | 财务「付款」tab 补备注列（「请款审批」tab 本来就有，付款这步反而没有） |

  - **⚠️ 周瑞这条我上次(#345)没修对，教训要记住**：
    上次只 `grep` 确认了下载按钮存在，**没量它的位置**——按钮确实在，但**在视口外面**。
    原来 `top=4vh` + iframe 写死 `height:82vh` + footer，1366×768 上实测
    弹窗底部 803px、按钮底 771px，而视口只有 768px：按钮被切掉，
    弹窗整体溢出又滚不到，人看得见 PDF 就是够不着下载。
    日志印证：他 07:32 那次下载接口返回 200、192653 字节——**字节到了客户端**，
    问题不在网络也不在后端。
    改法：弹窗 flex 列布局 + `max-height:92vh`，body 弹性填充可滚动；
    **标题栏再放一个「下载」**，布局再出意外顶部这个也够得着。
    - ⚠️ body 必须给 `flex-basis: calc(92vh - 130px)`：只写 `flex:1` 的话
      iframe 的 `height:100%` 撑不起父级，弹窗缩成一小条，预览区被压扁等于没法看。
    - ⚠️ 那段样式**不能加 scoped**：dialog 是 `append-to-body` 的，
      scoped 的属性选择器不会挂到 body 下那个节点上，写了等于没写。
  - **付款拆分的两个坑**：
    - 光加单据类型不够——新类型没有审批链，一提交就被「尚未配置审批流程」挡住，
      **功能上线即不可用**。`split_payment_doc_type` 会把旧 payment 的链复制到两个新类型。
    - `split_payment_doc_type` 必须排在 `backfill_oa_doc_types` **之后**（新类型得先存在）；
      放前面它什么也不做**而且不报错**，是静默失效。
    - 现金付款**不校验银行账户**——现金根本没有账号开户行，照 #348 一刀切要求填
      只会逼人瞎填一个，比不填更糟。
    - 连带把 `test_oa_payment_request` / `test_fb348_payment_account` 迁到 `payment_public`
      （不是放松校验，对公的必填口径跟原来一模一样）。
  - **⚠️ 一个差点静默失效的问题**：`split_payment_doc_type` 用了 `func` 但
    `data_migration.py` 没导入它 —— `run_all` 里每个迁移都包在 `try/except` 里，
    NameError 会被吞掉只留一行 warning，生产上就是"迁移跑了但什么都没发生"。
    是新写的测试逮到的。
  - 验证：新增 `test_fb0807_wh_po_items.py`(14 条) + `test_fb0807_payment_split.py`(13 条) 全绿；
    vue-tsc + vite build 通过；全量 96 个用例失败 13 个，与改动前基线一致。
    预览弹窗在 600/768 两种视口实测两个下载按钮都在屏内。

- **🔍 仓库全模块搜索（2026-08-06 仓库反馈）**
  - 原话「材料入库后需要支持搜索，物料主数据里需要支持搜索，仓库的所有功能都要增加搜索条件」。
  - **真正的病根不是"没有搜索框"，是搜索是假的**：出入库流水生产上已 **1083 条**，
    而接口只回最近 200 条、前端在这 200 条里 filter ——
    **前端能搜到的最早只到昨天（2026-08-05）**，仓库入完料第二天就搜不着了。
  - **改法（关键是分清两种情况，不要一刀切全下沉后端）**：
    | 情况 | 处理 | 为什么 |
    |---|---|---|
    | 数据在后端被截断（出入库流水） | **必须服务端搜** | 只在返回的那批里找 = 搜不全 |
    | 数据整份都在前端（库位41/汇总/需求/发货/采购申请） | 客户端过滤即可 | 已经全在浏览器里，搜得全，不必改接口 |
    | 本来就有后端搜索（采购收货） | **不动** | 它三个框都已下沉后端(#330)，再叠一层只会两套口径打架 |
  - `GET /wh/txns` 改动：加 `kw`/`date_from`/`date_to`，返回从数组变成
    **`{rows, total, shown}`**——`total>shown` 时前端明确提示「只显示了最近 N 条，请缩小范围」。
    ⚠️ 不提示的话人会以为搜完了，**比搜不到更糟**。
    kw 要 join `wh_materials`(物料名/规格) 和 `projects`(项目编号)——
    不 join 的话"搜密封圈""搜 2026-071"这两种最常用的搜法直接落空。
  - `GET /wh/materials`：kw 扩到 单位/库位/材质（**编码全库只有 6/551 有值，只按编码搜等于搜不到**），
    加 `location` 精确筛 + `low_only`。
    ⚠️ `low_only` 必须在**算完实时库存之后**过滤——low 是 stock 与 safety_stock 比出来的。
  - **9 个 tab 现在都能搜**：库存总览／收发存汇总／出入库流水／物料主数据／库位管理／
    出入库·物料需求／采购收货／发货清单／采购申请。
  - ⚠️ **改这块要注意的**：
    - `WhTxnOut` 的返回结构变了，`test_wh_batch_out` / `test_m07_warehouse` 三处
      `.json()` 要取 `["rows"]`（已改）。
    - 客户端过滤的那几个 computed **必须写在文件末尾**——它们引用的 ref 在几百行后才声明，
      写在前面会踩 TDZ，页面一开就白屏。
    - 采购申请的物料名在展开行 `lines` 里，主行看不到——搜索要下钻 lines，
      否则仓库按物料名找自己提过的申请永远找不到。
  - 验证：新增 `tests/test_wh_search.py` **29 条断言**全绿，其中最要害的一条是
    「limit=2 时 total 仍是 15」——证明是先筛后截断，不是在截断后的结果里搜。
    浏览器实测搜索/计数/空态提示；全量 94 个用例失败 13 个，与改动前一致。

- **📮 反馈#348（杨坛）：OA 付款申请加收款账户（2026-08-06）**
  - 原话「OA付款申请里面加一下收款账户」。他同时是 finance_lead——批完这张单要真去打款，
    而单子上只有收款单位、没有账号和开户行，只能回头一个个问，钱压在那儿付不出去。
  - `detail` 里加 `payee_account`（收款账号）+ `payee_bank`（开户行），**两个都必填**。
    ⚠️ 少一样照样打不出款，那这个字段就白加了，所以不是"加上去就行"而是必须都填。
  - 校验前后端同口径：只拦前端的话 H5／旧客户端／直接打接口都能绕过去。
  - 「收款单位」下加了提示「要跟银行账户的户名一致，对不上银行会退回」——
    没有另加"户名"字段，避免同一个信息两处填、两处不一致。
  - ⚠️ 改这条**连带打红了 `test_oa_payment_request`**（老 payload 没这两项）。
    处理方式是给老用例补上字段并把新必填也纳入它的 bad_cases，
    **不是放松校验**——放松了就等于没改。
  - 验证：新增 `tests/test_fb348_payment_account.py` 9 条断言全绿（含"别的单据类型不受影响"）；
    浏览器实测三档拦截（缺账号／缺开户行／填全可提交），审批详情抽屉里能看到
    收款账号和开户行。全量 93 个用例失败 13 个，与改动前一致。

- **🌲 并行会话开 worktree：`ops/worktree.sh`（2026-08-06）**
  - **起因**：今晚两个会话在同一个工作区改代码，互相把对方改到一半的文件卷进
    自己的 commit（`f4c98bb` 和 `682b24c` 各一次）。连锁反应才是真正的伤害：
    `git status` 里永远混着别人的文件 → 不敢 `git add -A` → 只能手打路径挑自己的
    → 一次打 10 个路径，`AfterSalesView.vue` 打成小写 s → macOS 不区分大小写
    **不报错只静默漏掉** → 209 行没进版本库、没上线，而本地一路绿灯。
  - **流程**：
    ```bash
    bash ops/worktree.sh new  agent     # 建 ../pms-wt-agent，分支 claude/agent
    cd ../pms-wt-agent                  # 在这里干活、提交
    bash ops/worktree.sh list           # 谁领先/落后多少、谁还有没提交的
    bash ops/worktree.sh merge agent    # 回主检出跑，合回 main 并推送
    bash ops/release.sh                 # 发网页版
    bash desktop/ship.sh                # 动了前端就必须发客户端
    bash ops/verify-shipped.sh <关键词>  # 查线上实物
    bash ops/worktree.sh rm   agent     # 收工删目录（分支保留）
    ```
  - **设计要点（改这个脚本前先看）**：
    | 点 | 为什么 |
    |---|---|
    | 依赖用**软链**不各装一份 | node_modules ×2 + venv 共 ~700MB；`.env.local`/`.deploy.local` 也软链，只有一份、不会误提交 |
    | 软链要写进 `$GIT_COMMON_DIR/info/exclude` | `.gitignore` 里是 `node_modules/`(带斜杠只匹配目录)，而软链在 git 眼里是**文件**，匹配不上会冒充未跟踪文件 |
    | 依赖清单变了要告警 | 共用 node_modules 的前提是 lock 没变；变了必须真装一次，否则用旧依赖跑新代码 |
    | merge 前拦「已跟踪文件改了没提交」 | 就是今天栽的那种；未跟踪(`??`)只提醒不拦，否则一堆分析稿会把人烦死 |
    | `list` 必须用 `git worktree list --porcelain` | 仓库路径里有空格(`excel share`)，按空白拆会把目录劈成两半 |
    | **发版只在主检出做** | 服务器拉的是 `origin/main`，在分支上发版等于发了个跟线上无关的东西 |

- **🚨 「发版成功」≠ 你的代码上线了（2026-08-06 的教训，必读）**
  - **出了什么事**：第二批的前端有两块（OaView 的成本报表 + AfterSalesView 的费用清单），
    只上了前者。`git add` 时把路径手打成 `AftersalesView.vue`（**小写 s**），
    而仓库里实际是 `AfterSalesView.vue`（**大写 S**）。
    macOS 文件系统不区分大小写，于是本地一路绿灯——vue-tsc 过、vite build 过、
    我还在浏览器里把「登记→审批→财务核对→报销」整条流程测通了——
    `ops/release.sh` 也报「发版成功」。但那 **209 行改动从没进过版本库**，线上自然没有。
  - **为什么所有检查都没拦住**：它们查的都是**工作区**，而漏的是**版本库**。
    工作区是对的，所以怎么测都对。
  - **⚠️ 客户端还要多一层**：桌面客户端加载的是**打进安装包的前端**
    （`main.js` → `app/index.html`），不是服务器上的。所以**任何前端改动都要重新
    `bash desktop/ship.sh`**，光发网页版客户端用户一行都看不到。
    这次 1.0.34 之后攒了 8 个动前端的提交才被问出来。
  - **新增 `ops/verify-shipped.sh`——以后回答「发版了吗」一律用它**：
    ```bash
    bash ops/verify-shipped.sh 售后费用清单 指定审批人
    ```
    | 它查什么 | 能抓到什么 |
    |---|---|
    | 本地 commit vs 生产 commit + 未提交改动清单 | 「改了但没提交」「提交了但没部署」 |
    | 仓库客户端版本 vs 更新通道版本；客户端打包后有没有新的前端提交 | 「只发了网页版，客户端还是旧的」 |
    | 到生产构建产物里 grep 功能特征字符串 | 「流程跑完了但功能不在里面」——**这条是实证，其余是对账** |
  - CI 的 `verify-asar.js` **只查 4 个文件在不在**（index.html/main.js/preload.js/health.js），
    不查前端内容是不是最新的，别指望它拦这类问题。

- **✅ 第二批：售后登记自带报销(④) + 费用按部门归成本(②)（2026-08-06，已完成）**
  设计稿：`docs/需求设计稿-2026-08-06-审批到人·成本归集·发货齐套·售后报销.html`
  **这两条必须捆在一起发**——单独发任何一条，同一笔售后费用都会被算两遍。
  - **重复是真的，生产上抓到实例**：售后登记里 `2025-120` 记了 ¥216，
    OA 里又有一张「2025-120行星搅拌机售后维修」¥1,136；
    `2026-024B金华压料机售后维修` 同样走了 OA。
  - **④ 售后/安装登记 → 费用清单**（新表 `aftersales_items`）：
    每行 费用项 + 金额 + **发票附件**；**总额由明细自动合计写回 `AfterSales.cost`**。
    保持 `cost` 是唯一成本口径，售后侵蚀预警/成本报表/历史查询一行都不用改。
    差旅、住宿也在这里加一行，不用再去 OA。
  - **报销支腿**：`AfterSales.pay_status` = checking(待财务核对) / invoice_fix(发票退回)
    / reimbursed(已安排报销)；NULL = 旧流程登记的记录。
    - ⚠️ **刻意不并进 `status`**：`status=approved` 是"费用已确认发生"的成本口径，
      现有成本统计和售后侵蚀预警都按它算。**退回发票时绝不能动 status**——
      退的是发票不是费用，打回 pending 会让这笔钱从售后成本里消失，月底对不上。
    - 核对用现有 `finance` 角色，不新建出纳；发票对不上退回**登记人**重传，
      改完直接回财务这一步，不用主管再批一遍。
    - 还有行没传发票时不许点「安排报销」（后端硬拦，不只是前端藏按钮）。
  - **② 成本归集**：`Department.cost_center`（销售成本/售后成本/制造费用/管理费用），
    在部门管理里维护。新端点 `GET /api/oa/reports/cost?period=YYYY-MM`。
    | 口径 | 说明 |
    |---|---|
    | 计入时点 | 审批通过即计入，**含 `pending_payment`** |
    | 金额 | 财务核定金额；为空回退申请金额 |
    | 采购申请 | **不计**，一律以采购单为准 |
    | 售后成本 | **只认售后登记**，不吃 OA |
    - ⚠️ 「审批通过」在数据上是**两个** status：`approved` 和 `pending_payment`。
      末环节是财务的报销类单审完会转 `pending_payment`，只按 approved 过滤
      这些单会从成本里凭空消失。现有的 `/reports/summary` 就有这个毛病（未改）。
    - 售后部走 OA 的存量报销单在报表里**单独说明、不并进合计**，避免重复。
  - **配套**：售后部再提 OA 报销类单据会被 400 挡住并**指路到售后登记**
    （只挡新提交，在途旧单按签字口径原路走完）。
  - **顺手修的两个隐患**：
    | 问题 | 后果 |
    |---|---|
    | `ALLOWED_BIZ_TYPES` 缺 `aftersales_invoice` | 发票压根传不上去（测试才暴露） |
    | `_NEW_COLUMNS` 里 `purchase_items`/`wh_materials` **各被写了两次** | dict 后者覆盖前者，前一条的 9 列（po_no/brand/invoice_no…）**从没被补过**；生产上碰巧都在，但拿旧备份恢复就缺列炸掉。已合并，并加 `tests/test_schema_columns_no_dup.py` 永久盯着 |
  - ⚠️ **浏览器实测才发现的坑**：财务核对的按钮我一开始做在**售后部**页面上——
    但**财务的菜单里根本没有「售后部」**，他们走不到。已挪到
    「财务部 → 安装/售后费用」tab（财务真正干活的地方），费用明细放展开行，
    否则那张表会被挤到要横向滚动才看得见操作按钮。
  - **验证**：`test_aftersales_reimburse_cost.py` 31 条断言全绿；
    `test_schema_columns_no_dup.py`（含反向自检）全绿；vue-tsc + vite build 通过；
    全量 92 个用例跑下来失败 13 个，**与改动前的 13 个完全一致**（历史欠账，见下节）。
    浏览器端到端实测：登记(3 行费用自动合计 ¥1,400) → 售后主管审批 → 财务缺发票被拦
    → 发票退回 → 登记人重传 3 张发票 → 财务核对无误 → 已安排报销；
    成本报表出「售后成本 ¥1,400 · 来源=售后/安装登记」且带 4 条口径说明。

- **✅ 第一批：OA 审批链「指定到人」+ 代理人(2026-08-06，已完成)**
  设计稿与口径：`docs/需求设计稿-2026-08-06-审批到人·成本归集·发货齐套·售后报销.html`
  - **为什么做**：审批链每一步存的是**角色**，凡是挂着这个角色的人都能收到单。
    有人同时挂销售部+财务部+采购部，于是很多本该别人批的单也落到他待办里。
  - **改法**：`OaApprovalStep.approver_user_id` / `OaRequestStep.approver_user_id`
    (提交时快照)。一步二选一：按角色(老行为，谁在岗谁批)或指定到人。
  - ⚠️ **指定到人必须同时有代理人**，否则人一休假单子就卡死。
    `User.deputy_uid`：本人超 `OA_DEPUTY_TAKEOVER_DAYS`(=3) 天没处理，代理人也能批——
    **本人始终能批，是"多一个人能批"不是"转移"**。按角色配的步骤天然不会卡，不受影响。
  - **计时从 `OaRequestStep.activated_at`(这一步真正轮到的时刻)起算**，不是建单时刻。
    建单时只写第一步，后面几步等 approve 推进到它时才写。
    用建单时刻起算的话，一张在前面几步排了两周的单，一轮到就直接进代理人待办。
  - ⚠️ **踩过的坑，改这块必看**：
    | 坑 | 后果 | 处理 |
    |---|---|---|
    | 待办查询漏了 `approver_user_id IS NULL` | 指定到人白配，同角色的人照样看到 | `pending_me` 三条件：指定给我 / (没指定人 且 我有那角色) / 我是代理人且晾够 |
    | `activated_at` 为 NULL 当成"很久以前" | 升级那一刻**存量在途单全部涌进代理人待办** | NULL = 还没开始计时，一律不放行 |
    | SQLite 还回 naive datetime | `TypeError: can't compare offset-naive and offset-aware` | `_as_utc()` 统一补 UTC；列虽声明 `timezone=True`，**只有 Postgres 会带时区** |
    | 想顺手收窄 `_can_view` 的按角色可见 | 同角色的人连单子都打不开，超出需求 | 只收窄「谁能批」和待办队列；能看见不等于能批 |
    | 配了个已停用的人 | 单子直接卡死且当时无感 | `_check_approver` 在**配置那一刻**就 400 拦住 |
  - 403 的文案会说清楚原因（"这一步指定由 钱一 审批。若 ta 不在，3 天后其代理人可以接手"），
    不再只说"无权审批此步骤"——那句会让人以为是权限配错了。
  - **验证**：`tests/test_oa_approver_user_deputy.py` 36 条断言全绿(含上表每个坑)；
    vue-tsc exit=0；浏览器实测配置页/一览 chip/步骤表/用户管理代理人下拉端到端跑通。
    `test_m01_roles_menus` 的 4 条 FAIL 是**改动前就红的**陈旧用例(见下一节)，与本次无关。

- **🔍 仓库报「更新版本后 071B 的到料日期/进度/仓库签字没了」——查证结论：数据没丢**
  - **证据链**（全部来自 `/backup` 里的每日 pg_dump，没碰生产库）：
    1. 新增 `ops/diff-backup-cells.py`：比对两份备份，数出每个「项目/表/列」的非空格子数，
       只报**变少**的。逐日跑 07-18→08-05 全程：**单日最多掉 3 个格子**（正常编辑），
       没有任何批量丢失。
    2. ds#1235(071B/激光件清单) 的字段 id 在五份备份里**完全一致**(17718~17729)、
       行数一直是 12 —— 列从没被删过重建过。
    3. 「到料日期」「仓库」两列在 07-30 起的**每一份备份里都是空的**，
       直到 2026-08-05 07:00(UTC) 王利利填上——那正是这次让他补录的动作。
    4. 07-29→07-30 之间 071B 的表确实被整体换过一批(ds#1160~1164 → ds#1231~1235，
       是重新导入 Excel)，但**旧的那批行数全是 0**，是建项目时的空模板壳，没覆盖掉数据。
  - **所以不是系统删的**。剩下的可能：填在 Excel 里以为会同步（详单只在导入那一刻映射，
    之后改 Excel 不回流）／填了但当时没提交成功／看的是别的项目。
  - ⚠️ **走过的弯路，别再走一遍**：
    - 我一开始怀疑 `cleanup_misaligned_known_sheets`（每次启动都跑，字段集合跟模板不严格
      相等就删光 fields+records 重建）。它确实是个危险设计——注释里的前置假设
      「真实数据保存在 Excel 里」对采购/仓库这几列**不成立**——但这次不是它干的：
      新增的 `ops/diagnose-sheet-wipe.py` 在生产上跑出来 **447 张表全部对齐、0 张会被清**。
      **这个脚本发版前值得跑一次**，尤其是给模板追加列的版本。
    - 我还一度认定 `DatasheetGrid.saveEdit` 的 `catch {}` 让保存失败静默——**也是错的**：
      `api/index.ts:122` 的全局响应拦截器已经对每次失败弹了错误提示（401 则清 token
      跳登录）。在 catch 里再弹一次会**连着弹两条重复提示**，浏览器实测确认后已撤回。
      现在那个 catch 里留了注释说明为什么刻意不弹。
  - **顺手修的**：`SalesView.submitEdit` 收件信息保存失败被吞掉后**照样弹「已保存」**，
    等于明确告诉用户存上了。改成「台账已保存，但收件信息没保存上」（具体原因仍由
    全局拦截器那条提示给）。
  - **新增两个排障工具**（都只读，不连改任何数据）：
    | 脚本 | 用途 |
    |---|---|
    | `ops/diff-backup-cells.py A.sql.gz B.sql.gz` | 两份备份之间**哪些列的已填值变少了**，报障说"数据没了"先跑这个 |
    | `ops/diagnose-sheet-wipe.py` | 这次启动**会清掉哪些表**，发版前预检（在后端容器里跑） |

- **📮 反馈批次 2026-08-05：#345 / #346 / #347(本次)**
  - **#347 卢照坤「待办、反馈按键无法移动，会有遮挡」**
    两个悬浮球都写死 `position:fixed; right:22px`，正好压住表格最右边的
    「编辑/删除」列，而表格是横向滚动的——用户没法把内容挪开，只能干瞪着。
    新增 `frontend/src/composables/useDraggableFab.ts`：按住拖到任意位置，
    位置记 localStorage，**挪一次一劳永逸**。两个球各自一个 key
    (`pms_fab_feedback` / `pms_fab_todo`)。
    - ⚠️ **拖动与点击必须分清**：按下到松开位移 >4px 算拖，不触发点击。
      不分的话用户想拖就会误开弹窗。
    - ⚠️ **记的是「距右下角的距离」不是 left/top**：窗口一变小，
      存 left/top 会让球跑到屏幕外面。
    - ⚠️ **夹进可视范围只能影响显示，绝不能回写记住的位置**。
      第一版直接把夹完的值写回 `pos`，结果挂载那一瞬 `window.innerWidth`
      可能是 **0**(Electron 最小化 / 渲染进程刚起 / 页面在后台标签页)，
      22 被夹成 8，窗口恢复后再夹读到的已经是 8——**永久回不去**，
      用户自己摆好的位置也会被一次临时窄窗口抹掉。现在量不出尺寸就不夹。
      这个坑是在浏览器里实测 `innerWidth===0` 才发现的，静态看代码看不出来。
  - **#346 李新新「采购涉及优惠券，价格能不能填负数」**
    后端从来没拒过负数，卡人的是前端 `el-input-number` 的 `:min="0"`。
    **一共七处**，只改下单那处等于没改(事后编辑、整单维护、仓库收货补价
    都还是填不了)。已全部放开：`PurchaseMgmtView` 下单明细/单条编辑/整单维护，
    `WarehouseView` 收货单价+金额/批量收货行。
    **数量仍保持 ≥0**(负数量没有业务含义)；物料主数据参考单价、
    出入库流水单价不在优惠券链路上，保留 ≥0。
  - **#345 周瑞「技术文档打开后，无法下载」** —— 两个原因叠在一起：
    1. PDF 是 blob URL 内嵌 `<iframe>`，Chrome 内置阅读器工具栏那个下载按钮
       下的是 blob，文件名变成一串 UUID，桌面客户端里干脆没反应。
       → `#toolbar=0&navpanes=0` 把内置工具栏隐掉，只留我们自己那个
       能正确命名的下载按钮(并放大 + 带上文件名)。
    2. **桌面客户端 `webPreferences` 没开 `plugins`**——Chrome 内置 PDF
       阅读器在 Electron 里算 plugin，而 `plugins` **默认 false**，
       所以客户端里 PDF 压根渲染不出来。已加 `plugins: true`。
       下载走前端自己的 XHR→blob→`<a download>`，命中 `will-download`，
       `item.getFilename()` 拿到的就是 `download` 属性的正确文件名。
  - **验证**：vue-tsc exit=0；浏览器实测拖动/持久化/拖动不误触发弹窗/
    原地点击仍开弹窗/瞬时 0 尺寸不毁位置 全过；后端 `test_fb329_received_price_edit`
    `test_purchase_feedback_batch14` `test_spare_and_purchase` `test_po_append_items`
    全绿；桌面端 `node --test test/*.test.js` 2 pass。

- **🧪 后端测试的跑法与现存红灯(2026-08-05 复核)**
  - `backend/tests/` 是**独立脚本**不是 pytest 用例，`pytest tests/` 只会
    给 86 个收集错误(exit code 5 = 一个用例都没收集到)。正确跑法：
    `./.venv/bin/python tests/test_xxx.py`，逐个跑。**必须用 `backend/.venv`(3.12)**。
    批量：`ls tests/test_*.py | xargs -P 6 -I{} sh -c '...'`，看退出码。
  - 本次顺手修好两个**陈旧**用例(不是产品 bug)：
    - `test_user_feedback.py`：列表接口早就改成分页信封
      `{items,total,page,page_size,stats}`，用例还当裸数组用(8 处)。
    - `test_m07_warehouse.py`：① 后来加了「出库必须挂领用项目，或勾非项目领用
      +填原因」，老 payload 全被 400 挡住(4 处)；② **用例依赖当前月份**——
      冲红单的 `biz_date` 取的是**冲红当天**(不能倒记进已结账月份)，
      用例写死「冲红反向入 25 计进 2026-06」，出了 6 月必红。已改成按冲红
      实际落在哪个月算期望值，并补了一条「冲红月期末 == 实时库存」的跨期断言。
  - **仍红的 14 个是历史欠账**，干净树上也红，跟本批改动无关：
    `test_agent_chat_logs` `test_balance_note` `test_e2e_business_flows`
    `test_e2e_full_lifecycle` `test_m01_roles_menus` `test_m02_sales`
    `test_m04_orders` `test_m08_logistics_e2e` `test_m12_detail` `test_m13_feedback`
    `test_m14_reports` `test_m15_overdue` `test_outsourcing_template` `test_void_sales_order`
    - 已初步排查：`test_m01_roles_menus` 的「sales/electrician list_fields 403 却得 200」
      **不是权限回归**——`datasheets_router.py:170` 的只读端点已有意改成走项目级
      授权 `user_can_view_project`，写端点(create_record/update_cell)仍走 #91 闸门
      且仍是 403。该改的是用例。
    - `test_balance_note` 报「批注后尾款金额不变: 0.0」，这条要重点看是不是
      产品真把尾款清零了，别当成陈旧用例直接改断言。
    - ⚠️ 修这些的时候**不许为了变绿就删断言或改松**，查出是产品问题就修产品。

- **🎴 动作卡第二批：回款登记 + 销售订单审批(2026-08-03，本次)**
  - **回款登记 `ledger_settle`** 是这次的重点：查询工具 `receivable_blind` 找出
    49 笔 ¥2,538,057 无人管的应收，这张卡让他**当场销账**——查得到就能动手，
    不必回电脑。两个动作都打 `PUT /api/sales/ledger/{id}/payment-note`，
    跟他在台账页点批注是同一个端点、同一个 token。
  - **副作用与可逆性(必须知道)**：
    - `field=balance` → balance 清零，原值存进 `balance_contract`，**催办立即停**
    - `field=before_ship` → `ship_receivable` 清零
    - 两者都可逆：删掉批注即恢复原值
    - ⚠️ 这条恢复路径生产上**从未在有值时执行过**(`balance_contract` 全库 0 条，
      因为大家习惯直接改数字而不是点批注)。上线前已在本地跑通完整往返
      「批注→清零→删批注→恢复」，测试 `test_agent_cards_settle.py` 锁死这条。
  - **销售订单审批 `sales_order_approve`**：打现成的 order-approve/order-reject。
    合同额为 0 时出 warn flag——批下去会让该项目毛利算成假亏损。
  - **接口层改成按类型分发**：`cards.ASSEMBLERS` 登记 type→装配函数，
    `verify-action` 按 type 重新装配拿最新 flags。原来写死 pay_req，
    新加的卡类型永远校验不过。新增 `GET /api/agent/cards/{card_type}`。
    **加新卡类型时 ASSEMBLERS 也要登记**，否则那类卡的动作校验不通过。
  - **门户目录加 `card` 字段**：有 card 的卡走卡片通道(能查也能动手)，
    其余走对话(LLM 流式)。直答端点 `/agent/tool` 保留但门户不用——
    绕过模型的话智能体就退化成普通报表页。
  - **验证**：`test_agent_cards_settle.py` 20 条断言 PASSED(含可逆往返、
    行级隔离、越界动作、状态变更后动作失效)；六个旧测试全绿；vue-tsc exit=0。

- **🧰 Agent 第二批工具：销售/台账域 6 个 + 能力清单(2026-08-03，本次)**
  - **为什么补这批**：原来 7 个工具里 3 个是采购的，而杨坛 2026-06-08~08-02 的真实
    操作轨迹是——销售台账 243 次(改台账 164/标记已开票 31/传附件 18/换技术文件 12/
    申请开票 10)、请款审批 40 笔、物流收货人 34 次、销售订单 29 次、**采购 0 次**。
    门户上摆着他不用的卡等于没有。
  - **新增 6 个只读工具**(`app/agent/tools_sales.py`)：
    `receivable_blind` 盯不住的应收 / `shipment_receiver` 待填收货人 /
    `ledger_incomplete` 台账缺件 / `leads_followup` 线索待跟进 /
    `order_pending` 待审销售单 / `invoice_pending` 待开票
  - **最有价值的是 receivable_blind**：现网 63 笔 ¥2,538,057 处于**完全无人管**状态——
    `balance_due` 与 `overdue.scan_balance_due` 的口径都是 `balance_date IS NOT NULL`，
    13 笔 ¥274,837 没填到期日的一条查不到；`ship_receivable` 字段(36 笔 ¥2,263,220)
    **全系统没有任何工具或提醒碰过**。对比能盯住的尾款只有 ¥207,550——差 12 倍。
  - **TOOL_DESC**：每个工具一句人话说明，同时供 ① 门户小字 ② `GET /agent/capabilities`
    ③ 自动生成 TOOL_SCHEMAS 的 description(不再手抄，避免手册 6.3 说的七处同步漂移)。
  - **admin/manager 自动拿到全部**：`menus.user_menu_keys` 对这两个角色走全量分支，
    `_allowed_tools` 无需特判。
  - **管理层默认门户重排**：`approvals / receivable_blind / shipment_receiver /
    morning_report / ledger_incomplete / overdue_orders`，采购三件套移出(给 buyer 角色)。
  - **必须记住的两条口径**：
    - 所有台账查询都要 `Project.is_deleted == False`——软删项目的台账行钱还留着，
      现网 28 行含 ¥40 万发货应收。我做分析时漏了这条，给出的数字全部偏高，
      业务侧工具/扫描/页面本身是过滤了的。
    - 行级隔离一律复用 `sales_router._all_view`，不重写(手册原则一)。
  - **挖出的业务问题(未修，需业务侧决策)**：
    - `sales_ledger.project_id` 是唯一约束，一个项目只能一条台账。
    - 38 行、¥1,872,311 的发货应收是**直接改字段清零**的，没走批注 →
      没有到账时间与确认人记录，数据上分不清真收了还是误清零。
    - `balance_contract` 全库 0 条：批注清零那条路生产上从未在有值时执行过，
      因为大家习惯直接改数字。有一行的批注文本是手打的「尾款金额13170」——
      用户在用手抄绕过这个设计。
  - **验证**：`tests/test_agent_tools_sales.py` 40+ 条断言 PASSED(含行级隔离、
    软删过滤、admin 全量、无权 403)；旧测试全绿；`vue-tsc --noEmit` exit=0。

- **📱 H5 智能体端一期：L3 卡片式人审门 + 独立拆包(2026-08-03，本次)**
  - **范围**：管理层杨坛的查询与审批。第一期审批只做**请款**——生产数据说话：
    杨坛做过 40 笔请款审批(¥80,260)，订单审批 9 笔、开票审批 5 笔，请款是第二名的 4 倍多。
  - **架构依据 `docs/ai-agent-erp-handbook` 第三章三原则**：
    - 原则二 事实源在服务端：卡片只带 ref，facts 由 `app/agent/cards/pay_req.py` 用 current
      重查回填。防的是「屏幕上写同意 ¥4,800、按钮实际批了 ¥48,000」。
    - 原则三 能力可枚举：`registry.py` 只登记 `pay_req_approve` 一类；URL 只写在前端
      `src/h5/cardRegistry.ts`，模型永远不产出 URL/method。
    - L3 不需要服务账号/写端点/幂等键：按钮用**用户自己的 token** 打现有业务端点，
      跟他在财务页面点「审批通过」是同一个 HTTP 请求。
  - **令牌**：`cards/token.py` 把 (user_id, type, ref) 绑死，TTL 15min。它不是权限凭证，
    挡的是卡片串改/过期复用/把 A 卡按钮接到 B 条记录。六种攻击路径都有测试。
  - **拆包(本次重点)**：H5 与网页端/客户端**三方互不影响**——
    - `npm run build` → `dist/`(网页端 + 桌面客户端复用)，**实测不含任何 H5 代码**
    - `npm run build:h5` → `dist-h5/`(vite.config.h5.js，base=/h5/)，**63 KB gzip**
    - H5 源码在 `src/h5/` 下且**不 import 任何 `@/` 路径**：自带 `http.ts`(不引 ElMessage，
      否则整个 element-plus 被拖进来)、`session.ts`(不引 pinia store)
    - 实测 H5 包内无 element-plus / vxe-table / pinia / ElMessage
    - Dockerfile.prod 跑两条构建线，nginx.conf 加 `/h5` 段(放在 `location /` 之前，
      否则被网页端 SPA 兜底吃掉)；H5 用 hash 路由，深链接不用额外配 try_files
  - **顺带补的两个审计缺口**：
    - 请款审批此前**不写审计**(只落业务表 finance_approver_id)，查不到「谁何时从哪个 IP 批的」
    - 供应商改收款账号此前**不留痕**(生产 0 条记录)，新增 `update_supplier_bank`，只记尾号不记明文
  - ⚠️ **卡上「账号 N 天前变更」只对以后生效**：留痕是本次才加的，历史数据一律查不到，
    此时不出这个标——宁可不出，也不能给一个「查过了，没问题」的假象。
  - **踩坑**：`vite.config.js` 与 `.ts` 两份并存，**Vite 优先读 `.js`**；只改 `.ts` 等于没改。
    dev 代理原本硬编码 `http://backend:8000`(docker 服务名)，已改为可用 `VITE_DEV_API` 覆盖。
  - **验证**：`tests/test_agent_cards.py` 35 条断言 PASSED；`vue-tsc --noEmit` exit=0；
    构建产物在真实 `/h5/` 路径下走通登录→待办→审批全流程，审批落库 + 审计两条记录齐全。
  - **待办**：卡片白名单扩到开票/OA 前，先按 registry.py 里的四步检查单走；批量审批第一期不做。

- **🤖 智能体 v2 阶段一+二：find→get 递进 + 明细代码渲染(2026-08-04)**
  设计见 `docs/agent-architecture-v2.md`。**服务对象拿生产 30 天审计日志定的**：
  杨坛 188 次操作里销售台账+订单占 **32%**；赵仁辉 635 次里**仓库占 36%**、
  物料字典 170 次 —— 而 v1 的 13 个工具对赵仁辉的覆盖率**只有 5%**（一个仓库工具都没有）。
  - **阶段一 `agent/tools_entity.py`**：`find_entity` +
    `get_customer` / `get_project` / `get_supplier` / `get_material`。
    ⚠️ **这是 ReAct 能成立的前提**：v1 的 12/13 个工具是「列一类」，
    `for _ in range(4)` 的多轮循环**实际永远只跑 1 轮**（第一轮列完没下一步）。
    生产实测：`get_customer('无锡迈克斯')` → 11 单 / 合同 ¥64 万 / 未收 ¥33.4 万 /
    回款率 47.8% / 其中 2 笔发货款其实还没发货；
    `get_supplier('诺朋')` → 准时率 0%、平均拖 7 天。这些 v1 一个都给不出。
    ⚠️ 字段坑：`PurchaseItem` 上**没有** project_id / name / received_qty，
    真实字段是 `project_code`(字符串) / `item_name` / `arrival_date`。
    「未到货」口径照抄 `_po_arrival_overdue_rows`，别自创第二套。
  - **阶段二 `agent/render.py`**：模型只出「结论 + render 编排块」，
    明细行、合计、截断声明**全部由代码渲染**。
    生产实测：46 条明细，模型输出从 **2621 字降到 70 字**。
    数字不再经模型的手 → 幻觉与截断误报归零。
    ⚠️ 流式要**吞掉三反引号之后的内容**，收尾时才把编排块换成渲染结果 ——
    否则用户会看到裸 JSON 一个字一个字滚过去。
    ⚠️ 模型给坏 JSON / 没有工具结果时，**原样删掉这个块**，绝不漏内部结构给用户。
  - ⚠️⚠️ **提示词里放 JSON 例子，花括号必须写两层**：
    `_SYSTEM_PROMPT.format()` 会把单层花括号的 JSON 当占位符 → `KeyError` →
    被外层 except 吞掉 → **每一次请求都静默降级**。
    已加测试：扫提示词里的占位符，只允许 today / user_name / roles 三个。

- **🔁 重载救不回来时升级成重启(2026-08-04，看生产上报发现)**
  故障上报链路修好之后**第一次真正拿到现场**（此前 0 条，因为 `recover()` 从不上报）。
  - **现象**（李新新那台，v1.0.32）：
    ```
    01:10:16 无响应 → 01:10:31 重载 → 01:10:32 又无响应   ← 重载后 0.5 秒
    01:10:47 重载 → 01:10:58 无响应 → 01:11:16 重载 → 01:11:17 又无响应
    …循环到 01:14:31
    ```
    **重载完 0.5 秒又卡** —— reload 对这类卡死无效，自恢复反而制造了循环，
    那 4 分钟他一条业务都没做成。
  - **影响面**：只此 1 台（同期 11 台在跑 1.0.32，其余零上报），01:14 后自愈，
    03:11 正常干活。所以不是 1.0.32 的普遍问题，但**「重载后立刻又卡」暴露了机制缺陷**。
  - **改法**：`reloadAction()` —— 同一轮里连续重载 3 次仍不恢复 →
    **升级成重启整个应用**（就是用户手工「杀进程重开」那一下，token 在 localStorage 不用重登）。
    ⚠️ **刚启动（<2 分钟）到上限时仍然只重载**，否则会变成开机重启循环。
    ⚠️ 计数有 5 分钟窗口：隔久了算新一轮，不累计 —— 否则跑一整天攒够 3 次也会莫名重启。
  - **验证**：测试里**回放了生产那次的真实时间线**，确认能在第 4 次故障（01:11:41）终止，
    比实际干转到 01:14:31 早近 3 分钟。
  - **顺带**：`update_failed` 那条实锤了 1.0.30/1.0.31 缺文件那次的落地伤害
    （zhaorenhui 下了 1.0.31 没装上，停在 1.0.29；下次开客户端会被强制更新到最新，无需处理）。

- **🐞 请款卡缺项目编号 + 电路图不能删(2026-08-04)**
  - **智能体的请款审批卡少了「项目」**（用户提的「原本会显示项目编号」）：
    ⚠️ 先查清楚**不是网页端的回归** —— 生产接口 `GET /api/finance/payment-requests`
    返回的 `project_codes` 齐全（51 张单全部有值），`test_finance_pr_project_codes` 也绿。
    是**智能体卡片从来就没做**（git 里查不到相关历史），我建卡时漏了。
    已补「项目」与「采购内容」，**项目摆第一** —— 审批人第一眼要知道钱花在哪个项目上。
    口径与网页端同源（关联采购明细的 `project_code`，去重排序）。
    ⚠️ **关联字段是 `PaymentRequestItem.item_id`，不是 `purchase_item_id`**（我猜错过一次）。
    生产实测：#83 → 项目 `2026-072`、采购内容「蒸汽发生器」，与网页端一致。
  - **反馈 #344（宋朴）电路图要能删了重传**：后端**早就放行了**
    （`orders_router.py:837` 的 `#197 电路图可替换`），是**前端卡片上没渲染删除按钮** ——
    同一个卡片里接单资料那块有删除图标，电路图这块漏了。补上，走同一个 `removeCardOutput`。
    已推送的删掉不会在物流残留：下游是按 attachment 关联查的，删了自动消失。

- **🔄 证书自动续期(2026-08-06，本次)**
  `ops/renew-cert.sh` + `/etc/cron.d/pms-certrenew`（每天 3:17 / 15:17）。

  ⚠️⚠️ **光跑 `certbot renew` 是不够的** —— 这是这个脚本存在的全部理由：
  certbot 只更新 `nginx/letsencrypt/live/<域名>/`，而 **nginx 容器挂的是
  `nginx/certs/`**（见 `docker-compose.prod.yml`）。不拷过去、不 reload，
  nginx 会一直捧着旧证书直到过期 —— **续期「成功」了，网站照样在到期那天挂掉，
  而且日志里一片绿**。所以脚本是三步：续期 → 拷到 `nginx/certs/` → reload。
  最后还从 443 真取一次证书复检，确认对外提供的就是新的那张。

  ⚠️ 续期走 webroot（HTTP-01），要求 80 端口能访问 `/.well-known/acme-challenge/`。
  **所以 80 那个 server 块的 ACME location 不能删，也不能整体 301 跳 HTTPS 把它挡掉**
  —— 将来做「第三步」加强制跳转时，这条 location 必须留在 301 之前。

  ⚠️ 一天跑两次是 Let's Encrypt 官方建议（某次失败当天还有第二次机会）；
  错开整点避开全网 ACME 高峰。没到续期窗口时 certbot 直接跳过，是空跑。

- **🔒 HTTPS 第一步：只加 443，80 端口一个字不动(2026-08-06，本次)**
  域名到位（`www.tonghuizhineng.top`，已确认与 IP 返回同一份内容）。
  证书已签（Let's Encrypt，到期 2026-11-07）。

  ⚠️⚠️ **没有用 `ops/enable-https.sh`** —— 那个脚本生成的配置里有
  `location / { return 301 https://$host$request_uri; }`，**会炸掉手机 APP 和桌面客户端**：

  | 钉死在 `http://8.141.123.141` 的地方 | 跳转后果 |
  |---|---|
  | `PmsUpdaterPlugin.java:22` APP **热更新清单地址** | 跳 `https://8.141.123.141` → 证书不含 IP → TLS 报错 |
  | `vite.config.h5.js:48` APP 内置前端的 API | APP 里每个接口全挂 |
  | `desktop/main.js:23/215` 桌面更新通道 + 上报 | 收不到更新 |

  ⚠️⚠️ **最要命的是热更新清单地址烧在 APK 里** —— 它一挂就**没法再用热更新去修**，
  只能让每个人重装 APK。**这是个单向门**，所以这一步坚决不加跳转。

  **正确顺序（三步，别跳）**：
  ① 只上 HTTPS，80 保持原样（本次）→ ② 客户端改到域名并发新版（手机必须重装）→
  ③ 确认大家都换了新版，才打开强制跳转 + HSTS。

  - **配置怎么组织**：两个 server 块（80 / 443）共用
    `nginx/conf.d/_shared-locations.inc`。⚠️ 文件名必须是 `.inc` 不能是 `.conf` ——
    `nginx.conf` 里是 `include conf.d/*.conf`，叫 `.conf` 会被当顶层配置加载，
    而裸 `location` 不能出现在 `http{}` 顶层，**nginx 直接起不来**。
  - ⚠️ **暂不加 HSTS**：它会让浏览器强制把本域 http 也转成 https，加了就退不回来。
  - 80 端口 `server_name _` 保持通配，域名和裸 IP 都照常直接服务。

- **📱 APP 图标换成公司标识（高清全套）(2026-08-06，本次)**
  原来是我随手画的一颗四角星，跟公司没关系。改用**真的公司标识** ——
  同辉的 TH 齿轮标（藏青 `#052337` + 金 `#D8AE57`），
  与 Windows 客户端 `desktop/build/icon.png` 同一个标，两端一致。
  - **源图**：`frontend/src/assets/logo.png`（1181×1082，**透明底**，是三份素材里
    唯一带 alpha 的，另两份是白底 RGB）。
  - **生成脚本 `mobile/tools/make-icons.py`，可重复运行** —— 换 logo 重跑即可。
    一套 Android 图标是 20 个文件（5 档密度 × 传统/圆形/自适应前景/单色），
    手工导必然漏、必然不一致，而且下次没人记得当初怎么导的。
  - ⚠️⚠️ **自适应图标画布 108dp，只有中间 72dp 保证可见**：各家启动器用圆形／
    方圆形／水滴形去裁，裁的就是外面那圈。而且圆形裁切下 72dp **方形的四角**
    也在圆外。这个标近似圆盘，取 **62% 见方**兼顾「不被切」与「不至于小得像邮票」。
    改这个数之前先跑 `--preview` 看三种裁切。
  - ⚠️ **按外接矩形居中看起来是偏的**：这个标左边一道厚月牙、右边是笔画细的 TH，
    实测重心比几何中心偏左 3.4%、偏下 2.2%。脚本按**不透明像素重心**校正，
    对比图上一眼能看出差别。
  - ⚠️ **自适应前景必须透明底**：画了底色，启动器裁切时会露出一圈方角。
  - 顺带补了 Android 13+ 的 **monochrome 主题图标**（纯剪影，系统按壁纸配色上色；
    带颜色的话主题图标会糊成一坨），以及 512px 商店图。
  - **自检加了一条**（`check.sh`）：20 个图标齐全、尺寸对得上、前景四角透明。
    ⚠️ 图标错了**照样能编译**，装上才发现是默认图标 —— 跟「构建成功≠装上能用」
    同一类。已用反向测试确认这条检查拦得住（故意给前景加底色 → FAIL）。
  - **验证**：三种裁切预览、48px 放大、重心校正前后对比都看过图；`check.sh` 全绿；
    **CI 打出 2.1.0 后开包按像素确认图标真的进去了**（432px 自适应前景 + 单色剪影、
    192px 圆形 + 方圆形，四张都导出来看过）。
  - ⚠️ **开包验图标有两个坑，别按文件名找**：
    ① AAPT2 在 release 里把资源**重命名成 `res/_q.png` 这种短名**，
       grep `ic_launcher` 一个都找不到 —— 只能按尺寸+像素认；
    ② 传统图标是圆角/圆形遮罩，**四角本来就是透明的**，
       拿「角不透明」当「这是传统图标」的判据会全部漏判（我第一版就是这么误判成 0 个的）。
  - ⏳ 真机上的实际观感待装机确认。

- **🎨 语义着色 + UI 表格；并纠正生产截止日期口径(2026-08-06，本次)**

  **① 口径纠正（用户指出）：生产的截止日期就是项目一览里的交货日期。**
  ⚠️⚠️ 早先把「produce 部门单没填 `due_date`」报成「盲区、算不出来」是**错的**：
  46 个在建项目里 produce 填了 due_date 的是 **0 个**，于是 39 个项目全被报成盲区 ——
  **等于把一句正常的话说成了系统性缺陷**。生产单上的 due_date 只是有人另外填的
  内部节点，**没填不代表没有截止**，按交货日倒推就行。
  - 风险改成真正要盯的两个：`produce_overdue`（生产没完成而交货日已过）、
    `produce_plan_conflict`（内部节点排到了交货日之后 —— 这才是用户最初说的
    「生产截止日期没跟交货日期匹配上」的真实形态）。
  - ⚠️ 计划冲突**只对还没到期的项目报**：已经过了交货日的，主风险就是「已经延期」，
    再补一句「按计划也交不了」是废话，只会稀释真正的告警。
  - 卡点文案里生产不再写「未设截止日期」，改成「（按交货日 2026-08-29）」。

  **② 语义着色**（用户：「加入字体颜色」）
  ⚠️ **后端不许吐 HTML** —— `markdown-it` 的 `html:false` 与下游 `v-html` 是成对的
  XSS 防线（markdown.ts 顶部那条）。所以后端只发**语义档位**
  `[[danger:已过 55 天]]`，前端一个 markdown-it 插件翻成受控 span：
  - 档位取自**白名单**（danger/warn/good/muted），class 名不可能被数据控制；
  - 文本用 `text` token 推进去、由 markdown-it 自己转义 —— 数据里带
    `<img onerror=…>` 只会变成字面量；白名单外的写法原样当普通文字。
  - ⚠️⚠️ **分隔符必须是冒号不能是竖线**：标记要放进表格单元格，
    而竖线是 markdown 表格的列分隔符 —— 用 `|` 会把一行切成好几列，整张表当场散掉。
  - **只给真正要一眼看见的上色**：已过期→红、7 天内→橙、还早的**不上色**。
    全都上色等于都没上色。
  - 老前端包遇到新标记会原样显示 `[[danger:…]]`。刻意的取舍：宁可露一段丑字符，
    也不为了兼容去放开 html。

  **③ UI 表格**：圆角卡片容器 + 表头浅底 + 斑马纹 + 细分隔线，替掉光秃秃的下划线。
  - ⚠️ **表格单元格默认是 `content-box`，百分比宽度不含 padding**：写 42% 实际占
    42%+18px，三列一挤第三列被压到 80px，「电工未完成」都放不下。
    加 `box-sizing: border-box` 才名副其实。**这条是量出来的，不是看出来的**
    （`td.clientWidth` 实测 153/89/80，而我以为是 135/71/115）。
  - 最终列宽 38% / 26% / 36%，375px 下实测 122/83/116，
    `document.body.scrollWidth === window.innerWidth` → **无横向滚动**。

  - **验证**：`test_agent_render_safety.py` 扩到 12 组断言（新增着色档位、
    冒号分隔不冲散列、后端不拼 HTML）；13 个 agent 测试全 PASSED；
    `vue-tsc` exit=0；**手机 375px 下截图看过**（不是只看 markdown 源码）。

- **📊 明细改成表格：文字墙 → 能扫的表(2026-08-06，本次)**
  用户原话：「就不能渲染文字加表格吗？这么一堆文字，用户肯定不愿看，
  **要以用户体验为中心。这个要记住**。」——已记进长期记忆。

  上一版把明细做成 `- A · B · C` 的列表，本质还是文字墙：
  一行 `2026-045B · 300L平台式中转罐 · 交货 2026-06-10 · 已过 55 天 · 卡在 电工未完成`
  在 375px 屏上折成三行，十几条就是一屏，人只会划过去。

  **改成 markdown 表格**（`render.py`，H5 的 markdown-it 默认就支持表格）：
  - `_FIELD_ORDER` 每项加**列名**，四元组 `(字段, 行内前缀, 是否金额, 表头)`。
  - **最多 3 列**（`_MAX_COLS`）。手机屏窄，再多就要横向滚动，
    而**横向滚动的表格等于没有表格**（没人去滑）。
  - 第一列合并「编号 + 名称」：编号用来对单、名称用来认是什么东西，
    拆成两列会挤掉「卡在哪」这种真正要看的信息。
  - ⚠️ **`_FIELD_ORDER` 的顺序就是列优先级**，排错了信息就被挤掉。
    原则：谁 → 多少 → 卡在哪 → 谁负责 → 具体哪天。
    绝对日期排最后 —— 已经有「已过 55 天」时再占一列写「2026-06-10」是重复。
    实测栽过两次：一次「交货日｜剩余」两列说同一件事，一次把「负责人 宋朴」挤没了。
  - ⚠️⚠️ **工具可以自己声明列**（结果里的 `columns`）。全局优先级是一套，
    而「哪几列重要」跟场景走：交期看板的行里同时有 `contract` 和 `days_left`，
    按全局顺序金额排前面，表格就成了「项目｜客户｜合同额」，
    把「剩余／卡在哪」全挤掉了。**口径归工具：谁产出的数据，谁最清楚该先看哪几列。**
  - 单元格里的 `|` 换成全角、换行压成空格，否则一条数据就能把整张表冲散。

  **手机上实测调过两轮**（375px，截图看的，不是只看 markdown 源码）：
  - 列宽 42% / 22% / 34%：三列均分时「卡在哪」会折成 4 行、行高撑到 60px 以上。
  - 「剩余」列 `white-space: nowrap`：「已过 55 天」被折成两行很难看。
  - `table-layout: fixed` + `word-break`：内容再长也在格子里换行，**不撑出横向滚动**。
  - 表头压暗一档（它是标签，值才是主角，与设计稿「标签一律比值小」一致）。
  - ⚠️ `blocked_at` 改成**短标签**（`_short()` 去掉括号补充）：
    「电工未完成（截止 2026-08-10）」在 34% 宽的列里要折 4 行。
    完整描述仍留在 `blockers` 里，模型分析时看得到，**不丢信息**。
  - ⚠️ 自查页抽 SFC 里的 CSS 时**必须整块规则一起抽**：第一版只抓了单行规则，
    多行的 `.md :deep(th), .md :deep(td) {...}` 漏了，
    预览出来表头居中、nowrap 没生效 —— **看到的是假象，差点照着假象继续改**。

  - **验证**：`test_agent_render_safety.py` 扩到 10 组断言（含「输出必须是表格」
    「表头是人话列名」「竖线被转义列数不散」「截断说明与表格间要空行」）；
    13 个 agent 测试全 PASSED；`vue-tsc` exit=0。

- **🎨 回答的条理性与重点性；顺带修「裸 dict 漏给用户」(2026-08-06，本次)**
  用户要求优化回答的 UI / 条理 / 重点。**先看线上真实输出再改**（id=101 的全文），
  发现分析部分其实写得很好（「26 条部门逾期里 23 条是电工部(88%)」正是要的重点），
  但**明细段整个崩了**：

  ```
  **其他**（26）
  - {'dept': 'electric', 'dept_name': '电工部', 'project_code': '2026-057', ...}
  ```

  ⚠️⚠️ **裸 Python dict 直接甩给了用户。** 成因链（本地已复现）：
   1. 模型这一轮调了**两个**工具（project_progress + overdue_orders）；
   2. `last_result` 留的是**最后一个**（overdue_orders）；
   3. 而编排块里写的是**前一个**工具的字段名；
   4. `row()` 按那份 fields 一个都没命中 → 走 `str(item)` 兜底 → **漏结构**；
   5. `group:"urgency"` 在这批数据上也不存在 → 全落进「其他」，标题信息量为零。

  **渲染层四处修**（`render.py`）：
  - **`row()` 绝不 `str(item)`**：fields 全不命中就**忽略它、按默认顺序自己挑**；
    仍然挑不出就返回 None 让调用方跳过这一行。
    与 `apply_render` 的「模型给坏 JSON 就只删块不渲染」是同一条纪律 ——
    **宁可少一段明细，也不能把内部结构甩出去。**
  - **分组字段必须真在数据上**，否则不分组（全落「其他」比不分组还糟）。
  - ⚠️ **分组时 `sort` 只在组内生效**。跨组重排会推翻工具排好的优先级 ——
    实测模型给 `sort:days_left,desc:false`，「已发货·只差收尾」（过期 181 天）
    被顶到第一组，而那恰恰最不该占首位。**组序由工具定，组内排序才轮到模型。**
  - ⚠️ **项目编号排在设备名前面**：`_FIELD_ORDER` 里原本没有 `project` 这个键，
    明细只显示「300L平台式中转罐」，人按编号认单，对不上号。

  **条理性/重点性（提示词）**：定死三段式 ——
  ① 第一行就是结论（一句话、加粗、带最关键的数）
  ② 要点**最多 3 条**，每条一行、先事实后动作
  ③ 编排块（明细代码渲染）
  并写明「不要复述工具结果，你的价值在归纳」「看集中度」「段落别超两行」。
  ⚠️ 提示词里明确**劝阻模型给 `fields`** —— 那正是上面翻车的直接来源。

  **手机可读性**：分组后每组最多 6 行，超出写「本组另有 N 条」。
  26 行铺满一屏没人看，「最急的看得见」比「全都列出来」有用。

  - **验证**：新增 `tests/test_agent_render_safety.py`（7 组断言：不漏裸 dict／
    不瞎分组／渲染不出的行跳过／分组条数上限／单行字段数／编号在前／
    sort 只在组内）PASSED；13 个 agent 测试全 PASSED。

- **⚡ 关掉思考模式：从 7.5 秒到 2.3 秒(2026-08-06，本次)**
  上一条的修法（预算不够就加码重试）救回了正确性，却**赔掉了整个请求**：
  ```
  模型未产出正文（finish=length，思维链 3718 字，预算 1600），提高预算到 3000 重试
  → CancelledError（客户端等不及断开）→ 用户看到 network error
  ```
  重试要多等一整轮，拖过客户端的耐心就是网络错误。**正解是别让它想那么多。**

  **拿生产这把 key 对 deepseek 实测**（问「用一句话说：今天天气不错」，max_tokens=300）：

  | 配置 | 耗时 | 思维链 | 正文 |
  |---|---|---|---|
  | v4-pro 默认（开思考） | 7487ms | 542 字 | **0 字** |
  | v4-pro + `reasoning_effort:"none"` | 2831ms | 0 | 95 字 |
  | v4-pro + `thinking:{type:disabled}` | 2831ms | 0 | 95 字 |
  | v4-flash 默认 | 2046ms | 27 字 | 7 字 |

  ⚠️ **开着思考时，它连这种琐事都给不出正文** —— 300 tokens 全被推理吃掉。
  这不是「偶尔失手」，是这个模型在这个场景下的常态。

  **带工具再测一次**（问「项目进度跟进」），确认关掉不会丢能力：

  | 配置 | 耗时 | 思维链 | 工具调用 |
  |---|---|---|---|
  | 开思考 | 3280ms | 104 字 | ✅ project_progress |
  | 关思考 | **2290ms** | 0 | ✅ project_progress |

  - **改法**：`_thinking_params()` 一处注入，**默认关**；
    想开回去改 `app_settings` 的 `agent_llm.thinking=on`（配置项，不是写死）。
  - ⚠️ **必须用 `reasoning_effort`**（OpenAI 兼容字段）。同批实测里
    `enable_thinking` / `chat_template_kwargs` 这个网关**不认也不报错，静默无效** ——
    看着像配上了，其实一点用没有。这种「配了等于没配」最难查。
  - ⚠️ **重试加了时间闸 `_RETRY_DEADLINE_S=12s`**：超过就带着现状收尾，不再重试。
    救回一次正确性、赔掉整个请求，不划算。
  - ⚠️ 加配置字段要**同时改 `_AGENT_CFG_FIELDS` 和 `AgentConfigIn`** ——
    `PUT /config` 是按前者 `getattr(body, f)` 取值的，漏一个直接 AttributeError（踩过）。
  - **顺带接通模型路由**：⚠️ `model_fast` 以前**根本没被读出来** ——
    `_effective_llm_config` 里压根没这个键，`_route_model` 里
    `cfg.get("model_fast")` 永远是 None，**模型路由整个是死代码**，
    所有请求（包括「今日晨报」）都在用大模型。
    实测（各 3 次取中位、均关思考、带工具）：

    | 问题 | v4-pro | v4-flash |
    |---|---|---|
    | 今日晨报 | 2318ms | **1691ms** |
    | 项目进度跟进 | 2448ms | 1937ms |

    现在单点查询走 flash、要分析的（`_HEAVY_HINT`/`_FULL_LIST_HINT`/`_THINK_HINT`）
    仍走 pro —— 小模型答得出格式，答不出「为什么这批都卡在电工」。
    ⚠️ **小模型必须一并进白名单**：`chat_stream` 入口有一道
    「model 不在白名单 → 无效模型」，漏了这步，配上 model_fast 反而让单点查询全挂。
    ⚠️ 生效还需要在 `app_settings` 里配 `agent_llm.model_fast=deepseek-v4-flash`。

- **🔥 交期看板「直接不能用」：预算被思维链吃光(2026-08-06，本次)**
  用户反馈交期看板不可用。**新加的审计维度当场就用上了** —— 一眼定位：
  ```
  id=97  项目进度跟进  via=rule-str  tools_used=[]  duration_ms=15628
         [agent] 流式失败，转规则降级: LLM 返回空内容
  ```
  `tools_used=[]` + 跑了 15.6 秒 = **模型第一轮就返回了空，工具压根没被调用**，
  然后整条降级成那段「我是 ERP 数据助手…」的功能菜单。

  - ⚠️⚠️ **根因不在工具、不在提示词，在 `max_tokens`**：线上模型是
    **deepseek 推理型**，`max_tokens` 是**思维链和正文共用**的。
    「项目进度跟进」这类要分析的问题，700 tokens 先被思维链吃光 →
    正文为空、工具一个没调 → 判成「LLM 返回空内容」→ 降级。
  - **700 是哪来的**：`ae4f781`(2026-08-03「Agent 全链路提速」)定的，理由是
    「耗时几乎线性于输出字数，平均 1300 字→18s，手机上没人读 1300 字」。
    **那个决定在当时没错，错的是前提变了** —— 它把 max_tokens 当成「答案长度」，
    而换成推理模型后它是「思考+回答的总额度」。
  - ⚠️ **`max_tokens` 不是上下文窗口**。上下文管**输入**能塞多少，
    `max_tokens` 管**输出**能生成多少；上下文再大也不放宽输出上限。
  - **两道防线**：
    ① `_THINK_HINT`（进度/跟进/卡在/交期/还剩/快到期/监控/盘点/汇总/统计/排查/梳理）
       命中就一开始给 1600，别等重试（重试要多等一整轮）。
       单点查询（今日晨报、待填收货人）仍走 700，不白烧钱。
    ② 仍然空了且 `finish_reason=length` 或有思维链 → **加码到 3000 重试一次**
       （只一次，防死循环）。这时还没往前端推过字，重试不会出现重复文字。
  - **顺带修的两个排障黑洞**：
    · 读 `reasoning_content` 只为**记长度**（思维链绝不推给用户）——
      否则「模型想了 15 秒、正文一个字没有」在日志里完全看不出来。
    · 流里夹带的错误负载（没有 `choices` 的块）以前被直接丢弃，
      现在带出来，不再统一报成含糊的「返回空内容」。
  - **验证**：新增 `tests/test_agent_thinking_budget.py` —— 用假 LLM 复刻
    「只吐思维链 + finish_reason=length」的形状，断言会加码重试并拿到正文；
    另锁「真空内容仍按失败处理」「流内错误要带出原文」「单点查询不被顺带抬价」。

- **💰 补「本月销售额」工具(2026-08-06，本次)**
  用户指出问销售额时模型答「没有这个功能」—— 属实，工具集里确实没有。
  - ⚠️⚠️ **口径是按项目「签订日期」归月，不是台账录入时间**。
    `sales_ledger.created_at` 是**谁什么时候把它敲进系统**，跟生意什么时候做成无关：
    生产上本月 `created_at` 是 **0 行**，照它算会得出「本月销售额 0」，
    而实际 7 月签了 16 个项目 113 万。**这种错最糟 —— 它给的是个看起来正常的数字。**
    签订日期在 `Project.extra["__o__签订日期"]`（与交货日期同一张一览表）。
  - 合同额为 0 的行单独计数（生产上 128 行台账里 21 行是 0），
    没签订日期的也单独报 —— 否则合计对不上没人知道为什么。
  - 生产实测各月：7 月 113.5 万/16 单、6 月 96.8 万/12 单、5 月 129.1 万/21 单。
  - 权限与 `get_customer` 同一道门控（finance/sales 菜单），并复用 `_all_view` 行级隔离。

- **🧾 问答审计日志补全：所有路径留痕 + 能拿来做优化(2026-08-05，本次)**
  用户问「现在所有的问答都有审计日志吗」，目的是**留作后续优化的依据**。查完的结论：
  代码里 5 个写入点覆盖了所有**正常返回**的路径（非流式 LLM / 非流式规则降级 /
  流式 LLM / 流式降级 / 技能 / 直答），生产 96 条日志里 9 种 via×model 组合都在。
  但拿来做优化**不够**，三个缺口：

  - ⚠️⚠️ **流式被中途放弃时一条都不留**。`_log_chat` 原来写在生成器末尾，
    用户划走 / 关页面 / 断网 → Starlette 取消生成器 → 后面的代码永不执行。
    而生产上 llm 路径平均 **14.0 秒**、降级路径 **20.8 秒**，这个时长下「等不及走人」
    很常见 —— **丢掉的恰恰是最该拿来优化的那批样本**（答得太慢没人等）。
    改法：状态先攒在闭包里，落库放进 `finally`，取消也照写，`outcome='aborted'`，
    并记下**放弃前已经等了多久**。
    ⚠️ 日志要用**独立 session**：被取消时 caller 的 db 可能已不可用，
    复用它会连这条 aborted 记录一起丢掉。
  - **没有会话 id**，多轮串不起来 —— 看不出「同一件事问了 3 遍才问明白」，
    而那是优化的首要信号。加 `session_id`（前端每开一段对话生成一个）+ `turn`。
    ⚠️ 它**只用于日志分组**，不参与鉴权、不决定任何数据可见性；
    用户伪造它也只能弄脏自己的日志分组。
  - **没有质量维度**：跑了几轮工具、查出多少条只给了几条（截断）、明细到底有没有
    渲染出来、回答多长。补 `tool_rounds / result_count / result_shown /
    rendered / answer_chars`。
    ⚠️ `answer_chars` 记的是**截断前**的真实长度 —— `answer` 列本身截到 5000，
    拿它算长度会把「模型答了 8000 字」这件事永远藏起来。

  - 查询接口把新维度一并吐出，并支持 `?outcome=aborted` 直接筛出被放弃的那批。
  - **踩坑**：`crypto.randomUUID` 需要安全上下文，**http 的网页版里取不到**，
    直接调会抛异常把发消息整条带崩 —— 前端留了兜底。
  - ⚠️ **测试差点假绿**：用 httpx 的 ASGI transport「读一片就 break」模拟客户端断开
    是**测不到**的，那一层会把响应整个缓冲完，生成器根本不会被取消，
    测出来 `outcome=ok`。真实的断开 = Starlette 对生成器调 `aclose()`，
    测试里直接照做才验到。
  - **不在这张表里的**：卡片审批动作走 `audit_logs`（它们不是问答，是写操作），
    每日简报是推送不是问答。做端到端分析时要知道要 join。
  - **验证**：新增 `tests/test_agent_chat_log_coverage.py`（非流式/多轮 turn 递增/
    直答/**中途放弃留痕**/完整流式不重复写/查询接口契约与筛选/via 截断兜底）PASSED；
    `test_agent_chat_logs` 的字段契约同步更新；11 个 agent 测试全 PASSED。

- **📅 项目交期跟进：还剩多少天 + 卡在哪(2026-08-05，本次)**
  管理层要的是「交货日期快到的项目到底卡在哪里」。做之前先把生产数据摸清楚：

  | 事实 | 数 |
  |---|---|
  | 在建项目 | 46 |
  | 其中**已过交货日** | 19（另有 1 个是已发货没收尾） |
  | 7 天内要交 | 1 |
  | 没填交货日期 | 6 |
  | 卡在采购未到货 | 18 |
  | **生产侧有截止日期的** | **0** |

  - ⚠️ **用户说的「生产截止日期没跟交货日期匹配上」，真实形态不是两个日期对不上，
    而是生产侧压根没有日期**：46 个在建项目里 produce 部门单填了 due_date 的是 **0 个**，
    130 条生产组任务里只有 9 条有。所以工具报的第一条是
    「**算不出来，这是盲区**」，而不是假装算出一个匹配结论 ——
    39 个项目落在这一档。提示词里专门写死：**不许把它改写成「生产进度正常」**，
    不报风险 ≠ 没风险。
  - **交货日期在 `Project.extra["__o__交货日期"]`**（项目一览表的列，前缀见
    `projects_router._extract_overview_meta`）。⚠️ 别去 `sales_ledger` 找：
    那里只有 `ship_date`（物流回传的**实际**发货日）和 `balance_date`（尾款到期日），
    都不是「答应客户哪天交货」。口径收在 `tools_entity.DELIVER_KEY` 一处。

  **① 修一个会「悄悄少给一个项目」的 bug（用户提的 071 只出 B）**
  - 生产上 `2026-071A`（双行星分散混合机）和 `2026-071B`（提升式压料机）是
    **两个独立项目**，同客户同交货日。而 `get_project` 是
    **模糊匹配 + `order_by(id.desc()).limit(1)`** —— `%071%` 两个都命中，
    倒序取到 B（id 263），**A 被静默丢掉**。
  - ⚠️ **这是最糟的一类错**：用户看到的是一份看起来完整的分析，
    没有任何迹象表明还漏了一个项目。跟之前「说系统里查不到客户」同一性质。
  - 改法：`_match_projects` **精确编号优先，否则返回全部模糊命中**；多命中时
    返回 `projects` 数组 + 一句给模型的硬约束「每个都要讲到」。
    编号带字母后缀是常态（99/104 个是 `2026-NNN[字母]`：043B~043E、045A/B、
    037A/B、050A/B…），所以这不是 071 一个个案。
  - 生产实测：`get_project('071')` → matched_count=2，A 和 B 都在；
    `get_project('2026-071A')` → 仍然只出一个。

  **② 新工具 `project_progress`（交期看板）——**走 ReAct，不做成技能**
  - 每行给：剩余天数、卡在哪一环（设计/电工/生产组/采购未到货/待发货）、交期风险。
  - ⚠️⚠️ **这条刻意不做成技能**（一开始做成了技能，用户当场指出「没走 Agent」）。
    技能的语义是「命中就不进 ReAct」——固定编排、固定模板、**模型完全不参与**。
    那对「库存低于安全线」这种一句话说得完的事合适，对交期不合适：管理层要的是
    「把项目所有数据串起来分析」，而技能只会甩一段套话加一张表，
    既没有跨项目归因，也没有「这批为什么都卡在电工」这类判断。
    现在「项目进度跟进 / 哪些项目快到期 / XX 卡在哪」**一律走 ReAct**，
    模型拿已经算好的卡点与风险自己写结论，明细仍由 `render.py` 渲染。
    **判断归模型，数字归代码** —— 这才是 v2 的分工。
    （`skills.py` 里留了一段注释说明为什么这里没有技能，免得后人又给加回去。）
  - 门户加「交期看板」卡，进管理层默认门户（生产实测 yangtan/zhaorenhui/admin 都可见）。

  **②-b 明细排版（用户说「格式还不好看」，属实）**
  - ⚠️ **数据字段里一律不许带 markdown**。原来风险文案写成 `**没有截止日期**`，
    渲染层外面还要包一层，结果是 `卡在**生产未完成（**没有截止日期**）**` ——
    嵌套的 `**` 让 markdown 直接裂开。**强调是渲染层和模型的事，不是数据的事。**
  - ⚠️ **`days_left` 不能把 `-55` 直接甩给人看**：负数是已过交货日，
    「剩 -55 天」既费解又容易读反。渲染成「已过 55 天 / 剩 25 天 / 今天交货」。
  - **补上了 `group`**：这个能力在 `render.py` 开头的注释里写了很久却一直没实现。
    46 个项目拉平成一串等长的行，人得一行行数才知道哪些是真急的。
    现在按 `urgency` 分档（已过交货日 / 7 天内 / 30 天内 / 30 天以上 /
    没填交货日期 / 已发货·只差收尾）加小标题和条数。
    ⚠️ 分组**保持工具给的原始顺序**，不重排 —— 重排会打乱「已发货待收尾排最后」。
  - 档名由代码给死，模型不参与命名，各次回答才一致。

  **③ 两处会让告警失真的判定，实测中发现并修掉**
  - `2026-008`「九张部门单全作废 + 货已发出 + 过期 181 天」原本被报成
    **「设计还没下单」**，并以 -181 天霸占「最急」第一位。
    它根本不是交期风险，是**状态没收尾** —— 排第一的那条没人管，整张看板就没人看了。
    改：已发货的单独一档 `shipped_not_closed`，排最后，且**不计入 overdue**
    （overdue 从 20 降到 19，虚高的告警没人信）。
  - ⚠️ **`plan` 里给了 `sort` 会覆盖工具排好的顺序**：`_rd.table` 见到 `sort`
    就**重排一遍**，把刚挪到最后的 2026-008 又顶回第一行。交期这类要么不给 sort、
    要么用 `group`。
  - ⚠️ `produce_group_tasks` 对 `(order_id, group)` 有唯一约束，
    造测试数据时 order_id 不能图省事都写 0。

  - **验证**：新增 `tests/test_agent_delivery.py`（含
    多命中/精确命中/生产无截止日期/生产截止晚于交货/已发货待收尾/within_days
    不漏已过期/**6 条交期问法都不被技能劫持**/天数渲染/分组/数据不带 markdown）PASSED；
    agent_cards / agent_chat / agent_perms / agent_tools_sales /
    agent_row_level_scope / agent_chat_logs / agent_v2 / agent_limit_truncation /
    agent_portal 全 PASSED；**并在生产库上实跑了工具、技能与门户**（只读，
    新代码用 importlib 挂成独立模块，没有覆盖容器里正在跑的代码）。

- **📱 手机 APP 改 Capacitor 打包 + 前端热更新(2026-08-05，本次)**
  用户反映「内嵌页面不兼容」。旧壳是**直接 loadUrl 服务器上的 `/h5/`**，
  下面这些问题都是那个架构本身带来的，不是某一处代码写错：

  | 症状 | 真因 |
  |---|---|
  | 麦克风按钮在 APP 里**根本不出现** | **Android WebView 没有 Web Speech API**。`webkitSpeechRecognition` 是 Chrome 浏览器的能力，WebView 不绑定语音识别服务，能力探测必然失败 |
  | 刘海屏/Home 条压住内容 | 壳是 `setDecorFitsSystemWindows(true)`，系统替 WebView 留白 → **`env(safe-area-inset-*)` 恒为 0**，H5 里那套安全区适配全白写 |
  | 需要安全上下文的浏览器能力全被拦 | 页面是 `http://8.141.123.141/...`，非 localhost 明文 → `isSecureContext=false` |
  | 弱网/断网开 APP 就是离线页 | 页面资源全在服务器上，冷启动必须先联网 |
  | 前端用了新原生能力时老壳直接白屏 | 壳与前端**没有版本协商**，谁也不知道对方是哪一版 |

  **改法：Capacitor 7 打包，前端资源打进 APK 从本地加载**（页面 origin 变 `http://localhost`）。
  - ⚠️ **`androidScheme` 必须显式设成 `http`，不能用 Capacitor 默认的 `https`**：
    默认值会让页面变成安全页面，而我们的 API 是明文 `http://` ——
    **混合内容拦截，APP 里每个接口直接失败**。改成 `http` 后页面不触发混合内容规则，
    而 localhost 本身仍属可信来源，安全上下文照样成立。
  - **跨域**：本地加载后页面在 localhost、API 在服务器，属跨域。
    后端 CORS 放行 `http://localhost` / `https://localhost` / `capacitor://localhost`
    （`main.py` 的 `_app_origins`）。实测预检：三个来源 200，未知来源 400。
    ⚠️ **不能用 CapacitorHttp 插件绕 CORS**：它会 patch `fetch`，
    而智能体问答是 **SSE 流式 fetch**，一 patch 就没有流了。
  - **一份源码两条构建线**（`H5_TARGET` 开关，不为 APP 单开配置文件——
    这个仓库已经栽过一次「两份 vite 配置只改了没被读的那份」）：
    · 网页版 `npm run build:h5` → base `/h5/`、API `'/api'`，**产物与改造前逐字节同类**
    · APP 版 `npm run build:h5:app` → base `'./'`、API 绝对地址 → `dist-h5-app/`
    ⚠️ Capacitor 只认 `index.html`，构建产物叫 `h5.html`，**打包时必须改名**。
  - **语音补回来了**：`PmsSpeechPlugin` 把系统 `SpeechRecognizer` 桥给 H5，
    `useSpeech.ts` 改成双后端（原生优先、退回 Web Speech），网页版行为一个字没变。

  **热更新（和桌面客户端同构）**：`/h5-ota/version.json` + `h5-<版本>.zip`，
  换包用的是 Capacitor 自带的 `serverBasePath` 机制 —— **换前端 = 换一个目录**。
  - ⚠️ **`beforeBridgeLoad` 与 `registerPlugin` 必须早于 `super.onCreate`**：
    Bridge 在 super.onCreate 末尾才 create，那时才读 serverBasePath。
    晚一步**不会报错**，只会「这次没生效、下次才生效」，查起来极费解。已写进 `check.sh`。
  - **坏包能自己退回去**（桌面端 1.0.30/1.0.31「装上打不开」的教训）：
    新包先标**试用**，前端 `router.isReady()` 后回一声 `notifyReady()` 才转正；
    10 秒内收不到 → **拉黑该版本 + 当场退回上一个好包并重载**，不让用户对着白屏，
    也不用他重启 APP。
    ⚠️ 回执要等 `isReady()` 不是 `mount()`：缺 chunk 的坏包照样能 mount 成功然后白屏。
    ⚠️ `notifyFailed` **报过平安之后一律忽略**：起来之后某个组件抛异常是业务 bug，
    为它回滚整个前端包是过度反应。
  - **验签，且失败即拒绝**：服务器是明文 HTTP，不验签等于同一个 WiFi 下谁都能把任意 JS
    推进这个 APP —— 而它手里有能审批付款的登录令牌。公钥打进 APK
    （`assets/ota_public_key.pem`），私钥只在发版机器（`.deploy.local.ota.pem`，已 gitignore）。
    **拿不到公钥就完全不更新**，不是「验不了就放行」。
    签名对象是 **sha256 的十六进制文本本身**，两侧口径必须一致；
    `ship.sh` 签完立刻用 APK 里那把公钥自验一遍，私钥/公钥不成对当场失败。
  - **换 APK 时热更新状态全清**：新壳自带新的内置前端包，旧的热更新包一律作废，
    否则会出现「装了新 APP 却还跑着老前端」。
  - 解压挡 **zip-slip**（判据是「解析后的绝对路径仍在目标目录下」，不是「名字里没有 ..」）；
    包体积、解压总量都有上限。
  - **发版顺序不能反**：`ship.sh` **先传 zip，从公网取回核对 sha256，最后才写 version.json**。
    反过来就是「清单说有 X 但服务器上没有 X」，所有手机去下一个不存在的包。
  - `min_shell`：新前端要求的最低 APK 版本。低于它**不热更新**，改为提示换 APK ——
    与桌面端 `min_version` 同一个意思。只在前端用了老壳没有的原生能力时才抬。

  **踩的坑**
  - ⚠️ **`unzip -l ... | grep -q` 在 `set -o pipefail` 下会误判**：grep 命中即退出，
    unzip 收到 SIGPIPE，整条管道判失败 —— **包是好的，检查却把它拦下来**。
    跟当初 `grep META-INF/*.RSA` 误判 APK 没签名是同一类错。改成先落文件再 grep。
  - ⚠️ **自检脚本查「顺序」时必须先剥注释**：`super.onCreate` 这个词在文档注释里也出现
    （正是在解释这条规则），直接 `find` 会命中注释，把写对了的代码判成写错。
  - ⚠️ 顺序检查要用 **`rfind` 不是 `find`**：要求**每一处** `registerPlugin` 都在前面。
    用 `find` 的话，第一个插件注册对了就放行，后加的插件排在后面照样溜过去 ——
    表现是 JS 侧「调用了不存在的方法」，且查不出原因。已用反向测试验证这条检查真能拦下来。
  - Capacitor 8 要 Node ≥22，本机是 20 → 用 **Capacitor 7**（要求 ≥20），CI 也钉 Node 20。
  - `String.join` 是 API 26 才有的，minSdk 24 的机器上直接 NoSuchMethodError，改用 `TextUtils.join`。
  - Capacitor 的 `BridgeActivity` **不处理硬件返回键**（那是 @capacitor/app 插件的活，我们没装），
    不自己接就是「按一下直接退出 APP」——迁移时最容易丢掉的一条。

  **验证到哪一步**（本机没有 Java / Android SDK，编译与装机验证只能在 CI 和真机上做）：
  - ✅ 两条构建线实测：网页版 `/h5/fonts/…` + `'/api'`；APP 版 `../fonts/…` + 绝对 API
  - ✅ 打包+签名链路端到端跑通；用 APK 里的公钥验签通过，**错误口径的反例确实失败**，
    篡改包内容能被 sha256 拦下
  - ✅ 后端 CORS 预检实测（三个 APP 来源 200 / 未知来源 400）
  - ✅ `check.sh` 全绿，且反向测试确认它拦得住写错的顺序
  - ✅ `vue-tsc --noEmit` exit=0；agent_cards / agent_chat / agent_perms 全 PASSED
  - ✅ **CI 打包通过**（run 30955623675）：Capacitor 构建 → 自检 → 打 APK →
    开包校验（内置前端包/公钥/签名/API 是绝对地址）全绿，产物 **2.75MB**
    ⚠️ 第一轮 CI 挂在 `mergeReleaseResources`：图标背景色那个 XML 的注释里写了
    「与 --h5-blue 同值」，而 **XML 注释中不允许出现 `--`**。报错埋在几十行
    SAXParseException 栈里，一眼看不出。已把资源 XML 合法性加进 `check.sh`。
  - ✅ **热更新通道已开通并发了第一个包**（`2026.08.05-1`，169KB）：
    `/h5-ota/version.json` + zip 已上线；按 APP 的验签口径从公网完整验过一遍 ——
    sha256 一致、APK 里的公钥验签 Verified OK、包里有 index.html、无越界路径、
    入口是相对引用、API 是绝对地址。
  - ⏳ **装机与真机上的换包/回滚 —— 待真机验证**：
    APK 在 Actions artifact `mobile-2.0.0`（2.75MB），装到手机上跑一轮即可确认。
    预期：首开用内置包 → 后台拉到 2026.08.05-1 → **再开一次**才生效（先试用后转正）。

- **📱 手机 APP（Android）：把 H5 包进 WebView，资源走服务器(2026-08-04，已被上一条取代)**
  `mobile/` 是一个**极简原生 WebView 壳**，只干一件事：打开
  `http://8.141.123.141/h5/`。**页面资源全部在服务器上** ——
  改一行前端不用重新发 APP，手机上看到的和网页端永远一致。
  - **为什么不用 Capacitor/Cordova**：这个 APP 干的事就是「打开一个网址」，
    上一整套框架只多出 npm 依赖链和几 MB 体积，没有收益。成品 APK **2.5MB**。
  - **几个死条件**（错了照样能编译成功，但装到手机上要么白屏、要么每次重登）：
    · 服务器只有 HTTP，**安卓 9+ 默认禁明文** → 必须 `usesCleartextTraffic`
      + `network_security_config`，且**只给自家两个域名放行**，不是全局放开
    · H5 把令牌存 localStorage → 必须开 `domStorageEnabled`
    · 入口必须是 `/h5/` 不是网页端
    · 返回键要在页面内后退，不是一按就退出 APP
    · 连不上要有可重试的离线页，不能只留白屏
    · 附件下载交给系统 DownloadManager，WebView 自己存不了
    这些做成了 `mobile/check.sh`（构建前）与 `mobile/check-apk.sh`（构建后开包看）。
  - **打包**：GitHub Actions `mobile-build.yml`（本机没有 Android 工具链）。
  - ⚠️ **CI 上没有 `~/.android/debug.keystore`**，而 release 用的正是 debug 签名 ——
    不生成的话产出的是**没签名的 APK，手机直接拒绝安装**。工作流里已加生成步骤。
  - ⚠️⚠️ **判断有没有签名不能 grep `META-INF/*.RSA`**：那是 v1(JAR) 签名的痕迹；
    minSdk≥24 的包 AGP 只做 **v2/v3 签名，签名块在 APK 尾部、不是文件条目**，
    一 grep 必然误判成「没签名」—— 包其实签好了，检查却把它拦下来（实测栽过一次）。
    正确判据是 `apksigner verify`。
  - ⚠️ `dispatches` 是**异步**的：推完代码立刻取 `runs?per_page=1` 会拿到**上一次**的 run，
    白等一轮还以为改动没生效。要轮询到 run id 变了才是新的那次。
  - **iOS 没做**：调研结论见 `docs/ios-打包评估.md`（2026-08-06）。
    ⚠️ 卡的不止是 $99 账号，还有**服务器只有 IP、没有域名**这件事：
    iOS 的 ATS 不允许把 IP 当作 `NSExceptionDomains` 的键，只能全局
    `NSAllowsArbitraryLoads`；而 Ad Hoc 用 `itms-services://` 装包
    **要求清单和 IPA 都走 HTTPS**，纯 IP 直接不可用。
    所以「搞个域名 + 跑 ops/enable-https.sh」是性价比最高的第一步。
    好消息：架构能原样移植（iOS 也有 `setServerBasePath`，热更新照搬），
    后端 CORS 早就放行了 `capacitor://localhost`。

- **📰 每日简报改成「按人给」(2026-08-04)**
  发现问题：两位管理层收到的简报**一模一样**，全是应收。
  而赵仁辉 635 次操作里**仓库占 36%**、物料字典 170 次 —— 对他等于没用。
  - **来源从 3 路扩到 6 路**：应收 / 待审请款 / 待审销售订单 / **库存缺料** /
    **部门任务逾期** / **采购到期未到货**。
  - ⚠️ **权限只决定「能不能看」，不决定「该先看哪个」**。两人菜单都是全量，
    光按权限过滤两人结果完全相同。侧重只能**从行为里来** ——
    按近 30 天审计日志算主场类别（占比 ≥15% 即主场，分数 ×1.5）。
  - ⚠️ **归类要看 `target_type` 不能只看 `action`**：`create`/`update`/`delete`
    这几个动作名各模块通用，赵仁辉 30 天 create 152 次、delete 60 次，
    只看动作名一条都归不了类。
  - ⚠️ **`login` / `user_feedback_submit` 这类非业务动作必须从分母里剔掉**：
    赵仁辉 40% 的操作是登录+提反馈，不剔的话所有业务类别占比都被压到阈值以下。
  - ⚠️ **跨类别必须先在类内归一化再比**：应收是几十万量纲、库存缺口才几百，
    ×1.5 的主场加成根本翻不过来（实测两人结果还是一样）。归一化后比的是
    「这一条在它那一类里有多突出」，不是「哪一类的数字大」。
  - **单一类别最多占 2 条**，否则应收数量多、金额大，会把仓库/派活/采购全挤掉。
  - **生产实测结果**：
    · 杨坛（主场 应收+审批）→ 应收 ¥22 万 / 待审订单 / 应收 ¥20.5 万
    · 赵仁辉（主场 仓库）→ **麻花桨库存 2 低于安全线 4** / 应收 / 待审订单

- **🧠 智能体 v2 阶段三~七：Skills / 记忆 / RAG / 模型路由 / ReAct 硬约束(2026-08-04)**
  - **Skills（`agent/skills.py`）** —— 一个技能 = 固定工具编排 + 固定输出模板。
    **命中就不进 ReAct**：省一次规划、答案稳定、能写测试。
    按两位管理层真实操作选的 4 个：客户回款画像 / 项目体检（杨坛，台账占 32%）、
    库存预警 / 供应商拖期复盘（赵仁辉，仓库占 36%）。
    ⚠️ **命中判定必须严（全词匹配）**：宁可漏判走 ReAct，也不能把用户真正的问题
    劫持成预设编排 —— v1 那个贪婪正则把「查询一下所有的待审批的待办?」
    劫持成查请款单。测试里专门压了 6 条「绝不能被劫持」的提问。
    技能执行失败**退回 ReAct**，不让整条请求挂掉。
  - **记忆三层（`agent/memory.py`）**：
    · 会话焦点 —— 从最近 6 轮里抓明确出现过的项目编号；**抓不到就返回空**，
      塞一个猜错的焦点比没有更糟
    · 术语别名 —— 「南京那个」→ 项目号，**整词替换、不做模糊**
    · 用户状态 —— 存 `app_settings`，无 schema 变更
  - **RAG**：**不索引业务数据**（那是工具的活，RAG 查数据只会带来幻觉），
    只索引**口径与 SOP**。出厂 6 条（发货款/尾款到期日/合同额 0 假亏损/账龄/软删/未到货口径）
    —— 这些原来**硬编码在提示词里，加一条改一次代码**，现在按需召回、业务可自维护
    （`GET/PUT /api/agent/knowledge`、`POST /api/agent/alias`）。
    ⚠️ 量级几百条，**不上向量库**：关键词命中够用，而且可解释——出问题能一眼看出为什么召回。
  - **模型路由**：v1 是 `model = body.model or cfg["model"]`，一个默认值走到底。
    现在按任务分：含「为什么/分析/复盘/靠谱/对比」或要全量清单 → 大模型；
    单点查询（待开票、库存多少）→ 小模型。**用户显式指定时永远不覆盖他的选择**；
    没配 `model_fast` 就退回默认。
  - **ReAct 硬约束**：同工具同参数**重复调用直接拦掉**并回一句提示。
    v1 靠提示词第 5 条口头约束「不要重复调用同一工具」，模型照样重复、白烧一轮往返。
    **硬拦比嘱咐可靠。**

- **🔍 「查数糊弄人」的三个真因(2026-08-04，用户截图指出)**
  - **① 说「系统里查不到客户」，其实数据一直都在**：`tool_shipment_receiver` 读的是
    `shipment.receiver_company` —— **那正是「还没填」的那个字段**，当然每条都空，
    于是助手回「记录均未关联客户信息，只有单据编号 #95」。
    实测生产 49 条待填里 **49 条有项目编号、44 条有客户名**，沿 `project_id` 一个 join 就有。
    ⚠️ **这是最糟的一类错：让用户以为数据不存在。** 已改为带出项目编号 + 客户名，
    单据号退到最后（「#95」对人没有任何意义，认不出是哪一单）。
    顺带修：历史收货人候选的**索引键与查找键要用同一套兜底**（客户名优先、退回
    receiver_company），我一度只改了查找侧，候选永远命中不了。
  - **② 三重上限，而且从不声明**：工具写死 `rows[:20]` + 提示词「最多 5 条、250 字」
    + `max_tokens=700`。用户问「全部列举 46 条」时**结构上就给不出来**，
    而且回答里完全不提自己截断了。
    改：截断统一收到 `agent_router._cap` 一处（工具本身返回全量，
    **别在各 tool 里各截各的，否则 limit 参数形同虚设**）；
    工具 schema 统一加 `limit`（默认 20、上限 200）；结果带 `count/shown/truncated`；
    `max_tokens` 按问题动态给（命中「全部/都列/详细列举」等 → 3000）。
  - **③ 「全部列举」回了功能菜单**：查 `agent_chat_logs`，**近 3 天一次降级都没有**，
    全走的 LLM —— 所以那不是兜底文案，是模型在给不出全量时自己吐的。根因还是 ②。
    ⚠️ 排障提示：`agent_chat_logs.model` 字段记了 `rule-fallback:<原因>`，
    先查它再判断是不是降级，别看着像菜单就以为是兜底。
  - **提示词重写**：分「铁律 / 输出格式 / 必须做的分析 / 数字口径」四段。
    要点：截断必须说出来；要全量时重调工具传 `limit=200`；明细格式定死
    `主体 · 关键量 · 时间/状态 · (补充)`；**必须做分析**（排序与集中度、异常点、
    口径提醒、算得出来的就算），禁止「需要关注」这类空话；
    比例一律用 `count` 算，不许拿截断后的 `shown` 去算。
    已知口径坑写进提示词：发货单大量停在「待发货」→ 别直接说"去催款"；
    尾款没到期日→催办扫不到；合同额为 0 → 毛利算成假亏损。

- **📱 H5 卡片改成三级钻取 + 卡片名字用错(2026-08-04)**
  - **三类卡片都叫「待我审批的请款单」**（用户发现）：
    `openApprovals(cardType, label)` 的 **label 写了默认值** `'待我审批的请款单'`，
    而我做「首页点简报直接开卡片」时只传了 cardType（`openApprovals(ct.trim())`），
    于是点回款登记、点销售订单，用户气泡统统显示请款单。
    改成从 `cardRegistry.cardTitle(type)` 取（回款登记 / 销售订单审批 / 请款审批）。
    ⚠️ **名字只存一处**：`CARD_REGISTRY[type].title`。别在调用处再写默认值。
  - **汇总卡展开后收不回去**（用户提的）：原来 `expanded` 是布尔，
    一点就把 20 张卡全铺开，**没有退路**。改成 `view: 'sum' | 'list' | 'one'`：
    ① 总账 → ② **列表**（一行一笔，警示标在行上）→ ③ 单笔明细，每级都有「‹ 返回」。
    列表行的文案直接从卡片 `facts` 里取（第一条=客户，`emphasis`=金额），
    不为列表另开接口——两处取数早晚会不一致。
    处理完一笔从列表里移除并退回列表（留在原地会让人以为没生效）。
  - **实测**：总账 ¥95 万/8 笔 → 列表 8 行 → 明细 1/8 → 逐级退回 → 再次进入，来回正常。

- **⚡ 异步取数一律并发，别写 `for ... await`(2026-08-04)**
  用户指出简报模块又退回了串行。`agent_router` 早就是
  `asyncio.gather` + 每任务独立 session，我新写的 `briefing.build` 却是
  `for fn in (...): items.extend(await fn(db, current))` —— 三路本来互不依赖，
  串起来白等两轮往返。
  - **改法**：`briefing.build` 三路并发；`daily.run_once` **读并发、写串行**
    （算简报是纯读可以并发；`push_message`/写 setting 共用 caller 的 session，
    并发写会 concurrent operations not permitted）。
  - ⚠️ **每个并发分支必须用自己的 `SessionLocal()`**：AsyncSession 不能并发复用。
    所以 `build()` 干脆**不收外部 session** —— 收了反而诱使人复用它。
  - **实测**：三路各注入 200ms 延迟，串行应是 600ms，实测 **228ms**；
    真实数据平均 2.0ms/次。
  - **约定**：本仓库里凡是多路互不依赖的 await，一律 `asyncio.gather`。

- **💥 事故：1.0.30/1.0.31 打包缺文件，装上开不起来(2026-08-03，本次)**
  - **现象**：客户端一开就是原生弹窗
    `A JavaScript error occurred in the main process / Cannot find module './lib/health'`。
  - **原因**：把判定逻辑抽到 `desktop/lib/health.js`，`main.js` 里 `require('./lib/health')`，
    但 **`package.json` 的 `build.files` 是白名单**（只列了
    `main.js / preload.js / renderer/** / app/**`），新建的 `lib/` 没加进去。
  - ⚠️ **为什么一路绿灯**：构建成功、artifact 正常、上传通过、`latest.yml` 校验也绿 ——
    因为这些查的全是「**包在不在**」，**没有一环查「包能不能启动**」。
  - ⚠️ **危害被强制更新放大**：那时 `force_latest` 开着，等于把所有人推到一个
    开不起来的包上；而客户端**启动即崩，autoUpdater 根本跑不到**，
    自动更新救不回来，只能手工重装。
    ⚠️ 中招的机器在 `desktop_clients` 里**是隐形的**——崩了就发不出请求，
    表里留的是它崩之前的版本，别拿这张表判断「有没有人中招」。
  - **止血**：`latest.yml` 回退到 1.0.29（`lib/` 是 1.0.30 才引入的，1.0.29 是最后一个能启动的），
    `version.json` 的 `min_version` 同步降回 1.0.29。
  - **根因修复**：`build.files` 加 `lib/**/*`。
  - **防线（这才是重点）**：`desktop/test/packaging.test.js` ——
    扫 `main.js`/`preload.js` 的本地 `require`（含递归、先剥注释），
    逐个比对 `build.files`，不在白名单里就红。
    自带反向自检：用出事那版的 `build.files` 跑必须能抓到，否则测试本身失效。
    接进 `npm test`，并且**加进 GitHub Actions 打包前的必经步骤** ——
    缺文件的包从此产不出来。`ship.sh` 里也从「版本变了才跑」改成**无条件跑**
    （lib 漏打那次恰恰是新建目录、不改逻辑的改动）。
  - **教训**：以后往 `desktop/` 下**新建任何目录**，先跑 `npm test --prefix desktop`。
    发版流水线里「产物存在」不等于「产物可用」，这两件事必须分开验。

- **🤖 修完自动发版 + 自动回复反馈(2026-08-03，本次)**
  用户原话：「后续修完问题，自动发版、自动反馈问题」。这两件事以前全靠手工，
  结果是修完的东西躺在仓库里没发出去、提出人也不知道修没修
  （#339~#343 五条躺了一整天，其中 #343 的黑屏已经有 8 个人踩了 23 次）。
  - **`desktop/ship.sh`** —— 客户端一条命令发完：
    bump 版本（package.json + lock 两处同步）→ 本地预检 `npm ci` + `npm test`
    → 提交推 main → 触发 GitHub Actions → 轮询等构建 → 下载 artifact
    → `release.sh --upload-only` 传通道 → **校验 latest.yml 与安装包真能下**
    → `--force-latest` 时才抬 `min_version`。
    ⚠️ **顺序不能反：必须先把包传上通道，再抬 min_version。**
    反过来是「要求升到 X 但通道上没有 X」，所有客户端卡在强制更新页，谁都进不去。
    ⚠️ bump 时 `package.json` 与 `package-lock.json`（根 + `packages[""]` 两处）必须一起改，
    否则 CI 在 `npm ci` 阶段 EUSAGE 直接挂 —— 脚本里已加本地预检，别再让 CI 替你发现。
    ⚠️ 改这类文件**不要**写 `open(p,'w').write(open(p).read())`：
    Python 先求值外层 `open(...,'w')`，文件当场被截断，读到的是空的 —— 我把两个文件清零过一次。
  - **`ops/reply-feedback.py`** —— 批量回复反馈，走 `/user-feedback/{id}/reply` 同一套逻辑
    （status=done、reply_read=False 触发登录弹窗、push_message 站内+企微双通道、写审计）。
    已回复的会跳过，不覆盖人工回复。
    ⚠️ **这是真发到同事企业微信的**，跑之前必须 `--dry-run` 核对发给谁、发什么。
    措辞照「反馈回复要短」：两三句说清改了什么、在哪看、要不要更新客户端。
  - **本次已发出**：#339~#343 五条全部回复 + 推送落地，生产上无遗留未处理反馈。

- **🐞 用户反馈 #339~#343 + 客户端两项(2026-08-03，本次)**
  - **#339 `¥¥`(赵仁辉)**：不是财务页一处，是**全库 34 处**。四个 `fmtMoney` 变体
    (`utils/format.ts` / `api/sales.ts` / PurchaseMgmtView / OaView)**全都自带 ¥**，
    模板里再写一个就双。已全部去掉模板侧的字面 ¥。
    ⚠️ **以后写金额一律 `{{ fmtMoney(x) }}`，前面不要加 ¥**；顺带 0/空值现在显示
    `—` 而不是 `¥—`。查残留：`grep -rn '¥{{ *fmt\|¥\${fmt' frontend/src` 应为 0。
  - **#340 不能单个下载(赵仁辉，语音转写作「不能耽搁下载」)**：采购资料抽屉只有
    勾选打 zip，想单拿一张 CAD 图也得先解压。每行加下载图标，走
    `POST /purchase/package` 加 `single:true` → 只勾一项时回原文件。
    ⚠️ **不要为此另开 GET 端点**：角色校验、项目归属、`pushed==True` 得重写一遍，
    写漏一条就是越权下载。也别改用 `/attachments/{id}/download`——那个是「登录即可」，
    比打包松。单文件与 zip 的 xlsx 生成已抽成 `_sheet_to_workbook` 共用，避免两处跑偏。
  - **#341 段码顺延(卢照坤)**：新增中类/细分类时段码空着，得自己数到第几个，
    猜错后端甩 409。改为按同级已用值**从 1 找第一个空位**并补零，输入框仍可改。
    从 1 找空位而不是 max+1：删掉中间某个之后要能补回去。
  - **#342 新增后整棵树重新展开(卢照坤)**：`el-tree` 用了 `default-expand-all`，
    而 `catTree` 是 computed —— 保存后 `loadCats()` 换掉 `catFlat`，整个 `:data` 被重建，
    el-tree 重新套用 `default-expand-all`，用户收起来的分支全弹开。
    改成自己维护 `expandedKeys`（首次进来仍全展开，之后听用户的），
    存完只把新增节点的父节点补进展开集。
  - **#343 黑屏 —— 第三版才是真身：401 跳登录页的地址在客户端解析不到**
    ⚠️ 前两版结论（客户端太老 / GPU 合成器卡死）**都错了**，下面这版是有复现的。
    - **决定性证据是登录时间**：王利利每天登录**两次**，第二次固定在第一次之后 8 小时——
      07-27 `08:03→16:03`、07-30 `08:17→16:23`、08-01 `08:15→16:17`、
      08-03 `08:15→16:25`（报黑屏这天）。令牌有效期正是 **8 小时**。
      他说的「4点多一点」= 令牌到期那一刻。**不是随机故障，是每天准点发作。**
      （反馈导出里的时间是 **UTC**，08:25 = 北京 16:25，别看错。）
    - **成因**：`frontend/src/api/index.ts` 的 401 拦截器写的是
      `location.href = '/login'`。**绝对路径**。桌面客户端的页面从
      `file:///.../desktop/app/index.html` 加载，绝对路径解析成 **`file:///login`**——
      不存在 → `ERR_FILE_NOT_FOUND` → Chromium 把文档换成空的错误页
      (`chrome-error://chromewebdata/`) → **一个像素都不画** →
      只剩 `BrowserWindow.backgroundColor` 的深蓝 → 「怎么点都没反应」是因为**真的什么都没有**。
      而 `did-fail-load` 那时只调了 `revealMainWindow()`，等于把空窗口亮给用户。
    - **为什么网页端没事**：网页端 `/login` 有 nginx SPA 兜底，正常跳转。
      只有客户端踩，因为只有客户端跑在 `file://` 上。
    - **为什么路由守卫没拦住**：客户端构建带 `VITE_API_BASE`，路由是 **hash 模式**，
      `location.pathname` 是那个 html 的文件路径，永远不等于 `/login`，
      原来那句 `if (location.pathname !== '/login')` 形同虚设。
    - **影响面**：近 14 天 **8 个人踩了 23 次**（按「两次登录间隔 7.5~8.6 小时」统计），
      只有王利利报上来。lixinxin 5 次、wangqin 4 次、admin 4 次…
    - **修法**：拦截器按路由模式分开跳（hash 模式走 `location.hash='#/login'` 再 reload），
      判据 `!!import.meta.env.VITE_API_BASE` 与 `router/index.ts` 保持同一个。
      客户端侧再加一道兜底：`did-fail-load` 主框架失败就退回内置首页，
      绝不把人扔在空文档里（`ERR_ABORTED(-3)` 除外，那是正常的导航打断）。
    - **复现与验证**：`/tmp` 下用真 Electron 跑过——
      `file:///login` → errno -6、文档 EMPTY、地址变 `chrome-error://chromewebdata/`；
      修完再跑：绝对路径失败能自动回到首页，hash 跳转页面根本不丢。
    ⚠️ **教训**：排客户端故障先查 `desktop_clients`（版本）+ `audit_logs` 的 **login 间隔**。
    我前两版都在客户端代码里找原因，而答案在「他每天什么时候重新登录」这张表里。

  - **#343 黑屏(王利利)** —— ⚠️ **第一版结论是错的，改后重写，别照旧的排查**：
    - 错在哪：我先按「上报为零 ⇒ 客户端太老 ⇒ 装上 1.0.21 就好」推的。
      两个前提都不成立。查 `desktop_clients` 表（**这张表记了每台设备的版本，
      以后排客户端问题先查它**）——王利利跑的是 **1.0.28**，自恢复本来就装着；
      而 `recover()` **只写本地 crash.log，从不调 `sendReport`**，
      所以「零上报」根本推不出「没崩过」，只说明我们看不见。
    - 真正的成因：截图是**纯色 #0f1d30**，不是渐变。页面背景 `--bg-page` 是
      **浅灰 #eef1f5**，Vue 挂了也该是浅灰；深蓝只可能是 `BrowserWindow.backgroundColor`，
      即**一个像素都没画** = 合成器层面，不是 DOM 层面。
      而 GPU/合成器死掉时渲染进程**还活着、还响应**，
      `render-process-gone` 和 `unresponsive` 一个都不触发 —— 老逻辑在这种情况下完全失效。
      原代码 `child-process-gone` 里那句注释「当前这次由页面自恢复兜着」是错的，已改。
    - 修法：preload 发 **rAF 画面心跳**（rAF 由合成器驱动，合成器没了就停，
      而 setTimeout 照走 —— 所以只能用 rAF，换成定时器就测不出来）。
      主进程在「窗口获得焦点 / 系统唤醒 / 解锁 / 人在操作」时判，45s 无心跳即卡死。
    - ⚠️ **判定必须用 `isFocused()`，不能用 `isVisible()`**：被别的窗口整个盖住时
      `isVisible()` 仍返回 true，而**实测不在前台时 rAF 就被节流到不跑**，
      拿 isVisible 判会在用户切去 Excel 看一分钟回来时误判，正在填的表单直接没。
    - ⚠️ **卡死了不能用 `webContents.reload()` 救**：合成器活在 **GPU 进程**里，
      重载换的是渲染进程的文档，用的还是那个坏掉的 GPU 进程。
      只能 `app.relaunch()`（并打 GPU 标记，下次启动禁用硬件加速，从根上不再犯）——
      就是把用户手工的「任务管理器杀进程重开」自动化。token 在 localStorage，重启不用重登。
      渲染进程崩溃是另一回事，那种情况 reload 是对的，仍走 `recover()`。
    - **上报补全**：`recover()` / `child-process-gone` / 画面卡死现在都会 `sendReport`。
      在此之前渲染层的崩溃对服务端**结构性不可见**。
    - 判定逻辑抽到 `desktop/lib/health.js`，`npm test --prefix desktop` 跑 14 条断言
      （无头环境拿不到真 OS 焦点，直接测 GUI 只会一路 return，什么都验不到）。
    - Electron 探针实测确认：心跳能从 preload 到主进程；掐掉 rAF 后心跳停而
      **渲染进程仍活着且响应**（`render-process-gone` 确实不触发）。
    ⚠️ 排障提示：**截图取色能直接分层**——纯 #0f1d30 = 合成器死了(窗口没画)，
    浅灰 #eef1f5 = 页面还在画但 Vue 挂了，渐变 = 登录页/启动页。
  - **补充1 下载到指定文件夹**：原来写死 `app.getPath('downloads')`。
    加 `userData/download-prefs.json`（目录 + 是否每次询问），菜单「操作」下多两项。
    ⚠️ 默认值保持原样（系统下载目录、不询问），**装了新版的人不会突然被弹窗**；
    目录被删/U盘拔了自动退回系统下载目录，不能让下载直接失败。
    `setSavePath` 必须在 `will-download` 回调里**同步**调用，所以用 `showSaveDialogSync`。
  - **补充2 强制更新才能登录**：机制本来就有（启动拉 `version.json`，低于
    `min_version` 只给「立即更新」），只是生产上 `min_version` 还是 `0.0.1` = 没开。
    本次补两块：
    - `force_latest: true` → 客户端拿 `latest.yml` 的版本当门槛，省得每次发版
      都要记得改 `min_version`（老客户端不认这个字段，只认 `min_version`，两个都要设）
    - **登录前再查一次**（`pms-desktop:enforce-version`）：客户端可能连开好几天不重启，
      还有 30 天免登录，只靠启动那一次查不到新版
    ⚠️ 网络不通时**一律放行**——宁可漏拦，也不能因为服务器抖一下把人锁在门外。
    ⚠️ 切进强制模式前必须 `autoUpdater.removeAllListeners()` + 清掉 4h 轮询定时器，
    否则常规更新流程和强制流程两套监听同时跑，用户会先看到常规的「立即重启更新」
    原生对话框，点取消就卡在强制页上不动。
  - **验证**：`tests/test_fb339_fb343.py` 全绿（single 的三道权限门逐条压过：
    非采购角色 403 / 跨项目 404 / 非白名单表 404 / 未推送附件 404）；
    `vue-tsc --noEmit` exit=0；浏览器实测四条：段码预填 06、收起的分支保持收起、
    KPI 显示 `—` 不是 `¥—`、点单项下载拿到 `spreadsheetml` 而非 zip。
  - **已发布**：客户端 1.0.29 已上通道，`version.json` 已置
    `min_version=1.0.29` + `force_latest=true` —— **强制更新已经生效**。
    低于 1.0.29 的客户端一开就进强制更新页，登录按钮也点不动。
    ⚠️ **网页版不受影响**：真出问题的人还能从 pms.tonghui-tech.com 进系统，
    所以这个开关不会把谁彻底挡在系统外。要回滚就把 version.json 的
    `min_version` 改回 `0.0.1`、`force_latest` 改回 false 再传一次，秒级生效。
  - ⚠️ **已知与本次无关的红灯**：`test_m07_warehouse` 与 `test_e2e_business_flows`
    在本次改动**之前就是失败的**（git stash 后跑基线确认过），别记到这批账上。

- **📱 H5 二期修正：汇总卡 / 流式 / 门户列表化 / 30 天免登录(2026-08-03，本次)**
  - **流式只出第一个字(我引入的 bug，值得记)**：`msgs.value.push(draft)` 之后仍拿
    **push 之前的原始对象** `draft.text +=` 累加。Vue3 的 `ref` 数组存进去的是 **Proxy**，
    改 raw target 不过 set 陷阱，视图收不到通知。第一个字之所以能出来，是同一批里
    `thinking.value=false` 顺带触发了一次渲染，**不是流式在工作**。
    修法：push 之后重新取 `msgs.value[msgs.value.length - 1]` 再往上累加。
    ⚠️ 排查时后端一度被误当嫌疑——实测后端始终正常（207 片、首字 3.4s、1.5s 推完）。
    **往响应式数组里塞对象后要接着改它，一律重新取回引用。**
  - **一次弹 20 张卡没法看** → 先出汇总卡（合计金额 / 发货款应收与尾款分两组 /
    最久挂了多少天），点「逐条处理」才展开明细。
    ⚠️ **汇总必须走全量 `blind_ledgers()` 再求和，不能对截断后的前 20 条求和**，
    否则「共 ¥253 万」与展开后的列表加起来对不上，一次对不上就再没人信这张卡。
    汇总与明细同源同口径（`summarize()` 和 `assemble_settle_cards()` 都调 `blind_ledgers`）。
  - **明细加厚**：补合同额 + 预付/发货前付条款及是否已收。只给「客户 + 金额」
    没法判断这笔应收在整单里占多少、收到哪一步了，也就没法决定要不要销账。
  - **门户去 AI 味**：光球与图标块下线，网格瓦片改一行一条的列表卡片。
  - **30 天免登录**：`create_access_token(subject, extra, minutes)` 加第三参，
    `remember=true` 时 8 小时 → `REMEMBER_MINUTES`(30 天)。
    ⚠️ **本地只记用户名，密码一个字都不存**；延长的是服务端令牌寿命。
    `LoginIn.remember` / `GateVerifyIn.remember` 都有默认值，
    **不带该字段的旧客户端行为不变**（生产实测仍是 8.0 小时）。
  - **生产实测**：`remember=False → 8.0h`、`remember=True → 720.0h`；四段改动逐个在
    容器内的构建产物里查到；主站 `/` 与 `/updates/latest.yml` 均 200，网页端/客户端未受影响。
  - **踩坑（验证方法本身的坑）**：H5 是**分片懒加载**的，`h5-*.js` 只是入口 chunk，
    视图代码在 `H5ChatView-*.js` / `H5LoginView-*.js` 里；grep 错 chunk 会得出「没上去」的假结论。
    scoped 样式类名在 `*.css` 而不在 JS 里；minify 后变量名没了，别拿变量名当验证依据。

- **🔒 客户端设备限制 device_gate + device_ids(2026-08-03，本次)**
  - **需求原话**：客户端按设备 ID 做登录控制，带开关默认关；登录页展示设备 ID；
    设备 ID 全部由管理层手工收集录入到「外网访问」页；不在名单里的客户端要验证码才能登录。
  - **为什么不是 IP 白名单**：先做过一版 `desktop_cidrs`（按来源 IP 限制），
    上生产查 `audit_logs` 实测**否掉了**——7 天内 18 个不同来源 IP，每个用户 2~5 个；
    办公室出口 07-30 前后从 `180.112.90.169` 换成 `114.225.218.156`；
    `212.74.6.34/.35/.36` 一天一个顺序递增。**动态 IP，白名单三天就失效**。
    那一版已整体撤掉，不要再走这条路。
  - **实现**：`app_settings` 加两个键 `device_gate_enabled`(默认 "0" **关**) 与
    `device_ids`(JSON 数组)。`gate.desktop_exempt(is_desktop, device_id, *, device_gate, device_ids)`：
    开关关 → 装了客户端就免闸（与加此功能前**完全一致**，存量零影响）；
    开关开 → 还要 `X-PMS-Device` 落在名单里。**无 schema 变更**。
  - **开关开 + 名单空 = 所有桌面端都要码**。这是刻意的字面语义：开关本身就是安全阀
    （默认关），主动打开即表示知道自己在做什么；前端保存时二次确认。admin 恒免闸不会锁死。
  - **顺带补的洞**：原先桌面端免闸只看 `X-PMS-Client` 头，这个头 curl 就能伪造，
    等于整道闸门形同虚设。开了设备闸后伪造者还得先知道某台在册机器的 UUID。
  - **客户端侧(需重新打包 1.0.28 派发)**：
    - `LoginView.vue` 页脚上方显示「本机设备 ID」+ 复制按钮（`navigator.clipboard`
      在 `file://` 下可能不可用，已加 `execCommand` 回退 + `user-select:all` 兜底）。
      放登录页是因为**被拦下的人正卡在这一页**，能当场复制发给管理层。
    - `main.js` 的 `loadDeviceId()` 写盘失败时置 `deviceIdPersisted=false` 并走
      `sendReport('error', ...)` 上报。**这条不可省**：写失败会导致每次启动换新 ID，
      那台机器永远不在名单里、批了也没用，而服务端完全看不出原因（常见诱因是杀软拦
      `%APPDATA%` 写入——old-uninstaller 那次崩溃三条线索也都指向杀软）。
  - **设备 ID 的稳定性(查证过，不是推测)**：`crypto.randomUUID()` 存
    `%APPDATA%\pms-desktop\device.json`。升级不变（只覆盖 `%LOCALAPPDATA%\Programs\`）；
    **卸载重装也不变**（`build.nsis.deleteAppDataOnUninstall` 未设置，默认 false）；
    换网/换 IP 不变。只有换电脑、重装系统、换 Windows 账号才变。
    注意目录名是 `pms-desktop` 不是中文名——package.json 顶层没有 `productName`，
    Electron 的 `app.getName()` 回落到 `name`。
  - **上线顺序(别搞反)**：① 后端+管理页先上，开关关着不影响任何人 →
    ② 打包 1.0.28 派发，用户登录页才看得到设备 ID → ③ 收齐设备 ID 录入名单 →
    ④ 最后才打开开关。
  - **验证**：`tests/test_gate_device_ids.py` 30 条断言 PASSED；
    旧的 `test_login_gate` / `test_start_push_gate` 仍 PASSED；`vue-tsc --noEmit` exit=0。
    （`pytest tests/` 全量收集报 76 个 OperationalError 是**旧问题**：这些测试都是
    独立脚本，import 时就建库互相撞，得单个 `python tests/xxx.py` 跑。）
  - **测试踩坑**：RFC 5737 文档保留段 `192.0.2/24`、`198.51.100/24`、`203.0.113/24`
    在 Python `ipaddress` 里 `is_private` **为 True**，会被判成内网直接免闸。
    拿它们当「外网 IP」写用例会得到假通过——第一版测试就是这么挂的。用真公网段。
  - **待办**：客户端里服务器地址是打包时写死的
    （`desktop/release.sh:145` `VITE_API_BASE=http://8.141.123.141`，且是 http 明文）。
    服务器公网 IP 一变，所有已派发客户端全部失联，只能重新打包重装。改成域名可根治。

- **🖥 1.0.27：跳过 old-uninstaller + 故障自动上报(2026-08-02)**
  - **崩溃不是噪音，是让升级彻底失败**（读 NSIS 模板确证，非推测）：
    `installUtil.nsh:213-241` 退出码非 0 → 等 1 秒重试**最多 5 次**（所以会弹好几个框）；
    5 次都败 → 弹「应用程序无法关闭」默认 CANCEL → `$R0` 仍非 0 →
    `handleUninstallResult:128-132` **`SetErrorLevel 2` + `Quit`，安装中止**。
    那台机器就**永远停在旧版本**。生产 23 台里 8 台散落在 1.0.1/1.0.4/1.0.5/1.0.19/1.0.20，
    3 台停在 1.0.1 十二天没动过。更新源 URL 从 1.0.0 至今没变过（排除死地址），
    且 `autoInstallOnAppQuit` 默认 true（点「稍后」退出时也会装）——所以**大概率是装失败，不是用户拖延**。
  - **修法**：`desktop/build/installer.nsh` 的 `!macro customInit` 删掉注册表 `UninstallString`。
    `uninstallOldVersion` 一进门读它，读到空就直接 `Return`（`installUtil.nsh:155-163`），
    整条崩溃路径不存在。`customInit` 跑在 `.onInit` 里（`installer.nsi:79`），
    早于 install section 的 `uninstallOldVersion`（`installSection.nsh:52`）。
    此时 `$R0=0`（152-153 先 Push 0）、无错误标志，`handleUninstallResult` 一路放行。
    另定义空的 `customUnInstallCheck`/`customUnInstallCheckCurrentUser` 作双保险。
    **代价**：旧版文件不删（同目录覆盖，只留孤儿文件）；卸载入口由安装收尾
    `installer.nsh:122-123` 写回，不会丢。
  - **崩溃根因仍未确证**，但三条线索都指向不是 electron-builder：
    ① 「错误收集程序」不是 Windows 自带对话框（有「错误分类号」等字段）＝第三方收集器；
    ② `virtdisk.dll`——NSIS 只用 kernel32/user32/advapi32/shell32，**没理由加载虚拟磁盘 API**，
       说明有东西注入了该进程；③ 异常码 `0x7fffffff` 不是合法 Windows 异常码
       （真的长这样：`0xC0000005`），是占位值，**收集器自己也不知道出了什么事**。
    合理解释：`old-uninstaller.exe` 是刚解压到 `%TEMP%` 的**无签名 exe**，杀软教科书级触发条件。
  - **纠正一条旧结论**：曾说「1.0.25→1.0.26 是验证修复的判据窗口」，**错的**。
    跑 1.0.25 的是 **0 台**——electron-updater 直接跳到最新版，1.0.25 被整个跳过。
    修复版 `uninstaller.nsh` 是随 1.0.25 落盘的，没人装过它。真正判据是 **1.0.26 → 1.0.27**。
  - **🆕 故障自动上报**（`desktop_reports` 表 + `POST /api/desktop/report`）：
    · **该端点故意不认证**——升级失败/启动崩溃都发生在**登录之前**，挂鉴权就永远收不到。
      防滥用：kind 白名单 + detail 截断 64KB（留尾部）+ 每 device_id 每天 20 条
      （超限返回 200 不是 429，不给探测者反馈阈值）。查询侧仍是 admin/manager 专属。
    · **关键设计**：安装器在 app 退出后才跑，崩溃时客户端进程已不在，没法当场上报。
      改成**下次启动回溯**——`update-downloaded` 把目标版本写 `pending-update.json`，
      下次启动版本没变即判定安装失败。上报成功才清标记，失败则累加 attempts 下次再报。
    · 顺手补了两个洞：`autoUpdater.logger` 原来接 `console.log`，**打包后没有控制台等于全丢**，
      现在落 `crash.log`；主进程原来没有 `uncaughtException`/`unhandledRejection` 钩子。
    · 管理页「桌面端」加「故障上报」tab（未处理数角标、可展开看日志原文、可标记已处理）。
  - 测试：`backend/tests/test_desktop_report.py`（22 项断言，含未认证可写/限流/截断/越权读）。

- **🔴 反馈 #336/#337/#338 采购（李新新）——三条根因各不相同(2026-08-02，本次)**
  - **#337「设计部二次上传，我没收到通知」= 真 bug，清单导入全程零通知**
    · 排查路径：查 2026-059B 的推送审计 → 07-31 只有 `sheetpkg`(→王芹)、
      `sealing_pkg`/`coldwork_pkg`(→生产组)，**08-01 一条推送都没有**；
      再查 datasheet → **五张清单全部 07-31 15:12 导入**，正是她截图里群消息那一刻。
    · **根因**：`excel_router.py` **完全没有 import 过 notify 模块**——图纸(附件)走
      `orders_router.start_push` 有推送，清单走 Excel 导入是静默的。她的三列里
      「外购附图」是附件(有推送)，「标准件清单/电工采购单」是清单(**零推送**)。
      设计师在微信群喊「图纸/清单已上传」，系统这边对清单一声不吭。
    · **修法**：`excel_router._notify_sheet_import()` 按 `_SHEET_DOMAIN`(清单名→分工域)
      查 `BUYER_SHEET_MAP` 找采购员，两条导入路径（整包 `/projects/{pid}/import-excel`、
      单表 `/datasheets/{did}/import-excel`）都发。**一个采购员一条消息**（多表合并，
      五表齐导不刷屏）；域内无活跃采购员就不发。
    · **坑（已踩并修）**：判「【更新】」不能用「项目里有没有旧数据表」——
      **新项目建好时六张模板表就已存在**（只是 `imported_at=None`），拿它当依据会让
      **首次导入也发成【更新】**。判据必须是**这张表自己的 `imported_at`**，
      整包路径要在删表**之前**把已导过的表名捞出来。
  - **#338 加推送时间 + 排序**：`Attachment.pushed_at` 新列（`start_push` 写入；
    存量由 `backfill_attachment_pushed_at` 从 `audit_logs(action=push,
    detail='start-push:{kind} x{n}')` 按 (任务单,kind) 逐批对时间，对不上的退回 `created_at`）。
    **回填要从最新一批倒着对**：生产上有一组审计声称 5 个、实际只剩 4 个（文件被换过，
    正是 2026-059B/任务单160/sheetpkg），正着对会把幸存的都算成较早那次的时间——
    这列是给采购员「别漏单」用的，宁可偏新不可偏旧。生产演练：1372 个已推送附件里
    审计能对上 153 个，其余 1219 个退回 `created_at`（#303 之前上传即推送，本就等价）。
    `/purchase/projects` 增 `sheet_times`(各清单导入时间) + 附件的 `pushed_at`。
    **行级「最近推送」故意放前端算**（`PurchaseView.rowLastPush`）——只统计该账号
    **实际可见的列**，否则李新新的排序会被王芹的激光件清单带偏；可见性规则只有前端一份。
    24 小时内的标红「新」。
  - **#336 物料查询 ≠ AI**：她要「查物料价格、货比三家、比对新供应商报价」。
    **生产数据实测**：807 行 / 266 个物料名；按「名称+规格」精确归并后**跨供应商比价的只有 1 组，
    还是"运费"**——历史上根本没有同一规格买过两家（轴承全从江苏鸿旭隆进）。
    所以她真正要的是**「上次这东西多少钱」拿来对新报价**，不是横向比三家。
    做法：`/purchase-mgmt/items` + `/items/summary` 加 `keyword`，
    **空格分词逐词 AND**——规格写法不统一（同一个 6016 轴承有的填 `6016.0`、
    有的填 `GB／T276-94深沟球轴承6016-2R`），整串 LIKE 命中率太低；单价列加 `sortable`。
  - 测试：`backend/tests/test_fb336_fb337_fb338.py`（30 项断言，含
    「关键字不绕过采购员行级隔离」「存量附件 pushed_at 为空时退回 created_at」）。
  - **注意**：`backend/tests/` 里的用例是**独立脚本**，不是 pytest 用例
    （模块级 `asyncio.run(main())` + 各自设 `DATABASE_URL`）。
    `pytest tests/` 会 74 个收集错误，**干净树上也一样**——要逐个 `python tests/xxx.py` 跑。
    当前全量：59 通过 / 15 个既有失败（与干净树逐字相同，非本次引入）。

- **🖥 桌面客户端 old-uninstaller 崩溃：升 electron-builder 到 26.15.3(2026-08-01，本次)**
  - **上一轮判断被证伪**：曾判定病根是「在 macOS 上交叉编译 Windows NSIS」，并说「第二个 CI 版本
    是判据时刻」。判据出来了——`desktop_clients` 显示 **6 台已从 1.0.22 升到 1.0.23，两版都是
    Windows 原生打的，崩溃框照弹**。交叉编译不是原因。
  - **机制**（读 electron-builder NSIS 模板得出，非推测）：`installUtil.nsh:209-240` —— 安装器把
    旧版卸载程序复制到 `$PLUGINSDIR\old-uninstaller.exe`，以 `/S` 静默执行，
    **退出码非 0 就重试，最多 5 次**（所以它才这么顽固，不是弹一次）。
    `installSection.nsh:52` 是**无条件调用**，electron-builder **没有任何配置开关能跳过**
    （issue #2675「Ability to skip uninstallOldVersion」至今未实现）。
  - **升级依据**（24.13.3 → 26.15.3 后 diff 模板）：`installUtil.nsh`（调用侧）**逐字未变**；
    但 `uninstaller.nsh`（**崩溃发生的那一侧**）有两处改动正好落在本场景：
    ① 显式解析 `/S` 并 `SetSilent silent`——我们正是用 `/S` 静默调用的，旧版没显式解析；
    ② `RMDir /r $INSTDIR` 之前先 `SetOutPath $TEMP`——旧版工作目录还停在 `$INSTDIR` 里就删自己。
    **不是铁证，但改动落在正确位置**，比上一轮的猜测扎实。
  - **时间差（必须记住）**：崩溃的是**旧版本带来的**卸载程序，所以本次改动要到
    **1.0.25 → 1.0.26** 那次升级才可能见效；**1.0.24 → 1.0.25 仍会弹**，这不代表修复失败。
  - `electron-updater` 运行时已是最新 6.8.9，未动；CI Node 20 满足要求；产物 79.4MB
    （`file` 确认为合法 NSIS 自解压包），`latest.yml` 与服务器实际大小一致。
  - **回滚路径**：若 26.15.3 打出的包在 Windows 上装不上，把 `desktop/package.json` 的
    `electron-builder` 改回 `^24.13.3`、`npm i`、发下一个 patch 版即可（CI 打包 2 分半）。
  - **另发现（待处理）**：`desktop_clients` 显示 **3 台今天仍在跑 1.0.1、2 台跑 1.0.20**，
    但当天有活动——说明这几台**自动更新根本没生效**，近期所有修复（越权、工作台数据、驳回流程）
    都没吃到。需查是用户一直点「稍后」还是更新链路对它们就是坏的。

- **🔴 反馈 #334/#335 工作台「已完成」看不到数据——我们自己引入的回归(2026-08-01，本次)**
  - **现象**：陈立新(design_lead/designer)、赵仁辉(designer/manager/sales)都反馈 `/dept/design`
    「我的订单（已完成）」为 0、任务跟踪也看不到东西。赵仁辉带 `manager`（绕过所有行级隔离）
    也看到 0 → 排除权限问题，是**状态筛选口径**。
  - **根因**：2026-07-30 `cfcc21a` 把 `/api/orders` 的 `proj_status` 从「项目状态」改成
    「**任务单状态**」（本身是对的，修的是"筛选与状态列对不上"），但**工作台一次取数供多个 tab 用**：
    · 前端 `projStatusFilter` 默认 `'进行中'`（`DeptWorkbenchView.vue:118`）→ 传 `proj_status=进行中`
    · 后端 → `q.where(DeptOrder.status != "done")` ← **已完成的单在服务端就被删光**
    · 前端 `myDone = orders.filter(o => o.status === 'done')`（`:562`）← 只能恒为空
    **顶部状态下拉与「我的订单」三个 tab 抢同一个维度**，这是设计错误，`cfcc21a` 触发了它。
  - **生产实证**：设计部 57 条 done 全被剔掉（其中 42 条项目还在进行中，改动前看得见）；
    陈立新名下 29 条、赵仁辉 5 条，页面均显示 0。
  - **修法**（不回退 `cfcc21a`）：工作台取数**不再传 `proj_status`**、`limit` 提到 500；
    顶部状态下拉改为**客户端过滤**（`statusFiltered`），且**只作用于「任务跟踪 / 外协」**这类
    不按状态分 tab 的列表；「我的订单(待接单/进行中/已完成)」与「待分派」各自按 status 分 tab、
    不受下拉影响。下拉加 tooltip 说明作用范围。
    **`@change="load"` 必须保留**——生产部三个组(钣金/装配/封板)仍按 `group_done` 走服务端筛选，
    需要重新取数（`c993edd` 的口径）。
  - **新增测试 `tests/test_fb334_workbench_done_tab.py`**：锁后端契约——不传 `proj_status` 时
    结果里必须**同时含 done 与非 done**（前端才分得出三个 tab）；同时保留 `cfcc21a` 的语义断言
    （显式传状态时按任务单状态筛、项目为「进行中」而任务已 done 的必须筛得出来），
    防止有人图省事把 `cfcc21a` 回退掉。
  - **教训（已进 AGENTS.md）**：一次取数供多个 tab 用时，**服务端不能按 tab 所依据的同一个字段收窄**，
    否则客户端 tab 必然出现"永远为空"且不报错。改筛选口径前先查清这份数据还被谁消费。
  - 测试：fb334 / status_filter_consistency / produce_groups / agent_row_level_scope /
    start_push_gate / push_domain_routing 全 PASSED；vue-tsc exit=0，vite build 通过。

- **🔴 AI 助手行级越权修复 + #331 收口(2026-08-01，本次)**
  - **这是线上正在发生的越权，不是设计缺陷讨论**。7 个工具里只有 `tool_project_status` 接收 `current`，
    其余 6 个**函数签名里根本没有用户对象**，因此不可能做行级过滤。门控只到菜单级，
    而 AI 助手菜单对所有登录用户无条件开放（`user_menu_keys()` 无条件追加 `agent`）。
    · 采购：`agent_router.py:66` 无 buyer 过滤 ↔ 页面 `purchase_mgmt_router.py:349` 有 `_buyer_restricted`
      → 普通采购员页面只看得到自己录的单，问 AI 拿到全公司采购明细（含各采购员姓名）
    · 销售：`agent_router.py:138` 无 sales 过滤 ↔ 页面 `sales_router.py:239` 有 `_all_view`
      → 普通销售员问一句「尾款」，拿到**全公司客户名 + 尾款金额 + 归属销售**
    · 任务：只做部门级过滤 ↔ 页面 `orders_router` 有 `worker_id == me`
  - **修法：复用页面本体的谓词，不在工具层重写角色判断**。这是根因——重写必然漂移，
    页面改了规则工具不会跟。新增 import：`_buyer_restricted`(purchase_mgmt_router)、
    `_all_view`(sales_router)、`_is_mgr`/`_is_lead`(orders_router)。
    六个工具签名统一加 `current`；新增 `_scope_po()` 收口采购过滤。
    任务口径：管理层看全部；部门主管看本部门全部；其余只看 `worker_id == 自己`（主管可能只管一个部门，
    故按部门逐个放行再并上本人）。
  - **顺带修口径 bug**：`po_overdue_by_supplier` 此前在**已截断的 Top-20 明细**上聚合、
    `item_total` 却用全量 count，两个数字对不上。抽出 `_po_arrival_overdue_rows()` 返回全量行，
    聚合基于全量、只在最后截断供应商条数。
  - **新增测试 `tests/test_agent_row_level_scope.py`**：断言口径刻意选成
    「**AI 返回行数 == 页面接口返回行数**」而不是「等于某个常量」——常量断言会随种子数据漂移，
    对照断言能持续捕获"页面改了规则、工具没跟"。**这正是当初缺失、因而没拦住越权的那类测试**。
    覆盖采购/销售/任务三域 × 受限/主管/管理层三种身份 + 聚合口径 + 晨报收窄 + 菜单门控回归。
  - 既有 `tests/test_agent_chat.py` 因签名变更需同步（5 处直接调用改为传 admin），已改并通过。
  - **#331 财务职责分离收口**：业务确认**规则保留**（需求.docx 第十六项，审批人≠付款人，
    admin/manager 也不留后门）。出口是 2026-08-01 上线的**撤回审批**：若某张单的审批人恰好
    记成了付款人本人，由审批人 `PUT /payment-requests/{id}/withdraw-approval` 退回、
    发起人重提一次即可解开，**不需要改内控**。此项从待办移除。
  - 测试：agent_chat / agent_chat_logs / agent_perms / agent_row_level_scope 四个全 PASSED；
    回归 payment_reject_flow / fb332_fb333 / fb329 / batch14 / sales_fixes / preq_visibility /
    spare_and_purchase / produce_groups / status_filter_consistency 全 PASSED。
  - 配套产出：`docs/AI助手架构设计稿.html`（L3 架构与六期路径）、
    `docs/ai-agent-erp-handbook/index.html`（《AI Agent 落地企业 ERP 完整手册》十二章约 20 万字）。

- **🆕 桌面客户端改 Windows 原生打包(2026-08-01)**:用户反映**每次自动更新都弹**
  「old-uninstaller.exe 遇到问题已经停止工作」。那个 exe 是 electron-updater 的更新机制产物——
  新包会把**上一版留下的卸载程序**复制成 `old-uninstaller.exe` 静默执行以删旧版,崩的是它。
  两个关键判断:①**崩的是上一版带来的 exe**,所以任何改动都要**再更新一轮**才看得出效果;
  ②我们一直在 **macOS 上交叉编译 Windows NSIS**(`electron-builder 24.13.3, os=darwin`),
  上游有一批长期未解的「macOS 打的 Windows 包卸载会出问题」issue(electron-userland/electron-builder#4875、
  #8131 24.13.2 的回归 closed as not planned)。因此**先换打包机器,不换框架**——最便宜且直指病根,
  应用代码一行不动。
  - 新增 `.github/workflows/desktop-build.yml`(`windows-latest`,手动 Run workflow,可选传
    version / min_version),产物为 artifact(exe + blockmap + latest.yml + version.json,留 14 天)。
  - `desktop/release.sh` 加 `--upload-only <目录>`:只上传不打包,版本号从 exe 文件名反解
    (不依赖本地 package.json——本地可能已被后续改动 bump 过)。上传仍用本机 `.deploy.local` 的密钥。
  - **产物不自动上传服务器**:`github.com/zhangang12/excel-share-system` 是**公开仓库**,
    生产 SSH 私钥不进 Actions Secrets。放 artifact 不新增暴露面——`http://8.141.123.141/desktop/`
    本来就匿名可取(实测 latest.yml 200)。
  - CI 里**不设 npmmirror 镜像**(那是国内网络防超时用的,GitHub runner 直连原站更快),
    `--x64` 仍必须显式给。本 workflow **不改仓库版本号**,默认用 `desktop/package.json` 里的当前值,
    保持「版本 bump 随代码提交入库」的既有规矩。
  - **验证方式(尚未验证)**:必须在 Windows 机器上**连装两个版本**才能确认弹框消失——
    先装一版 CI 产物,再更新到下一版 CI 产物。1.0.21 及以前都是 macOS 打的,升到第一个 CI 版时
    仍会弹(崩的是 1.0.21 带来的卸载程序),**从第二个 CI 版起才是真正的观察点**。
  - 备选方案(已评估未采纳):Tauri v2 在 Windows 上默认**也是 NSIS**,不保证解决,且要引入 Rust
    工具链 + 重写 `main.js` 那 373 行;浏览器/PWA 路线可行(前端本来就是同源 SPA,浏览器构建走
    `/api`),唯一拦路虎是外网登录闸门只免桌面端(`X-PMS-Client` 头),留作后续可选方向。

- **🆕 用户反馈第 20 批 #332/#333 + 口述需求两项(2026-08-01，本次)**:已过 vue-tsc + vite build +
  后端实跑(`test_fb332_fb333_sales.py` / `test_payment_reject_flow.py` 全绿,并回归
  fb329/purchase_feedback_batch14/preq_visibility/spare_and_purchase/recv_po_project_filter/sales_fixes)。
  - **#333 销售主管可为所有销售员申请开票(杨坛)**:`sales_router.py` 的 `_is_sales(u)` = `has_role("sales")`,
    **不排除主管**;杨坛同时持有 sales + sales_lead → 恒为 True 被行级校验拦下。同文件其余 9 处行级隔离
    都带 `_all_view` 豁免,只有 `invoice_apply` 与 `upload_contract` 两处漏了。**铁证**:`invoice_apply`
    内部第 850 行「管理层/销售主管提交时直接进入待开票」那段对兼任 sales 的主管是**死代码**;而合并开票
    (`_merge`)口径一直是对的,所以他勾两个项目能过、单个反被拦。两处补成 `_is_sales and not _all_view and ...`。
    顺带:前端成功提示原来写死「等待销售主管审批」,主管提交其实是直连财务,改用后端返回的 message;
    该 catch 里重复的 `ElMessage.error` 去掉(拦截器已弹过一次,截图里就是两条一模一样的红条)。
  - **#332 尾款插入到款时间→尾款金额自动清零(杨坛)**:`balance` 是**双语义共用列**——催办/账龄/13周现金流/
    AI 助手只认 `balance>0` 判"未收",而台账合计、销售报表「收款计划汇总·尾款」、Excel 导出把它当**合同条款额**。
    已把"清零会让合同尾款额永久丢失、回款越好报表数字越小"当面讲明,**用户明确选择直接清零**。
    实现在 `update_payment_note` 的 `balance` 分支(与上方 `before_ship` 分支并列)。
    **兜底**:清零前把合同额存进新列 `balance_contract`,删批注即原样回填、该列清空——把"不可逆"这条补上;
    台账尾款列到账后显示 0,悬停可见「已到账,合同尾款 ¥X」。真实痛点是 `overdue.py` 的 `scan_balance_overdue`
    每周三重抄送催他(销售/主管/管理层全是他),而全仓**没有任何一处读 `balance_note`**,清零后立即停催。
  - **🆕 请款驳回闭环(用户口述,非反馈单)**:出纳(杨倩)付款时发现收款账户名称/账号不对,原来只能硬付或不动。
    新增三个端点 + 发起人重提:
    * `PUT /payment-requests/{id}/pay-reject` 出纳付款驳回(原因必填)、
      `PUT /{id}/withdraw-approval` 审批人批完再撤回、`PUT /{id}/resubmit` 发起人改完重新提交。
    * 三种驳回共用 `_do_reject()`,落 `reject_stage`(approve/withdraw/pay)/`rejected_by`/`rejected_at`,
      并 push_message 通知发起人(站内+企微)。**`rejected_by` 必须独立于 `finance_approver_id`**:
      出纳驳回若复用审批人字段,既抹掉真审批人,又会让「审批人不能给自己审过的单付款」这条职责分离判错人。
    * **重提后必须重新审批**(用户 2026-08-01 定:改收款账户属实质变更,审批人须复核)——`resubmit` 把
      `finance_approver_id`/`approved_at` 一并清空,回到 pending。
    * 前端:财务页待审「驳回」由 link 细字改 `danger plain`(原来像说明文字)、已审批未付款加「撤回审批」、
      付款页加「驳回」;采购页「请款记录」显示驳回环节标签+退回人,已驳回行加「重新提交」(二次确认里提示
      先去供应商档案改账号)。
  - **🆕 桌面客户端黑屏/闪退**:用户截图里那片深蓝正是 `main.js` 给 BrowserWindow 设的
    `backgroundColor:'#0f1d30'`——**窗口还在、渲染进程已死**,只剩底色在画。主进程此前只监听
    `did-fail-load`(管"页面没加载成功",管不了"加载成功后进程死掉"),所以一挂就永远黑着,只能任务管理器结束进程。
    修:① `render-process-gone` → 自动 reload(5s 内防重入,webContents 不可用则重建窗口),
    token 在 localStorage,重载后不用重新登录;② `unresponsive` 卡满 15s 自动重载,`responsive` 取消;
    ③ `child-process-gone` 记录 GPU 崩溃并打标记 `.gpu-crashed`,**下次启动自动 `disableHardwareAcceleration()`**
    (Windows 集显驱动崩 GPU 进程是"闲置就黑屏"最常见成因,只对真崩过的机器降级);
    ④ 崩溃日志落 `userData/crash.log`(200KB 自动截断)——以后排障让用户发这个文件,不用再猜;
    ⑤ `backgroundThrottling:false`,切后台不掐定时器。

- **🆕 用户反馈第 19 批 #329~#331(2026-07-31，本次)**:3 条做了 2 条,**#331 用户明确指示搁置**;
  已过 vue-tsc + vite build + 后端实跑(`backend/tests/test_fb329_received_price_edit.py` 全绿,
  并回归 recv_po_project_filter / purchase_feedback_batch14 / po_append_items / spare_and_purchase /
  wh_batch_out / po_arrival_reminder 六个邻近测试):
  - **#329 仓库收货价填错、采购要能改(王芹)**:先实证——`PUT /purchase-mgmt/items/{id}` 本来就允许采购在
    收货后改单价/收货金额(`_buyer_restricted` 只卡「他人明细」),所以不是权限缺失。真正的缺口三处:
    * **合并单父行没有改价入口**:父行只有「整单维护」,而 `set-group-summary` 只写开票金额/已付款/付款日期/对账状态,
      改价必须展开逐条点「编辑」(#321 起合并组默认收起,更难发现)。`PurchaseMgmtView` 整单维护弹窗**并入逐条改价表格**
      (名称/规格/数量/单价/收货金额,单价↔收货金额互算),外加「整单重填总价 → 按数量分摊」助手
      (与后端 `receive_batch` 的权重分摊+末行兜余数**完全同口径**)。提交时先对改动行逐条 `PUT /items/{id}`
      (只发 unit_price/received_amount,走 exclude_unset),再发原来的 set-group-summary。
    * **同一字段两个名字**:列表列叫「收货金额」、编辑弹窗叫「合计金额」,统一成列表口径。
    * **★真 bug:仓库改价不回写入库流水**。`_auto_stock_in` 幂等(同一明细只过账一次,第二次直接 return),
      所以仓库在「已收货」点「修改」改价时,`PurchaseItem` 更新了但 `WhTxn.amount/unit_price` 永远停在旧价 →
      库存金额与项目材料成本跟着错。抽出 `_sync_txn_amount()`,`update_item` 与 `_finish_receive`(覆盖
      receive_item + receive_batch)共用。**口径**:整条一次性入库(`t.qty == item.qty`)时流水金额直接跟
      **收货金额**走,而不是 qty×单价——合并收货是按数量权重分摊总价、单价是反算的,乘回去有分位差,
      跟收货金额走才能保证「库存金额合计 == 收货金额合计」。
  - **#330 待收货被 300 截断、老单搜不到(王利利)**:三个缺陷叠加,截图里「待收货(300)」「已收货(300)」
    两个数恰好相等就是证据。
    * `list_receiving` 硬编码 `.limit(300)` 且无分页 → 默认放到 **2000**(`_RECV_LIMIT_DEFAULT`,可传 `limit`,上限 5000)。
      排序是 `po_no desc nullslast`,而单号是 `TH{yyyymmdd}-{序号}` 定长格式 → 字典序倒序==时间倒序,
      **被丢掉的正是最老的长周期单**(无单号的散单更靠后、最先丢),精确命中用户抱怨。
    * 三个搜索框(单号/项目编号、物料名、下单月份)**全是对已加载那 300 条做前端过滤**(#286/#290/#315 留下的),
      搜的是已经被截断的集合 → 全部下沉为后端 SQL 参数 `keyword`/`item_name`/`order_month`,回车或点「查询」触发。
      **`keyword` 必须带第三个分支**:`source_sheet_id IN (按项目编号匹配的清单)`——#253 那批明细自己
      `project_code` 是空的,页面上的项目编号是 `_item_out` 之后回溯出来的,只比对存量字段会比前端过滤时代**更少**(回归)。
    * 供应商下拉只从那 300 条去重、徽标数直接取被截断数组的 `.length` → 新增 `GET /receiving/meta`
      (真实 `count(*)` + 两个视图各自的**全量**供应商),`loadBadgeCounts` 不再拉两坨全量列表。
      前端保留一层同口径本地过滤只为打字时的即时反馈;命中上限时弹 el-alert 明说真实条数并指路用筛选。
  - **#331 财务职责分离(杨倩「需要同一个账号操作」)——本轮不动,等业务回复规则**。事实备查:
    校验在 `purchase_mgmt_router.py` 付款端点,比较 `PaymentRequest.finance_approver_id == current.id`,
    来源是需求.docx 第十六项内控要求(`ce94ced`),**admin/manager 也不留后门**(按 user.id 比,与角色无关)。
    她能撞上这条说明**这张请款单就是她自己审批的**;上游诱因是 `approve` 端点开给了 `finance` 角色,
    而 seed 里的设计意图是 **finance_lead 审批 / finance 付款**两个人分权(#313 也实证过唯一审批人=杨坛)。
    附带发现:`PaymentRequest` **没有 `paid_by` 字段**,付款人不落库,事后审计查不到谁付的款。
    **球在业务侧**:2026-07-31 01:50 超级管理员已回复杨倩「业务规则是,一人审批,另外一人付款。
    请与管理层确认此规则是否需要拿掉?」——等她带管理层结论回来再决定走哪条路
    (收窄 approve 到 finance_lead 恢复分权 / 加 app_settings 开关可关 / 维持现状只改前端提示)。
  - **本批反馈已回复**:#329→王芹(user_id=22)、#330→王利利(user_id=4),站内+企微各推 1 条;
    #331 早在本批之前已由管理员回复(见上),脚本幂等跳过、未覆盖。

- **🆕 用户反馈第 11 批 #253~#257(2026-07-18，本次)**:5 条,做了 4 条(#255 语义不清待用户澄清);
  已过 vue-tsc + vite build + 后端实跑(`scratchpad/verify_batch18.py` 全绿):
  - **#256 财务可看「全部申请」明细**:`oa_router` scope=="all" 由 admin/manager 放开到含 **finance**;
    `_can_view` 加「finance 可查看任意申请明细」(对账/付款需要);前端 `OaView.canViewAll` 由 `isAdmin` 改为 `isAdmin||hasRole('finance')`。
  - **#257 汇总报表加单据类型筛选**:`OaView` 汇总报表 tab 加「单据类型(全部)」下拉,`summaryView` 客户端过滤
    `summaryRows`(纯前端,后端 oa_summary 不动)。
  - **#254 全流程图生产部显示全部3组**:`collab_router` 生产部节点原来只列**已派发**的组(ProduceGroupTask 有行的),
    装配组没派就看不到。改为**始终列 钣金/封板/装配 三组**,未派发的占位(负责人—)。
  - **#253 采购收货显示订单编号**:外协/加工件下单时常不带项目号,`PurchaseItem.project_code` 空 → 仓库收货列表
    「订单编号」列全 `—`。两头修:①`list_receiving` 对 project_code 空但有 source_sheet_id 的明细,**回溯来源表所属项目编号**
    补显示(修存量);②`create_order_from_list` 未显式带 project_code 时,从来源清单回溯**落库**(修新单)。散单(无来源表)不臆造。
    **🆕 追加(2026-07-18,用户「两处都加」)**:上面只兜「从清单下单」的单;**手工新建采购单**(无 source_sheet_id)下单没填订单编号的兜不到。
    * **收货可补/改订单编号**:`PurchaseReceiveIn`/`BatchReceiveIn` 加 `project_code`;`receive_item`/`receive_batch` 在 `_finish_receive` **之前**
      写 `item.project_code`(这样自动入库流水 `WhTxn.project_id` 也能挂上项目);传 None 不清空、传空串才清。前端 `WarehouseView`
      收货弹窗 + 合并收货弹窗各加「订单编号」el-select(项目下拉 + allow-create 可直接输),`recvForm`/`batchRecvForm` 带 `project_code`。
    * **下单订单编号必填**:`PurchaseMgmtView.saveOrder` 校验——表头默认订单编号或逐行订单编号必须有其一,否则拦下(提示无项目可去字典维护通用编号)。
    * 验证 `scratchpad/verify_253b.py`(收货补填单条/合并/不误清 + 入库挂项目)。
  - **#255 采购负责人下单回写改「不覆盖手填」**:赵仁辉「表格的负责人 直接填写 不用再后面填写」——用户确认
    是「确认下单回写逻辑」。原来采购下单会把 `采购负责人` 列**无条件回写**为下单人,覆盖设计阶段手填的负责人。
    `_writeback_sheet_row` 加 `only_if_empty` 参数;两处下单回写(create_order_from_list / 成套 create_kit)对
    `采购负责人` 传 `only_if_empty={"采购负责人"}`——**已手填则保留、为空才自动填下单人**;下单日期仍照常覆盖回写。
    纯后端(数据表单元格本就可编辑)。验证 `scratchpad/verify_255.py`。

- **🆕 用户反馈列表分页 + 概览统计(2026-07-18，本次)**:用户要求「用户反馈做分页查询，优化性能」。
  原 `GET /api/user-feedback` 一次最多返回 500 条(超出静默丢弃)，前端一次性拉全量。已过 vue-tsc + vite build +
  SQLite 实跑(`scratchpad/verify_fb_paging.py` 5 断言全绿)。
  - **响应结构变了(破坏性)**:`GET /api/user-feedback` 由 `List[UserFeedbackRow]` 改为
    `{items, total, page, page_size, stats}`(`schemas.UserFeedbackListOut`)。加 `page`(≥1)/`page_size`(1..100,默认20)。
    **两个消费方都改了**:admin `UserFeedbackView.vue`(服务端分页 + `el-pagination`)、`HelperFloating.vue`「我的反馈」
    (`.list({mine:true, page_size:100}).items`)。导出走独立 export.html 端点,不受影响。
  - **概览统计移到服务端**:KPI(总数/待处理/已处理/bug/建议/其它)由后端一条聚合查询算(`func.sum(case(...))`,
    **无 GROUP BY,PG 安全**),按「可见范围」全量算——**不受 kind/status 过滤与分页影响**(比原来前端按当前列表算更准)。
    前端 `stats` 由 computed 改为 ref 存服务端返回。过滤变更用 `reload()`(回第1页)。标记已处理后 `load()` 刷新(状态变化会影响过滤/计数)。

- **🆕 用户反馈第 10 批 #247~#252(2026-07-17，本次)**:6 条全做;已过 vue-tsc + vite build + 后端实跑
  (`scratchpad/verify_batch17.py` 全绿 + mgmt/demand 回归):
  - **#247 OA 汇总报表下钻明细**:汇总报表(部门×单据类型)行原来点了没反应。加后端
    `GET /oa/reports/summary/detail?department_id=&doc_type=`(门槛 finance,返回该组已批准申请逐条:
    单号/申请人/事由/金额[核定优先]/批准时间);前端 `OaView.vue` 行可点 + 「查看明细」→ 弹窗列表。
    **注**:OaView 里已有 `detailVisible/detailLoading`(申请详情抽屉),下钻变量必须另起名 `sumDetail*`,否则 vue-tsc 重名报错。
  - **#248 工资表加个税**:`EmployeeSalaryMonthly` 加 `personal_tax`(_NEW_COLUMNS 迁移);
    `_salary_net` 改为 实发=基本+绩效+加班费+补贴−社保公积金−**个税**−其他扣款(用 `getattr(s,"personal_tax",0)` 兜旧行);
    get/save_salary + `SalaryRowIn` 带上;前端工资表在社保与其他扣款间插「个税」列 + 改提示公式。
  - **#249 工资批量导入**:仿考勤导入,加 `GET /hr/salary/import-template` + `POST /hr/salary/{period}/import`
    (`_SAL_IMPORT_COLS`,按工号 zfill(5) 匹配,整行覆盖,表里没出现的人不动);前端工资 tab 加「批量导入/下载模板」。
  - **#250 采购收货回写库位**:`_finish_receive` 回写清单时原只写 到货日期/仓库签字/进度,漏了库位。
    加 `_ALL_WH_LOC_COLS=("库位",)`,收货填了库位就回写到清单「库位」列(`_writeback_sheet_row` 对不存在该列的表自动跳过)。
  - **#251 管理层待办加截止时间**:`ManagementTodo` 加 `due_date`(_NEW_COLUMNS 迁移,因表已上线);
    创建表单加「截止时间」选填 date;`_is_overdue(t,today,due_date)` 逾期口径改为"未完成且已过 **due_date 或 承诺日** 最早者"
    ——**设了 due_date 即使收件人没回复承诺,过期也算逾期**;overdue.scan_management_todos 同步(文案区分 承诺/要求);
    收件人卡片显示「截止」、监控卡片显示「截止 X」tag。
  - **#252 清单「已到货」显示绿色**:`DatasheetGrid.vue` 的 `STATUS_DONE_WORDS` 加 `已到货/已收货/已备齐`——
    进度列(进度/状态/完成度…)值命中即整格绿(与"完成"同款)。纯前端一词之改。

- **🆕 管理层待办(全局浮动挂件)(2026-07-16，本次)**:管理层下达待办→勾选收件人→收件人回复承诺完成时间→
  承诺日到仍未点「已完成」则每日推逾期提醒;收件人可申请顺延承诺日(管理层审批)。已出设计草图
  `docs/管理层待办-需求与界面草图.html`,用户答完 6 个问题后开发。已过 vue-tsc + vite build + 后端路由/schema 实跑
  (`scratchpad/verify_mgmt_todo.py` 全绿)。
  - **用户确认口径(2026-07-16)**:①收件人=管理层每次自己勾选(复用 `/api/admin/users` 名单);②只有 admin/manager 能建待办;
    ③逾期=承诺日到了还没点「已完成」→ 开始每日提醒;④收件人回复后可**申请顺延**承诺日,需管理层同意才改;
    ⑤图标=清单打勾(方案 A);⑥浮动挂件对**全部人**显示。
  - **数据模型**(`models.py`):`ManagementTodo`(一条待办:title/content/priority/created_by)+ `ManagementTodoTarget`
    (一人一行处理态:status `pending`待回复→`committed`已承诺→`done`已完成;committed_at 承诺日;progress 进展;
    extend_status/extend_to/extend_reason 顺延申请三件套)。**新表由 create_all 自动建**,无需 `_NEW_COLUMNS`。
  - **后端**`routers/management_todo_router.py`(prefix `/api/management-todos`,10 端点):建待办(admin/manager,校验收件人有效)、
    `/sent` 监控(admin/manager 看全部+每人汇总)、`/mine` 我收到的、`/mine/count` 角标数、`/{tid}/reply` 回复承诺时间、
    `/{tid}/done` 完成、`/{tid}/progress` 更新进展、`/{tid}/extend` 申请顺延、`/{tid}/extend/decide` 管理层审批顺延、
    `DELETE /{id}` 撤销。所有收件人操作校验 `target.user_id == 当前人`(防改他人待办)。
  - **逾期扫描**`overdue.scan_management_todos`(挂进 `overdue_scheduler`,并加 `POST /api/internal/management-todo-scan`
    手动触发):承诺日已过未完成→每日推收件人+抄送下达人;下发满 1 天仍 pending(没回复)→每日催回复。
    **幂等键=(biz_type='mgmt_todo_remind', biz_id=target.id, 当日)**,同一待办·同一人一天只推一次(同逾期提醒红线口径)。
  - **前端**:`components/ManagementTodoFloating.vue` 全局浮动挂件(在「反馈」按钮上方 `right:22px bottom:84px`,避免重叠;
    青色 `#0f766e` + 清单打勾 inline SVG + 未处理角标)。弹窗两 tab:「我收到的」(全员:回复承诺/标记完成/更新进展/申请顺延)、
    「下发/监控」(仅管理层:新建待办 + 每条待办各收件人进展表 + 顺延审批 同意/驳回)。`api/managementTodo.ts`。
    挂载于 `MainLayout.vue`(与 HelperFloating 并列),每 60s 轮询 `myCount` 刷角标。
  - **py3.9 本地导入坑复用**:新 `management_todo_router.py` **不用 PEP-604 `X|None`**(全用 `Optional[...]`),
    这样 `fbvenv` 的 py3.9 能独立导入该路由验证(不经 create_app,避开 ws_router 的 `WebSocket|None`)。生产是 py3.12 无所谓。

- **🆕 采购收货：固定列直达采购单(2026-07-16，本次)**:用户反馈——合并(kit/同采购单多行)的收货行,采购单号链接
  在窄屏/树形缩进下被截断成「...」,很难点进采购单。改法:`WarehouseView.vue` 采购收货表「操作」列(fixed=right,
  永不截断)对**任何有 po_no 的行**(合并父行/子行/单行)都加一个「采购单」link 按钮,复用 `viewPoPdf(po_no)`
  (打开采购单 PDF,即 #234/#235 的目的地)。操作列宽 112→168。纯前端,零后端改动。

- **🆕 物料需求纳入「采购单入库到项目」的物料(2026-07-16，本次)**:用户反馈——出入库/物料需求里**只**显示了
  从标准件清单下单收货的物料;通过**新建采购单**买的物料虽然也关联了项目编号(采购收货时 `PurchaseItem.project_code`
  → 入库流水 `WhTxn.project_id`),却无法在这里汇总展示和出库。已修,前后端过 vue-tsc + vite build + SQLite 实跑
  (`scratchpad/verify_demand_purchase.py` 4 断言全绿)。
  - **根因**:`warehouse_router._demand_rows` 原本**只**读项目「标准件清单」(BOM) datasheet 的行来产出需求,
    采购单买的非清单物料(收货时可能自动新建 category="采购入库" 的 WhMaterial)从不进这个列表。
  - **改法**(后端 `_demand_rows`):产出两类行、合并返回、都可勾选领用出库——
    ① `source="清单"`:材料清单需求行;
    ② `source="采购"`:`WhTxn(direction=in, project_id=本项目, 非冲红)` 入库、但**不在清单**里的物料。
       其 `demand_qty`=本项目采购入库量,`issued_qty`=已领量 → 待出库=采购量−已领,与清单行同一套勾选/批量出库口径。
    清单已覆盖的物料(bom_mat_ids)不重复列,需求量以清单为准。
  - **`demand_overview`**:项目来源改为并集——有标准件清单的项目 ∪ 有"关联项目号入库"的项目(哪怕没清单),
    否则纯采购单项目根本不在总览里出现。新增的 GROUP BY 查询与相邻 `issued_map` 同形(仅分组列+聚合),PG 安全。
  - **`schemas.WarehouseDemandRow`** 加 `source` 字段;前端 `WarehouseView.vue` 需求明细表加「来源」标签列
    (清单/采购),并放宽相关空态/提示文案(不再只提"标准件清单")。**出库端点 `issue_demand` 不用改**——它本就按
    material_id+qty 出库到项目,不关心物料是不是清单来的,采购行直接复用。
  - **🆕 追加(同日,用户提醒后)**:「清单需求」原只读 `Datasheet.name=="标准件清单"` 是旧逻辑——但从清单下单
    (`purchase_mgmt._PURCHASABLE_SHEETS`)支持 **5 张**表。用户确认只纳入**走库存的 3 张材料清单**:
    **标准件清单 / 电工采购单 / 不锈钢原料下料单**(都有数量列);外协加工 / 激光件清单是定制直发件,**不算**仓库物料需求。
    * 新增 `warehouse_router._demand_sheet_cols()`——惰性 import `_PURCHASABLE_SHEETS`(唯一权威,避免二次硬编码;
      规避与采购路由的循环依赖),取 standard/elec_po/material 三键的 (名称列,规格列,数量列)。**各表列名不同**:
      标准件/电工采购的名称列是「项目」、不锈钢是「材料类别」;都用「规格型号」「数量」。
    * `_demand_rows` 由"读一张标准件清单"改为"遍历项目下这 3 张表按各自列名读";`demand_overview` 的项目并集
      第①类也从"有标准件清单"改为"有这 3 张任一"。收货后的物料仍由 `source="采购"` 分支兜住(与来源表无关)。
    * 验证:`scratchpad/verify_demand_sheets.py`(4 断言:3 表列名各异都读对/外协排除/仅清单项目进总览)+
      `verify_demand_purchase.py` 回归(4 断言),全绿。

- **🆕 仓库「出入库登记」并入「物料需求」(2026-07-16，本次)**:用户要求两 tab 合并;已出界面草图
  `docs/仓库-出入库与物料需求合并-界面草图.html` 经用户确认后开发。已过 vue-tsc + vite build + tab_registry 校验。
  - **纯前端布局合并 + 静态注册表改名**,后端逻辑零改动(io/demand 的函数、端点全复用)。
  - `WarehouseView.vue`:删除独立 `io` tab;`demand` tab 改名「出入库 / 物料需求」,顶部加两个登记按钮
    (入库登记/出库登记,`.io-bar` 样式,v-if canWrite),逻辑不变;下方仍是「按项目汇总总览 → 查看进入逐行需求 →
    复选批量领用出库(#244)」两级结构(草图确认的形态)。
  - `menus.py` TAB_REGISTRY:移除 `("io","出入库登记")`,`demand` 改名「出入库/物料需求」。**旧账号若 hidden_tabs
    里有 `warehouse:io` 会失效(无害)**;若谁之前单独隐藏了 `warehouse:demand`,合并后会连登记按钮一起看不到(极少见)。
  - 默认 tab 仍是 `ov`,无处依赖 `io`,删除安全。

- **🆕 字典设置放开给所有人(2026-07-16，本次)**:用户要求「字典设置菜单权限放开给每个人」,确认要开到「菜单+查看+编辑」全放开。
  - 前端 `MainLayout.vue`:字典设置链接原硬编码在 `v-if="auth.isAdmin"` 的管理组里,移到业务菜单区(bizMenus 之后),所有登录用户可见。
  - 后端 `warehouse_router.py`:material-categories / material-dict 的**增删改** 6 个端点由 `require_roles(*_DICT_ADMIN_ROLES)`(原=buyer_lead+管理层)改为 `get_current_user`——所有登录用户可编辑。`_DICT_ADMIN_ROLES` 已废弃(删)。读取本就全员开放。
  - 路由 `dict-admin` 本就无 menuKey/requireAdmin 守卫,不用改。已实跑验证普通装配工能增改字典。
  - **口径提醒**:这是有意的权限放宽——任何登录员工都能改物料类别/单位/材质/供应商分类/订单编号规则(影响全系统下拉/编号)。若日后要收回,把这 6 个端点和 MainLayout 链接改回 admin/manager 即可。

- **🆕 用户反馈第 9 批 #241~#246(2026-07-16，本次)**:6 条,#243 是排查题(结论=数据条件,未改码),其余 5 条已做;
  已过 vue-tsc + vite build + 后端实跑(`scratchpad/verify_batch9.py` 全绿):
  - **#241 采购申请「驳回」按钮被裁**:PurchaseMgmtView 采购申请 tab 操作列 width=230 装不下「查看/打印+已处理+驳回」,
    驳回被裁成「…」采购员找不到。加宽到 320,驳回按钮从 link 改 plain(更显眼)。功能本就都在(rejectIncoming)。
  - **#242 采购申请推送口径回调(与 #205 相反)**:#205 曾改成"一律推全体采购员",现用户(王芹,buyer 副角色)反馈
    指派后仍全员通知太吵。改 `create_purchase_request`:**指定了 buyer_id → 只推给他一人**(文案「指派给你」);
    未指定 → 才推全体(主/副角色并集)。**列表可见性不动**(仍全员可见),只改推送。
  - **#244 物料需求勾选批量出库**:WarehouseView 物料需求表加 `type=selection`(仅 stock>0 且有未领需求的行可勾)+
    「批量领用出库(已选N)」按钮。复用 `whApi.issueDemand(projectId, lines[])`——它本就支持多行(一键领用就是全量调它)。
    注:单行「领用出库」(issueOne)一直都有,只是 stock=0 时不显示,王利利看到的全是 0 库存所以以为只有一键全部。
  - **#245/#246 请购单二选一(逐行填 / 上传文件)**:电工/设计/生产(DeptWorkbenchView)+仓库(WarehouseView)的请购单弹窗
    加单选「逐行填写 / 上传文件」。上传走**先传附件(biz_id 空)→ 建单时带 attachment_ids → 后端回绑 biz_id**:
    * `attachments_router.ALLOWED_BIZ_TYPES` 加 `purchase_request`。
    * `PurchaseRequestCreate` 加 `attachment_ids`;`create_purchase_request` 放宽为"明细行 或 附件 至少有其一",
      回绑时**只认 biz_type=purchase_request、biz_id 空、uploaded_by=当前人** 的附件(防盗绑他人/已绑附件,已测)。
    * `_preq_out` 加 `attachments` + 新 `_preq_atts()` 批量取;三处采购申请列表(电工/仓库/采购管理)的 expand 都能下载文件,
      纯文件单「物料」摘要列显示「📎 上传文件(N)」。
  - **#243 出库项目号不反显(排查,未改码)**:上批 c8a800e 的 `_material_projects` 逻辑正确且保守——物料的入库流水
    (WhTxn)必须带 project_id 且全部指向同一项目才反显;赵仁辉的物料没反显=**入库时没关联项目 或 跨了多项目**,
    属数据条件,不是 bug。**不盲改匹配逻辑**(猜一个项目填上会填错,比手填更糟)。需拿他具体物料到服务器追库确认。
    → 待办:问用户要一个具体物料/项目,或让其确认该物料入库时是否带了项目号。

- **🆕 用户反馈第 8 批 #229/#236~#240(2026-07-15，上一批)**:6 条全做;已过 vue-tsc + vite build + 后端实跑
  (`scratchpad/verify_batch8.py` 23 断言、`verify_batch8b.py` 9 断言,全绿):
  - **#229 离职当月仍要发工资(口径修正,影响面最大)**:原 `hr_router._active_emps` 写死 `status != "离职"` 且被考勤/工资**共用**——
    人一标离职就立刻从工资表消失,**最后一个月工资发不出来**。改为 `_active_emps(db, period=None)`：传 period 时按
    `status != 离职 OR (leave_date 非空 AND leave_date[:7] >= period)` 取人,即**离职日期所在月及之前仍在列、之后不在**;
    离职但没填离职日期的仍排除(无法判断哪个月离的)。不传 period 退化为老口径,兼容其他调用方。
    考勤/工资行都带 `left_this_month`/`leave_date`,前端在姓名后打「离职」标签(避免照上月全额发)。
    **✅ 2026-07-16 线上验证**:人事翻当月离职员工,确认已回到工资表。
    **注**:用户原话「人员离职处理功能显示」极含糊,上批曾因此暂缓;本次确认口径=当月离职留在当月考勤/工资。
    离职本身的功能(状态/离职日期/停用账号提醒/入职离职统计)**早就齐全**,不用再加。
  - **#239 考勤加迟到/早退/缺卡 + 是否全勤**:`AttendanceMonthly` 加 `late_count`/`early_leave_count`/`missing_card_count`
    (已进 `_NEW_COLUMNS["attendance_monthly"]` 迁移)。**「是否全勤」不存字段**,由 `hr_router._is_full_attendance()` 自动推导:
    应出勤>0 且 实出勤≥应出勤 且 请假/迟到/早退/缺卡全为 0。前端 `isFullAttendance()` 是同口径的镜像(为了随录入实时变色),
    **改口径要同时改这两处**。
  - **#240 考勤批量导入**:`GET /hr/attendance/import-template`(xlsx 模板+填写说明) + `POST /hr/attendance/{period}/import`。
    **按工号匹配**(Excel 会把 00015 读成数字 15 → 代码里 `zfill(5)` 补零重试);该月已有考勤的人**整行覆盖**(不是只更新非空列),
    表里没出现的人不动;工号不在该月名单的跳过并在返回消息里列出来。导入到哪个月由页面上选的月份决定,不在表里填。
  - **#237 请款审批不该流转到王芹**:她角色是 buyer+finance(兼任),`/finance` 的「请款审批」tab 里全是**她自己提的单**,能自审自批。
    两手:①**代码**——`purchase_mgmt_router.approve_payment_request` 加 `requester_id == current.id` 硬校验(**管理层也不留后门**,
    与既有的「审批人不能给自己审过的单付款」同属职责分离);前端自己的单「审批通过」按钮禁用+tooltip 说明。
    ②**配置**——管理员到「权限管理」把 `finance:pay_requests`(请款审批) 对王芹隐藏,用的是 #7 的 `hidden_tabs` 机制,零代码。
    **✅ 2026-07-16 已配置完成**(用户操作)。以后再有兼任账号提类似诉求,照这个办:先看 `TAB_REGISTRY` 里有没有对应 tab,有就配置解决,别改代码口径。
  - **#238 财务审批完单据「不见了」**:末环节是财务审批的单据审完即 `status="pending_payment"` 且 `current_step_order=None`,
    于是 `pending_me`(要求 status=pending + 有匹配角色的待办环节)查不到、任何环节队列也查不到——**单据从所有待办里蒸发**,
    财务想点「标记已付款」都没入口,只有 admin/manager 能去「全部申请」里翻。新增 `scope=pending_pay`(门槛 finance/admin/manager)
    + OA 页新增「待付款」tab(v-if=canPay)。`mark-paid` 端点本来就有,只是没人找得到它。
  - **#236 销售提成申请改为按月+多项目+总计**:原 #217 的单项目平铺表单作废。现在:部门**固定销售部不可选**(watch 到提成类型自动锁),
    加**提成月份**(默认本月),项目明细改为**可增删的多行表格**——序号|项目编号|成单日期|客户名称|回款金额|回款类型|提成点|提成,
    底部**回款合计 + 总计提成**;申请金额=提成总计。明细存 `OaRequest.detail.commission_items`(JSON 列,**无需迁移**),
    照抄 #149 报销明细行的现成模式。详情页用带 `show-summary` 的表格渲染明细+总计,平铺渲染里排除 `commission_items` 数组。

- **🆕 电工部「外协订单」tab(2026-07-15，本次)**:用户口径=「外协订单跟订单跟踪一样,只是人员只有 zhourui,需要监控他的订单状态;
  zhourui 本人看不到这个 tab」。已过 vue-tsc + vite build + 后端实跑(`scratchpad/verify_outsource.py`,7 断言全绿):
  - **不是新单据类型**:复用现有 `DeptOrder`,就是「任务跟踪」按人筛出来的只读监控视图(项目/负责人/状态/接线开始/预计完成/接线完成/效率/资料)。
    不给换人/作废/删除——那些留在「任务跟踪」里,避免两处入口。
  - **外协人员按账号配置,不硬编码**:`dept_config.OUTSOURCE_WORKERS: dict[str, list[str]]`(dept -> [username]),现为 `{"electric": ["zhourui"]}`。
    **增减外协人员改这一处即可**,不要把账号名写进路由/前端。配置里写了不存在的账号会静默跳过(已测)。
  - **解析走 username 而非姓名**:`GET /orders/options?dept=X` 新增 `outsource_workers: [{id,name}]`(按 username 查库、返回显示名);
    前端按 `worker_id` 筛(不按姓名,避免重名/改名失效)。没新增后端接口。
  - **✅ 2026-07-16 线上验证**:zhourui 本人登录确认看不到该 tab;负责人/管理层可见。(本地只能验逻辑,真实账号的角色配置只有线上能确认。)
  - **可见性**:`showOutsourceTab = (isLead || isMgr) && !isOutsourceSelf && 有配置外协人员`。`isOutsourceSelf` 是双保险——
    zhourui 是普通电工,本来就不满足 isLead/isMgr;万一日后给他加了负责人角色也仍看不到。
    注:tab 只是把负责人/管理层**本就能看到的**本部门订单按人筛一遍,不涉及新的数据暴露,故未加后端闸门。
  - **本地无法跑后端的坑(重要)**:生产是 **Python 3.12**(`backend/Dockerfile.prod`),而这台 mac 只有 3.9 且无 brew/uv/pyenv,
    `dept_config.py`/`sheet_templates.py` 的 `str | None`(3.10+)在 3.9 直接 import 会 TypeError。
    解法见 `scratchpad/verify_outsource.py` 顶部:给真实源码前置 `from __future__ import annotations` 后 exec 进 sys.modules,
    **不改源文件**即可在 3.9 上实跑真实逻辑。以后本地验后端可照抄这个垫片。

- **🆕 问题反馈「死信」断链修复 + 过期文案(2026-07-15，本次)**:用户问「生产部提交的反馈,设计部哪里能看到」时暴露的三处断链;
  已过 vue-tsc + 后端实跑测试(`scratchpad/verify_fb.py`,内存 SQLite 建真表跑 list_feedbacks/assign_feedback,6 断言+指派闭环+越权拒绝全绿):
  - **背景**:反馈入口在「设计部」工作台底部的 `FeedbackPanel`(`DeptWorkbenchView` 里 `dept==='produce'||dept==='design'` 时渲染)。
    #227/#228 取消生产主管审批时移除了 isPm 分支,面板 `v-if` 只剩 `isAssembler || isDesigner`,**design_lead/管理层从此看不到面板**。
  - **死信无处指派(#29 断链)**:`create_feedback` 按项目设计任务(`DeptOrder.dept='design'`)的 `worker_id` 自动指派;若项目没派设计任务/负责人不在岗
    → `designer_uid=None`(死信),只推一条企微给 design_lead,但他**界面上没有入口**——后端 `POST /feedbacks/{id}/assign`(#29 就是为此加的)
    **前端 `api/feedback.ts` 压根没接**。本次接上:`feedbackApi.assign(id, worker_id)`(worker_id 走 query),面板加「指派」按钮+弹窗,
    设计师名单复用 `ordersApi.options('design').workers`(= `DEPTS.design.worker_role='designer'`,多角色感知),不新增后端接口。
  - **管理层看不到**:`list_feedbacks(mine=True)` 的 conds 没有 admin/manager 分支 → 管理层恒返回空(前端 `auth.hasRole` 也无管理层兜底,
    与后端 `require_roles` 始终放行 admin/manager 的口径不一致)。本次后端加 `codes & {"admin","manager"}` → 全量 `pending_design`;
    前端 `canAssign = isDesignLead || isMgr`,面板 `v-if` 加 `canAssign`。
  - **口径(沿用 #29 注释的原设计,未擅自扩大)**:设计师=只看指派给自己的(死信也可直接接收,与 `design_accept` 的
    `if fb.designer_uid and fb.designer_uid != current.id` 校验一致);**design_lead=只看死信(`designer_uid IS NULL`)可指派**,
    看不到已指派给具体设计师的;管理层=全量。若日后要「设计负责人看本部门全部反馈」,改 `list_feedbacks` 的 design_lead 分支即可。
  - `FeedbackRow` 新增 `designer_uid`(前端靠它区分「指派给我」/「死信」;面板对 canAssign 角色加「设计师」列,空=红标「未指派」)。
  - **过期文案**:#227/#228 早已取消审批,但提交成功仍弹「已提交,等待生产主管审批」、输入框提示「经生产主管审批回馈设计师」——
    改为「已提交,已推送设计师接收」/「提交后直接推送设计师接收」。
  - **已知遗留**:面板在 `produce` 和 `design` 两个工作台都渲染,管理层在生产部 tab 也会看到同一张卡(内容一致,无害,未改)。

- **🆕 用户反馈第 7 批 #227~#235(2026-07-15，本次)**:8 条,#229(人员离职处理)用户暂缓待澄清,其余全做;已过 vue-tsc + vite build + ASGI/SQLite 测试(`scratchpad/test_feedback_0715.py`):
  - **#227/#228 装配问题反馈直达设计,取消生产主管审批**:`feedback_router.create_feedback` 建单即 `pending_design` 并推该项目设计师
    (无在岗设计师降级推 design_lead);前端 `FeedbackPanel` 去掉 pm_lead/manager 的「问题反馈审批」面板(移除 isPm 分支)。
    存量迁移 `route_pending_pm_feedbacks_to_design`(data_migration run_all 注册):把卡在 pending_pm 的反馈转 pending_design。
    pm_approve/pm_reject 端点保留但不再产生新 pending_pm(可后续清理)。
  - **#230 封板组去掉「激光件清单(编辑)」列**(封板组工人反馈):`DeptWorkbenchView` 删该列 + 删 laser 弹窗/viewLaserSheet;
    **回退 #224 的可编辑改动**:`canEditSheet` 去 isSealing;`datasheets_router` 生产组可编辑表还原为仅「钣金装配」,
    3 个 produce-edit 端点 require_roles 去掉 sealing。封板组保留 钣金装配表(查看)/CAD激光图纸/机架横梁图 分列。
  - **#232 采购申请可查看/打印成正式申请单**:`purchase_mgmt_router` 新增 `_render_preq_pdf`(reportlab) + `GET /purchase-requests/{id}/pdf`
    (置于 `_PREQ_VIEW`/`_preq_warehouse_only` 定义之后,否则模块加载 NameError);仓库/设计师(非采购)只能看自己提的。
    前端 `PurchaseMgmtView` 采购申请行加「查看/打印申请单」→ 新标签开 PDF。
  - **#234/#235 仓库采购收货:采购单号可点开查看/打印采购单**:`download_order_pdf` 的 require_roles 放开 `warehouse/warehouse_lead`;
    前端 `WarehouseView` 采购收货待收货列的采购单号改为可点击 → `viewPoPdf` 新标签打开采购单 PDF(可看+打印)。
  - **#233 销售尾款到账批注 + 尾款日期显斜杠**:复用现有「收款批注(预付/发货前付,支持插入时间戳)」机制扩到尾款:
    `SalesLedger.balance_note`(新列,_NEW_COLUMNS 迁移);`payment-note` 端点 field 加 `balance`;序列化/schema 带 `balance_note`。
    前端 `SalesView` 尾款列加批注图标;**尾款有到账批注 → 尾款日期显示「/」**(已到账不再是待收日期)。
  - **#229 人员离职处理**:用户「先不做,去问清楚」,本批未动(花名册已可改状态在职/试用/离职)。

- **🆕 仓库出库登记「项目物料反显」(2026-07-15，本次)**:出库登记选物料时,若该物料是**按项目入库**(入库流水
  `WhTxn` 有**唯一**关联 project),自动反显该项目数据、简化录入。已过 vue-tsc + vite build + ASGI/SQLite 测试(`scratchpad/test_wh_projmat.py`):
  - 后端 `warehouse_router`:新增 `_material_projects`——取每个物料入库流水(非冲红)的 project_id,**仅当全部入库指向同一项目**才反显
    (多项目/无项目入库不反显,走现有手填);`WhMaterialOut` 加 `project_id/project_code`;`list_materials`/`_mat_out` 带出。
  - 前端 `WarehouseView` 出库弹窗:物料下拉 label 末尾显示【项目号】;选中「项目物料」时自动带出 **领用项目=该项目、
    数量=现存、单价→金额**(均可改),并**隐藏「领用方」**(领用方=项目);物料下方给蓝色提示。非项目物料完全不变,按原逻辑手填。
  - 口径:物料↔项目的关联来自**入库流水**(采购收货入库 `purchase_mgmt_router` 已按 `item.project_code` 写 `WhTxn.project_id`),
    不是物料主数据字段;所以同名物料若跨项目入库就不反显(避免张冠李戴)。

- **🆕 用户反馈第 6 批 #224/#225(2026-07-14，本次)**:两条,已过 vue-tsc + vite build + ASGI/SQLite 测试(见 `scratchpad/test_feedback_0714.py`):
  - **#225 考勤/工资也按工号排序**:上一批 #214 只改了花名册(`list_employees`);考勤/工资走 `hr_router._active_emps`,其排序由 `(department_id, emp_no)`
    改为 `(emp_no is null, emp_no, id)`——纯按工号升序(不再按部门分组),无工号排最后,与花名册一致。
  - **#224 封板组 tab 照抄钣金组模板 + 分列推送资料**(经与赵仁辉确认列/布局照抄钣金组):
    - **激光件清单可编辑**(同钣金组「编辑装配表」):`datasheets_router` 把生产组可编辑表白名单 `SHEETMETAL_DS_NAME` 扩为
      `PRODUCE_EDITABLE_DS={"钣金装配","激光件清单"}`,三个 produce-edit 端点 `require_roles` 加 `"sealing"`;
      `_produce_edit_check` 仍按 `ProduceGroupTask.worker_id` 校验派单(封板工只能编辑自己被派项目)。
      前端 `canEditSheet` 加 `isSealing`,封板组激光件清单弹窗 `canEdit` 由 false 改 `canEditSheet`。
    - **设计推送资料分列**(用户补充:钣金装配表 / CAD激光图纸带「待推送-已推送N」/ 新增机架横梁图列):封板组 tab 列改为
      激光件清单(编辑) | 钣金装配表(查看) | CAD激光图纸(StatusPill 待推送/已推送 N + 下载) | 机架/横梁图(同,数据=`sealing_files` 封板文件) | 预计完成 | 封板完成。
    - **注**:CAD激光图纸=`laser_files`(order_start_output kind=sheetpkg),机架/横梁图=`sealing_files`(kind=sealing_pkg),均为设计推送,后端上一会话(9991ccc)已populate。

- **🆕 用户反馈第 4 批 #214~#220(2026-07-13，本次)**:七条里 #215/#216 拉代码时已由上一会话(#199 删除/#200 流程级抄送)实现,本次仅验证;
  其余五条本次修复,已过 vue-tsc + vite build + ASGI/SQLite 端到端测试(见 `scratchpad/test_feedback_0713b.py`):
  - **#214 人事花名册按工号排序**:`hr_router.list_employees` 排序由 `(status,dept,id)` 改为 `(emp_no is null, emp_no, id)`——按工号升序,无工号排最后。
  - **#217 OA 新增「销售提成申请」**(单条表单):`data_migration.backfill_oa_doc_types` DEFAULTS 加 `("sales_commission","business","销售提成申请")`(重启幂等灌入)。
    前端 `OaView` 专用表单字段:项目编号/客户名称/设备名称/回款类型/回款金额/提成点(%);**提成金额=回款金额×提成点% 自动算**(只读),存 `detail` JSON,
    `amount`=提成金额。走通用 OA 审批(管理员在【审批流程配置】按部门+单据类型配审批链);详情抽屉复用通用 detail 渲染(加了中文字段标签)。
  - **#218 OA「全部申请」加筛选**:`OaView` 全部申请 tab 加「单据类型 / 申请人」下拉(客户端过滤已加载列表 `allRowsView`),换 tab 自动清筛选。
  - **#219/#220 全流程图显示生产部分组**:生产部走分组派单,父生产单 `worker_id` 恒空 → 老全流程图里生产部节点显示"负责人—/效率—"(#219)、且看不到装配组(#220)。
    `collab_router.project_workflow` 给 produce 部门补 `ProduceGroupTask` 分组(钣金/装配/封板:负责人+完成);`WfDept.groups` 新字段;
    `WorkflowGraph.vue` 生产部节点改为展开三组(负责人+进行中/已完成),不再显示空的负责人/效率;顺带把下游误标的「钣金组(图纸包)」节点改名「钣金图纸包」(它其实是设计 CAD 激光图纸计数,非生产钣金组)。
    **注**:封板组在此图里"派了才显示"(可选组口径,与反馈#209 一致)。
  - **#215 流程级抄送 / #216 管理层删除**:均为上一会话已实现(`OaView` flowCcRoles/saveFlowCc = #200;deleteRequest = #199),UI 在位,**部署即生效**;
    #216 用户确认"删除够了,不加编辑"(已提交审批单不开放改金额/内容)。

- **🆕 用户反馈第 3 批 #206~#212(2026-07-13，本次)**:七条里 #206/#212 拉代码时已由上一会话修好(本次仅验证),
  #207 用户确认"保持现状即可"不改;其余四条本次修复,已过 vue-tsc + build + ASGI/SQLite 端到端测试:
  - **#208 人事部隐藏项目目录/项目详单**:`menus.py` `ROLE_MENUS["hr"]` 由 `["catalog","list","hr"]` 收窄为 `["hr"]`
    (纯 hr 用户不再看到项目目录/详单;多角色仍取并集)。配套 `router/index.ts` 给 `/overview` 加闸门:
    无 `catalog` 菜单者(纯人事/售后)登录后不落在概览页,跳其首个可见菜单。**此前"老8角色不回收"红线因用户明确反馈而放开(仅 hr)**。
  - **#210 装配问题反馈"对应不上"根修**:装配工在手项目走**分组派单** `ProduceGroupTask`(group=assembly),
    `DeptOrder.worker_id` 对生产单恒为空。旧代码 `/api/feedbacks/projects` 下拉列**全部**进行中项目,
    而 `create_feedback` 却校验 `DeptOrder.worker_id==本人`(永不命中)→ 一提交就被"只能对自己在手的项目提交反馈"拦。
    改:下拉与校验**同源**——都查 `ProduceGroupTask` 装配组派单。`feedback_router` 两处。
  - **#211 电工发货准备去掉发货清单**:`DeptWorkbenchView` 发货准备(res)弹窗的"发货清单(推仓库备货)"section
    加 `dept !== 'electric'`,电工发货准备只传电路图(发货清单由设计部上传)。
  - **🆕🆕 #209 完整新增「封板组」(第三生产小组)+ 推送激光图**(改动较大,多文件):
    - 新角色 `sealing`(生产部-封板组),`seed.py` ROLES 加(重启自动建,无需迁移);`deps.py` 受限岗位集/`ProduceGroupTask`
      在手项目集都加 sealing;`menus.py` `["catalog","produce"]`(同装配/钣金,无详单)。
    - `produce_router`:`GROUP_NAME/GROUP_ROLE` 加 sealing;**父单完成口径**改用 `REQUIRED_GROUPS=(钣金,装配)`——
      钣金+装配为必派(都完成才算生产完成),**封板组为可选组**(派了则也须完成,没派不影响,向后兼容存量单)。
      `dispatch`/`DispatchIn`/`dispatch-options` 加 `sealing_worker_id`/`sealing`;新端点 `GET /produce/sealing-projects`。
      **推送激光图**:封板组 tab 每行给出**激光件清单**(数据表,只读查看)+ **CAD激光图纸**(设计产出
      `order_start_output kind=sheetpkg`,可下载)——`GroupProjectRow` 加 `laser_datasheet_id`/`laser_files`,
      新助手 `_laser_files_by_pid`。`reports_router.GROUP_NAMES` 与 `orders_router._PG_NAME` 加 sealing(报表/任务跟踪含封板)。
    - 前端 `DeptWorkbenchView`:`isSealing`/`sealingRows`/加载/默认tab;**封板组 tab**(派给/换人/激光图/预计完成/标记完成);
      派发弹窗加封板组选择器(激光图会推给该组);组换人支持 sealing;`orders.ts` 加类型/`sealingProjects`/`dispatch` 第4参。
    - **部署后**管理员需在「用户」里给封板组工人**分配 `sealing` 角色**才会出现在派发下拉。**封板组默认可选**,
      如需强制每单必派封板,把 `produce_router.REQUIRED_GROUPS` 加 `"sealing"` 即可。
  - **验证脚本**:`scratchpad/test_feedback_0713.py`(#208 菜单/#209 派发+封板+激光+完成口径+向后兼容/#210 下拉与校验/#212 电工完成),全 PASS。
    注:仓库里 `tests/test_m01_menus/m04_orders/m13_feedback/m14_reports` 等**独立脚本**已与上一会话改动脱节(stash 基线同样失败),非本次回归。

- **🆕 6 条需求 + 采购隔离根修(2026-07-08，待推 main)**:
  - **采购明细行级隔离根修**:`_buyer_restricted` 原把 `finance` 放进"看全部"排除名单,导致 buyer+finance
    兼职账号(王芹 buyer/finance/logistics)被判为看全部→看到别人下的单。改为 = 有 buyer 家族角色
    (buyer/buyer_standard/buyer_outsource) 且非(buyer_lead/admin/manager);**兼任财务不解除隔离**;纯 finance 仍看全部。
    13 种角色组合脚本验证通过。(commit 3f7c44a 已单独推)
  - #1 合并收货**去掉"合并总价按数量分摊"**填价方式,只逐行填单价/收货金额。
  - #2 仓库采购申请**加"指定采购员"**(新端点 `GET /buyers`;`PurchaseRequest.buyer_id`),推送定向到该采购员;
    采购管理采购申请 tab 显示指定采购员。
  - #3 收货同步清单列:**现有(到货日期/进度/仓库签字)已够,不改**。
  - #4 合并父行**「整单维护」**(新端点 `POST /items/set-group-summary`):开票金额/已付款按整单总额**不分摊**记在
    首行(其余置0,汇总合计正确),对账状态套所有子行;父行操作列「整单维护」弹窗。
  - #5 **仓库角色在库存总览看不到单价/总价**(KPI库存总价+单价+总价列 gated by isManager)。
  - #6 采购员**只看自己新增的供应商**(created_by==本人),别人建/无归属(NULL)老供应商对采购员隐藏、仅管理层可见;
    供应商账目同理(只列本人新增供应商)。`list_suppliers`+`supplier_statements` 收紧。
- **🆕 #7 按账号分配二级菜单(tab)权限已完成(2026-07-08，待推 main)**:
  - 数据:`User.hidden_tabs`(JSON,存被隐藏的 tab key 如 "finance:pay_payment") + `_NEW_COLUMNS` 补列。
  - 注册表:`menus.TAB_REGISTRY`(采购管理/财务部/仓库三个多 tab 页的可控 tab)→ `GET /admin/tab-registry`。
  - 设置:`PUT /admin/users/{id}` 传 `hidden_tabs`;`/auth/me`+login 的 UserOut 下发 hidden_tabs。
  - 前端:auth store `tabHidden/tabVisible(menuKey,tabName)`(管理层不受限);三视图每个 `<el-tab-pane>` 加
    `v-if="tv('name')"`;`UsersView` 每个用户「二级菜单权限」按钮→弹窗勾选(勾=可见)。
  - **扩展**:新增可控 tab 只需改 `TAB_REGISTRY` + 对应 pane 的 `v-if="tv(...)"`。tab_name 必须与前端 pane name 一致。

- **🆕 图表(LineChart)财务口径重做 + #170/#171(2026-07-08,待推 main)**:
  - **LineChart.vue 重做**(用户嫌"图不好看"):①原 `preserveAspectRatio="none"` 把固定 820 viewBox 拉伸到容器宽,
    圆点变椭圆、描边不匀——改成 **ResizeObserver 按容器实宽 1:1 渲染**,不再畸变;②默认 `smooth` 由 true 改 **false
    (直线段)**,财务图不该用平滑曲线过冲显示没发生过的金额;③配色换成 **dataviz 校验器验证过**的分类色
    (收货蓝`#2a78d6`/已开票琥珀`#eda100`/已付绿`#008300`,light+dark 六项校验全过);④加基线、tabular-nums、克制网格。
    用 headless Chrome 实截图确认过效果。**改的是共享组件,汇总报表的图也一并变直线+新配色。**
  - #170 维护开票号限同一供应商(前端按钮 `:disabled=!selSameSupplier`+拦截,后端 `set_invoice_no` 兜底);
    #171 财务采购单改预览(新标签内联打开 PDF)。

- **🆕 用户反馈第 2 批 #159-#169 已处理(2026-07-08,待推 main)**:B 财务付款/请款采购单号可点下载PDF;
  C 钱票对上显示「已清」(前端派生 `isCleared`);D 供应商账目含本人自建供应商;E 付款收款账户可复制;
  F 采购请款记录状态改显示条;A 不锈钢图纸名称(描述符 hasDrawing,下单折进 spec);G 供应商明细抽屉加月度趋势图;
  **H #167 仓库采购申请**(新功能):`PurchaseRequest`+`PurchaseRequestLine` 两张新表(create_all 自动建,无需迁移);
  仓库页「采购申请」tab 提申请→采购管理「采购申请」tab 处理/驳回,双向消息通知。端点在 `purchase_mgmt_router`
  (`POST/GET/PUT handle|reject/DELETE /purchase-requests`)。**待下单已删(见下)**。

- **⚠️ 待下单工作区已按用户要求删除(2026-07-08)**:用户认为"跟采购明细重复、没用",且跨项目聚合有性能坑,
  整块移除(前端 `pending` tab + 工作区脚本;后端 `/purchasable-pending`、`/purchasable-pending-all` 两端点)。
  **保留**:清单类型描述符(`SHEET_META`)、采购明细按类型分 tab(④)、不锈钢图纸名称(A)、`_purchasable_rows` helper
  (`/purchasable` 单项目从清单下单仍用)。下面这条的 ②③ 已作废,①④A 仍在。
- **🆕 按清单下单重构:清单类型异构 + ~~待下单工作区~~(2026-07-08,待推 main)**:
  - **痛点**:5 类清单(标准件/电工/不锈钢/外协/激光)列名各异(外协的"规格"其实是图纸名、不锈钢有"材质"、
    外协/激光无数量/品牌),硬塞一张统一表别扭。
  - **①清单类型描述符**(前后端各一份):`SHEET_META`(前端)/`_PURCHASABLE_SHEETS`(后端归一化)——每类的
    名称/规格显示名、有无品牌/数量/材质。`PurchasableRow` 加 `material`(材质单独成列,不再塞备注)+
    `sheet_key/project_*`(跨项目聚合用)。**加第 6 类清单只改配置**。
  - **②待下单端点**:`GET /purchasable-pending-all?project_id=X` 返回该项目 `{类型: 未下单行[]}`,受 R4 权限约束。
    **⚠ 性能坑(踩过两次,最终按项目加载才根治)**:①最初 `/purchasable-pending?sheet=` 单类型端点前端并发发 5 个,
    每个全表算库存+逐张清单 N+1 → 转圈;②改聚合端点(库存只算一次+批量查询)仍**把全部项目的所有清单记录全捞**,
    数据一多照样卡死。③**最终改为按项目加载**:`project_id` 必传,只处理该项目的清单,查询量与项目总数无关。
    前端 `待下单` 工作区顶部**先选项目**再看该项目各类型未下单(不再跨项目聚合)。`_purchasable_rows` 支持传入
    预取的 name2id/by_rec/records 避免 N+1。**教训:跨项目全量聚合在数据量大时不可行,该做成按项目/懒加载。**
  - **③待下单常驻工作区**(采购管理新增顶层 `待下单` tab):二级 tab 按清单类型(仅 R4 权限内)+ 角标(未下单数);
    描述符驱动列;两种下单模式——**普通下单**(按 供应商×项目 拆多张单,`submitPendingOrder`)/**打包成套**
    (同一项目一组零件打成一套,`submitPendingKit`,复用 `/orders/kit-from-list`)。均回写清单。
  - **④采购明细列表按类型分 tab**:`/items`+`/items/summary` 加 `sheet_type` 参数
    (`_apply_sheet_type_filter`:按 source_sheet_id 的来源表名过滤;`loose`=散单无来源);前端顶部
    二级 tab(全部/各类型/散单),名称/规格列标签随类型走描述符。
  - 验证:前端 `vue-tsc` 过;后端语法过;端点镜像已验证的 `purchasable`/`create_order_from_list`。
    **本机无 PG/后端运行环境未起服务实调**,上线后建议实测(待下单 tab 各类型下单 + 明细分类 tab 过滤)。
- **🆕 成套采购(按套下单)已完成(2026-07-08,待推 main)**:
  - **核心**:「一套」= **一条被标记 `is_kit` 的 `PurchaseItem`**(不新建表):`item_name`=套名、`qty`=套数、
    `unit_price`=套单价、`received_amount`=套总价、`kit_parts`(JSON)=套内零件清单[{name,spec,qty}](描述性)。
    → 收货/入库/开票/请款/付款/权限/对账**全部零改动复用单条明细流程**,天然"维护一个总"。
  - **四个业务决策**(用户拍板):套=**从清单勾选打包**;一套同一供应商;**整套为一个库存单位**(入库物料名=套名、
    qty=套数,套内零件不单独算库存/领料);**一套一个总价**;整单一次收全;套名手填。
  - **按套下单 = 从清单下单的扩展版(成套模式)**:复用「从清单下单」弹窗(`listOrderMode='kit'`)——选项目/清单→
    勾选一组零件→填套名/套数/套总价/供应商→打包成一套。勾中的零件成为 `kit_parts` **并回写清单为「已下单」**
    (与从清单下单同一套 `_writeback_sheet_row` 逻辑)。前端隐藏成套模式下的逐行供应商/单价/付款列与批量条,
    改为套级面板;数量列改「每套数量」。
  - 改动:models 加 `is_kit`/`kit_parts`(+`_NEW_COLUMNS` 幂等补列);新端点 `POST /orders/kit-from-list`
    (建一条成套明细+回写清单,镜像 `create_order_from_list`);`_item_out` 带 kit 字段;采购单 PDF 明细表下附
    「套内零件清单」表。前端采购明细页「按套下单」按钮走成套模式弹窗;成套行打「套」标签+hover 弹零件清单、数量显示"N 套"。
  - 验证:前端 `vue-tsc` 过;后端语法过;**PDF 用 reportlab 实测**成套+普通单均正常渲染;端点镜像
    已验证的 `create_order_from_list`。**本机无 PG/后端运行环境,端点未起服务实调**,上线后建议实下一单验证。
- **🆕 财务/采购四项已完成(2026-07-08,待推 main)**:
  1. **财务请款/付款拆两个 tab**(`FinanceView.vue`:`请款审批` + `付款`,纯前端按状态分流,详见第五节)。
  2. **仓库采购收货列表按 `po_no` 合并**(`WarehouseView.vue` 新增 `groupedRecv` 树表 + 父行「合并收货」,
     复用 `/items/receive-batch`;存量数据靠 po_no 自然分组,无需迁移)。
  3. **开票号/开票日期上主汇总父行**(前端父行聚合展示 + `invoice_date` 补进 `_NEW_COLUMNS`)。
  4. **请款记录按下单采购员隔离**(`GET /payment-requests` 及 overview 待审计数由 `requester_id` 改为
     经 `PaymentRequestItem→PurchaseItem.buyer_id` 反查,详见第五节)。
  - 验证:前端 `vue-tsc --noEmit` 过;后端改动为标准 `IN(SELECT…JOIN…)` 子查询(无 GROUP BY,PG 兼容),
    已用独立 SQLite 脚本复现子查询语义(含代发起/混单场景)通过。**本机无 Postgres/无后端运行环境
    (Python 3.9 缺依赖),上线前建议在真 PG 跑一遍 `GET /payment-requests` 冒烟。**
- **🆕 用户反馈 7 条 + 采购员越权审计已处理(2026-07-08,待推 main)**:
  - 反馈:#152 采购明细供应商筛选下拉只列本人供应商(`/suppliers?owned_only=true`,排除遗留共享);
    #153 供应商账目**改回原来的平铺列表**(去掉需求十一的分类卡片下钻);#154 维护开票号弹窗不再残留
    (仅勾选同一开票号时才预填);#155 采购明细金额列宽排均匀;#156 采购单订单编号加"非项目"预设
    (固定资产/耗材等,`allow-create` 本就可手输);#157 物料需求 tab **去掉项目下拉**,直接列出有清单的项目 +
    待出库/已出库条数(新增只读端点 `GET /wh/demand-overview`,点「查看」下钻到原逐行需求)。
    #158 售后登记「以往项目(系统里没有的)可直接填名称」:`aftersales.project_id` 改可空 + 新增 `project_name`,
    登记时 project_id 与 project_name 二选一;前端 el-select 加 `allow-create` 手输名称。**⚠ 需 PG 迁移**:
    `data_migration._DROP_NOTNULL` 启动时对 PG 执行 `ALTER TABLE aftersales ALTER COLUMN project_id DROP NOT NULL`
    (幂等,仅 PG;SQLite create_all 已按模型建可空)。上线 rebuild 会自动跑。本机无 PG,未在真库验证,建议上线后确认。
  - **🐞 生产 bug 修正:采购员"建的采购明细单自己看不到供应商"**——`#152` 初版把"自己填的供应商"错按
    `Supplier.created_by==本人` 过滤,漏掉本人用了**遗留共享(NULL)或别人建**的供应商下的单;改为按
    `buyer_id` 反查(供应商出现在本人采购明细里即可见)。`/suppliers` 默认列表也放宽为
    「本人建 + 遗留共享 + 本人下过单」,保证用过的供应商始终可见/可选。**多角色已确认无问题**:
    `has_role()` 取全部角色并集(`role_codes`),`_buyer_restricted` 对 `buyer/sales` 判受限、`buyer/manager` 判放行。
  - **🔴 越权审计(用户要求"权限没控好,找 bug"),已修 6 处**(均在 `_buyer_restricted` 受限采购员下):
    1. **CRITICAL** `/statements/{sid}/export` 对账单导出**零过滤**——任意 sid 可导出含他人单据的整表 Excel。
       已加 `buyer_id` 过滤 + 无本人明细则 403。
    2. **HIGH** `/statements` 列表:供应商行筛对了,但每行金额与合计按**全量**聚合,共享供应商上泄漏他人
       收货/开票/已付额(列表合计 > 下钻明细,自相矛盾)。已改为受限时只汇总本人明细。
    3. **MEDIUM** `/items/batch-invoice` 写越权(可改他人开票状态)——加 `buyer_id` 过滤。
    4. **MEDIUM** `/suppliers/{sid}/opening-balance` GET 无归属校验——受限时校验供应商 created_by∈(本人,NULL)。
    5. **LOW** `/items/{iid}/receipts` GET 无归属校验——受限时校验明细 buyer_id==本人。
    6. **LOW** `POST /payment-requests` 未校验引用明细归属——受限时校验所有 item 均本人下单,否则 403。
    - **报表类经审计全部正确**(`_report_items` 按 buyer_id 过滤,overview/monthly-trend/by-buyer/by-project/
      top-suppliers/supplier-trend 均走它或内联同款过滤)。附:overview 期初欠款口径用 `Supplier.created_by`
      与"经手供应商"略有出入,方向是少算不泄漏,暂未动。
- **已完成**:增量清单 M01-M17 全部完成;近期批次(清单类型权限默认、隐藏单条明细、
  清空脚本、OA 抄送、OA 流程总览、请款删除、采购单 PDF 等)已推 main。
- **🆕 需求.docx 十六项已完成并推 main(2026-07-07)**:
  1. 登记安装(售后/安装共用 `aftersales.kind`) 2. 物料需求领用出库自动登记(采购收货入库本就有)
  3. 物料参考单价 `wh_materials.unit_price` + 库存总览总价 4. 合并零件收货只填总价(receive-batch 按数量分摊)
  5. 采购员"谁建谁看"(明细/供应商/报表行级隔离) 6. 库存/成本仅管理层可见
  7. 售后费用→"安装/售后费用" 8. 修复脏数据删除(FK 安全:先置 mat_file_id=NULL 再删附件)
  9. 合并父行展示送货单号/到货日期 + 可勾选合并行汇总请款 10. 设计部查库存弹窗调大(70%/70vh)
  11. 供应商账目卡片式(按分类大类汇总卡片→点击下钻供应商列表) 12. 供应商报表折线图(自建
  `LineChart.vue` SVG 组件,无三方依赖;月度采购趋势 + Top供应商趋势 `/reports/supplier-trend`)
  13. 采购明细开票号(统一维护 + 开票金额=收货金额) 14. 采购收货上传收货单图片(`receipt_doc` 附件)
  15. 仓库一键清空(确认词"清空仓库") 16. 请款审批↔付款职责分离 + 付款展示账户信息/采购单。
  - 新增列(models+`_NEW_COLUMNS`):`aftersales.kind`、`wh_materials.unit_price`、
    `suppliers.created_by`、`purchase_items.invoice_no`。**上线 rebuild 后自动 ADD COLUMN(幂等)**。
  - 验证:后端每个新端点用 httpx ASGI 实测过(含 SQLite FK 强制开启复现需求八外键场景);
    前端 `vue-tsc --noEmit` + `vite build` 均过。**沙箱无 Postgres**(Windows 机),SQL/FK 类改动
    已用 `PRAGMA foreign_keys=ON` 的 SQLite 覆盖,但上生产前若能过一遍真 PG 更稳。
- **🆕 用户反馈 #146/#147/#148 采购单号重号 bug 已修并推 main(2026-07-07)**:
  - 根因:`_next_po_no` 原用 `COUNT(DISTINCT po_no)+1` 算当日序号,**删单留下空档后会算出一个已存在的号**,
    新生成的采购单就并进老单里 → 同一 po_no 串多个供应商(账目/PDF 抬头全乱)、采购员下载看到别人的单。
  - 修复:①`_next_po_no` 改「当日现有最大后缀 +1」(永不重用已删号);②`get_order`/PO PDF 下载按
    `_buyer_restricted` 收紧(采购员只看/下自己建的单),PDF 兜底只渲染首供应商那部分。
  - **存量数据**:`data_migration.split_mixed_supplier_po`(启动幂等)把历史已被误并的
    `TH{日期}-NNN` 单,按 (供应商, 采购员, 下单日期) 拆开——最早一组留原号,其余各分「最大后缀+1」新号;
    历史导入的自定义单号不动。**上线 rebuild 后自动跑一次**,把已串台的老数据拆正。
  - 看了反馈里的内嵌截图(base64,导出 HTML 里),结论:
    - **#151 已修**:销售下单选/填「客户单位」后按同一家客户带出上次收货信息
      (新端点 `GET /sales/receiver-by-customer`,与原「同数字编号」并存)。
    - **#150 已修**:/sales 底部合计条是 `position:fixed;bottom:0`,会盖住分页/末行;给 `.pager`
      加 `padding-bottom:66px` 净空。**注**:没在用户真实屏幕上复现,若仍显示不全让用户说清是哪个元素被切。
    - **#145 经核实后端/前端本就正确**:`_group_rows` 自 2026-06-19(d3700cc)起对非主管按
      `ProduceGroupTask.worker_id==current.id` 过滤,前端组员分支也只拉 `assembly-projects`(已隔离),
      实测甲/乙装配工各自只见自己派工。**若生产上仍看到别人的,多半是那批项目确实都派给了他,或生产未部署到该版本**——deploy 即可。
    - **#146「采购单位不对」= 供方错**(截图证实:列表挂"无锡吉明"但 PDF 供方是"宁波雷宇"),
      正是重号串台,已随上条修复解决;需方抬头 `同辉智能装备有限公司` 是对的,不用改。
    - **#149 已修**:OA 报销申请新增「费用明细」多行表(费用类型/说明/金额/逐行发票),报销金额=各行合计
      (服务端按明细重算,不信前端传值);发票走 `oa_request` 附件(kind=`expense_invoice`),
      提交时把本人上传的未归属发票挂到该申请(`create_request` 里 `sa_update` biz_id=req.id,
      只认 `uploaded_by==本人 & biz_id IS NULL` 防冒挂);详情抽屉展示明细表 + 逐行发票下载。
      明细存 `OaRequest.detail.expense_items`(无需改表)。至此反馈 7 条全部处理完
      (#145 核实本就对、#146~148 重号修复、#149~151 已修/已做)。
- **待用户操作**:需求八的那条脏数据(2026-001 急急急 已驳回),现在管理层可直接在售后台账点删除;
  或仍可用 `ops/fix-deleted-aftersales.sh`。
- **待用户操作**:试运行数据清理——用户需自己在服务器跑
  `bash ops/clear-purchase-warehouse.sh --dry-run` 预览后正式执行(清采购明细+仓库,不动供应商/项目/字典)。
- **下一步开发**:按 `盈利改善功能规划.md` 推进。第一档(项目毛利+成本审计)与第二档(资金面板)
  **均已于 2026-07-11 交付**(见下方记录)。下一期 = 第三档改行为习惯:收款流水登记与核销(receipts 表,
  规划§三🥉第1条,也是 13 周现金排程流入侧精度的前提)→报价复盘→客户记分卡→比价看板/五类月报;
  随车字段(运费/OA项目/保外收费)可搭任何一期。
- 未做的长期项:金额 float→Numeric、收款流水表、人工成本分摊、变更补价字段。

- **🆕 生产事故修复：弹窗遮罩只盖半屏(2026-08-27,卡片化V2引入,当天修复)**:
  生产部报表等**没 append-to-body 的弹窗**,遮罩和本体被困在页面内容区里(内容多高遮罩多高,
  底部一条不遮,弹窗还会跟着侧栏偏移)。根因:`.main > *` 切页动画带 `transform` 且
  `fill-mode: both`——动画结束后浏览器保留的不是 `transform: none` 而是**插值出的单位矩阵
  `matrix(1,0,0,1,0,0)`**,不等于 none → 视图根永久成为 `position:fixed` 的包含块。
  **修复纪律:所有挂在容器上的入场动画(pagefade/fadein)只准动 opacity,绝不能动 transform**
  ——el-card / el-tab-pane / el-overlay 里都可能有弹窗,哪怕动画完会还原,期间也会短暂困住。
  dialogIn(弹窗自己的入场)保留 transform,它不包含 fixed 子元素。
  同批顺手修:①报表页「—%」(空值渲染成「—%」且被染红,改为空值只显示「—」不带%不染色,
  DeptWorkbench 4 处 + ReportView 7 处);②删掉 .kpi::after 角部微光(报表弹窗的大 KPI 卡上
  糊成一团像屏幕脏了)。
  ⚠️ 本次因 macOS 收回桌面文件夹权限,改动在 GitHub 克隆副本完成并推送,桌面工作副本
  恢复权限后需 `git pull` 同步。

- **🆕 UI 卡片化升级 V2 第二批(2026-08-27,纯视觉,零业务逻辑改动)**:
  - **界面骨架去 emoji**：tab 标签 40 处 + 弹窗标题/按钮/卡片头/区块标题 69 行（18 个文件）——
    emoji 放进 navy 分段卡和金渐变按钮里压不住风格。**只清「骨架位」，语义状态标记
    （✅已备齐/⚠预警/✓✗校验/⬜未导入）和说明文字里的 💡🆕 一律保留**；
    「📎 数字」类按钮不是删符号而是改成「凭证 N」，不然光剩数字看不懂。
  - **表格加载骨架屏（纯 CSS，零业务改动）**：v-loading 遮罩里藏掉转圈，`::after` 画灰条假行 +
    `::before` 扫高光（shimmer）。表头留在遮罩外照常显示。所有 el-table 自动生效。
  - **路由切换轻淡入**：MainLayout `.main > *` 挂载动画。**特意不用 `<transition>`**——
    多根节点的视图会告警且不生效，CSS 挂载动画多根也各自淡入。
  - 下拉/日期/自动补全浮层（`.el-popper.is-light`）圆角 12 + navy 阴影；选中项用可读深金。
  - KPI 卡右上角语义色径向微光（`::after`），不加图标也有层次，30 处 .kpi 零标记改动。
  - el-message-box / el-drawer 跟上弹窗的圆角与阴影。
- **🆕 UI 卡片化升级 V2(2026-08-26,全站视觉+交互,style.css token 层 + MainLayout 侧栏)**:
  设计稿(Claude Design 画布)定稿后落地。**品牌色(navy #16293f + 金 #c8a24f)、字体、
  信息密度全部没动**，只升级「面」与交互反馈：
  - 阴影五档全部从纯黑换成 navy 色 `rgba(22,41,63,·)`，新增 `--shadow-top`(卡面顶部 1px 白高光,
    3D 感来源)和 `--shadow-navy`(navy 主卡)；圆角 6/10/14 → 8/12/14；页面底改 160° 微渐变(fixed)。
  - **tab 下划线式 → 浮起分段卡**(全站生效)：`.el-tabs--top > .el-tabs__header` 白底圆角浮条,
    选中项 navy 底 + 金字自带投影。⚠️ EP 对首尾 item 有 nth-child 去 padding 的规则，
    pill 的 padding 必须 `!important`；超宽时 EP 滚动箭头照常工作(财务部 9 个 tab 实测)。
  - 表格整表包进圆角浮卡(`overflow:hidden`,列筛选面板是 teleport 的不受影响)，表头 navy 竖向渐变。
  - KPI 卡浮起 + hover 抬 2px；`.kpi.is-primary` 升级成 navy 主卡 + 金字(全站 8 处,全是数值+标签结构)。
  - 交互：主按钮金渐变+按压回弹、普通按钮 hover 抬起、输入框聚焦金圈光晕、行 hover 过渡、
    分页当前页 navy 圆片、弹窗入场「上浮+轻回弹」+遮罩淡入、侧栏菜单 hover 右移 2px/选中金雾圆片、
    `:focus-visible` 键盘焦点环、`prefers-reduced-motion` 全关动效。
  - **纪律：hover 位移只给小卡(.kpi)和按钮，整页大容器(el-card 包整个内容区)只加深阴影不位移。**
  - H5 独立构建不 import style.css，不受影响；vxe-table 只在 main.ts 注册、无视图使用，
    全站表格都是 el-table，覆盖完整。
  - 设计稿画布(可继续改)：https://claude.ai/code/artifact/2760c44f-f9b4-4feb-a147-18c874576d5c

- **🆕 反馈 #417 财务售后费用表可填备注(2026-08-26,王芹)**:
  「安装/售后费用」表新增「备注」列，财务可随时填/改/清空（安排完报销补记打款批次、核对情况）。
  - 后端 `POST /aftersales/{aid}/pay-note`（仅 finance，Form 传 note，存 `pay_note`，写审计）。
  - **唯独 `pay_status=invoice_fix`（发票退回中）不许改**——那时 `pay_note` 存的是给登记人看的
    退回原因（pay-reject 写入、resubmit-invoice 清空），盖掉登记人就不知道发票哪里要改；
    前端同口径隐藏「填/改」按钮，后端 400 兜底。
  - 前端 FinanceView 那张表从 11 列变 **12 列**：`summary-method` 是按数组下标对列的，
    合计仍落下标 7（费用）——再加/删列必须同步改那个数组（#361 踩过）。
  - 测试 `tests/test_fb417_aftersales_pay_note.py`（13 项，含退回原因保护/权限/清空/落库）。
- **🆕 反馈 #410/#414 两组重复物料已手工合并(2026-08-26,生产直改)**:
  耳锅把手 1286(20个,收货) 并进 1218(编码405050001,期初6套)——王利利确认共 26 个，
  即 1套=1个：单位统一「个」、规格取「27款大蟹耳【孔距85mm】」。
  ⚠️ 踩坑：`wh_materials` 有唯一约束 `uq_wh_material_name_spec`，合并要把 drop 的规格
  写到 keep 上时**必须先删 drop 再改 keep**，顺序反了会撞唯一键（autoflush 阶段就炸）。

- **🆕 反馈 #410 手工合并了一条重复物料(2026-08-26,生产直改,无代码变更)**:
  仓库里「不锈钢黑色把手」出现两条 —— `id=1276`(收货时自动建的,无编码,收货 100 个 ¥280)
  和 `id=1281`(王利利手工建档,编码 405050002、材质 304不锈钢、安全库存 20、期初 20)。
  两条同规格「黑色铝合金拉手孔距120mm」、同库位 1区-8-3-03、同单价 2.8,是同一样东西。
  - 处理:流水 `WhTxn id=2559` 改挂 1276→1281;**采购明细 `PurchaseItem id=2224` 的
    `item_name` 一并从「不锈钢黑色把手」改成「不锈钢把手」**;删除 1276。
  - **第二步不能省**:`purchase_mgmt_router` 收货那段按「名称+规格」找料(约 1445 行),
    采购明细里的名字不跟着改,下次这条料再收一次货就又新建一条重复的,等于白合。
    这也是 #410 重复料的根本产生机制 —— 收货时找不到同名同规格的料就自动建档。
  - 合并后 1281 现存 = 120(期初 20 + 收货 100),**库存金额 +¥56**:1281 原来 0 条流水、
    没有加权均价,那 20 个期初在库存金额里算 ¥0;并进来之后跟着吃 ¥2.8 的均价。
    **待仓库盘点确认**那 20 个期初和后收的 100 个是不是同一批;若是同一批,把 1281 的期初改回 0。
  - **没做的**:同一批里的「耳锅把手」`id=1218`(6 **套**,期初)与 `id=1286`(20 **个**,收货 ¥138.80)
    单位不同,一套几个没问清之前不能合,用户明确说先不做。
  - 一次性脚本不进仓库(在会话 scratchpad 里),做法是:搬 `WhTxn.material_id`(全库唯一指向
    `wh_materials.id` 的外键,models.py:415)→ 改采购明细名称 → 删旧料。**没做成通用功能**:
    生产上 10 组疑似重复里只有 4 组真该合,2 组混着项目料和通用料(合并会在库存金额与项目材料成本
    之间搬钱),1 组是录入错误(【锅体】那两条挂的是脚轮的规格和价格),自动识别只会合错。

## 七、给接手的 AI 助手的一句话

先读本文档 + `盈利改善功能规划.md`,再动手;改 SQL 先想 Postgres;新接口要实测;
推完代码提醒用户 rebuild;密钥永不落仓库。

- **🆕 表格版面紧凑化 + 折叠层级配色(2026-07-09，待推 main)**:
  - 全局 `.el-table`: 字号 14.5→13.5、行距 11→7px、单元格一律 nowrap+省略号(不再换行)；移除采购/仓库/财务各表的
    `wrap-cells`(原来是强制换行)——所有数据尽量一行显示、适应屏幕、更紧凑。
  - 折叠合并表(采购明细 groupedItems / 采购收货 groupedRecv)加 `:row-class-name="grpRowClass"`：父(汇总)行
    `grp-row`=主题浅蓝底+加粗；子(零件)行走 Element 自动 `--level-1`=浅灰底+左侧主题色竖条，层级/归属一眼可辨。
    全局样式在 style.css;仅这两张树表用 tree-props,不影响他处。

- **🆕 #1 隐藏「按套下单」+ #2 合并开票校验(2026-07-09，待推 main)**:
  - #1 采购明细工具栏「按套下单」按钮已注释隐藏（成套下单不再需要；已有 is_kit 成套明细仍正常显示/收货/开票）。
  - #2 合并开票（维护开票号）加校验：① 勾父(汇总)行→自动全选其下全部零件(`@select` onItemSelect 可视化联动)；
    多采购单(父行)同供应商可一起勾；② 未收货(收货金额≤0)的零件拦截，必须先全部收货；
    ③ 新增「合并开票金额(发票总额)」输入，须==Σ勾选零件收货金额(±0.01)才放行，不一致弹窗+禁用确认按钮；
    ④ 放行后每个零件 开票号/开票日期 统一、开票金额=各自收货金额、置已开票。前后端双校验
    (`set_invoice_no` 加 未收货拦截 + invoice_amount 比对；`SetInvoiceNoIn.invoice_amount`)。

- **🆕 用户反馈-2026-07-09-2 批次(2026-07-09，待推 main)**:
  - #180 采购单 PDF 需方公司名 → 「同辉智能装备（无锡）有限公司」(`_PO_COMPANY`)。
  - #179 站内消息按推送时间倒序(`created_at desc, id desc`)。
  - #177 防重复请款：同一采购明细若已有未完成(pending/approved)请款单，`create_payment_request` 拦截。
  - #176 采购明细合并(汇总父)行聚合显示 付款状态/对账状态/采购员(groupedItems 聚合;去掉列模板 `!_isGroup` 门控)。
  - #175 供应商账目下钻表也按采购单号合并折叠(`groupedDrawerItems` + tree-props + grpRowClass)。
  - 合并开票**开票日期未同步**修复：`set_invoice_no` 原 `if body.invoice_date` 选填留空则不写；改为**总是**写
    (空则默认今天 `_date.today()`)；前端弹窗开票日期改必填、默认今天。
  - 已由前批覆盖(待部署验证)：#181(王芹只看本人明细=3f7c44a)、#174/#173(折叠层级配色=c7cd9e0)、#184(版面换行=c7cd9e0)。
  - 已随「按套下单」隐藏而失效(#1)：#172/#182/#183(都是按套下单改进项)。
  - #178「请款不显示合并价格」实为**正常**：该合并单已付清(已付=收货)，请款可请=收货-已付=0，非缺陷。

- **🆕 数据清空脚本(2026-07-09)**：`ops/clear_purchase_supplier_data.sh` —— 清空「采购明细+供应商资料」及
  全部关联(请款单/审批、期初余额、收货凭证、采购收货自动入库流水[库存相应减少]、采购申请)。执行前自动
  pg_dump 全库备份、需输入 CLEAR 二次确认、单事务 all-or-nothing。用法:服务器项目目录 `bash ops/clear_purchase_supplier_data.sh`
  (容器名默认 pms2_postgres,可 PG_CONTAINER= 覆盖)。不影响物料主数据/手工出入库/项目清单/权限。

- **🆕 采购收货入/出库策略 + 订单编号字典 + 字典设置菜单(2026-07-10，待推 main)**:
  - `PurchaseItem.is_stock`(备货标记，存量默认TRUE=只入库)。收货自动过账 `_auto_stock_in`：始终入库；
    **非备货(is_stock=False)再追加一笔「采购领用」出库**(直发项目、数量=收货数量、净库存过账0)；备货只入库留库存。幂等不变。
  - **按清单下单/按套下单→is_stock=False**(入库+出库)；**新建采购单→「是否备货」开关**(默认是=只入库)，
    `PurchaseOrderCreate.is_stock`；前端新建单弹窗加 el-switch + 说明。
  - **订单编号字典**：`MaterialDict` 复用，新增 `dtype=order_no`(schema pattern 已放开)。新建采购单「默认订单编号」
    非项目值改为**只能从字典 order_no 选、不可手打**(去掉 allow-create)；项目编号仍从项目下拉选。
  - **字典设置一级菜单**(admin+manager)：`menus.ADMIN_MENU_DEFS` +`dict-admin`；前端 `views/admin/DictSettingsView.vue`
    (tab 管理 订单编号/物料类别/单位/材质/供应商分类，走 `/wh/material-dict` CRUD)；路由 `admin/dict`、MainLayout 管理组加链接、
    auth store deptMenus skip +dict-admin。
  - 前端 PO 抬头 PO_COMPANY 同步为「同辉智能装备（无锡）有限公司」。
  - **注意**：非项目订单编号需先在「字典设置-订单编号」里维护，新建单下拉才有可选值。

- **🆕 用户反馈-2026-07-09(17:00) 新增3条(2026-07-10，待推 main)**:
  - #186 单条采购收货弹窗：填「收货金额(总价)」→ 自动按数量算单价(onRecvAmountCalc，与原填单价算总价互补)。
  - #187 采购收货列表：单价列后新增「总价」列(=received_amount，父行显示合计)。
  - #185 已确认「保持现状」：finance 本就设计成可见 项目目录/项目详单/财务部/采购管理(采购管理内的
    供应商账目/请款记录是财务对账要用的)，finance_lead 同样不改，无需改动。
  - #172~#184 见前批(已修/随部署生效)。

- **🆕 全站表格/表单 UI 紧凑化(2026-07-10，待推 main)**（除 项目目录/项目详单/项目详情 三页外全部）:
  - **气泡**：19 个视图共 80 张 `<el-table>` 全部加 table 级 `show-overflow-tooltip`——单元格一行放不下时省略号+悬停气泡看全文(EP 2.14 支持表级属性)。
  - **操作列**：40 个 `label="操作"` 列全部补齐 `fixed="right"`(缺20处) + `:show-overflow-tooltip="false"`(缺37处)——小屏横向滚动时操作按钮恒可见、不被气泡遮挡。
  - **全局 style.css**：表单行距 18→14px、label-top 标签贴近控件；el-alert 提示条压矮(12.5px)；tabs 头距 12px；
    表格内小按钮 3px 10px/12.5px、link 按钮加字重。**新增 ≤1440px 窄幅笔记本媒体档**(表格 12.8px/行距5px、
    按钮/卡片/KPI/弹窗同步收紧)，与既有 ≤800px 高度档、94vw 弹窗上限配合适配小笔记本。
  - 验证：vue-tsc ✓、vite build ✓、1366px 密度 mock 截图 ✓。排除页未动(ProjectsView/OverviewView/ProjectDetailView 0 处改动)。

- **🆕 4件套(2026-07-11，物料编码/库位/设计师请购/支出总览)**:
  - **物料编码体系**：`MaterialCategory` 3级分类树(段码:大类1位/中类2位/细分2位)，编码=前缀+4位流水
    (1·01·01→101010001)。字典设置新增「物料编码分类」树管理 tab；物料主数据选到细分类保存自动发码
    (`_gen_material_code`,改分类自动重发码)；物料表/库存总览加编码列。删除分类有子类/被物料引用会拦截。
  - **库位填写点**：`PurchaseItem.stock_location`。⚠️**2026-07-12 已调整为「仓库收货时填」**(见下方 #204 条),
    原「采购下单三路必填」已废除;采购明细/收货列表仍显示同一字段(现由收货写入)。
  - **设计师请购单**：`_PREQ_WAREHOUSE`+designer/design_lead(与仓库采购申请同一后端流程/推送/行级隔离)；
    设计工作台新增「🛒请购单」tab(提单+指定采购员+我的列表)。
  - **财务支出总览**(=盈利改善规划第一档的第一块)：`GET /api/finance/expense-overview?year=`——
    采购付款(按付款日期)+安装/售后费用(已审批)+OA业务/报销(已审批,核定金额优先,采购大类不计防双算)
    按月汇总；财务部新增「💸支出总览」tab(年份/4KPI/月度表/未记日期提示)，已注册进 TAB_REGISTRY。
    项目毛利红黑榜+成本黑洞审计已随后于 2026-07-11 完整交付(见下一条)。

- **🆕 盈利改善第一档完整交付(2026-07-11，项目毛利红黑榜+成本黑洞审计)**:
  - **1a 项目毛利榜** `GET /api/reports/project-pnl`(finance/finance_lead+管理层)：逐项目
    毛利=合同额−材料领料(加权均价)−直发/外协采购(received_amount,按 project_code)−安装/售后(已审批)。
    **防双算**:收货已生成 wh_txn 的采购明细走仓库领料腿(领料×均价),`id.not_in(有purchase_item_id的txn)`
    的才计直发腿,两腿互斥。每行带成本完整度 flags(无销售台账/合同额为0/领料缺价N项)。
    财务部新增「📈项目毛利」tab：口径警示条(**不含人工/运费,材料边际贡献**)、亏损行淡红、
    按客户/客户类型/销售员/订单类型/机型/年份前端分组汇总、售后侵蚀Top5(机型/客户切换)。
  - **1b 成本审计** `GET /api/reports/cost-audit`：①无主领料(out无项目,×均价估值,排除已明确
    「非项目领用」的) ②无价入库(收货流水 amount NULL,join 采购明细标注"可回填") ③孤儿采购
    (project_code 既非项目编号也非字典 order_no) + 双口径对账(采购收货金额 vs 领料×均价,差异倒序)
    + 本月/累计未归集合计。修复动作：`PATCH /reports/cost-audit/txns/{tid}/project` 补选项目、
    `POST /reports/cost-audit/backfill-prices` 一键回填。财务部新增「🕳️成本审计」tab(4张卡片式子tab)。
  - **修真 bug(规划§二#2)**:`PUT /purchase-mgmt/items/{iid}` 补价现在**回写**该明细所有非冲红
    wh_txn 的 unit_price/amount——此前补价只更新 PurchaseItem,收货流水永久无价。
  - **冲红口径修正(规划§二#4)**:`_avg_price_map`/`project-cost`/`_demand_rows.issued_map`/
    pnl 聚合统一加 `reversed == False`——此前被冲红的原单仍计入加权价与项目成本。
  - **出库必选项目(规划§二#1 流程约束)**:`POST /wh/txns` 出库无 project_id 时必须
    `non_project=true`+原因,否则 400;前端出库弹窗项目必选+「非项目领用」勾选框。
  - **售后侵蚀率报警(1a 附带)**:售后审批通过时,项目累计已审批售后费÷合同额>5% →
    push_message 给 manager 角色(warn)。
  - TAB_REGISTRY finance 加 ("pnl","项目毛利")/("audit","成本审计"),支持按账号授权。
  - **⚠️ 顺手修了上一批(d884356)的启动崩溃**:`warehouse_router._DICT_ADMIN_ROLES` 定义在
    使用之后(L396 定义、L287 使用),Depends 默认参数 import 时求值 → 整个后端 NameError 起不来,
    `ast.parse` 查不出(语法合法)。已把定义前移。**若生产已部署 d884356/81d4da3 会启动失败,
    必须立即拉本次提交重新 --build**。教训已补进第二节:改后端必须真实 import/起服务验证。
  - 验证：py3.9 venv+future-annotations 副本起真服务(sqlite),三项目/冲红对/无价入库/孤儿采购
    全场景实测——pnl 三腿手算核对一致、防双算与冲红排除生效、四清单/两修复动作/出库400拦截/
    售后预警消息全部按预期;vue-tsc ✓ vite build ✓。Postgres 沙箱本机不可用,SQL 均为既有
    portable 模式(group_by 全列/not_in 子查询已滤 NULL),部署后建议点开两个新 tab 冒烟。

- **🆕 盈利改善第二档交付(2026-07-11，资金面板)**:
  - `GET /api/reports/fund-panel`(finance/finance_lead+管理层)一个接口五段数据,
    财务部新增「💰资金面板」tab(顶部4 KPI+五个卡片子tab),TAB_REGISTRY 加 ("fund","资金面板")。
  - **①逾期应收账龄**:尾款(balance>0,balance_date<今天)+发货款(ship_receivable>0,按 ship_date),
    30/60/90/90+ 分档,按客户/销售 Top10 排名。※四段款是合同条款数字,预付/发货前付无日期不进账龄。
  - **②预付敞口**:paid_amount>0 且未到货,按供应商汇总押款金额+最早付款日起押款天数。
  - **③应付账期利用**:到期日=到货日+Supplier.credit_days;逾期未付(断供风险)/14天内到期(排程付款)/
    提前付统计(合计+平均白放弃免息天数)/未维护 credit_days 的供应商清单(提示补主数据)。
    **请款审批/付款列表每张单加「账期到期」标签**(_pr_out 新增 earliest_due/due_in_days=
    min(明细到货日)+credit_days,schemas.PaymentRequestOut 同步)。
  - **④呆滞库存**:≥90天无出库动销(从未出库按最后入库日),现存×加权均价,90/180/365 分档,
    经 purchase_item_id 回溯采购员+项目;附安全库存体检(safety>2×月均出库=偏高压钱/现存<safety=常报警)。
  - **⑤未来13周现金排程**:流入=尾款按约定日挂周(已逾期进W0),发货款无日期单列;流出=已批未付请款(W0)
    +应付到期按周+OA pending_payment(W0)。**防双算:已批请款关联的明细从应付排程剔除(仅 approved,
    pending 不剔——其请款金额不计流出,明细仍按到期排)**。累计净额为负=缺口行标红。
  - **尾款逾期每周升级催办**(补掉"终身只提醒一次"盲区):overdue.py 新增 `scan_balance_overdue`,
    已挂进 scheduler——逾期第1周催销售本人、第2周+销售主管、第3周起+管理层,每7天一遍
    (biz_type=balance_overdue,按最近一条消息时间做7天窗口去重);原到期前14天的一次性提醒保留。
  - 验证:真服务实测——账龄分档/预付60天/逾期应付40天/提前付白放弃25天/缺账期清单/呆滞两档+
    从未出库标记/安全体检/13周排程(W0=110000流入、10000流出)全部与手算一致;周催办跑两遍验证
    7天去重与升级层级;请款单 due_in_days=-40 正确;vue-tsc ✓ vite build ✓。

- **🆕 用户反馈批次 2026-07-11(#188~#198,2修+6优化+3告知)**:
  - **BUG#188 打包下载超时**:axios 全局 30s 超时掐断大文件(zip/CAD)下载→报"请求超时/打包
    下载失败"。修法:请求拦截器对所有 `responseType==='blob'` 的请求 `timeout=0`(nginx 300s 兜底)。
  - **BUG#196 电工采购单'项目'列=项目编号**:`_populate_elec_po_from_excel` 原来硬编码
    '项目'列填项目编号,把电工 Excel 的物料名称整列丢掉(该列口径=物料名称,同标准件清单)。
    已改为优先取 Excel 的 项目/名称/品名/物料名称 列,空才回退项目编号;
    存量由迁移 `fix_elec_po_item_names` 修复(只动"全部行==项目编号"的表,按原 Excel 行序回填,
    行数不匹配跳过)。
  - **#197 电工接线完成即完成**:`electric_done` 现在=status done+计考核(同设计部口径);
    电路图上传/发货准备挪到「已完成」tab(资源弹窗 shipprep=电路图),`output-upload`/`ship-prep-done`/
    附件替换均放行 done 态电工单。
    **止损迁移(用户确认"尽快止损")**:`close_legacy_electric_done_orders` 把卡在旧二步流的存量单
    (in_progress+electric_done_flag)置完成,done_date=部署日(系统没记接线完成日,取部署日比继续
    累计逾期准);配套 **reopen 现在会清 design/electric_done_flag**(改回=返工回到未完成状态机,
    也是迁移不误关返工单的前提)。验证:2张卡住置完成/普通单不动/幂等/reopen后不误关。
  - **#198 电工部请购单**:`_PREQ_WAREHOUSE/_PREQ_VIEW` + electrician/electric_lead;
    电工工作台显示「🛒请购单」tab(与设计/仓库同流程)。
  - **#195 每张进度表加「库位」列**:五张模板+电工采购单列尾追加(⚠️必须追加在末尾——Excel导入按
    位置映射);老形态全部登记进 LEGACY_SHEET_TEMPLATES 防 cleanup 清表;存量表由
    `add_location_column_to_known_sheets` 迁移补列(验证:补列后数据行保留、cleanup 不误清)。
  - **#194 钣金/装配组换人**:`POST /produce/group/{task_id}/reassign`(pm_lead+管理层,
    校验组角色/已完成拦截/通知新负责人);两组 tab 派给列加「换人」按钮。
  - **#193 问题反馈带现场照片**:`POST /feedbacks` 改 multipart(files 多张选填,
    biz_type=feedback 已加进附件白名单);FeedbackRow.images;提交弹窗选图、
    审批/接收列表点📷预览(带鉴权 blob)。
  - **#191 供应商 Excel 批量导入**:`GET /purchase-mgmt/suppliers/import-template` +
    `POST /purchase-mgmt/suppliers/import`——按名称匹配,已存在只补空缺字段不覆盖,新建 created_by=
    导入人(保持采购员隔离口径);供应商账目 tab「批量导入」下拉。
  - **非问题(已告知用户)**:#189/#190 供应商分类加值=字典设置自助(buyer_lead/管理层);
    #192 设计部请购单已在 324917d 上线。
  - 验证:真服务两轮(迁移修复#195/#196 实测回填与不误清、电工完成流/请购/换人/带图反馈/
    供应商导入全部实测通过,含新上传解析别名+空名回退);vue-tsc ✓ vite build ✓。

- **🆕 库位管理+权限统一+装配表编辑批次(2026-07-11)**:
  - **库位管理**:新表 `wh_locations`(name unique/note/enabled/sort_order),仓库菜单新增「库位管理」tab
    (仓管维护;改名级联更新物料与未收货采购单的库位;在用库位删不掉只能停用)。
    `wh_txns.location` 新列(_NEW_COLUMNS):入库=放到哪(手工入库可选,默认物料当前库位,选了回写物料;
    采购收货=下单选的库位并**更新物料当前库位**——单库位模型,最近收货处=当前库位),
    出库=从物料当前库位领,冲红复制原单库位。采购下单三路径库位字段改为从 /wh/locations 下拉。
  - **⚠️ 取消收货自动出库**:_auto_stock_in 里 is_stock=False 自动「采购领用」出库块已删——
    收货一律只入库;出库统一走仓库(出入库登记/物料需求一键领用)挂项目计成本。
    「是否备货」开关从新建采购单表单移除,is_stock 列保留(历史数据),下单不再写 False。
    **口径影响**:按清单下单的料收货后留在库存,须由仓库领料出库到项目,项目材料成本才归集。
  - **生产部请购单**:_PREQ_WAREHOUSE 加 pm_lead/production_clerk/assembler/sheetmetal,
    生产部工作台显示「🛒请购单」tab(与设计/电工/仓库同流程)。
  - **权限统一(用户要求:二级菜单可见性只由权限页决定)**:去掉写死的 tab 角色限制——
    采购管理 采购部/采购申请/请款记录 tab 的 showPurchaseTab(buyer族)前置、汇总报表的
    isLeadOrAbove 前置、财务 库存/成本 tab 的 isManager 前置,全部改纯 tv()。
    配套放宽读接口:/wh/inventory-value与/wh/project-cost→finance/finance_lead;
    downstream /purchase/* → +buyer_lead/finance/finance_lead;_PURCHASE_ROLES/_PREQ_VIEW→+finance_lead等。
    **默认可见性有变化**:财务账号现在默认能看到采购部/请款记录/库存成本等 tab,
    不该看的去「用户管理-二级菜单权限」按账号隐藏(admin/manager 仍豁免)。
  - **钣金装配表编辑增强**(SheetmetalGrid,钣金/装配组共用):日期列(名称以"日期"结尾)→日期选择器;
    进度列→完成/进行中下拉;其他列→el-autocomplete 下拉复制本列已有值(仍可自由输入);
    悬停单元格出现 ⬇ 填充柄=把该值向下复制到本列所有空单元格(逐格调 produceUpdateCell)。
  - 验证:真服务——库位CRUD/重名拦截/改名级联/在用删除保护;下单选库位→收货只入库(无自动出库)
    流水带库位;手工入库换库位更新物料/出库记当时库位;生产内勤请购 200;财务调
    inventory-value 与 purchase/projects 200。vue-tsc ✓ vite build ✓。

- **🆕 人事部一期(2026-07-11,设计稿见《人事部功能设计.md》,用户确认"先做一期")**:
  - 新一级菜单 `hr`「人事部」(hr 角色+管理层;hr 角色 ROLE_MENUS 追加,老权限不动);
    TAB_REGISTRY 加 hr:roster/payroll;新路由 /hr → HrView.vue(导航图标 Avatar)。
  - **员工花名册** `employees` 表(姓名/部门=OA departments/岗位/入职/转正/合同到期/状态
    试用·在职·离职/身份证选填/电话/紧急联系人/可选关联登录账号 user_id/备注)：
    CRUD+筛选+KPI(在职/试用/30天内合同到期/本月入职离职)+Excel导入(部门名不匹配跳过并提示,
    重名非离职只补空缺);状态改「离职」自动填离职日期,挂了账号的推送 admin 提醒停用。
  - **部门月度工资总额** `payroll_monthly`(month+department_id 唯一)：按月整表录入/覆盖,
    年度逐月汇总。**这是盈利改善规划§五-1「含分摊人工」毛利口径的数据源**——
    下一步(第三期)毛利榜读此表按部门任务时间戳权重分摊,纯只读计算。
  - **到期提醒** overdue.scan_hr_reminders(已挂 scheduler):合同到期前30天(含已过期)、
    试用期转正前7天,推 hr+管理层,按员工7天窗口去重。
  - 两新表由 create_all 自动建(无需手工迁移)。二期(证件台账)/三期(OA请假+人工分摊口径)见设计稿。
  - 验证:真服务——hr 菜单下发/非 hr 403/CRUD/KPI(临期合同计数)/导入(坏部门行跳过)/
    工资 upsert+年度汇总/提醒扫描两遍验7天去重/离职联动 admin 提醒+自动填离职日期;
    vue-tsc ✓ vite build ✓。

- **🆕 用户反馈 #199/#200(2026-07-11,OA两条)**:
  - **#200 流程级固定抄送**:新表 `oa_flow_cc`(department_id+doc_type+role_code 唯一)——
    「审批流程配置」页步骤表下新增「自动抄送」多选角色+保存(整组覆盖 PUT /oa/flow-cc);
    提交申请时该配置角色的在职用户自动进抄送(与手选抄送合并去重,同样收【OA抄送】通知)。
    提交表单里原有的手选抄送人保持不变。
  - **#199 管理层删除申请单**:`DELETE /oa/requests/{rid}`(仅 admin/manager)——附件文件一并删、
    报销↔业务的 related_request_id 引用置空、步骤/抄送级联删,操作写审计;
    「全部申请」tab 操作列加「删除」。
  - 该导出里 #188~#198 为上一批已修条目(导出时状态未更新),无需重复处理。
  - 验证:真服务——配链+配抄送→销售提单→财务自动收抄送通知;删除 销售403/管理层成功,
    步骤/抄送/附件级联清零;vue-tsc ✓ vite build ✓。

- **🆕 表格列宽整齐化(2026-07-11,用户反馈"有些列特别宽有些列窄")**:
  - **排查(忠实复刻 Element Plus updateColumnsWidth 做 headless 截图)确认两个根因**:
    ①**数字列被设成 min-width(flex)**→宽屏下按 min-width 比例被拉伸,数字列出现大空隙(库存总览
    现存/单价/总价/安全库存);②**内容少的表只有 1 个 flex 文本列**→该列独吞全部余量,宽屏下爆到
    ~1000px+,其余列却窄(采购应付/预付敞口的供应商列等)。
  - **修**:①库存总览 数字/短列(现存/单价/总价/安全库存/单位/库位)min-width→固定 width 右对齐,
    只留 名称/规格型号/类别 flex 填充;②对"单文本列+若干数字列"的内容少表(采购应付/预付敞口/
    安全库存体检/13周现金/项目毛利分组/采购汇总月份/物料需求总览)加 `:fit="false"`——列宽按各自
    设定不拉伸、表格紧凑左对齐、右侧留白(类电子表格),避免单列爆宽;供应商列 min-width 提到 220 保证
    完整显示。多 flex 列的表(待开票/项目毛利明细/出入库流水/花名册/采购明细)本就均匀填充,未动。
  - **口径**:数字/日期/状态/编码/操作/单位=固定窄列;名称/规格/客户/供应商/备注=flex 文本列填充。
    后续新表沿用此口径。若某表希望恒满宽拉伸,去掉其 `:fit="false"` 即可。
  - 验证:忠实 EP 算法 mock 前后对比截图确认整齐;vue-tsc ✓ vite build ✓。
    ⚠️踩坑:批量给 `<el-table>` 注入 `:fit="false"` 的脚本若用"预取 finditer 位置+循环内改串"会
    位置漂移损坏文件;且正则 `[^>]*>` 会被属性里的箭头函数 `=>` 的 `>` 截断(13周现金的
    :row-class-name)。改为**逆序replace**并对含 `=>` 的标签手工修。

- **🆕 用户反馈 #201/#202/#203(2026-07-11)**:
  - **#203 增加"管理部"部门**:backfill_oa_departments 默认部门加 ("管理部","manager"),幂等,
    存量库启动补建→HR/OA 部门下拉出现。
  - **#202 企业员工工号**:Employee.emp_no(String16 唯一);创建/导入自动=现有数字工号 max+1 补零5位
    (00001);离职是状态变更不删→工号天然保留不复用(录错物理删才可能空号)。花名册表首列+编辑弹窗
    只读展示;存量由 backfill_employee_no 按 id 顺序补号。data_migration `_NEW_COLUMNS["employees"]`。
  - **#201 物料运输费**(=盈利改善规划"随车搭载"运费项落地):Shipment 加 freight_cost/freight_payer
    (我方/到付)/freight_note;物流发货部列表加"运费"列+录入弹窗(`PUT /logistics/{sid}/freight`,
    发货前后均可维护)。**我方**承担的:进财务「💸支出总览」(按发货月,新增运费列+KPI)与
    「📈项目毛利」(新增运费腿,毛利口径注释改为已含运费、不再"不含运费");**到付**=客户承担不计。
    data_migration shipments 段加 freight_cost/payer/note。
  - ⚠️`_NEW_COLUMNS` 里 "purchase_items" 键出现两次(dict 后者覆盖前者)——本次给 shipments 段
    追加、并新增 employees 段,避开重复键。历史那个重复是既有遗留,存量列已在,未动。
  - 验证:真服务——管理部入列;工号 00001/00002/00003 且离职后新员工 00004 不复用;运费我方
    进支出总览月列(发货后)与项目毛利运费腿,改到付后两处均归0;vue-tsc ✓ vite build ✓。

- **🆕 用户反馈 #205(2026-07-12,采购申请通知)**:王芹反馈"采购申请推送给别人的看不到"。
  排查:列表本就全员可见(实测 buyer 能看到指派给别人的申请),真问题是**通知只推给「指定采购员」
  那一位**——`create_purchase_request` 里 `if body.buyer_id: 只 db.add 给该 buyer`;且原"无指定"
  分支的采购员查询只按主角色(漏掉 buyer 为副角色的用户如王芹 buyer+finance)。修:提交采购申请时
  通知**全体采购员家族(buyer/buyer_lead/buyer_standard/buyer_outsource,主+副角色并集去重)**,
  指定的那位文案点名"指派给 X"。实测:指派给李新新时王芹也收到通知。#204 追问后明确为新功能(见下);#206(考勤板块)待用户定打卡/月度汇总口径后做。
- **🆕 用户反馈 #204(2026-07-12,库位占用/空闲)**:用户想要中转区等库位有「占用/空闲」状态、且跟
  出入库流水联动。实现:`WhLocationOut` 加 `occupied`+`occupied_items`;`list_locations` 按
  `_stock_map` 算——库位上有物料现存>0=占用(否则空闲),占用内容列出物料×现存。纯派生(不存状态,
  每次读实时算),入库到该库位→占用、出库腾空→空闲。仓库「库位管理」tab 加"占用状态"列(占用N项/
  空闲,悬停看内容)。实测:入库5台电机→占用、出库→空闲。
- **🆕 UI 重构·品牌换肤(2026-07-12,设计稿驱动)**:按《网站样式重构.zip》把品牌 蓝→金+navy。
  **纯样式,零业务逻辑变更**(约束:不动前后端逻辑/不引次生缺陷)。改 3 文件+1 资源:
  ①`src/assets/logo.png`(设计稿 logo,登录+侧栏);②`LoginView.vue` 视觉重做(深蓝科技底+毛玻璃卡+金按钮,
  登录逻辑 username/password/onSubmit 一字未动;设计稿的「记住登录/忘记密码」**未加**=新逻辑违约束);
  ③`style.css` 主题变量:`--primary` 蓝→金 #c8a24f(**填充**);新增 `--link` #9a7220(**白底文字/链接/编号**,
  金做文字不可读);`--brand-navy` #16293f(侧栏/表头);表头 navy+浅字+排序箭头适配;功能色不动;
  ④`MainLayout.vue` 侧栏真 logo、active 金左条+金字、头像/角标金。**口径:金=填充,--link=白底文字,navy=深色面**。
  验证:登录 standalone 渲染 ✓;真 EP CSS+新 style.css 组件 mock 对比度达标 ✓;vue-tsc/vite build ✓。
  - **🆕 补:交易页统一(2026-07-12,反馈「只统一了侧边框」)**:线上 CSS 实为已换金(抓包 `c8a24f`✓/`2563eb`已无),
    交易页里的「蓝」多是 `var(--primary,#2563eb)` **回退值**、已随主题变金;感知不统一是缺**设计签名**。
    补两处全局(纯样式):①`.page-header h1::before` 金色标题条(全站 24 页头统一,设计稿签名元素);
    ②`.el-tabs__item.is-active` 选中标签用可读深金 `--link`+金下划线(金 `#c8a24f` 白底做文字太浅)。
    验证:交易页 mock(真 EP CSS)标题金条/金标签/navy 表头/金编号/金 KPI 一致 ✓;vite build ✓。
  - **🆕 补:表单/弹窗品牌感(2026-07-12,反馈「表格表单按设计稿美化了吗」)**:
    核实设计稿(zip)仅 2 张——登录页 + 项目目录**表格页**(app.png),**无系统内表单/弹窗稿**。
    表格已对齐 app.png(navy 表头/金编号/状态剩余语义色/干净行)。表单跟随主题,补 1 处全局(纯样式):
    `.el-dialog__title::before` 金色小竖条,与页头标题条呼应,让弹窗表单也带品牌签名。vite build ✓。
    未做(需动视图层、待用户确认):app.png「导出 Excel」是金描边幽灵按钮,现次要按钮为中性灰描边。
- **🆕 员工紧急联系人电话独立列(2026-07-12)**:原「紧急联系人」姓名+电话挤一格,拆出
  `Employee.emergency_contact_phone`(VARCHAR32,可空)。改:models + data_migration(employees 加列,
  与 emp_no 同 ADD COLUMN 模式)+ hr_router(EmployeeIn/_emp_out/导入列「紧急联系人电话」)+ HrView(表单加输入框、
  列表姓名后灰字显电话)。setattr 循环自动流转 create/update。验证:sqlite 迁移+读写+Pydantic round-trip ✓、vue-tsc ✓。
- **🆕 物料编码显示加横线 + 弃用物料类别 + 编码列说明(2026-07-12,纯前端)**:
  ①`utils/format.ts` 加 `fmtMatCode`:9 位纯数字编码(大类1+中类2+细分2+流水4)显示为 `1-0101-0001`,
    **仅改显示、不动存储值**(非标编码原样返回,零迁移风险)——用户明确选「仅展示加横线」。
  ②WarehouseView 两张物料表 code 列套 `fmtMatCode` + 表头加 `QuestionFilled` 悬浮说明;表单 disabled 编码框也套。
  ③弃用「物料类别」(自由文本 category,与编码分类树重复):隐藏两列+表单下拉+移除 matCatOptions;
    **DB 字段 category 保留不删**,编辑时原值不动。搜索仍按原始 code;后端无编码 Excel 导出,故未动后端。验证:vue-tsc ✓。
  ④**编码文字说明(用户澄清:对每个编码进行文字说明,#5)**:每个物料编码是按编码分类树发的,故「说明」=**该编码解码出的分类路径名**。
    后端 `WhMaterialOut.category_path`(`_category_path_map` 从 category_id 沿树向上取「大类/中类/细分」名,带环保护),
    `list_materials` 填;前端两张物料表在 code 列后加「编码说明」列显示路径(如 1-0101-0001 → 机械件/轴承/深沟球轴承)。
    (表头 QuestionFilled 讲格式规则、编码说明列讲每个编码含义,互补保留。)验证:真分类树 category_path 实测 ✓、vue-tsc ✓。
- **🆕 考勤 + 个人工资(人事按月每人录入)(2026-07-12,#206 落地)**:用户定「人事自己录入考勤,并录每人工资」。
  新增 2 表(create_all 自动建,无需迁移):`AttendanceMonthly`(employee_id+period 唯一;应出勤/实出勤/请假/加班工时/备注)、
  `EmployeeSalaryMonthly`(同唯一键;基本/绩效/加班费/补贴/社保公积金扣款/其他扣款,**实发=前四项−后两项,computed 不落库**)。
  **区别于 PayrollMonthly**(那是部门汇总)。后端 hr_router 加 4 端点:GET/PUT `/hr/attendance`、`/hr/salary`,
  全部 `require_roles(*_HR)`(=hr+admin+manager),故工资敏感数据天然只对人事/管理层;upsert 按 employee_id 幂等。
  前端 HrView 加两 tab(考勤🗓️/工资🧾):月份选择+逐人可编辑表(el-input-number)+保存本月;工资表实发列前端 salNet 实时算、
  与后端 _salary_net 一致。tab 权限走 tabVisible(默认可见,整个 /hr 已限人事+管理层)。
  验证:py3.9+httpx ASGI 在进程内实测——建表/建员工/考勤 GET-PUT-GET-UPSERT/工资 GET-PUT-GET+实发 7300 核对全过 ✓;vue-tsc ✓。
- **🆕 #204/#3 材料库位跨页展示(2026-07-12,进行中)**:目标——库位展示给采购明细/装配组/钣金组/设计部/仓库。
  排查发现**多处已有库位**:采购明细(`stock_location` 列已在)、仓库物料主数据/总览/流水/收货(均有)、设计部「查库存」弹窗(有)。
  本次补:**仓库·物料需求表**加库位列——`WarehouseDemandRow.location`,`_demand_rows` 用已解析的 `m.location` 填(名称+规格→WhMaterial);
  前端 demandRows 表加「库位」列 + DemandRow 接口加 location。验证:py_compile + vue-tsc ✓。
  **装配组/钣金组**(用户澄清:不是往钣金装配表里加列,是在**装配组/钣金组各自的项目列表**里加):
  两处项目表加「材料库位」列,值=`material_locations`——**该项目入库流水(WhTxn direction=in、非冲红、location 非空)去过的库位,去重保序**。
  后端:produce_router `GroupProjectRow.material_locations` + `_project_loc_map` 助手,`_group_rows` 统一填(覆盖装配组 assembly + produce 钣金);
  downstream_router `SheetMetalRow.material_locations` 内联同款聚合(覆盖 SheetMetalView 钣金组)。
  前端:DeptWorkbenchView 装配组表 + SheetMetalView 钣金组表各加一列,库位用 `el-tag warning plain` 展示;orders.ts GroupProjectRow 加字段。
  验证:sqlite 聚合语义(去重/去出库/去冲红/去空)✓、py_compile ✓、vue-tsc ✓。
  请购单(设计部)items 多为「待采购未入库」,库位常空、价值低,暂不加。
- **🆕 #204 收尾:库位改由收货填 + 材料库位补齐设计/电工/钣金tab(2026-07-12,用户追问「你到底把库位设计在哪」)**:
  用户审计发现之前实现**不一致**(只在装配组tab+钣金独立页加了列,且数据源接的是入库流水→当时全空)+
  两处漏加(**钣金组工作台tab、设计部/电工部**)。用户明确 ①库位数据源**只用实际入库库位**(空到收货为止=正确);
  ②**取消采购下单填库位,统一仓库收货时填,数据同步出去即可**。据此:
  - **库位录入点迁移(动了业务流,用户明确要求)**:采购下单三路径(新建/清单/按套)去掉库位字段+必填校验,payload 传 null;
    `PurchaseReceiveIn`/`BatchReceiveIn` 加 `stock_location`;`receive_item`/`receive_batch` 收货时写回 `item.stock_location`
    (填了才写、不覆盖为空)→ 既有 `_auto_stock_in` 顺势同步到 `WhTxn入库.location` + 物料当前库位(下游逻辑未改)。
    仓库收货弹窗(单收/合并)加库位**必选**下拉(取 enabledLocations);采购明细库位列不变(现由收货填的值驱动)。
  - **材料库位补齐**:钣金组**工作台tab**(sheetmetalRows 早有 material_locations,只差前端列)+ **设计部/电工部任务跟踪表**
    (`OrderOut.material_locations` 新字段;`list_orders` 加 `loc_map` 按 project_id 聚合入库库位;`_order_to_out` 透传;
    orders.ts DeptOrder 加字段;track 表在项目名后加列)。装配组/钣金独立页沿用原列。
  - 验证:**进程内 httpx ASGI 全链路实测**——下单不填库位→单收/合并收货填库位 A-01/B-09→
    采购明细.stock_location、入库流水.location(挂对项目)、物料.location、设计部 orders.material_locations **全部同步一致** ✓;vue-tsc ✓。
- **🆕 HR「登录账号」绑定闭环(2026-07-13,用户反馈「感觉是个孤儿」)**:`Employee.user_id`(员工↔系统账号)之前
  有列有字段但**无绑定入口**(表单无选择器、导入无此列)→ user_id 永远空、登录账号列永远「—」、离职停用提醒永不触发=孤儿。补全:
  ①后端 `GET /hr/bindable-users`(列可绑账号,标注已被别的员工绑定的);②`_check_user_unique`(一账号只绑一员工,
  **在员工写入前**查——放 `_apply_emp` 里会 autoflush 半成品员工报 name NOT NULL,故移到 create/update 端点开头)。
  ③前端 HrView 员工表单加「登录账号」选择器(filterable/clearable,已绑他人的选项禁选+提示「已绑 X」),onMounted 拉列表。
  绑定后:花名册列显示账号名、员工改离职→admin 收「停用账号」提醒(既有逻辑,现终于可达)。**未登录系统的车间工人可不绑**。
  验证:进程内 ASGI 全链路——可选账号→绑定→花名册显示→唯一拦截 400→离职提醒触发 ✓;vue-tsc ✓。
- **🆕 用户反馈第5批(#221/#222/#223)(2026-07-13)**:（注:本批开工时发现 origin/main 已有并行开发——封板组#209+反馈第3/4批#208~#220，已 ff 合并后在其上做第5批）
  - **#223 封板组(sealing)进「请购单」tab 无权操作(403)**:封板组作第三生产组(#209)漏加进请购单角色白名单
    `_PREQ_WAREHOUSE`(原只有 assembler/sheetmetal)→ sealing 用户 GET/POST 请购单都 403(前端全局拦截器把 403 弹「无权操作」)。
    修:白名单补 `"sealing"`(GET 走 `_PREQ_VIEW`=白名单+采购/财务，POST 走白名单)。
    验证:进程内 ASGI——建 sealing 用户→GET 请购单 200、POST 提请购单 200(修前 403)✓。
  - **#221 收货单价小数位太多**:采购收货(单收/合并)单价 `el-input-number :precision` 4→2(收货金额本就 2 位);
    `onRecvAmountCalc` 反算单价 `toFixed` 4→2。纯前端。
  - **#222 仓库·物料需求表太紧凑**:现有库存/库位/建议采购三列挤一起(#204 加的库位列 96px 夹在两数字列间)。
    加宽:需求量 90→104、现有库存/建议采购 100→112、库位 96→`min-width 130`。纯前端。vue-tsc ✓。
- **🆕 封板文件(机架图/横梁图)设计→封板组 + 封板组 tab 显钣金装配表(2026-07-14,建在并行组「封板组#209」之上)**:
  用户要:设计推送 机架图/横梁图 给封板组;封板组能看 钣金装配表+激光件清单(后两者 #209 已取数,但钣金装配表之前没在封板组 tab 显示)。
  - **设计部加「封板文件」产出(配置驱动)**:`dept_config` design.start_outputs 加
    `{k:"sealing_pkg", label:"封板文件(机架图/横梁图)", to_role:"sealing"}` —— 设计工作台起始上传区是 `v-for start_outputs`
    **自动多出这个上传槽**,上传即走既有通用推送(`to_role`)通知封板组;`orders_router` start-upload「完成后仍可换」豁免
    kind 加 `sealing_pkg`(与 CAD激光图纸同口径)。
  - **封板组 tab 显示**:`produce_router._laser_files_by_pid` 加 `kind` 参数(默认 `sheetpkg` 不破坏原激光图逻辑),
    group=sealing 时多取 `kind="sealing_pkg"` → `GroupProjectRow.sealing_files`;前端封板组 tab 把「激光图」列改成
    **「推送资料」列**,集中显示 钣金装配表(`viewSheet` 只读,封板组无编辑权)/激光件清单/CAD激光图纸/封板文件 下载。orders.ts 加字段。
  - 验证:进程内 ASGI 全链路——options 出封板文件槽→设计上传机架图/横梁图→封板组 sealing-projects 见 2 文件+钣金装配表字段齐→
    上传即推送 sealing 角色收到通知 ✓;vue-tsc ✓。
