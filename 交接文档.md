# 交接文档(维护者/AI 助手接手必读)

> 写给接手本项目的维护者——无论是人,还是新开的 Claude Code 会话。
> 这里记录的是**不在其他文档里的隐性知识**:开发工作流约定、生产环境踩过的坑、
> 排障流程、当前状态。系统性内容请按下面的文档地图去读对应文档。
> 最后更新:2026-07-08。
>
> **⚠️ 维护规则(用户指定):每次代码/流程变更,推送前必须同步更新本文档**——
> 至少刷新「六、当前状态与待办」;若变更涉及新的坑/约定/工作流调整,补进对应章节,
> 并更新顶部的"最后更新"日期。与变更同一个 commit 提交。

---

## 一、文档地图(按用途读)

| 想了解 | 读 |
|---|---|
| 业务需求与流程全貌 | `需求分析.md` → `业务确认结论.md` → `流程评审与问题清单.md` |
| 技术架构 | `技术方案.md`(FastAPI async + SQLAlchemy 2.0 + Vue3 + Element Plus + Postgres) |
| 角色与权限 | `权限矩阵.md` |
| 各模块设计 | `采购模块实施方案.md`、`仓库管理功能设计.md` |
| 历史需求台账 | `增量开发需求清单.md`(M01-M17 已全部完成) |
| **下一步做什么** | **`盈利改善功能规划.md`**(经营分析结论+三档功能路线图,当前的开发依据) |
| 给财务部的功能说明(可直接发给用户) | `财务部新功能说明.html`(支出总览/项目毛利/成本审计/资金面板 使用与口径) |
| 人事部模块(一期已上线,二三期待做) | `人事部功能设计.md`(花名册/证件/部门月度工资总额→毛利人工分摊/请假台账,分三期) |
| 部署/运维/应急 | `OPERATIONS.md`(全新部署 10 分钟流程、备份恢复、升级回滚)+ `DEPLOY-*.md` |
| 运维脚本 | `ops/` 目录(每个脚本头部有用法注释),`ops/README.md` |

## 二、开发工作流约定(Claude Code 会话)

### 分支与推送

- 开发分支:当前用 `claude/elegant-newton-vibso6`,同时推 `main`(用户习惯直接从 main 部署)。
  新会话会分配新的 claude/* 分支,沿用"开发分支 + main 双推"即可。
- **推送方式**:沙箱默认的 git 代理是只读的(push 返回 403)。可用方案是 PAT 直推:
  1. 仓库根目录建 `.env.local`(已在 .gitignore),内容一行:`GITHUB_PAT=github_pat_xxx`
     (**接手者用自己 GitHub 账号新生成 PAT**,旧 PAT 不随仓库/容器转移,也不应共用);
  2. 推送命令模式:
     ```bash
     set -a; source .env.local; set +a
     GIT_CONFIG_COUNT=0 GIT_TERMINAL_PROMPT=0 \
       git push "https://x-access-token:${GITHUB_PAT}@github.com/<owner>/excel-share-system.git" \
       "HEAD:main" "HEAD:refs/heads/<开发分支>" 2>&1 | sed -E 's#x-access-token:[^@]*@#x-access-token:***@#g'
     ```
     (末尾的 sed 是为了不在日志里泄露 token)
  3. 推送前检查快进安全:`git merge-base --is-ancestor origin/main HEAD`。
- **绝对不要**把 `.env.local`、PAT、任何密钥提交进仓库或写进代码/文档。

### 修改后的验证习惯(教训换来的,别省)

- 前端:`cd frontend && npx vue-tsc --noEmit` 必须过。
- 后端:**新写/改动的接口必须实际调用一遍**,不能只靠类型检查。
  (教训:`/sales/equipment-names` 曾引用不存在的 `SalesLedger.name` 字段上线才炸——
  设备名在 `Project.name` 上。)
- 后端改完至少要**真实 import 起一次服务**,`ast.parse` 只查语法查不出 NameError。
  (教训:d884356 里 `_DICT_ADMIN_ROLES` 定义在使用之后,Depends 默认参数 import 时求值,
  整个后端启动即崩,ast.parse 全绿。本机无 3.12 时可用 py3.9 venv + 给副本统一加
  `from __future__ import annotations` 起服务验证,见 2026-07-11 批次记录。)
- **涉及 SQL/schema 的改动要在真 Postgres 上验证**,不能只跑 SQLite(见第三节第 1 条)。
  沙箱里起真 PG:`service postgresql start`,已有测试用户 `pms`/`pms`,
  可 `su - postgres -c "psql -c 'CREATE DATABASE xxx OWNER pms;'"` 建临时库。
- 后端测试在 `backend/tests/`,可参考 `test_e2e_full_lifecycle.py` 的写法。

### 部署提醒(每次推完代码要告诉用户)

- **🆕 首选:用户在自己电脑跑 `bash ops/release.sh`**(2026-07-12 起)——本地一键 SSH 发版:
  自动 SSH 进服务器执行 `ops/upgrade.sh`(备份→拉码→重建→健康检查→失败自动回滚)。
  首次要 `cp .deploy.local.example .deploy.local` 填服务器信息(SSH 密钥免密,`.deploy.local` 已 gitignore)。
  用法见 `ops/README.md`「本地一键发版」。**推完代码提醒用户跑 `bash ops/release.sh` 即可**。
- 手动等价动作(用户直接在服务器上执行):
  ```bash
  git pull
  docker compose -f docker-compose.prod.yml --env-file .env.prod up -d --build
  ```
- **改了前端、或 backend/requirements.txt 加了依赖 → 必须 `--build` 全量重建**,只 restart 不生效。
  (release.sh 走的 upgrade.sh 默认就带 `--build`,已覆盖此要求。)
- 只改后端 py 代码理论上也走 `--build`(镜像里打包代码,不是挂载)。

## 三、生产环境踩坑记录(按重要性排序)

1. **SQLite vs Postgres 差异是本项目最大的隐性雷区**(沙箱开发用 SQLite,生产是 Postgres):
   - SQLite 不校验 `VARCHAR(n)` 长度,PG 严格校验。踩过:`material_dict.dtype VARCHAR(16)`
     存 `'supplier_category'`(17 字符)在生产炸掉。已修(现为 32)+ 生产已手工
     `ALTER TABLE material_dict ALTER COLUMN dtype TYPE VARCHAR(32);`(一次性,勿重复)。
   - PG 严格要求 ORDER BY 列出现在 GROUP BY 或聚合中(GroupingError),SQLite 不管。
     踩过:`oa_summary` 按 `Department.sort_order` 排序但没进 GROUP BY,生产 500。
   - **规则:任何带 GROUP BY/ORDER BY 组合、或新增字符串列的改动,上真 PG 跑一遍。**
2. **时区**:容器日志是 UTC,服务器/诊断包时间是 CST(UTC+8)。对日志时间时记得 +8
   (日志里 15:15 = 北京时间 23:15)。
3. **healthcheck 噪音**:`pg_isready` 不带 `-d` 时 dbname 默认=username,曾造成日志刷
   378 条 `FATAL: database "pms_prod" does not exist`。docker-compose 两个文件的
   healthcheck 都已加 `-d ${POSTGRES_DB}`,改 compose 时别丢掉。
4. **诊断脚本必须带超时**:`ops/diagnose.sh`/`health-check.sh` 里所有 docker/psql 调用
   都包了 `timeout -k 3 <N>`、curl 带 `--connect-timeout/--max-time`。
   原因:被诊断的服务卡死时,不带超时的诊断脚本会跟着一起卡死。新加诊断命令保持这个模式。
5. **中文 PDF**:服务端 PDF 用 reportlab 内置 CID 字体 `STSong-Light`,无需外部字体文件。
   沙箱里 pdftoppm 渲染中文报 "Missing language pack for Adobe-GB1" 是渲染器缺
   `poppler-data`,不是 PDF 本身的问题。
6. **金额字段全是 float**:增量清单 M10 就标注过应改 Numeric/分存储,一直未做。
   做财务精算类功能时注意累计误差,新表建议直接用 Numeric。

## 四、生产排障流程(用户报"服务器报错"时)

1. 让用户在服务器跑 `bash ops/diagnose.sh`,会产出 `pmsdiag<时间戳>.tar` 诊断包
   (含各容器日志、健康检查、可能附带报错截图 base64)。
2. 拿到包后:解包 → 后端日志里 **grep `未捕获异常`**(这是 500 traceback 的可靠锚点),
   截图类文件是 base64,解码出来看。
3. 定位后:本地复现(优先真 PG)→ 修 → 推 main → 提醒用户 pull + rebuild。
4. 常用:`ops/logs.sh`(看日志)、`ops/db-shell.sh`(进库)、`ops/health-check.sh`(体检)。

## 五、关键业务约定(代码里不明显、口头确认过的)

- **采购员分表权限(R4)**:`fangbusen`=外协;`wangqin`=不锈钢+激光;`lixinxin`=标准件+电工;
  其余(采购主管/管理员)全可见。"从清单下单"的清单类型下拉和默认值都跟这个权限走
  (与采购部项目一览的可见列权限一致)。
- **采购员"谁建谁看"(需求五, 与 R4 并存但语义不同)**:`_buyer_restricted(u)` = 有 `buyer` 角色
  且无 `buyer_lead/finance/admin/manager`。命中者:采购明细按 `buyer_id`、供应商按 `Supplier.created_by`、
  汇总报表按 `buyer_id` 只看自己建的;管理层看全部。**遗留供应商 `created_by=NULL` 对所有采购员可见**
  (否则老供应商没人能选)。R4 是"按清单类型可见列",需求五是"按创建人行级隔离",两者叠加。
  - **请款记录按下单采购员隔离(2026-07-08)**:`PaymentRequest` 无 `buyer_id`,请款列表
    (`GET /payment-requests`)与 overview 待审计数原按 `requester_id`(发起人)过滤,现改为经
    `PaymentRequestItem.item_id → PurchaseItem.buyer_id` 反查——"单子的采购员是本人"即可见
    (覆盖代发起/一单混多采购员场景;任一关联明细是本人下的单就可见)。口径与采购明细/供应商账目一致。
- **安装/售后统一(需求一)**:`aftersales.kind` = `aftersales`(售后,problem 必填) / `install`(安装,
  problem 选填,空则存"安装登记")。同一张表、同一审批流、同一财务同步;财务口径叫"安装/售后费用"。
- **请款审批↔付款职责分离(需求十六)**:`/payment-requests/{id}/pay` 校验 `finance_approver_id != current.id`,
  审批人不能给自己审过的单付款(内控)。付款弹窗展示供应商 bank_name/bank_account/tax_no + 关联采购单(po_no)。
  若财务只有 1 人,审批后需 admin/manager 或另一财务付款。
  - **财务页请款/付款拆两个 tab(2026-07-08)**:`FinanceView.vue` 原 `pay_requests` 单 tab 拆成
    `请款审批`(name=`pay_requests`,状态 pending/approved/rejected,不含已付款;操作:审批通过/拒绝/删除)
    + `付款`(name=`pay_payment`,状态 approved待付/paid已付;操作:记录付款/删除)。后端端点不变,纯前端按状态分流。
- **物料需求领用出库(需求二)**:`/wh/demand/{pid}/issue` 按 lines(material_id+qty)登记 `out` 流水
  (source=领料出库, project=项目号),超现存自动截断。采购收货→自动入库(`_auto_stock_in`)本就有,这是补出库侧。
- **仓库一键清空(需求十五)**:`/wh/clear-all` 需 body `confirm=="清空仓库"`,清空 wh_txns+wh_materials
  (不动供应商/采购/项目/字典),仅 warehouse_lead+管理层。前端 prompt 要求输入确认词。
- **库存/成本仅管理层(需求六)**:`/wh/inventory-value`、`/wh/project-cost` 收紧为 `require_roles()`
  (只 admin/manager);财务页"库存/成本" tab 也 `v-if isManager`。财务(非管理层)看不到。
- **开票号(需求十三)**:`/items/set-invoice-no` 对多条明细统一 `invoice_no`,并把各行
  `invoice_amount = received_amount`、状态置"已开票"。前端在采购明细勾选行(含合并父行)→"维护开票号"。
- **合并零件收货只填总价(需求四)**:`/items/receive-batch` 传 `total_amount` 则按各行 `qty` 权重分摊到
  `received_amount`(末行兜余数),单价=金额/数量;或传 `lines` 逐行填价。收货单(图片)走 `/items/{id}/receipt`。
- **"合并父行"是前端虚拟分组,后端无父行字段**:`PurchaseItem` 无 `parent_id/is_merged`,同一采购单多零件行
  共享 `po_no`,前端按 `po_no` 分组(≥2 行成可展开父行,单行/无 po_no 散单平铺)。采购明细页
  (`PurchaseMgmtView.vue` `groupedItems`)与**仓库采购收货页(2026-07-08 新增,`WarehouseView.vue` `groupedRecv`)**
  用同一套分组:父行聚合数量/金额,送货单号/到货日期/开票号/开票日期多值时显示"多个"。存量数据靠 `po_no` 自然分组,无需迁移。
  - **仓库收货合并(需求二)**:采购收货列表按 `po_no` 合并;父行「合并收货」按钮直接对整单各零件行走
    `/items/receive-batch`(送货单号/到货日期/收货单图片统一维护写整批),叶子行仍单条收货。选择列对父行禁用
    (`:selectable`),避免选中虚拟父行。
  - **开票号/开票日期上主汇总(需求三)**:`invoice_no`/`invoice_date` 字段/端点(`/items/set-invoice-no`)/表单
    本已打通,本次补:①`invoice_date` 登记进 `_NEW_COLUMNS`(存量 PG 库自动 ADD COLUMN 防缺列);
    ②合并父行聚合展示 `invoice_no`/`invoice_date`(原父行开票号列写死空,已改为显示聚合值/"多个")。
- **受管理字典**:`material_dict` 用 dtype 区分 category/unit/material_grade/supplier_category,
  物料类别与供应商分类共用一套,管理层在界面维护。
- 采购单号前缀是 `TH`(曾是 CG,已改)。
- 出库"领用项目"**必选**(2026-07-11 起):либо选项目либо勾「非项目领用」+原因(写入 source/party)。
  历史无主出库在财务部「成本审计」页可事后补选项目归集。
- 项目↔销售台账严格 1:1(`uq_ledger_project`);销售下单一个事务建
  Project+SalesLedger+Shipment+各部门 DeptOrder。

## 六、当前状态与待办(2026-07-15)

- **🆕 用户反馈第 8 批 #229/#236~#240(2026-07-15，本次)**:6 条全做;已过 vue-tsc + vite build + 后端实跑
  (`scratchpad/verify_batch8.py` 23 断言、`verify_batch8b.py` 9 断言,全绿):
  - **#229 离职当月仍要发工资(口径修正,影响面最大)**:原 `hr_router._active_emps` 写死 `status != "离职"` 且被考勤/工资**共用**——
    人一标离职就立刻从工资表消失,**最后一个月工资发不出来**。改为 `_active_emps(db, period=None)`：传 period 时按
    `status != 离职 OR (leave_date 非空 AND leave_date[:7] >= period)` 取人,即**离职日期所在月及之前仍在列、之后不在**;
    离职但没填离职日期的仍排除(无法判断哪个月离的)。不传 period 退化为老口径,兼容其他调用方。
    考勤/工资行都带 `left_this_month`/`leave_date`,前端在姓名后打「离职」标签(避免照上月全额发)。
    **✅ 2026-07-16 线上验证**:人事翻当月离职员工,确认已回到工资表。
    **注**:用户原话「人员离职处理功能显示」极含糊,上批曾因此暂缓;本次确认口径=当月离职留在当月考勤/工资。
    离职本身的功能(状态/离职日期/停用账号提醒/入职离职统计)**早就齐全**,不用再加。
  - **#239 考勤加迟到/早退/缺卡 + 是否全勤**:`AttendanceMonthly` 加 `late_count`/`early_leave_count`/`missing_card_count`
    (已进 `_NEW_COLUMNS["attendance_monthly"]` 迁移)。**「是否全勤」不存字段**,由 `hr_router._is_full_attendance()` 自动推导:
    应出勤>0 且 实出勤≥应出勤 且 请假/迟到/早退/缺卡全为 0。前端 `isFullAttendance()` 是同口径的镜像(为了随录入实时变色),
    **改口径要同时改这两处**。
  - **#240 考勤批量导入**:`GET /hr/attendance/import-template`(xlsx 模板+填写说明) + `POST /hr/attendance/{period}/import`。
    **按工号匹配**(Excel 会把 00015 读成数字 15 → 代码里 `zfill(5)` 补零重试);该月已有考勤的人**整行覆盖**(不是只更新非空列),
    表里没出现的人不动;工号不在该月名单的跳过并在返回消息里列出来。导入到哪个月由页面上选的月份决定,不在表里填。
  - **#237 请款审批不该流转到王芹**:她角色是 buyer+finance(兼任),`/finance` 的「请款审批」tab 里全是**她自己提的单**,能自审自批。
    两手:①**代码**——`purchase_mgmt_router.approve_payment_request` 加 `requester_id == current.id` 硬校验(**管理层也不留后门**,
    与既有的「审批人不能给自己审过的单付款」同属职责分离);前端自己的单「审批通过」按钮禁用+tooltip 说明。
    ②**配置**——管理员到「权限管理」把 `finance:pay_requests`(请款审批) 对王芹隐藏,用的是 #7 的 `hidden_tabs` 机制,零代码。
    **✅ 2026-07-16 已配置完成**(用户操作)。以后再有兼任账号提类似诉求,照这个办:先看 `TAB_REGISTRY` 里有没有对应 tab,有就配置解决,别改代码口径。
  - **#238 财务审批完单据「不见了」**:末环节是财务审批的单据审完即 `status="pending_payment"` 且 `current_step_order=None`,
    于是 `pending_me`(要求 status=pending + 有匹配角色的待办环节)查不到、任何环节队列也查不到——**单据从所有待办里蒸发**,
    财务想点「标记已付款」都没入口,只有 admin/manager 能去「全部申请」里翻。新增 `scope=pending_pay`(门槛 finance/admin/manager)
    + OA 页新增「待付款」tab(v-if=canPay)。`mark-paid` 端点本来就有,只是没人找得到它。
  - **#236 销售提成申请改为按月+多项目+总计**:原 #217 的单项目平铺表单作废。现在:部门**固定销售部不可选**(watch 到提成类型自动锁),
    加**提成月份**(默认本月),项目明细改为**可增删的多行表格**——序号|项目编号|成单日期|客户名称|回款金额|回款类型|提成点|提成,
    底部**回款合计 + 总计提成**;申请金额=提成总计。明细存 `OaRequest.detail.commission_items`(JSON 列,**无需迁移**),
    照抄 #149 报销明细行的现成模式。详情页用带 `show-summary` 的表格渲染明细+总计,平铺渲染里排除 `commission_items` 数组。

- **🆕 电工部「外协订单」tab(2026-07-15，本次)**:用户口径=「外协订单跟订单跟踪一样,只是人员只有 zhourui,需要监控他的订单状态;
  zhourui 本人看不到这个 tab」。已过 vue-tsc + vite build + 后端实跑(`scratchpad/verify_outsource.py`,7 断言全绿):
  - **不是新单据类型**:复用现有 `DeptOrder`,就是「任务跟踪」按人筛出来的只读监控视图(项目/负责人/状态/接线开始/预计完成/接线完成/效率/资料)。
    不给换人/作废/删除——那些留在「任务跟踪」里,避免两处入口。
  - **外协人员按账号配置,不硬编码**:`dept_config.OUTSOURCE_WORKERS: dict[str, list[str]]`(dept -> [username]),现为 `{"electric": ["zhourui"]}`。
    **增减外协人员改这一处即可**,不要把账号名写进路由/前端。配置里写了不存在的账号会静默跳过(已测)。
  - **解析走 username 而非姓名**:`GET /orders/options?dept=X` 新增 `outsource_workers: [{id,name}]`(按 username 查库、返回显示名);
    前端按 `worker_id` 筛(不按姓名,避免重名/改名失效)。没新增后端接口。
  - **✅ 2026-07-16 线上验证**:zhourui 本人登录确认看不到该 tab;负责人/管理层可见。(本地只能验逻辑,真实账号的角色配置只有线上能确认。)
  - **可见性**:`showOutsourceTab = (isLead || isMgr) && !isOutsourceSelf && 有配置外协人员`。`isOutsourceSelf` 是双保险——
    zhourui 是普通电工,本来就不满足 isLead/isMgr;万一日后给他加了负责人角色也仍看不到。
    注:tab 只是把负责人/管理层**本就能看到的**本部门订单按人筛一遍,不涉及新的数据暴露,故未加后端闸门。
  - **本地无法跑后端的坑(重要)**:生产是 **Python 3.12**(`backend/Dockerfile.prod`),而这台 mac 只有 3.9 且无 brew/uv/pyenv,
    `dept_config.py`/`sheet_templates.py` 的 `str | None`(3.10+)在 3.9 直接 import 会 TypeError。
    解法见 `scratchpad/verify_outsource.py` 顶部:给真实源码前置 `from __future__ import annotations` 后 exec 进 sys.modules,
    **不改源文件**即可在 3.9 上实跑真实逻辑。以后本地验后端可照抄这个垫片。

- **🆕 问题反馈「死信」断链修复 + 过期文案(2026-07-15，本次)**:用户问「生产部提交的反馈,设计部哪里能看到」时暴露的三处断链;
  已过 vue-tsc + 后端实跑测试(`scratchpad/verify_fb.py`,内存 SQLite 建真表跑 list_feedbacks/assign_feedback,6 断言+指派闭环+越权拒绝全绿):
  - **背景**:反馈入口在「设计部」工作台底部的 `FeedbackPanel`(`DeptWorkbenchView` 里 `dept==='produce'||dept==='design'` 时渲染)。
    #227/#228 取消生产主管审批时移除了 isPm 分支,面板 `v-if` 只剩 `isAssembler || isDesigner`,**design_lead/管理层从此看不到面板**。
  - **死信无处指派(#29 断链)**:`create_feedback` 按项目设计任务(`DeptOrder.dept='design'`)的 `worker_id` 自动指派;若项目没派设计任务/负责人不在岗
    → `designer_uid=None`(死信),只推一条企微给 design_lead,但他**界面上没有入口**——后端 `POST /feedbacks/{id}/assign`(#29 就是为此加的)
    **前端 `api/feedback.ts` 压根没接**。本次接上:`feedbackApi.assign(id, worker_id)`(worker_id 走 query),面板加「指派」按钮+弹窗,
    设计师名单复用 `ordersApi.options('design').workers`(= `DEPTS.design.worker_role='designer'`,多角色感知),不新增后端接口。
  - **管理层看不到**:`list_feedbacks(mine=True)` 的 conds 没有 admin/manager 分支 → 管理层恒返回空(前端 `auth.hasRole` 也无管理层兜底,
    与后端 `require_roles` 始终放行 admin/manager 的口径不一致)。本次后端加 `codes & {"admin","manager"}` → 全量 `pending_design`;
    前端 `canAssign = isDesignLead || isMgr`,面板 `v-if` 加 `canAssign`。
  - **口径(沿用 #29 注释的原设计,未擅自扩大)**:设计师=只看指派给自己的(死信也可直接接收,与 `design_accept` 的
    `if fb.designer_uid and fb.designer_uid != current.id` 校验一致);**design_lead=只看死信(`designer_uid IS NULL`)可指派**,
    看不到已指派给具体设计师的;管理层=全量。若日后要「设计负责人看本部门全部反馈」,改 `list_feedbacks` 的 design_lead 分支即可。
  - `FeedbackRow` 新增 `designer_uid`(前端靠它区分「指派给我」/「死信」;面板对 canAssign 角色加「设计师」列,空=红标「未指派」)。
  - **过期文案**:#227/#228 早已取消审批,但提交成功仍弹「已提交,等待生产主管审批」、输入框提示「经生产主管审批回馈设计师」——
    改为「已提交,已推送设计师接收」/「提交后直接推送设计师接收」。
  - **已知遗留**:面板在 `produce` 和 `design` 两个工作台都渲染,管理层在生产部 tab 也会看到同一张卡(内容一致,无害,未改)。

- **🆕 用户反馈第 7 批 #227~#235(2026-07-15，本次)**:8 条,#229(人员离职处理)用户暂缓待澄清,其余全做;已过 vue-tsc + vite build + ASGI/SQLite 测试(`scratchpad/test_feedback_0715.py`):
  - **#227/#228 装配问题反馈直达设计,取消生产主管审批**:`feedback_router.create_feedback` 建单即 `pending_design` 并推该项目设计师
    (无在岗设计师降级推 design_lead);前端 `FeedbackPanel` 去掉 pm_lead/manager 的「问题反馈审批」面板(移除 isPm 分支)。
    存量迁移 `route_pending_pm_feedbacks_to_design`(data_migration run_all 注册):把卡在 pending_pm 的反馈转 pending_design。
    pm_approve/pm_reject 端点保留但不再产生新 pending_pm(可后续清理)。
  - **#230 封板组去掉「激光件清单(编辑)」列**(封板组工人反馈):`DeptWorkbenchView` 删该列 + 删 laser 弹窗/viewLaserSheet;
    **回退 #224 的可编辑改动**:`canEditSheet` 去 isSealing;`datasheets_router` 生产组可编辑表还原为仅「钣金装配」,
    3 个 produce-edit 端点 require_roles 去掉 sealing。封板组保留 钣金装配表(查看)/CAD激光图纸/机架横梁图 分列。
  - **#232 采购申请可查看/打印成正式申请单**:`purchase_mgmt_router` 新增 `_render_preq_pdf`(reportlab) + `GET /purchase-requests/{id}/pdf`
    (置于 `_PREQ_VIEW`/`_preq_warehouse_only` 定义之后,否则模块加载 NameError);仓库/设计师(非采购)只能看自己提的。
    前端 `PurchaseMgmtView` 采购申请行加「查看/打印申请单」→ 新标签开 PDF。
  - **#234/#235 仓库采购收货:采购单号可点开查看/打印采购单**:`download_order_pdf` 的 require_roles 放开 `warehouse/warehouse_lead`;
    前端 `WarehouseView` 采购收货待收货列的采购单号改为可点击 → `viewPoPdf` 新标签打开采购单 PDF(可看+打印)。
  - **#233 销售尾款到账批注 + 尾款日期显斜杠**:复用现有「收款批注(预付/发货前付,支持插入时间戳)」机制扩到尾款:
    `SalesLedger.balance_note`(新列,_NEW_COLUMNS 迁移);`payment-note` 端点 field 加 `balance`;序列化/schema 带 `balance_note`。
    前端 `SalesView` 尾款列加批注图标;**尾款有到账批注 → 尾款日期显示「/」**(已到账不再是待收日期)。
  - **#229 人员离职处理**:用户「先不做,去问清楚」,本批未动(花名册已可改状态在职/试用/离职)。

- **🆕 仓库出库登记「项目物料反显」(2026-07-15，本次)**:出库登记选物料时,若该物料是**按项目入库**(入库流水
  `WhTxn` 有**唯一**关联 project),自动反显该项目数据、简化录入。已过 vue-tsc + vite build + ASGI/SQLite 测试(`scratchpad/test_wh_projmat.py`):
  - 后端 `warehouse_router`:新增 `_material_projects`——取每个物料入库流水(非冲红)的 project_id,**仅当全部入库指向同一项目**才反显
    (多项目/无项目入库不反显,走现有手填);`WhMaterialOut` 加 `project_id/project_code`;`list_materials`/`_mat_out` 带出。
  - 前端 `WarehouseView` 出库弹窗:物料下拉 label 末尾显示【项目号】;选中「项目物料」时自动带出 **领用项目=该项目、
    数量=现存、单价→金额**(均可改),并**隐藏「领用方」**(领用方=项目);物料下方给蓝色提示。非项目物料完全不变,按原逻辑手填。
  - 口径:物料↔项目的关联来自**入库流水**(采购收货入库 `purchase_mgmt_router` 已按 `item.project_code` 写 `WhTxn.project_id`),
    不是物料主数据字段;所以同名物料若跨项目入库就不反显(避免张冠李戴)。

- **🆕 用户反馈第 6 批 #224/#225(2026-07-14，本次)**:两条,已过 vue-tsc + vite build + ASGI/SQLite 测试(见 `scratchpad/test_feedback_0714.py`):
  - **#225 考勤/工资也按工号排序**:上一批 #214 只改了花名册(`list_employees`);考勤/工资走 `hr_router._active_emps`,其排序由 `(department_id, emp_no)`
    改为 `(emp_no is null, emp_no, id)`——纯按工号升序(不再按部门分组),无工号排最后,与花名册一致。
  - **#224 封板组 tab 照抄钣金组模板 + 分列推送资料**(经与赵仁辉确认列/布局照抄钣金组):
    - **激光件清单可编辑**(同钣金组「编辑装配表」):`datasheets_router` 把生产组可编辑表白名单 `SHEETMETAL_DS_NAME` 扩为
      `PRODUCE_EDITABLE_DS={"钣金装配","激光件清单"}`,三个 produce-edit 端点 `require_roles` 加 `"sealing"`;
      `_produce_edit_check` 仍按 `ProduceGroupTask.worker_id` 校验派单(封板工只能编辑自己被派项目)。
      前端 `canEditSheet` 加 `isSealing`,封板组激光件清单弹窗 `canEdit` 由 false 改 `canEditSheet`。
    - **设计推送资料分列**(用户补充:钣金装配表 / CAD激光图纸带「待推送-已推送N」/ 新增机架横梁图列):封板组 tab 列改为
      激光件清单(编辑) | 钣金装配表(查看) | CAD激光图纸(StatusPill 待推送/已推送 N + 下载) | 机架/横梁图(同,数据=`sealing_files` 封板文件) | 预计完成 | 封板完成。
    - **注**:CAD激光图纸=`laser_files`(order_start_output kind=sheetpkg),机架/横梁图=`sealing_files`(kind=sealing_pkg),均为设计推送,后端上一会话(9991ccc)已populate。

- **🆕 用户反馈第 4 批 #214~#220(2026-07-13，本次)**:七条里 #215/#216 拉代码时已由上一会话(#199 删除/#200 流程级抄送)实现,本次仅验证;
  其余五条本次修复,已过 vue-tsc + vite build + ASGI/SQLite 端到端测试(见 `scratchpad/test_feedback_0713b.py`):
  - **#214 人事花名册按工号排序**:`hr_router.list_employees` 排序由 `(status,dept,id)` 改为 `(emp_no is null, emp_no, id)`——按工号升序,无工号排最后。
  - **#217 OA 新增「销售提成申请」**(单条表单):`data_migration.backfill_oa_doc_types` DEFAULTS 加 `("sales_commission","business","销售提成申请")`(重启幂等灌入)。
    前端 `OaView` 专用表单字段:项目编号/客户名称/设备名称/回款类型/回款金额/提成点(%);**提成金额=回款金额×提成点% 自动算**(只读),存 `detail` JSON,
    `amount`=提成金额。走通用 OA 审批(管理员在【审批流程配置】按部门+单据类型配审批链);详情抽屉复用通用 detail 渲染(加了中文字段标签)。
  - **#218 OA「全部申请」加筛选**:`OaView` 全部申请 tab 加「单据类型 / 申请人」下拉(客户端过滤已加载列表 `allRowsView`),换 tab 自动清筛选。
  - **#219/#220 全流程图显示生产部分组**:生产部走分组派单,父生产单 `worker_id` 恒空 → 老全流程图里生产部节点显示"负责人—/效率—"(#219)、且看不到装配组(#220)。
    `collab_router.project_workflow` 给 produce 部门补 `ProduceGroupTask` 分组(钣金/装配/封板:负责人+完成);`WfDept.groups` 新字段;
    `WorkflowGraph.vue` 生产部节点改为展开三组(负责人+进行中/已完成),不再显示空的负责人/效率;顺带把下游误标的「钣金组(图纸包)」节点改名「钣金图纸包」(它其实是设计 CAD 激光图纸计数,非生产钣金组)。
    **注**:封板组在此图里"派了才显示"(可选组口径,与反馈#209 一致)。
  - **#215 流程级抄送 / #216 管理层删除**:均为上一会话已实现(`OaView` flowCcRoles/saveFlowCc = #200;deleteRequest = #199),UI 在位,**部署即生效**;
    #216 用户确认"删除够了,不加编辑"(已提交审批单不开放改金额/内容)。

- **🆕 用户反馈第 3 批 #206~#212(2026-07-13，本次)**:七条里 #206/#212 拉代码时已由上一会话修好(本次仅验证),
  #207 用户确认"保持现状即可"不改;其余四条本次修复,已过 vue-tsc + build + ASGI/SQLite 端到端测试:
  - **#208 人事部隐藏项目目录/项目详单**:`menus.py` `ROLE_MENUS["hr"]` 由 `["catalog","list","hr"]` 收窄为 `["hr"]`
    (纯 hr 用户不再看到项目目录/详单;多角色仍取并集)。配套 `router/index.ts` 给 `/overview` 加闸门:
    无 `catalog` 菜单者(纯人事/售后)登录后不落在概览页,跳其首个可见菜单。**此前"老8角色不回收"红线因用户明确反馈而放开(仅 hr)**。
  - **#210 装配问题反馈"对应不上"根修**:装配工在手项目走**分组派单** `ProduceGroupTask`(group=assembly),
    `DeptOrder.worker_id` 对生产单恒为空。旧代码 `/api/feedbacks/projects` 下拉列**全部**进行中项目,
    而 `create_feedback` 却校验 `DeptOrder.worker_id==本人`(永不命中)→ 一提交就被"只能对自己在手的项目提交反馈"拦。
    改:下拉与校验**同源**——都查 `ProduceGroupTask` 装配组派单。`feedback_router` 两处。
  - **#211 电工发货准备去掉发货清单**:`DeptWorkbenchView` 发货准备(res)弹窗的"发货清单(推仓库备货)"section
    加 `dept !== 'electric'`,电工发货准备只传电路图(发货清单由设计部上传)。
  - **🆕🆕 #209 完整新增「封板组」(第三生产小组)+ 推送激光图**(改动较大,多文件):
    - 新角色 `sealing`(生产部-封板组),`seed.py` ROLES 加(重启自动建,无需迁移);`deps.py` 受限岗位集/`ProduceGroupTask`
      在手项目集都加 sealing;`menus.py` `["catalog","produce"]`(同装配/钣金,无详单)。
    - `produce_router`:`GROUP_NAME/GROUP_ROLE` 加 sealing;**父单完成口径**改用 `REQUIRED_GROUPS=(钣金,装配)`——
      钣金+装配为必派(都完成才算生产完成),**封板组为可选组**(派了则也须完成,没派不影响,向后兼容存量单)。
      `dispatch`/`DispatchIn`/`dispatch-options` 加 `sealing_worker_id`/`sealing`;新端点 `GET /produce/sealing-projects`。
      **推送激光图**:封板组 tab 每行给出**激光件清单**(数据表,只读查看)+ **CAD激光图纸**(设计产出
      `order_start_output kind=sheetpkg`,可下载)——`GroupProjectRow` 加 `laser_datasheet_id`/`laser_files`,
      新助手 `_laser_files_by_pid`。`reports_router.GROUP_NAMES` 与 `orders_router._PG_NAME` 加 sealing(报表/任务跟踪含封板)。
    - 前端 `DeptWorkbenchView`:`isSealing`/`sealingRows`/加载/默认tab;**封板组 tab**(派给/换人/激光图/预计完成/标记完成);
      派发弹窗加封板组选择器(激光图会推给该组);组换人支持 sealing;`orders.ts` 加类型/`sealingProjects`/`dispatch` 第4参。
    - **部署后**管理员需在「用户」里给封板组工人**分配 `sealing` 角色**才会出现在派发下拉。**封板组默认可选**,
      如需强制每单必派封板,把 `produce_router.REQUIRED_GROUPS` 加 `"sealing"` 即可。
  - **验证脚本**:`scratchpad/test_feedback_0713.py`(#208 菜单/#209 派发+封板+激光+完成口径+向后兼容/#210 下拉与校验/#212 电工完成),全 PASS。
    注:仓库里 `tests/test_m01_menus/m04_orders/m13_feedback/m14_reports` 等**独立脚本**已与上一会话改动脱节(stash 基线同样失败),非本次回归。

- **🆕 6 条需求 + 采购隔离根修(2026-07-08，待推 main)**:
  - **采购明细行级隔离根修**:`_buyer_restricted` 原把 `finance` 放进"看全部"排除名单,导致 buyer+finance
    兼职账号(王芹 buyer/finance/logistics)被判为看全部→看到别人下的单。改为 = 有 buyer 家族角色
    (buyer/buyer_standard/buyer_outsource) 且非(buyer_lead/admin/manager);**兼任财务不解除隔离**;纯 finance 仍看全部。
    13 种角色组合脚本验证通过。(commit 3f7c44a 已单独推)
  - #1 合并收货**去掉"合并总价按数量分摊"**填价方式,只逐行填单价/收货金额。
  - #2 仓库采购申请**加"指定采购员"**(新端点 `GET /buyers`;`PurchaseRequest.buyer_id`),推送定向到该采购员;
    采购管理采购申请 tab 显示指定采购员。
  - #3 收货同步清单列:**现有(到货日期/进度/仓库签字)已够,不改**。
  - #4 合并父行**「整单维护」**(新端点 `POST /items/set-group-summary`):开票金额/已付款按整单总额**不分摊**记在
    首行(其余置0,汇总合计正确),对账状态套所有子行;父行操作列「整单维护」弹窗。
  - #5 **仓库角色在库存总览看不到单价/总价**(KPI库存总价+单价+总价列 gated by isManager)。
  - #6 采购员**只看自己新增的供应商**(created_by==本人),别人建/无归属(NULL)老供应商对采购员隐藏、仅管理层可见;
    供应商账目同理(只列本人新增供应商)。`list_suppliers`+`supplier_statements` 收紧。
- **🆕 #7 按账号分配二级菜单(tab)权限已完成(2026-07-08，待推 main)**:
  - 数据:`User.hidden_tabs`(JSON,存被隐藏的 tab key 如 "finance:pay_payment") + `_NEW_COLUMNS` 补列。
  - 注册表:`menus.TAB_REGISTRY`(采购管理/财务部/仓库三个多 tab 页的可控 tab)→ `GET /admin/tab-registry`。
  - 设置:`PUT /admin/users/{id}` 传 `hidden_tabs`;`/auth/me`+login 的 UserOut 下发 hidden_tabs。
  - 前端:auth store `tabHidden/tabVisible(menuKey,tabName)`(管理层不受限);三视图每个 `<el-tab-pane>` 加
    `v-if="tv('name')"`;`UsersView` 每个用户「二级菜单权限」按钮→弹窗勾选(勾=可见)。
  - **扩展**:新增可控 tab 只需改 `TAB_REGISTRY` + 对应 pane 的 `v-if="tv(...)"`。tab_name 必须与前端 pane name 一致。

- **🆕 图表(LineChart)财务口径重做 + #170/#171(2026-07-08,待推 main)**:
  - **LineChart.vue 重做**(用户嫌"图不好看"):①原 `preserveAspectRatio="none"` 把固定 820 viewBox 拉伸到容器宽,
    圆点变椭圆、描边不匀——改成 **ResizeObserver 按容器实宽 1:1 渲染**,不再畸变;②默认 `smooth` 由 true 改 **false
    (直线段)**,财务图不该用平滑曲线过冲显示没发生过的金额;③配色换成 **dataviz 校验器验证过**的分类色
    (收货蓝`#2a78d6`/已开票琥珀`#eda100`/已付绿`#008300`,light+dark 六项校验全过);④加基线、tabular-nums、克制网格。
    用 headless Chrome 实截图确认过效果。**改的是共享组件,汇总报表的图也一并变直线+新配色。**
  - #170 维护开票号限同一供应商(前端按钮 `:disabled=!selSameSupplier`+拦截,后端 `set_invoice_no` 兜底);
    #171 财务采购单改预览(新标签内联打开 PDF)。

- **🆕 用户反馈第 2 批 #159-#169 已处理(2026-07-08,待推 main)**:B 财务付款/请款采购单号可点下载PDF;
  C 钱票对上显示「已清」(前端派生 `isCleared`);D 供应商账目含本人自建供应商;E 付款收款账户可复制;
  F 采购请款记录状态改显示条;A 不锈钢图纸名称(描述符 hasDrawing,下单折进 spec);G 供应商明细抽屉加月度趋势图;
  **H #167 仓库采购申请**(新功能):`PurchaseRequest`+`PurchaseRequestLine` 两张新表(create_all 自动建,无需迁移);
  仓库页「采购申请」tab 提申请→采购管理「采购申请」tab 处理/驳回,双向消息通知。端点在 `purchase_mgmt_router`
  (`POST/GET/PUT handle|reject/DELETE /purchase-requests`)。**待下单已删(见下)**。

- **⚠️ 待下单工作区已按用户要求删除(2026-07-08)**:用户认为"跟采购明细重复、没用",且跨项目聚合有性能坑,
  整块移除(前端 `pending` tab + 工作区脚本;后端 `/purchasable-pending`、`/purchasable-pending-all` 两端点)。
  **保留**:清单类型描述符(`SHEET_META`)、采购明细按类型分 tab(④)、不锈钢图纸名称(A)、`_purchasable_rows` helper
  (`/purchasable` 单项目从清单下单仍用)。下面这条的 ②③ 已作废,①④A 仍在。
- **🆕 按清单下单重构:清单类型异构 + ~~待下单工作区~~(2026-07-08,待推 main)**:
  - **痛点**:5 类清单(标准件/电工/不锈钢/外协/激光)列名各异(外协的"规格"其实是图纸名、不锈钢有"材质"、
    外协/激光无数量/品牌),硬塞一张统一表别扭。
  - **①清单类型描述符**(前后端各一份):`SHEET_META`(前端)/`_PURCHASABLE_SHEETS`(后端归一化)——每类的
    名称/规格显示名、有无品牌/数量/材质。`PurchasableRow` 加 `material`(材质单独成列,不再塞备注)+
    `sheet_key/project_*`(跨项目聚合用)。**加第 6 类清单只改配置**。
  - **②待下单端点**:`GET /purchasable-pending-all?project_id=X` 返回该项目 `{类型: 未下单行[]}`,受 R4 权限约束。
    **⚠ 性能坑(踩过两次,最终按项目加载才根治)**:①最初 `/purchasable-pending?sheet=` 单类型端点前端并发发 5 个,
    每个全表算库存+逐张清单 N+1 → 转圈;②改聚合端点(库存只算一次+批量查询)仍**把全部项目的所有清单记录全捞**,
    数据一多照样卡死。③**最终改为按项目加载**:`project_id` 必传,只处理该项目的清单,查询量与项目总数无关。
    前端 `待下单` 工作区顶部**先选项目**再看该项目各类型未下单(不再跨项目聚合)。`_purchasable_rows` 支持传入
    预取的 name2id/by_rec/records 避免 N+1。**教训:跨项目全量聚合在数据量大时不可行,该做成按项目/懒加载。**
  - **③待下单常驻工作区**(采购管理新增顶层 `待下单` tab):二级 tab 按清单类型(仅 R4 权限内)+ 角标(未下单数);
    描述符驱动列;两种下单模式——**普通下单**(按 供应商×项目 拆多张单,`submitPendingOrder`)/**打包成套**
    (同一项目一组零件打成一套,`submitPendingKit`,复用 `/orders/kit-from-list`)。均回写清单。
  - **④采购明细列表按类型分 tab**:`/items`+`/items/summary` 加 `sheet_type` 参数
    (`_apply_sheet_type_filter`:按 source_sheet_id 的来源表名过滤;`loose`=散单无来源);前端顶部
    二级 tab(全部/各类型/散单),名称/规格列标签随类型走描述符。
  - 验证:前端 `vue-tsc` 过;后端语法过;端点镜像已验证的 `purchasable`/`create_order_from_list`。
    **本机无 PG/后端运行环境未起服务实调**,上线后建议实测(待下单 tab 各类型下单 + 明细分类 tab 过滤)。
- **🆕 成套采购(按套下单)已完成(2026-07-08,待推 main)**:
  - **核心**:「一套」= **一条被标记 `is_kit` 的 `PurchaseItem`**(不新建表):`item_name`=套名、`qty`=套数、
    `unit_price`=套单价、`received_amount`=套总价、`kit_parts`(JSON)=套内零件清单[{name,spec,qty}](描述性)。
    → 收货/入库/开票/请款/付款/权限/对账**全部零改动复用单条明细流程**,天然"维护一个总"。
  - **四个业务决策**(用户拍板):套=**从清单勾选打包**;一套同一供应商;**整套为一个库存单位**(入库物料名=套名、
    qty=套数,套内零件不单独算库存/领料);**一套一个总价**;整单一次收全;套名手填。
  - **按套下单 = 从清单下单的扩展版(成套模式)**:复用「从清单下单」弹窗(`listOrderMode='kit'`)——选项目/清单→
    勾选一组零件→填套名/套数/套总价/供应商→打包成一套。勾中的零件成为 `kit_parts` **并回写清单为「已下单」**
    (与从清单下单同一套 `_writeback_sheet_row` 逻辑)。前端隐藏成套模式下的逐行供应商/单价/付款列与批量条,
    改为套级面板;数量列改「每套数量」。
  - 改动:models 加 `is_kit`/`kit_parts`(+`_NEW_COLUMNS` 幂等补列);新端点 `POST /orders/kit-from-list`
    (建一条成套明细+回写清单,镜像 `create_order_from_list`);`_item_out` 带 kit 字段;采购单 PDF 明细表下附
    「套内零件清单」表。前端采购明细页「按套下单」按钮走成套模式弹窗;成套行打「套」标签+hover 弹零件清单、数量显示"N 套"。
  - 验证:前端 `vue-tsc` 过;后端语法过;**PDF 用 reportlab 实测**成套+普通单均正常渲染;端点镜像
    已验证的 `create_order_from_list`。**本机无 PG/后端运行环境,端点未起服务实调**,上线后建议实下一单验证。
- **🆕 财务/采购四项已完成(2026-07-08,待推 main)**:
  1. **财务请款/付款拆两个 tab**(`FinanceView.vue`:`请款审批` + `付款`,纯前端按状态分流,详见第五节)。
  2. **仓库采购收货列表按 `po_no` 合并**(`WarehouseView.vue` 新增 `groupedRecv` 树表 + 父行「合并收货」,
     复用 `/items/receive-batch`;存量数据靠 po_no 自然分组,无需迁移)。
  3. **开票号/开票日期上主汇总父行**(前端父行聚合展示 + `invoice_date` 补进 `_NEW_COLUMNS`)。
  4. **请款记录按下单采购员隔离**(`GET /payment-requests` 及 overview 待审计数由 `requester_id` 改为
     经 `PaymentRequestItem→PurchaseItem.buyer_id` 反查,详见第五节)。
  - 验证:前端 `vue-tsc --noEmit` 过;后端改动为标准 `IN(SELECT…JOIN…)` 子查询(无 GROUP BY,PG 兼容),
    已用独立 SQLite 脚本复现子查询语义(含代发起/混单场景)通过。**本机无 Postgres/无后端运行环境
    (Python 3.9 缺依赖),上线前建议在真 PG 跑一遍 `GET /payment-requests` 冒烟。**
- **🆕 用户反馈 7 条 + 采购员越权审计已处理(2026-07-08,待推 main)**:
  - 反馈:#152 采购明细供应商筛选下拉只列本人供应商(`/suppliers?owned_only=true`,排除遗留共享);
    #153 供应商账目**改回原来的平铺列表**(去掉需求十一的分类卡片下钻);#154 维护开票号弹窗不再残留
    (仅勾选同一开票号时才预填);#155 采购明细金额列宽排均匀;#156 采购单订单编号加"非项目"预设
    (固定资产/耗材等,`allow-create` 本就可手输);#157 物料需求 tab **去掉项目下拉**,直接列出有清单的项目 +
    待出库/已出库条数(新增只读端点 `GET /wh/demand-overview`,点「查看」下钻到原逐行需求)。
    #158 售后登记「以往项目(系统里没有的)可直接填名称」:`aftersales.project_id` 改可空 + 新增 `project_name`,
    登记时 project_id 与 project_name 二选一;前端 el-select 加 `allow-create` 手输名称。**⚠ 需 PG 迁移**:
    `data_migration._DROP_NOTNULL` 启动时对 PG 执行 `ALTER TABLE aftersales ALTER COLUMN project_id DROP NOT NULL`
    (幂等,仅 PG;SQLite create_all 已按模型建可空)。上线 rebuild 会自动跑。本机无 PG,未在真库验证,建议上线后确认。
  - **🐞 生产 bug 修正:采购员"建的采购明细单自己看不到供应商"**——`#152` 初版把"自己填的供应商"错按
    `Supplier.created_by==本人` 过滤,漏掉本人用了**遗留共享(NULL)或别人建**的供应商下的单;改为按
    `buyer_id` 反查(供应商出现在本人采购明细里即可见)。`/suppliers` 默认列表也放宽为
    「本人建 + 遗留共享 + 本人下过单」,保证用过的供应商始终可见/可选。**多角色已确认无问题**:
    `has_role()` 取全部角色并集(`role_codes`),`_buyer_restricted` 对 `buyer/sales` 判受限、`buyer/manager` 判放行。
  - **🔴 越权审计(用户要求"权限没控好,找 bug"),已修 6 处**(均在 `_buyer_restricted` 受限采购员下):
    1. **CRITICAL** `/statements/{sid}/export` 对账单导出**零过滤**——任意 sid 可导出含他人单据的整表 Excel。
       已加 `buyer_id` 过滤 + 无本人明细则 403。
    2. **HIGH** `/statements` 列表:供应商行筛对了,但每行金额与合计按**全量**聚合,共享供应商上泄漏他人
       收货/开票/已付额(列表合计 > 下钻明细,自相矛盾)。已改为受限时只汇总本人明细。
    3. **MEDIUM** `/items/batch-invoice` 写越权(可改他人开票状态)——加 `buyer_id` 过滤。
    4. **MEDIUM** `/suppliers/{sid}/opening-balance` GET 无归属校验——受限时校验供应商 created_by∈(本人,NULL)。
    5. **LOW** `/items/{iid}/receipts` GET 无归属校验——受限时校验明细 buyer_id==本人。
    6. **LOW** `POST /payment-requests` 未校验引用明细归属——受限时校验所有 item 均本人下单,否则 403。
    - **报表类经审计全部正确**(`_report_items` 按 buyer_id 过滤,overview/monthly-trend/by-buyer/by-project/
      top-suppliers/supplier-trend 均走它或内联同款过滤)。附:overview 期初欠款口径用 `Supplier.created_by`
      与"经手供应商"略有出入,方向是少算不泄漏,暂未动。
- **已完成**:增量清单 M01-M17 全部完成;近期批次(清单类型权限默认、隐藏单条明细、
  清空脚本、OA 抄送、OA 流程总览、请款删除、采购单 PDF 等)已推 main。
- **🆕 需求.docx 十六项已完成并推 main(2026-07-07)**:
  1. 登记安装(售后/安装共用 `aftersales.kind`) 2. 物料需求领用出库自动登记(采购收货入库本就有)
  3. 物料参考单价 `wh_materials.unit_price` + 库存总览总价 4. 合并零件收货只填总价(receive-batch 按数量分摊)
  5. 采购员"谁建谁看"(明细/供应商/报表行级隔离) 6. 库存/成本仅管理层可见
  7. 售后费用→"安装/售后费用" 8. 修复脏数据删除(FK 安全:先置 mat_file_id=NULL 再删附件)
  9. 合并父行展示送货单号/到货日期 + 可勾选合并行汇总请款 10. 设计部查库存弹窗调大(70%/70vh)
  11. 供应商账目卡片式(按分类大类汇总卡片→点击下钻供应商列表) 12. 供应商报表折线图(自建
  `LineChart.vue` SVG 组件,无三方依赖;月度采购趋势 + Top供应商趋势 `/reports/supplier-trend`)
  13. 采购明细开票号(统一维护 + 开票金额=收货金额) 14. 采购收货上传收货单图片(`receipt_doc` 附件)
  15. 仓库一键清空(确认词"清空仓库") 16. 请款审批↔付款职责分离 + 付款展示账户信息/采购单。
  - 新增列(models+`_NEW_COLUMNS`):`aftersales.kind`、`wh_materials.unit_price`、
    `suppliers.created_by`、`purchase_items.invoice_no`。**上线 rebuild 后自动 ADD COLUMN(幂等)**。
  - 验证:后端每个新端点用 httpx ASGI 实测过(含 SQLite FK 强制开启复现需求八外键场景);
    前端 `vue-tsc --noEmit` + `vite build` 均过。**沙箱无 Postgres**(Windows 机),SQL/FK 类改动
    已用 `PRAGMA foreign_keys=ON` 的 SQLite 覆盖,但上生产前若能过一遍真 PG 更稳。
- **🆕 用户反馈 #146/#147/#148 采购单号重号 bug 已修并推 main(2026-07-07)**:
  - 根因:`_next_po_no` 原用 `COUNT(DISTINCT po_no)+1` 算当日序号,**删单留下空档后会算出一个已存在的号**,
    新生成的采购单就并进老单里 → 同一 po_no 串多个供应商(账目/PDF 抬头全乱)、采购员下载看到别人的单。
  - 修复:①`_next_po_no` 改「当日现有最大后缀 +1」(永不重用已删号);②`get_order`/PO PDF 下载按
    `_buyer_restricted` 收紧(采购员只看/下自己建的单),PDF 兜底只渲染首供应商那部分。
  - **存量数据**:`data_migration.split_mixed_supplier_po`(启动幂等)把历史已被误并的
    `TH{日期}-NNN` 单,按 (供应商, 采购员, 下单日期) 拆开——最早一组留原号,其余各分「最大后缀+1」新号;
    历史导入的自定义单号不动。**上线 rebuild 后自动跑一次**,把已串台的老数据拆正。
  - 看了反馈里的内嵌截图(base64,导出 HTML 里),结论:
    - **#151 已修**:销售下单选/填「客户单位」后按同一家客户带出上次收货信息
      (新端点 `GET /sales/receiver-by-customer`,与原「同数字编号」并存)。
    - **#150 已修**:/sales 底部合计条是 `position:fixed;bottom:0`,会盖住分页/末行;给 `.pager`
      加 `padding-bottom:66px` 净空。**注**:没在用户真实屏幕上复现,若仍显示不全让用户说清是哪个元素被切。
    - **#145 经核实后端/前端本就正确**:`_group_rows` 自 2026-06-19(d3700cc)起对非主管按
      `ProduceGroupTask.worker_id==current.id` 过滤,前端组员分支也只拉 `assembly-projects`(已隔离),
      实测甲/乙装配工各自只见自己派工。**若生产上仍看到别人的,多半是那批项目确实都派给了他,或生产未部署到该版本**——deploy 即可。
    - **#146「采购单位不对」= 供方错**(截图证实:列表挂"无锡吉明"但 PDF 供方是"宁波雷宇"),
      正是重号串台,已随上条修复解决;需方抬头 `同辉智能装备有限公司` 是对的,不用改。
    - **#149 已修**:OA 报销申请新增「费用明细」多行表(费用类型/说明/金额/逐行发票),报销金额=各行合计
      (服务端按明细重算,不信前端传值);发票走 `oa_request` 附件(kind=`expense_invoice`),
      提交时把本人上传的未归属发票挂到该申请(`create_request` 里 `sa_update` biz_id=req.id,
      只认 `uploaded_by==本人 & biz_id IS NULL` 防冒挂);详情抽屉展示明细表 + 逐行发票下载。
      明细存 `OaRequest.detail.expense_items`(无需改表)。至此反馈 7 条全部处理完
      (#145 核实本就对、#146~148 重号修复、#149~151 已修/已做)。
- **待用户操作**:需求八的那条脏数据(2026-001 急急急 已驳回),现在管理层可直接在售后台账点删除;
  或仍可用 `ops/fix-deleted-aftersales.sh`。
- **待用户操作**:试运行数据清理——用户需自己在服务器跑
  `bash ops/clear-purchase-warehouse.sh --dry-run` 预览后正式执行(清采购明细+仓库,不动供应商/项目/字典)。
- **下一步开发**:按 `盈利改善功能规划.md` 推进。第一档(项目毛利+成本审计)与第二档(资金面板)
  **均已于 2026-07-11 交付**(见下方记录)。下一期 = 第三档改行为习惯:收款流水登记与核销(receipts 表,
  规划§三🥉第1条,也是 13 周现金排程流入侧精度的前提)→报价复盘→客户记分卡→比价看板/五类月报;
  随车字段(运费/OA项目/保外收费)可搭任何一期。
- 未做的长期项:金额 float→Numeric、收款流水表、人工成本分摊、变更补价字段。

## 七、给接手的 AI 助手的一句话

先读本文档 + `盈利改善功能规划.md`,再动手;改 SQL 先想 Postgres;新接口要实测;
推完代码提醒用户 rebuild;密钥永不落仓库。

- **🆕 表格版面紧凑化 + 折叠层级配色(2026-07-09，待推 main)**:
  - 全局 `.el-table`: 字号 14.5→13.5、行距 11→7px、单元格一律 nowrap+省略号(不再换行)；移除采购/仓库/财务各表的
    `wrap-cells`(原来是强制换行)——所有数据尽量一行显示、适应屏幕、更紧凑。
  - 折叠合并表(采购明细 groupedItems / 采购收货 groupedRecv)加 `:row-class-name="grpRowClass"`：父(汇总)行
    `grp-row`=主题浅蓝底+加粗；子(零件)行走 Element 自动 `--level-1`=浅灰底+左侧主题色竖条，层级/归属一眼可辨。
    全局样式在 style.css;仅这两张树表用 tree-props,不影响他处。

- **🆕 #1 隐藏「按套下单」+ #2 合并开票校验(2026-07-09，待推 main)**:
  - #1 采购明细工具栏「按套下单」按钮已注释隐藏（成套下单不再需要；已有 is_kit 成套明细仍正常显示/收货/开票）。
  - #2 合并开票（维护开票号）加校验：① 勾父(汇总)行→自动全选其下全部零件(`@select` onItemSelect 可视化联动)；
    多采购单(父行)同供应商可一起勾；② 未收货(收货金额≤0)的零件拦截，必须先全部收货；
    ③ 新增「合并开票金额(发票总额)」输入，须==Σ勾选零件收货金额(±0.01)才放行，不一致弹窗+禁用确认按钮；
    ④ 放行后每个零件 开票号/开票日期 统一、开票金额=各自收货金额、置已开票。前后端双校验
    (`set_invoice_no` 加 未收货拦截 + invoice_amount 比对；`SetInvoiceNoIn.invoice_amount`)。

- **🆕 用户反馈-2026-07-09-2 批次(2026-07-09，待推 main)**:
  - #180 采购单 PDF 需方公司名 → 「同辉智能装备（无锡）有限公司」(`_PO_COMPANY`)。
  - #179 站内消息按推送时间倒序(`created_at desc, id desc`)。
  - #177 防重复请款：同一采购明细若已有未完成(pending/approved)请款单，`create_payment_request` 拦截。
  - #176 采购明细合并(汇总父)行聚合显示 付款状态/对账状态/采购员(groupedItems 聚合;去掉列模板 `!_isGroup` 门控)。
  - #175 供应商账目下钻表也按采购单号合并折叠(`groupedDrawerItems` + tree-props + grpRowClass)。
  - 合并开票**开票日期未同步**修复：`set_invoice_no` 原 `if body.invoice_date` 选填留空则不写；改为**总是**写
    (空则默认今天 `_date.today()`)；前端弹窗开票日期改必填、默认今天。
  - 已由前批覆盖(待部署验证)：#181(王芹只看本人明细=3f7c44a)、#174/#173(折叠层级配色=c7cd9e0)、#184(版面换行=c7cd9e0)。
  - 已随「按套下单」隐藏而失效(#1)：#172/#182/#183(都是按套下单改进项)。
  - #178「请款不显示合并价格」实为**正常**：该合并单已付清(已付=收货)，请款可请=收货-已付=0，非缺陷。

- **🆕 数据清空脚本(2026-07-09)**：`ops/clear_purchase_supplier_data.sh` —— 清空「采购明细+供应商资料」及
  全部关联(请款单/审批、期初余额、收货凭证、采购收货自动入库流水[库存相应减少]、采购申请)。执行前自动
  pg_dump 全库备份、需输入 CLEAR 二次确认、单事务 all-or-nothing。用法:服务器项目目录 `bash ops/clear_purchase_supplier_data.sh`
  (容器名默认 pms2_postgres,可 PG_CONTAINER= 覆盖)。不影响物料主数据/手工出入库/项目清单/权限。

- **🆕 采购收货入/出库策略 + 订单编号字典 + 字典设置菜单(2026-07-10，待推 main)**:
  - `PurchaseItem.is_stock`(备货标记，存量默认TRUE=只入库)。收货自动过账 `_auto_stock_in`：始终入库；
    **非备货(is_stock=False)再追加一笔「采购领用」出库**(直发项目、数量=收货数量、净库存过账0)；备货只入库留库存。幂等不变。
  - **按清单下单/按套下单→is_stock=False**(入库+出库)；**新建采购单→「是否备货」开关**(默认是=只入库)，
    `PurchaseOrderCreate.is_stock`；前端新建单弹窗加 el-switch + 说明。
  - **订单编号字典**：`MaterialDict` 复用，新增 `dtype=order_no`(schema pattern 已放开)。新建采购单「默认订单编号」
    非项目值改为**只能从字典 order_no 选、不可手打**(去掉 allow-create)；项目编号仍从项目下拉选。
  - **字典设置一级菜单**(admin+manager)：`menus.ADMIN_MENU_DEFS` +`dict-admin`；前端 `views/admin/DictSettingsView.vue`
    (tab 管理 订单编号/物料类别/单位/材质/供应商分类，走 `/wh/material-dict` CRUD)；路由 `admin/dict`、MainLayout 管理组加链接、
    auth store deptMenus skip +dict-admin。
  - 前端 PO 抬头 PO_COMPANY 同步为「同辉智能装备（无锡）有限公司」。
  - **注意**：非项目订单编号需先在「字典设置-订单编号」里维护，新建单下拉才有可选值。

- **🆕 用户反馈-2026-07-09(17:00) 新增3条(2026-07-10，待推 main)**:
  - #186 单条采购收货弹窗：填「收货金额(总价)」→ 自动按数量算单价(onRecvAmountCalc，与原填单价算总价互补)。
  - #187 采购收货列表：单价列后新增「总价」列(=received_amount，父行显示合计)。
  - #185 已确认「保持现状」：finance 本就设计成可见 项目目录/项目详单/财务部/采购管理(采购管理内的
    供应商账目/请款记录是财务对账要用的)，finance_lead 同样不改，无需改动。
  - #172~#184 见前批(已修/随部署生效)。

- **🆕 全站表格/表单 UI 紧凑化(2026-07-10，待推 main)**（除 项目目录/项目详单/项目详情 三页外全部）:
  - **气泡**：19 个视图共 80 张 `<el-table>` 全部加 table 级 `show-overflow-tooltip`——单元格一行放不下时省略号+悬停气泡看全文(EP 2.14 支持表级属性)。
  - **操作列**：40 个 `label="操作"` 列全部补齐 `fixed="right"`(缺20处) + `:show-overflow-tooltip="false"`(缺37处)——小屏横向滚动时操作按钮恒可见、不被气泡遮挡。
  - **全局 style.css**：表单行距 18→14px、label-top 标签贴近控件；el-alert 提示条压矮(12.5px)；tabs 头距 12px；
    表格内小按钮 3px 10px/12.5px、link 按钮加字重。**新增 ≤1440px 窄幅笔记本媒体档**(表格 12.8px/行距5px、
    按钮/卡片/KPI/弹窗同步收紧)，与既有 ≤800px 高度档、94vw 弹窗上限配合适配小笔记本。
  - 验证：vue-tsc ✓、vite build ✓、1366px 密度 mock 截图 ✓。排除页未动(ProjectsView/OverviewView/ProjectDetailView 0 处改动)。

- **🆕 4件套(2026-07-11，物料编码/库位/设计师请购/支出总览)**:
  - **物料编码体系**：`MaterialCategory` 3级分类树(段码:大类1位/中类2位/细分2位)，编码=前缀+4位流水
    (1·01·01→101010001)。字典设置新增「物料编码分类」树管理 tab；物料主数据选到细分类保存自动发码
    (`_gen_material_code`,改分类自动重发码)；物料表/库存总览加编码列。删除分类有子类/被物料引用会拦截。
  - **库位填写点**：`PurchaseItem.stock_location`。⚠️**2026-07-12 已调整为「仓库收货时填」**(见下方 #204 条),
    原「采购下单三路必填」已废除;采购明细/收货列表仍显示同一字段(现由收货写入)。
  - **设计师请购单**：`_PREQ_WAREHOUSE`+designer/design_lead(与仓库采购申请同一后端流程/推送/行级隔离)；
    设计工作台新增「🛒请购单」tab(提单+指定采购员+我的列表)。
  - **财务支出总览**(=盈利改善规划第一档的第一块)：`GET /api/finance/expense-overview?year=`——
    采购付款(按付款日期)+安装/售后费用(已审批)+OA业务/报销(已审批,核定金额优先,采购大类不计防双算)
    按月汇总；财务部新增「💸支出总览」tab(年份/4KPI/月度表/未记日期提示)，已注册进 TAB_REGISTRY。
    项目毛利红黑榜+成本黑洞审计已随后于 2026-07-11 完整交付(见下一条)。

- **🆕 盈利改善第一档完整交付(2026-07-11，项目毛利红黑榜+成本黑洞审计)**:
  - **1a 项目毛利榜** `GET /api/reports/project-pnl`(finance/finance_lead+管理层)：逐项目
    毛利=合同额−材料领料(加权均价)−直发/外协采购(received_amount,按 project_code)−安装/售后(已审批)。
    **防双算**:收货已生成 wh_txn 的采购明细走仓库领料腿(领料×均价),`id.not_in(有purchase_item_id的txn)`
    的才计直发腿,两腿互斥。每行带成本完整度 flags(无销售台账/合同额为0/领料缺价N项)。
    财务部新增「📈项目毛利」tab：口径警示条(**不含人工/运费,材料边际贡献**)、亏损行淡红、
    按客户/客户类型/销售员/订单类型/机型/年份前端分组汇总、售后侵蚀Top5(机型/客户切换)。
  - **1b 成本审计** `GET /api/reports/cost-audit`：①无主领料(out无项目,×均价估值,排除已明确
    「非项目领用」的) ②无价入库(收货流水 amount NULL,join 采购明细标注"可回填") ③孤儿采购
    (project_code 既非项目编号也非字典 order_no) + 双口径对账(采购收货金额 vs 领料×均价,差异倒序)
    + 本月/累计未归集合计。修复动作：`PATCH /reports/cost-audit/txns/{tid}/project` 补选项目、
    `POST /reports/cost-audit/backfill-prices` 一键回填。财务部新增「🕳️成本审计」tab(4张卡片式子tab)。
  - **修真 bug(规划§二#2)**:`PUT /purchase-mgmt/items/{iid}` 补价现在**回写**该明细所有非冲红
    wh_txn 的 unit_price/amount——此前补价只更新 PurchaseItem,收货流水永久无价。
  - **冲红口径修正(规划§二#4)**:`_avg_price_map`/`project-cost`/`_demand_rows.issued_map`/
    pnl 聚合统一加 `reversed == False`——此前被冲红的原单仍计入加权价与项目成本。
  - **出库必选项目(规划§二#1 流程约束)**:`POST /wh/txns` 出库无 project_id 时必须
    `non_project=true`+原因,否则 400;前端出库弹窗项目必选+「非项目领用」勾选框。
  - **售后侵蚀率报警(1a 附带)**:售后审批通过时,项目累计已审批售后费÷合同额>5% →
    push_message 给 manager 角色(warn)。
  - TAB_REGISTRY finance 加 ("pnl","项目毛利")/("audit","成本审计"),支持按账号授权。
  - **⚠️ 顺手修了上一批(d884356)的启动崩溃**:`warehouse_router._DICT_ADMIN_ROLES` 定义在
    使用之后(L396 定义、L287 使用),Depends 默认参数 import 时求值 → 整个后端 NameError 起不来,
    `ast.parse` 查不出(语法合法)。已把定义前移。**若生产已部署 d884356/81d4da3 会启动失败,
    必须立即拉本次提交重新 --build**。教训已补进第二节:改后端必须真实 import/起服务验证。
  - 验证：py3.9 venv+future-annotations 副本起真服务(sqlite),三项目/冲红对/无价入库/孤儿采购
    全场景实测——pnl 三腿手算核对一致、防双算与冲红排除生效、四清单/两修复动作/出库400拦截/
    售后预警消息全部按预期;vue-tsc ✓ vite build ✓。Postgres 沙箱本机不可用,SQL 均为既有
    portable 模式(group_by 全列/not_in 子查询已滤 NULL),部署后建议点开两个新 tab 冒烟。

- **🆕 盈利改善第二档交付(2026-07-11，资金面板)**:
  - `GET /api/reports/fund-panel`(finance/finance_lead+管理层)一个接口五段数据,
    财务部新增「💰资金面板」tab(顶部4 KPI+五个卡片子tab),TAB_REGISTRY 加 ("fund","资金面板")。
  - **①逾期应收账龄**:尾款(balance>0,balance_date<今天)+发货款(ship_receivable>0,按 ship_date),
    30/60/90/90+ 分档,按客户/销售 Top10 排名。※四段款是合同条款数字,预付/发货前付无日期不进账龄。
  - **②预付敞口**:paid_amount>0 且未到货,按供应商汇总押款金额+最早付款日起押款天数。
  - **③应付账期利用**:到期日=到货日+Supplier.credit_days;逾期未付(断供风险)/14天内到期(排程付款)/
    提前付统计(合计+平均白放弃免息天数)/未维护 credit_days 的供应商清单(提示补主数据)。
    **请款审批/付款列表每张单加「账期到期」标签**(_pr_out 新增 earliest_due/due_in_days=
    min(明细到货日)+credit_days,schemas.PaymentRequestOut 同步)。
  - **④呆滞库存**:≥90天无出库动销(从未出库按最后入库日),现存×加权均价,90/180/365 分档,
    经 purchase_item_id 回溯采购员+项目;附安全库存体检(safety>2×月均出库=偏高压钱/现存<safety=常报警)。
  - **⑤未来13周现金排程**:流入=尾款按约定日挂周(已逾期进W0),发货款无日期单列;流出=已批未付请款(W0)
    +应付到期按周+OA pending_payment(W0)。**防双算:已批请款关联的明细从应付排程剔除(仅 approved,
    pending 不剔——其请款金额不计流出,明细仍按到期排)**。累计净额为负=缺口行标红。
  - **尾款逾期每周升级催办**(补掉"终身只提醒一次"盲区):overdue.py 新增 `scan_balance_overdue`,
    已挂进 scheduler——逾期第1周催销售本人、第2周+销售主管、第3周起+管理层,每7天一遍
    (biz_type=balance_overdue,按最近一条消息时间做7天窗口去重);原到期前14天的一次性提醒保留。
  - 验证:真服务实测——账龄分档/预付60天/逾期应付40天/提前付白放弃25天/缺账期清单/呆滞两档+
    从未出库标记/安全体检/13周排程(W0=110000流入、10000流出)全部与手算一致;周催办跑两遍验证
    7天去重与升级层级;请款单 due_in_days=-40 正确;vue-tsc ✓ vite build ✓。

- **🆕 用户反馈批次 2026-07-11(#188~#198,2修+6优化+3告知)**:
  - **BUG#188 打包下载超时**:axios 全局 30s 超时掐断大文件(zip/CAD)下载→报"请求超时/打包
    下载失败"。修法:请求拦截器对所有 `responseType==='blob'` 的请求 `timeout=0`(nginx 300s 兜底)。
  - **BUG#196 电工采购单'项目'列=项目编号**:`_populate_elec_po_from_excel` 原来硬编码
    '项目'列填项目编号,把电工 Excel 的物料名称整列丢掉(该列口径=物料名称,同标准件清单)。
    已改为优先取 Excel 的 项目/名称/品名/物料名称 列,空才回退项目编号;
    存量由迁移 `fix_elec_po_item_names` 修复(只动"全部行==项目编号"的表,按原 Excel 行序回填,
    行数不匹配跳过)。
  - **#197 电工接线完成即完成**:`electric_done` 现在=status done+计考核(同设计部口径);
    电路图上传/发货准备挪到「已完成」tab(资源弹窗 shipprep=电路图),`output-upload`/`ship-prep-done`/
    附件替换均放行 done 态电工单。
    **止损迁移(用户确认"尽快止损")**:`close_legacy_electric_done_orders` 把卡在旧二步流的存量单
    (in_progress+electric_done_flag)置完成,done_date=部署日(系统没记接线完成日,取部署日比继续
    累计逾期准);配套 **reopen 现在会清 design/electric_done_flag**(改回=返工回到未完成状态机,
    也是迁移不误关返工单的前提)。验证:2张卡住置完成/普通单不动/幂等/reopen后不误关。
  - **#198 电工部请购单**:`_PREQ_WAREHOUSE/_PREQ_VIEW` + electrician/electric_lead;
    电工工作台显示「🛒请购单」tab(与设计/仓库同流程)。
  - **#195 每张进度表加「库位」列**:五张模板+电工采购单列尾追加(⚠️必须追加在末尾——Excel导入按
    位置映射);老形态全部登记进 LEGACY_SHEET_TEMPLATES 防 cleanup 清表;存量表由
    `add_location_column_to_known_sheets` 迁移补列(验证:补列后数据行保留、cleanup 不误清)。
  - **#194 钣金/装配组换人**:`POST /produce/group/{task_id}/reassign`(pm_lead+管理层,
    校验组角色/已完成拦截/通知新负责人);两组 tab 派给列加「换人」按钮。
  - **#193 问题反馈带现场照片**:`POST /feedbacks` 改 multipart(files 多张选填,
    biz_type=feedback 已加进附件白名单);FeedbackRow.images;提交弹窗选图、
    审批/接收列表点📷预览(带鉴权 blob)。
  - **#191 供应商 Excel 批量导入**:`GET /purchase-mgmt/suppliers/import-template` +
    `POST /purchase-mgmt/suppliers/import`——按名称匹配,已存在只补空缺字段不覆盖,新建 created_by=
    导入人(保持采购员隔离口径);供应商账目 tab「批量导入」下拉。
  - **非问题(已告知用户)**:#189/#190 供应商分类加值=字典设置自助(buyer_lead/管理层);
    #192 设计部请购单已在 324917d 上线。
  - 验证:真服务两轮(迁移修复#195/#196 实测回填与不误清、电工完成流/请购/换人/带图反馈/
    供应商导入全部实测通过,含新上传解析别名+空名回退);vue-tsc ✓ vite build ✓。

- **🆕 库位管理+权限统一+装配表编辑批次(2026-07-11)**:
  - **库位管理**:新表 `wh_locations`(name unique/note/enabled/sort_order),仓库菜单新增「库位管理」tab
    (仓管维护;改名级联更新物料与未收货采购单的库位;在用库位删不掉只能停用)。
    `wh_txns.location` 新列(_NEW_COLUMNS):入库=放到哪(手工入库可选,默认物料当前库位,选了回写物料;
    采购收货=下单选的库位并**更新物料当前库位**——单库位模型,最近收货处=当前库位),
    出库=从物料当前库位领,冲红复制原单库位。采购下单三路径库位字段改为从 /wh/locations 下拉。
  - **⚠️ 取消收货自动出库**:_auto_stock_in 里 is_stock=False 自动「采购领用」出库块已删——
    收货一律只入库;出库统一走仓库(出入库登记/物料需求一键领用)挂项目计成本。
    「是否备货」开关从新建采购单表单移除,is_stock 列保留(历史数据),下单不再写 False。
    **口径影响**:按清单下单的料收货后留在库存,须由仓库领料出库到项目,项目材料成本才归集。
  - **生产部请购单**:_PREQ_WAREHOUSE 加 pm_lead/production_clerk/assembler/sheetmetal,
    生产部工作台显示「🛒请购单」tab(与设计/电工/仓库同流程)。
  - **权限统一(用户要求:二级菜单可见性只由权限页决定)**:去掉写死的 tab 角色限制——
    采购管理 采购部/采购申请/请款记录 tab 的 showPurchaseTab(buyer族)前置、汇总报表的
    isLeadOrAbove 前置、财务 库存/成本 tab 的 isManager 前置,全部改纯 tv()。
    配套放宽读接口:/wh/inventory-value与/wh/project-cost→finance/finance_lead;
    downstream /purchase/* → +buyer_lead/finance/finance_lead;_PURCHASE_ROLES/_PREQ_VIEW→+finance_lead等。
    **默认可见性有变化**:财务账号现在默认能看到采购部/请款记录/库存成本等 tab,
    不该看的去「用户管理-二级菜单权限」按账号隐藏(admin/manager 仍豁免)。
  - **钣金装配表编辑增强**(SheetmetalGrid,钣金/装配组共用):日期列(名称以"日期"结尾)→日期选择器;
    进度列→完成/进行中下拉;其他列→el-autocomplete 下拉复制本列已有值(仍可自由输入);
    悬停单元格出现 ⬇ 填充柄=把该值向下复制到本列所有空单元格(逐格调 produceUpdateCell)。
  - 验证:真服务——库位CRUD/重名拦截/改名级联/在用删除保护;下单选库位→收货只入库(无自动出库)
    流水带库位;手工入库换库位更新物料/出库记当时库位;生产内勤请购 200;财务调
    inventory-value 与 purchase/projects 200。vue-tsc ✓ vite build ✓。

- **🆕 人事部一期(2026-07-11,设计稿见《人事部功能设计.md》,用户确认"先做一期")**:
  - 新一级菜单 `hr`「人事部」(hr 角色+管理层;hr 角色 ROLE_MENUS 追加,老权限不动);
    TAB_REGISTRY 加 hr:roster/payroll;新路由 /hr → HrView.vue(导航图标 Avatar)。
  - **员工花名册** `employees` 表(姓名/部门=OA departments/岗位/入职/转正/合同到期/状态
    试用·在职·离职/身份证选填/电话/紧急联系人/可选关联登录账号 user_id/备注)：
    CRUD+筛选+KPI(在职/试用/30天内合同到期/本月入职离职)+Excel导入(部门名不匹配跳过并提示,
    重名非离职只补空缺);状态改「离职」自动填离职日期,挂了账号的推送 admin 提醒停用。
  - **部门月度工资总额** `payroll_monthly`(month+department_id 唯一)：按月整表录入/覆盖,
    年度逐月汇总。**这是盈利改善规划§五-1「含分摊人工」毛利口径的数据源**——
    下一步(第三期)毛利榜读此表按部门任务时间戳权重分摊,纯只读计算。
  - **到期提醒** overdue.scan_hr_reminders(已挂 scheduler):合同到期前30天(含已过期)、
    试用期转正前7天,推 hr+管理层,按员工7天窗口去重。
  - 两新表由 create_all 自动建(无需手工迁移)。二期(证件台账)/三期(OA请假+人工分摊口径)见设计稿。
  - 验证:真服务——hr 菜单下发/非 hr 403/CRUD/KPI(临期合同计数)/导入(坏部门行跳过)/
    工资 upsert+年度汇总/提醒扫描两遍验7天去重/离职联动 admin 提醒+自动填离职日期;
    vue-tsc ✓ vite build ✓。

- **🆕 用户反馈 #199/#200(2026-07-11,OA两条)**:
  - **#200 流程级固定抄送**:新表 `oa_flow_cc`(department_id+doc_type+role_code 唯一)——
    「审批流程配置」页步骤表下新增「自动抄送」多选角色+保存(整组覆盖 PUT /oa/flow-cc);
    提交申请时该配置角色的在职用户自动进抄送(与手选抄送合并去重,同样收【OA抄送】通知)。
    提交表单里原有的手选抄送人保持不变。
  - **#199 管理层删除申请单**:`DELETE /oa/requests/{rid}`(仅 admin/manager)——附件文件一并删、
    报销↔业务的 related_request_id 引用置空、步骤/抄送级联删,操作写审计;
    「全部申请」tab 操作列加「删除」。
  - 该导出里 #188~#198 为上一批已修条目(导出时状态未更新),无需重复处理。
  - 验证:真服务——配链+配抄送→销售提单→财务自动收抄送通知;删除 销售403/管理层成功,
    步骤/抄送/附件级联清零;vue-tsc ✓ vite build ✓。

- **🆕 表格列宽整齐化(2026-07-11,用户反馈"有些列特别宽有些列窄")**:
  - **排查(忠实复刻 Element Plus updateColumnsWidth 做 headless 截图)确认两个根因**:
    ①**数字列被设成 min-width(flex)**→宽屏下按 min-width 比例被拉伸,数字列出现大空隙(库存总览
    现存/单价/总价/安全库存);②**内容少的表只有 1 个 flex 文本列**→该列独吞全部余量,宽屏下爆到
    ~1000px+,其余列却窄(采购应付/预付敞口的供应商列等)。
  - **修**:①库存总览 数字/短列(现存/单价/总价/安全库存/单位/库位)min-width→固定 width 右对齐,
    只留 名称/规格型号/类别 flex 填充;②对"单文本列+若干数字列"的内容少表(采购应付/预付敞口/
    安全库存体检/13周现金/项目毛利分组/采购汇总月份/物料需求总览)加 `:fit="false"`——列宽按各自
    设定不拉伸、表格紧凑左对齐、右侧留白(类电子表格),避免单列爆宽;供应商列 min-width 提到 220 保证
    完整显示。多 flex 列的表(待开票/项目毛利明细/出入库流水/花名册/采购明细)本就均匀填充,未动。
  - **口径**:数字/日期/状态/编码/操作/单位=固定窄列;名称/规格/客户/供应商/备注=flex 文本列填充。
    后续新表沿用此口径。若某表希望恒满宽拉伸,去掉其 `:fit="false"` 即可。
  - 验证:忠实 EP 算法 mock 前后对比截图确认整齐;vue-tsc ✓ vite build ✓。
    ⚠️踩坑:批量给 `<el-table>` 注入 `:fit="false"` 的脚本若用"预取 finditer 位置+循环内改串"会
    位置漂移损坏文件;且正则 `[^>]*>` 会被属性里的箭头函数 `=>` 的 `>` 截断(13周现金的
    :row-class-name)。改为**逆序replace**并对含 `=>` 的标签手工修。

- **🆕 用户反馈 #201/#202/#203(2026-07-11)**:
  - **#203 增加"管理部"部门**:backfill_oa_departments 默认部门加 ("管理部","manager"),幂等,
    存量库启动补建→HR/OA 部门下拉出现。
  - **#202 企业员工工号**:Employee.emp_no(String16 唯一);创建/导入自动=现有数字工号 max+1 补零5位
    (00001);离职是状态变更不删→工号天然保留不复用(录错物理删才可能空号)。花名册表首列+编辑弹窗
    只读展示;存量由 backfill_employee_no 按 id 顺序补号。data_migration `_NEW_COLUMNS["employees"]`。
  - **#201 物料运输费**(=盈利改善规划"随车搭载"运费项落地):Shipment 加 freight_cost/freight_payer
    (我方/到付)/freight_note;物流发货部列表加"运费"列+录入弹窗(`PUT /logistics/{sid}/freight`,
    发货前后均可维护)。**我方**承担的:进财务「💸支出总览」(按发货月,新增运费列+KPI)与
    「📈项目毛利」(新增运费腿,毛利口径注释改为已含运费、不再"不含运费");**到付**=客户承担不计。
    data_migration shipments 段加 freight_cost/payer/note。
  - ⚠️`_NEW_COLUMNS` 里 "purchase_items" 键出现两次(dict 后者覆盖前者)——本次给 shipments 段
    追加、并新增 employees 段,避开重复键。历史那个重复是既有遗留,存量列已在,未动。
  - 验证:真服务——管理部入列;工号 00001/00002/00003 且离职后新员工 00004 不复用;运费我方
    进支出总览月列(发货后)与项目毛利运费腿,改到付后两处均归0;vue-tsc ✓ vite build ✓。

- **🆕 用户反馈 #205(2026-07-12,采购申请通知)**:王芹反馈"采购申请推送给别人的看不到"。
  排查:列表本就全员可见(实测 buyer 能看到指派给别人的申请),真问题是**通知只推给「指定采购员」
  那一位**——`create_purchase_request` 里 `if body.buyer_id: 只 db.add 给该 buyer`;且原"无指定"
  分支的采购员查询只按主角色(漏掉 buyer 为副角色的用户如王芹 buyer+finance)。修:提交采购申请时
  通知**全体采购员家族(buyer/buyer_lead/buyer_standard/buyer_outsource,主+副角色并集去重)**,
  指定的那位文案点名"指派给 X"。实测:指派给李新新时王芹也收到通知。#204 追问后明确为新功能(见下);#206(考勤板块)待用户定打卡/月度汇总口径后做。
- **🆕 用户反馈 #204(2026-07-12,库位占用/空闲)**:用户想要中转区等库位有「占用/空闲」状态、且跟
  出入库流水联动。实现:`WhLocationOut` 加 `occupied`+`occupied_items`;`list_locations` 按
  `_stock_map` 算——库位上有物料现存>0=占用(否则空闲),占用内容列出物料×现存。纯派生(不存状态,
  每次读实时算),入库到该库位→占用、出库腾空→空闲。仓库「库位管理」tab 加"占用状态"列(占用N项/
  空闲,悬停看内容)。实测:入库5台电机→占用、出库→空闲。
- **🆕 UI 重构·品牌换肤(2026-07-12,设计稿驱动)**:按《网站样式重构.zip》把品牌 蓝→金+navy。
  **纯样式,零业务逻辑变更**(约束:不动前后端逻辑/不引次生缺陷)。改 3 文件+1 资源:
  ①`src/assets/logo.png`(设计稿 logo,登录+侧栏);②`LoginView.vue` 视觉重做(深蓝科技底+毛玻璃卡+金按钮,
  登录逻辑 username/password/onSubmit 一字未动;设计稿的「记住登录/忘记密码」**未加**=新逻辑违约束);
  ③`style.css` 主题变量:`--primary` 蓝→金 #c8a24f(**填充**);新增 `--link` #9a7220(**白底文字/链接/编号**,
  金做文字不可读);`--brand-navy` #16293f(侧栏/表头);表头 navy+浅字+排序箭头适配;功能色不动;
  ④`MainLayout.vue` 侧栏真 logo、active 金左条+金字、头像/角标金。**口径:金=填充,--link=白底文字,navy=深色面**。
  验证:登录 standalone 渲染 ✓;真 EP CSS+新 style.css 组件 mock 对比度达标 ✓;vue-tsc/vite build ✓。
  - **🆕 补:交易页统一(2026-07-12,反馈「只统一了侧边框」)**:线上 CSS 实为已换金(抓包 `c8a24f`✓/`2563eb`已无),
    交易页里的「蓝」多是 `var(--primary,#2563eb)` **回退值**、已随主题变金;感知不统一是缺**设计签名**。
    补两处全局(纯样式):①`.page-header h1::before` 金色标题条(全站 24 页头统一,设计稿签名元素);
    ②`.el-tabs__item.is-active` 选中标签用可读深金 `--link`+金下划线(金 `#c8a24f` 白底做文字太浅)。
    验证:交易页 mock(真 EP CSS)标题金条/金标签/navy 表头/金编号/金 KPI 一致 ✓;vite build ✓。
  - **🆕 补:表单/弹窗品牌感(2026-07-12,反馈「表格表单按设计稿美化了吗」)**:
    核实设计稿(zip)仅 2 张——登录页 + 项目目录**表格页**(app.png),**无系统内表单/弹窗稿**。
    表格已对齐 app.png(navy 表头/金编号/状态剩余语义色/干净行)。表单跟随主题,补 1 处全局(纯样式):
    `.el-dialog__title::before` 金色小竖条,与页头标题条呼应,让弹窗表单也带品牌签名。vite build ✓。
    未做(需动视图层、待用户确认):app.png「导出 Excel」是金描边幽灵按钮,现次要按钮为中性灰描边。
- **🆕 员工紧急联系人电话独立列(2026-07-12)**:原「紧急联系人」姓名+电话挤一格,拆出
  `Employee.emergency_contact_phone`(VARCHAR32,可空)。改:models + data_migration(employees 加列,
  与 emp_no 同 ADD COLUMN 模式)+ hr_router(EmployeeIn/_emp_out/导入列「紧急联系人电话」)+ HrView(表单加输入框、
  列表姓名后灰字显电话)。setattr 循环自动流转 create/update。验证:sqlite 迁移+读写+Pydantic round-trip ✓、vue-tsc ✓。
- **🆕 物料编码显示加横线 + 弃用物料类别 + 编码列说明(2026-07-12,纯前端)**:
  ①`utils/format.ts` 加 `fmtMatCode`:9 位纯数字编码(大类1+中类2+细分2+流水4)显示为 `1-0101-0001`,
    **仅改显示、不动存储值**(非标编码原样返回,零迁移风险)——用户明确选「仅展示加横线」。
  ②WarehouseView 两张物料表 code 列套 `fmtMatCode` + 表头加 `QuestionFilled` 悬浮说明;表单 disabled 编码框也套。
  ③弃用「物料类别」(自由文本 category,与编码分类树重复):隐藏两列+表单下拉+移除 matCatOptions;
    **DB 字段 category 保留不删**,编辑时原值不动。搜索仍按原始 code;后端无编码 Excel 导出,故未动后端。验证:vue-tsc ✓。
  ④**编码文字说明(用户澄清:对每个编码进行文字说明,#5)**:每个物料编码是按编码分类树发的,故「说明」=**该编码解码出的分类路径名**。
    后端 `WhMaterialOut.category_path`(`_category_path_map` 从 category_id 沿树向上取「大类/中类/细分」名,带环保护),
    `list_materials` 填;前端两张物料表在 code 列后加「编码说明」列显示路径(如 1-0101-0001 → 机械件/轴承/深沟球轴承)。
    (表头 QuestionFilled 讲格式规则、编码说明列讲每个编码含义,互补保留。)验证:真分类树 category_path 实测 ✓、vue-tsc ✓。
- **🆕 考勤 + 个人工资(人事按月每人录入)(2026-07-12,#206 落地)**:用户定「人事自己录入考勤,并录每人工资」。
  新增 2 表(create_all 自动建,无需迁移):`AttendanceMonthly`(employee_id+period 唯一;应出勤/实出勤/请假/加班工时/备注)、
  `EmployeeSalaryMonthly`(同唯一键;基本/绩效/加班费/补贴/社保公积金扣款/其他扣款,**实发=前四项−后两项,computed 不落库**)。
  **区别于 PayrollMonthly**(那是部门汇总)。后端 hr_router 加 4 端点:GET/PUT `/hr/attendance`、`/hr/salary`,
  全部 `require_roles(*_HR)`(=hr+admin+manager),故工资敏感数据天然只对人事/管理层;upsert 按 employee_id 幂等。
  前端 HrView 加两 tab(考勤🗓️/工资🧾):月份选择+逐人可编辑表(el-input-number)+保存本月;工资表实发列前端 salNet 实时算、
  与后端 _salary_net 一致。tab 权限走 tabVisible(默认可见,整个 /hr 已限人事+管理层)。
  验证:py3.9+httpx ASGI 在进程内实测——建表/建员工/考勤 GET-PUT-GET-UPSERT/工资 GET-PUT-GET+实发 7300 核对全过 ✓;vue-tsc ✓。
- **🆕 #204/#3 材料库位跨页展示(2026-07-12,进行中)**:目标——库位展示给采购明细/装配组/钣金组/设计部/仓库。
  排查发现**多处已有库位**:采购明细(`stock_location` 列已在)、仓库物料主数据/总览/流水/收货(均有)、设计部「查库存」弹窗(有)。
  本次补:**仓库·物料需求表**加库位列——`WarehouseDemandRow.location`,`_demand_rows` 用已解析的 `m.location` 填(名称+规格→WhMaterial);
  前端 demandRows 表加「库位」列 + DemandRow 接口加 location。验证:py_compile + vue-tsc ✓。
  **装配组/钣金组**(用户澄清:不是往钣金装配表里加列,是在**装配组/钣金组各自的项目列表**里加):
  两处项目表加「材料库位」列,值=`material_locations`——**该项目入库流水(WhTxn direction=in、非冲红、location 非空)去过的库位,去重保序**。
  后端:produce_router `GroupProjectRow.material_locations` + `_project_loc_map` 助手,`_group_rows` 统一填(覆盖装配组 assembly + produce 钣金);
  downstream_router `SheetMetalRow.material_locations` 内联同款聚合(覆盖 SheetMetalView 钣金组)。
  前端:DeptWorkbenchView 装配组表 + SheetMetalView 钣金组表各加一列,库位用 `el-tag warning plain` 展示;orders.ts GroupProjectRow 加字段。
  验证:sqlite 聚合语义(去重/去出库/去冲红/去空)✓、py_compile ✓、vue-tsc ✓。
  请购单(设计部)items 多为「待采购未入库」,库位常空、价值低,暂不加。
- **🆕 #204 收尾:库位改由收货填 + 材料库位补齐设计/电工/钣金tab(2026-07-12,用户追问「你到底把库位设计在哪」)**:
  用户审计发现之前实现**不一致**(只在装配组tab+钣金独立页加了列,且数据源接的是入库流水→当时全空)+
  两处漏加(**钣金组工作台tab、设计部/电工部**)。用户明确 ①库位数据源**只用实际入库库位**(空到收货为止=正确);
  ②**取消采购下单填库位,统一仓库收货时填,数据同步出去即可**。据此:
  - **库位录入点迁移(动了业务流,用户明确要求)**:采购下单三路径(新建/清单/按套)去掉库位字段+必填校验,payload 传 null;
    `PurchaseReceiveIn`/`BatchReceiveIn` 加 `stock_location`;`receive_item`/`receive_batch` 收货时写回 `item.stock_location`
    (填了才写、不覆盖为空)→ 既有 `_auto_stock_in` 顺势同步到 `WhTxn入库.location` + 物料当前库位(下游逻辑未改)。
    仓库收货弹窗(单收/合并)加库位**必选**下拉(取 enabledLocations);采购明细库位列不变(现由收货填的值驱动)。
  - **材料库位补齐**:钣金组**工作台tab**(sheetmetalRows 早有 material_locations,只差前端列)+ **设计部/电工部任务跟踪表**
    (`OrderOut.material_locations` 新字段;`list_orders` 加 `loc_map` 按 project_id 聚合入库库位;`_order_to_out` 透传;
    orders.ts DeptOrder 加字段;track 表在项目名后加列)。装配组/钣金独立页沿用原列。
  - 验证:**进程内 httpx ASGI 全链路实测**——下单不填库位→单收/合并收货填库位 A-01/B-09→
    采购明细.stock_location、入库流水.location(挂对项目)、物料.location、设计部 orders.material_locations **全部同步一致** ✓;vue-tsc ✓。
- **🆕 HR「登录账号」绑定闭环(2026-07-13,用户反馈「感觉是个孤儿」)**:`Employee.user_id`(员工↔系统账号)之前
  有列有字段但**无绑定入口**(表单无选择器、导入无此列)→ user_id 永远空、登录账号列永远「—」、离职停用提醒永不触发=孤儿。补全:
  ①后端 `GET /hr/bindable-users`(列可绑账号,标注已被别的员工绑定的);②`_check_user_unique`(一账号只绑一员工,
  **在员工写入前**查——放 `_apply_emp` 里会 autoflush 半成品员工报 name NOT NULL,故移到 create/update 端点开头)。
  ③前端 HrView 员工表单加「登录账号」选择器(filterable/clearable,已绑他人的选项禁选+提示「已绑 X」),onMounted 拉列表。
  绑定后:花名册列显示账号名、员工改离职→admin 收「停用账号」提醒(既有逻辑,现终于可达)。**未登录系统的车间工人可不绑**。
  验证:进程内 ASGI 全链路——可选账号→绑定→花名册显示→唯一拦截 400→离职提醒触发 ✓;vue-tsc ✓。
- **🆕 用户反馈第5批(#221/#222/#223)(2026-07-13)**:（注:本批开工时发现 origin/main 已有并行开发——封板组#209+反馈第3/4批#208~#220，已 ff 合并后在其上做第5批）
  - **#223 封板组(sealing)进「请购单」tab 无权操作(403)**:封板组作第三生产组(#209)漏加进请购单角色白名单
    `_PREQ_WAREHOUSE`(原只有 assembler/sheetmetal)→ sealing 用户 GET/POST 请购单都 403(前端全局拦截器把 403 弹「无权操作」)。
    修:白名单补 `"sealing"`(GET 走 `_PREQ_VIEW`=白名单+采购/财务，POST 走白名单)。
    验证:进程内 ASGI——建 sealing 用户→GET 请购单 200、POST 提请购单 200(修前 403)✓。
  - **#221 收货单价小数位太多**:采购收货(单收/合并)单价 `el-input-number :precision` 4→2(收货金额本就 2 位);
    `onRecvAmountCalc` 反算单价 `toFixed` 4→2。纯前端。
  - **#222 仓库·物料需求表太紧凑**:现有库存/库位/建议采购三列挤一起(#204 加的库位列 96px 夹在两数字列间)。
    加宽:需求量 90→104、现有库存/建议采购 100→112、库位 96→`min-width 130`。纯前端。vue-tsc ✓。
- **🆕 封板文件(机架图/横梁图)设计→封板组 + 封板组 tab 显钣金装配表(2026-07-14,建在并行组「封板组#209」之上)**:
  用户要:设计推送 机架图/横梁图 给封板组;封板组能看 钣金装配表+激光件清单(后两者 #209 已取数,但钣金装配表之前没在封板组 tab 显示)。
  - **设计部加「封板文件」产出(配置驱动)**:`dept_config` design.start_outputs 加
    `{k:"sealing_pkg", label:"封板文件(机架图/横梁图)", to_role:"sealing"}` —— 设计工作台起始上传区是 `v-for start_outputs`
    **自动多出这个上传槽**,上传即走既有通用推送(`to_role`)通知封板组;`orders_router` start-upload「完成后仍可换」豁免
    kind 加 `sealing_pkg`(与 CAD激光图纸同口径)。
  - **封板组 tab 显示**:`produce_router._laser_files_by_pid` 加 `kind` 参数(默认 `sheetpkg` 不破坏原激光图逻辑),
    group=sealing 时多取 `kind="sealing_pkg"` → `GroupProjectRow.sealing_files`;前端封板组 tab 把「激光图」列改成
    **「推送资料」列**,集中显示 钣金装配表(`viewSheet` 只读,封板组无编辑权)/激光件清单/CAD激光图纸/封板文件 下载。orders.ts 加字段。
  - 验证:进程内 ASGI 全链路——options 出封板文件槽→设计上传机架图/横梁图→封板组 sealing-projects 见 2 文件+钣金装配表字段齐→
    上传即推送 sealing 角色收到通知 ✓;vue-tsc ✓。
