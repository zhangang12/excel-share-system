# 个人待办功能设计（**已确认，按此实施**）

> ✅ **2026-08-12 业务确认结果**（覆盖下文里所有「❓待定」）：
>
> | 问题 | 决定 |
> |---|---|
> | 角标怎么算 | **两个加起来算一个数**（管理层待办未回复 + 个人待办未完成）——与第五节我原先的建议相反，按业务定的来 |
> | 挂到项目上 | **要** |
> | 紧急/普通两档 | **要** |
> | H5 入口 | **底部「我的」里** |
> | 到期推企微 | **推，到期当天推一次** |


> 背景：反馈 **#363（赵仁辉）「给我增加个人待办，单独开一列」**、
> **#381「管理层待办，增加一个个人待办功能，跟 363 一样」**、
> **#382「个人待办的功能同步做到 APP 上」**。2026-08-12。
>
> 这是新模块，按约定先出设计稿给业务确认，再写代码。**下面每一节都可以直接改**，
> 尤其是第三节的字段和第五节的取舍——不确定的地方我都标了「❓待定」。

---

## 一、先说清楚它和现有「管理层待办」的区别

系统已经有 `management_todos`（管理层下发 → 收件人回承诺时间 → 报进度 → 完成）。
个人待办**不是**它的翻版，两者是不同的东西：

| | 管理层待办（已有） | 个人待办（本方案） |
|---|---|---|
| 谁建 | 管理层给别人下发 | **自己给自己记** |
| 有没有交代 | 有：要回承诺完成时间、报进度、可申请延期 | **没有**，就是自己的备忘 |
| 谁看得见 | 下发人 + 收件人 | **只有自己**（❓待定：要不要允许分享给同事） |
| 逾期怎么办 | 推送催办 + 通知下发人 | 只在自己的列表里标红（❓待定：要不要也推企微） |
| 存在的意义 | 管理动作、要留痕 | 减少「记在纸上/记在脑子里」 |

**关键判断：绝不把两者合并成一张表。** 管理层待办的每一条都是有交代的管理动作，
它的「承诺完成时间/进度/延期审批」是留痕证据；个人待办随手记随手删。
混在一起的后果是：要么个人待办被迫填一堆用不上的字段（没人会用），
要么管理层待办的留痕被个人待办的随意删除污染。

---

## 二、放在哪（UI 位置）

反馈原话是「**单独开一列**」，理解为：和现有「我的待办」并排，一眼能看到两类事。

现有入口是右下角悬浮的「待办」按钮（`ManagementTodoFloating.vue`），点开是一个抽屉。
**方案：抽屉里改成左右两栏 / 上下两段**

```
┌─────────────────────────────────────────────────┐
│  待办                                      ✕    │
├──────────────────────┬──────────────────────────┤
│  📋 别人交办我的 (3) │  ✅ 我自己的 (5)   ＋新增 │
│                      │                          │
│  · 买一个快装弯头    │  ☐ 给张工回电话          │
│    赵仁辉 · 8-13到期 │    今天                  │
│    [回复承诺时间]    │  ☐ 核对 2026-075 图纸    │
│                      │    明天 · 🔴逾期1天       │
│  · 核对售后费用      │  ☑ 订下周车票  (已完成)  │
│    杨坛 · 已承诺8-15 │                          │
│    [报进度][完成]    │  ＋ 直接输入一行就能加   │
└──────────────────────┴──────────────────────────┘
```

要点：
- **左边完全不动**（现有管理层待办逻辑一行不改）
- 右边一个输入框回车即建，**不弹窗、不必填**——个人待办的成败全在录入成本
- 手机/H5 上左右栏改成上下两段（❓待定：H5 上要不要默认只显示「我自己的」）

---

## 三、数据表 `personal_todos`

| 字段 | 类型 | 说明 |
|---|---|---|
| id | int | |
| user_id | int FK users | **只属于创建人**，查询永远带 `user_id == 当前用户` |
| title | str(200) | 必填，就是待办内容本身 |
| note | text | 选填，补充说明 |
| due_date | str(10) | 选填，`YYYY-MM-DD`。空=没有期限 |
| priority | str(8) | normal / urgent（❓待定：要不要这一档，不要就砍） |
| done | bool | 完成打勾 |
| done_at | datetime | 完成时间 |
| sort_order | int | 手工拖动排序 |
| project_id | int FK 选填 | ❓**待定**：要不要能挂到项目上（挂上之后可以在项目详单里看到"我在这个项目上还有 2 件事"） |
| created_at | datetime | |

**刻意不做的字段**：收件人、承诺完成时间、进度、延期申请、附件、提醒时间点。
这些一加，个人待办就变成第二个管理层待办了。

---

## 四、接口（都在 `/api/personal-todos`，全部强制按 user_id 隔离）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/personal-todos` | 我的全部；`?done=false` 只看未完成 |
| POST | `/personal-todos` | 新建（只要 title） |
| PUT | `/personal-todos/{id}` | 改内容/日期/优先级 |
| POST | `/personal-todos/{id}/toggle` | 打勾 / 取消打勾 |
| DELETE | `/personal-todos/{id}` | 删除（个人的东西，直接物理删，不做软删） |
| PUT | `/personal-todos/reorder` | 拖动排序 |
| GET | `/personal-todos/count` | 未完成条数（给悬浮按钮角标用） |

⚠️ **每个接口都必须 `where(user_id == current.id)`，不能只在列表接口过滤**。
只过滤列表、详情/修改/删除按 id 直接取，是最典型的越权口子——
换个 id 就能改别人的待办。

---

## 五、角标怎么算（要业务确认）

右下角「待办」按钮现在的角标 = 管理层待办未回复数。加了个人待办之后：

- **方案 A**：角标 = 管理层待办未回复 + 个人待办未完成（一个数）
- **方案 B**：角标只数管理层待办（保持现状），个人待办的数只显示在抽屉里
- **方案 C**：显示两个数，如 `3 / 5`

❓**建议 B**：角标是"有人在等你"的信号，自己给自己记的事不该往里掺——
个人待办堆 20 条没完成，角标常年红着，真正别人交办的事反而被淹没。
（这条一定要你拍板，改起来很便宜但改晚了用户已经习惯了。）

---

## 六、#382 同步到 APP

现状：`mobile/` 是 H5 打包，和网页版共用同一套接口。
所以只要接口做好，**H5 侧只需加一个页面**，不存在"再做一遍"。

- H5 入口：底部「我的」页里加一个「我的待办」（❓待定：还是放首页更显眼？）
- 交互按手机改：一行一条 + 左滑删除 + 点一下打勾，不做拖动排序（手机上拖动很难用）
- ❓**待定：要不要做企微推送**。个人待办到期推企微是很自然的想法，但那是
  "自己设的闹钟"，推多了会让人把整个系统的通知都静音掉——建议第一期不做。

---

## 七、分期

| 期 | 内容 | 大致工作量 |
|---|---|---|
| 一期 | 表 + 7 个接口 + 网页抽屉右栏（增删改查打勾） | 1 天 |
| 二期 | H5 页面（#382） | 半天 |
| 三期 | 拖动排序、挂项目、逾期标红 | 半天 |

---

## 八、需要你确认的（就这 5 条，回一句话就行）

1. **角标**怎么算？（第五节，建议 B：角标只数别人交办的）
2. 个人待办要不要能**挂到项目上**？
3. 要不要 **紧急/普通** 这一档？
4. H5 入口放**底部「我的」里**还是**首页**？
5. 到期要不要**推企业微信**？（建议不推）

确认完我就按一期→二期做。
