# 复制本文件为 .env 后按需修改

# PostgreSQL
POSTGRES_USER=pms
POSTGRES_PASSWORD=pms_dev_pwd
POSTGRES_DB=pms

# Backend
SECRET_KEY=dev-secret-key-please-change-in-prod
ACCESS_TOKEN_EXPIRE_MINUTES=480

# 默认管理员（初始化时使用）
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=admin123
